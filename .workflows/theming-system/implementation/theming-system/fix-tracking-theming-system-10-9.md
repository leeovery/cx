## Attempt 1

ISSUES:

- `.workflows/portal-observability-layer/specification/portal-observability-layer/specification.md:1008` — the clause "and is handed to every theme seam constructed there" is **false** and contradicts the "Mechanical rule — emission control" paragraph four lines above (`:1004`). `cmd` constructs three theme seams: `cmd/open.go:761` gets `themeLogger`, but `cmd/theme.go:120` (`portal theme export`) and `cmd/doctor_theme.go:112` both get `theme.NewEventLogger(log.Discard())`. Two of the three cmd-constructed seams do NOT receive the binding — which is the whole point of the used-vs-diagnosed rule the section is built on. **This is a false claim the correction itself introduced.**

  FIX: replace the clause so it agrees with `:1004`. Suggested: "`internal/theme` **binds nothing**: the single `log.For("theme")` binding lives in `cmd`, and is what the seams on the used paths are constructed with — the diagnosis-shaped seams get `log.Discard()` instead (above)."

  ALTERNATIVE: drop the trailing clause entirely — "the single `log.For("theme")` binding lives in `cmd`." — since `:1004` already states the distribution rule in full. Shorter and unfalsifiable, but loses the pointer from the binding to its consumers. The reviewer recommends the first: it keeps the cross-reference and costs one clause.

  CONFIDENCE: high

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant — §12.3's attr keys, the WARN-not-INFO rule, the used-not-diagnosed rule, `token`'s sole consumer and the injected-logger dedup are all carried; §8.9's multi-package emission is carried and corrected against the code (three sites, two packages); §15.1's amendment is landed. All acceptance criteria met. CONVENTIONS followed — corrigendum in the protocol's exact one-line form, beneath the title, one entry per correction, dated 2026-08-07; no "was X, now Y" residue in any section body. ARCHITECTURE sound — the bind-once block sits at the rule it corrects rather than being duplicated into the theme section, and the theme catalogue is its own roadmap entry (14) with Hook-firing renumbered to 15.
- **Component membership derived independently**: all non-test `log.For("…")` / package-internal `For("…")` gives `aliases, bootstrap, capture, clean, daemon, hooks, hydrate, log-rotate, notify, preview, process, projects, resolve, restore, saver, signal, spawn, theme` = **18**. Matches the fenced block (18 names) and the stated total. `init-probe` is test-only. No component-vocabulary guard test exists, so grep is the source of truth.
- **Attr keys recounted independently**: 14 + 14 + 7 + 3 + 7 + 8 + 2 + 5 + 4 = **64**, with `session`/`target`/`reason`/`path` cross-listed once each. Old total 49 + 8 + 2 + 5 = 64. Every stated count in the file (`:21`, `:160`, `:195`, `:222`, `:237`, `:251`) agrees. Every declared key is emitted at a real site and every emitted key is declared — verified in both directions.
- **All five code disagreements verified correct.** (1) `internal/theme` calls `log.OrDiscard`, never `log.For`; the sole binding is `cmd/open.go:49`, while `spawn` genuinely binds in two packages (`internal/spawn/detect.go:21` + `cmd/spawn_seams.go:16`) and `bootstrap` in two (`cmd/state_common.go:22` + `internal/tmux/hooks_unregister.go:18`) — so "not exactly as spawn and bootstrap" is right. (2) `reportSlot` calls `reportFallback` then `events.Loaded(r.Resolved, r.Slot)`; `byNamePass`/`commitPass` → `reportSlot`, `enumerationPass` → `reportFallback` only. (3) `Rejected` fires only from `enumerate.go:78`. (4)/(5) confirmed against the diff.
- **Seven-event catalogue verified line by line** against `internal/theme/events.go`, `cmd/config.go:324-331`, `cmd/theme_persister.go:85-92`. Doctor/export/capturetool all construct `theme.NewEventLogger(log.Discard())`.
- `:125` — "alongside that package's own binding for its detection path" understates where `internal/spawn`'s binding is used: `spawnLogger` also drives the `terminals.json` WARNs at `terminalsconfig.go:68,74`, `configadapter.go:120,124` and `recipe.go:83`. Not false (additional uses, not additional bindings), and the Spawn attr-group header at `:224` already names the `terminals.json` load. Non-blocking.
- `:125` — "both burst surfaces pass their own in" is loose: `internal/tui` holds no `log.For("spawn")`; `WithSpawnLogger` injects cmd's binding (`internal/tui/burst_observability.go:29`, wired from `cmd/open.go:1070`). `internal/tui` is in fact a third instance of the bullet's own rule. Non-blocking.
- `:990` — "The panel's re-resolutions (open, `Esc`, the post-commit recompute) … emit nothing" is scoped to the `loaded` column, but read out of the table cell it conflicts with the `fallback applied` row at `:994`, which names those same three sites among its cadences. The rows are co-chunked so a retrieval sees both. Non-blocking.
- `:991` — a panel open refused by `themePanelFloor` (`internal/tui/theme_panel.go:582`) still emits `enumerated`, since `Open` has already run. The code documents this as accepted; the spec's "once per panel open" does not mention it. Non-blocking.
- The `prefs.json` parenthetical at `:193` is **accurate and in scope** — the audit trail's closed file set (`:713-716`) is `hooks.json` / `aliases` / `projects.json`, and `prefs` is a leaf that must not import `internal/log`. `terminals.json` is likewise outside that set and is not mentioned, but it is read-only at spawn time so it has no mutation to audit.
- **The six swept-not-corrected items are all genuinely other units'**: `11-step bootstrap` (`:173`, `:869`, `:902` — CLAUDE.md documents ten after `CleanStale` was re-homed), the `clean` component/verb (`:183`) and the `clean`/`attach` `process_role` rows (`:103-104`), `portal hooks set/rm` (`:177`), `ShowGlobalHooks` (`:854`), and the Public API block (`:55-81`) missing `OrDiscard`/`Discard`/`Took`/`ResolveProcessRole`/`CombinedOutputWithContext`/`SweepLogsForClean`. The last is this unit's own staleness (its task 7-1 added `Discard`), not theming-system's. The task brief's Edge Cases explicitly directed the `clean` item to be surfaced rather than swept.
- Out of scope, surfaced for the orchestrator: the same unit's **discussion** artifact (`.workflows/portal-observability-layer/discussion/portal-observability-layer.md:472,644,1288`) still carries "15 total" / "15 components" and its chunks are live. It self-defers at `:644` ("the … Subsystem prefix taxonomy Mechanical rule is the single source of truth") and at `:515` ("or a successor specification amendment"), so the retrieval risk is contained — and the protocol's phase judgment says a discussion is a record of a conversation, not a fact sheet. Flagged, not recommended for edit.
- Re-index verified live: the theme catalogue chunk is retrievable, dated 2026-08-07, and no stale 15/49 chunk survives outside the corrigendum quotes. Owning manifest read `completed` before the edit and is untouched. Nothing committed.
- Full unit lane green (34 packages). `go vet ./...` clean; `golangci-lint run` → 0 issues; `gofmt -l .` → only the three documented pre-existing offenders. No Go file touched.
