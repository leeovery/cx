AGENT: duplication
FINDINGS:
- FINDING: The "make this path unreadable" test fixture is re-authored in ~14 places, with three different root policies
  SEVERITY: medium
  FILES: internal/theme/load_test.go:554, internal/theme/enumerate_test.go:379, cmd/theme_test.go:568, cmd/theme_test.go:581, cmd/theme_test.go:411, cmd/doctor_theme_test.go:70, cmd/doctor_theme_test.go:125, cmd/doctor_theme_test.go:197, cmd/doctor_theme_test.go:273, cmd/doctor_theme_test.go:376, cmd/doctor_persisted_theme_test.go:306, cmd/doctor_persisted_theme_test.go:476, cmd/doctor_persisted_theme_test.go:686, cmd/open_theme_nomination_test.go:72, internal/prefs/read_shared_test.go:121
  DESCRIPTION: The `unreadable` rung of the rejection ladder is exercised from four packages, and every consumer wrote its own staging fixture: `chmod 0o000` + `t.Cleanup` restore, optionally preceded by a root guard. `internal/theme/load_test.go:writeUnreadableTheme` and `internal/theme/enumerate_test.go:unreadableDir` are near-identical (down to a verbatim-identical doc comment) and differ only in file-vs-directory; `cmd/theme_test.go:unreadableThemeFile`/`unreadableThemesDir` are a second near-identical pair of the same two shapes; `cmd/open_theme_nomination_test.go:poisonThemesDir` and `cmd/doctor_persisted_theme_test.go`'s inline `deniedDir` closure are a third and fourth; and at least eight further call sites inline the same five lines. The copies have already drifted on the part that matters: `internal/theme` and `cmd/doctor_theme_test.go:skipUnlessModeBitsDeny` skip under root, `cmd/theme_test.go:requireDeniedRead` instead fatals if the read succeeds, and several inline sites (e.g. cmd/theme_test.go:411, cmd/doctor_persisted_theme_test.go:689) apply neither guard, so they degrade to a vacuous assertion when run as root rather than skipping or failing loudly. This is the single largest copy-paste surface the topic introduced, and the drift is a correctness divergence, not just repetition.
  RECOMMENDATION: Put one pair of helpers in `internal/themetest` (already the shared, test-only owner of theme fixtures and already imported by `internal/theme`, `cmd`, `cmd/capturetool` and `internal/tui` tests) — e.g. `DenyRead(t, path) error` and `DenyDir(t, dir) error`, each staging the chmod, registering the restore, applying ONE root policy, and returning the verified OS error. Replace `writeUnreadableTheme`, `unreadableDir`, `unreadableThemeFile`, `unreadableThemesDir`, `poisonThemesDir`, the `deniedDir` closure and every inline `os.Chmod(..., 0o000)` block with calls to it; keep `requireDeniedRead`'s absent-versus-denied assertion by folding it into the returned error's verification.

- FINDING: commitConstant and commitSlot are parallel implementations of one commit protocol
  SEVERITY: medium
  FILES: internal/tui/theme_panel_commit.go:11, internal/tui/theme_panel_commit.go:30, internal/tui/theme_panel_commit.go:76, internal/tui/theme_panel_commit.go:86
  DESCRIPTION: Two pairs of functions differ only in which persister method is called and which `RawKeys` mutator is applied. `commitConstant` and `commitSlot` each run the identical five-step protocol — nil-persister short-circuit, write, `applyCommitResult(err)`, bail on error, mutate `themeState.keys`, `recomputeThemePanel()` — and the source acknowledges it ("Carries commitConstant's rules"), which is exactly the shape that drifts: a sixth step added to one path is silently absent from the other, and the ordering constraints the comments defend (keys mirror only after a landed write; recompute last) are stated twice. `commitSelectedConstant` and `commitSelectedSlot` repeat the same `committableThemeSlug` guard-and-delegate wrapper.
  RECOMMENDATION: Extract one private `func (m *Model) commit(write func() error, mirror func(theme.RawKeys) theme.RawKeys) error` holding the whole protocol, and reduce `commitConstant`/`commitSlot` to the two closures (`p.CommitTheme(slug)` + `keys.WithConstant(slug)`; `p.CommitThemeSlot(slug, member)` + `keys.WithMember(member, slug)`). Collapse the two `commitSelected*` wrappers the same way — one `commitSelected(func(slug string) error)` taking the per-shape committer.

- FINDING: The slot-to-shipped-default pairing is stated three times
  SEVERITY: medium
  FILES: internal/theme/setting.go:76, internal/theme/setting.go:86, internal/theme/resolution.go:210
  DESCRIPTION: Which default belongs to which slot — light→`DefaultLightSlug`, dark→`DefaultDarkSlug` — is written out independently in `ResolveSetting` (the `cmp.Or` pair building the Setting), in `Setting.Slug` (the per-slot `cmp.Or` switch, documented as "the per-slot half of ResolveSetting's substitution") and in `fallbackSlugFor` (the mode-matched fallback). All three encode the same mapping, and the invariant they jointly protect is the one the code most cares about: a light slot must never resolve or fall back to a dark palette. Three copies is three chances for one to be edited alone, and nothing structural couples them.
  RECOMMENDATION: Keep exactly one mapping function — `fallbackSlugFor` renamed to something use-agnostic (e.g. `defaultSlugFor(slot Slot) string`) — and express the other two through it: `ResolveSetting` builds `Light: cmp.Or(raw.Light, defaultSlugFor(SlotLight))` / `Dark: cmp.Or(raw.Dark, defaultSlugFor(SlotDark))`, and `Setting.Slug` returns `cmp.Or(s.Light, defaultSlugFor(slot))` per arm. The fallback path keeps calling it unchanged.

- FINDING: The parse-bytes-then-build-Result tail is repeated across the three loader entry points
  SEVERITY: low
  FILES: internal/theme/load.go:76, internal/theme/load.go:93, internal/theme/builtins.go:77
  DESCRIPTION: `LoadFile`, `LoadPath` and `LoadBuiltin` each end with the same block: call `parseThemeBytes(data)`, return the zero `Result` with the rejection on failure, otherwise build a `Result` carrying `Theme` and the verbatim `Source` bytes. The three differ only in whether `Slug` is populated. The `Source`-is-verbatim-never-re-serialised rule (load.go:51) therefore has to hold at three construction sites rather than one.
  RECOMMENDATION: Extract `func resultFromBytes(slug string, data []byte) (Result, *Rejection)` in `load.go` holding the parse, the rejection short-circuit and the `Result` literal; have all three entry points return it (`LoadPath` passing the empty slug, `LoadBuiltin` passing the built-in's).

- FINDING: The ThemeSource slot-collapse rule is mirrored in four independent implementations
  SEVERITY: low
  FILES: internal/theme/dir_theme_source.go:34, internal/capture/theme_fake.go:51, internal/tui/theme_seams_test.go:31, internal/tui/theme_source_fake_test.go:58
  DESCRIPTION: Every `ThemeSource.ResolveSlot` implementation re-derives the nominated slug the same way — `setting, _ := theme.ResolveSetting(keys)` then `setting.Slug(slot)` — once in the production adapter, once in the capture harness's fake, and twice in the tui test fakes. Each fake carries a comment explaining that it must run "the same collapse the production adapter runs", which is precisely the contract that silently rots if the adapter's collapse ever changes: the fakes keep compiling and keep answering the old way.
  RECOMMENDATION: Expose the collapse once in `internal/theme` — `func SlugForSlot(keys RawKeys, slot Slot) string { setting, _ := ResolveSetting(keys); return setting.Slug(slot) }` — and call it from `DirThemeSource.ResolveSlot` and from all three fakes, so a change to the tiebreak or the default substitution reaches every implementation by construction.

- FINDING: The Enumeration-from-a-directory-read invariant is built in two places
  SEVERITY: low
  FILES: internal/theme/union.go:118, cmd/doctor_theme.go:58
  DESCRIPTION: `Assembler.Open` and `cmd/doctor_theme.go:enumerateThemesDir` each call `Loader.Enumerate(dir)` and then hand-assemble the `Enumeration`, both encoding the same invariant that `DirUnusable` is `rejection != nil` and that `DirPath` carries the directory just read. Doctor adds only the empty-path short-circuit. Two independent statements of "what a directory read means" is what lets the panel and doctor disagree about a directory — the one thing the topic single-sources everywhere else (both already share `unresolvedRejection` and the rejection ladder).
  RECOMMENDATION: Add one constructor to `internal/theme` — e.g. `func (l Loader) OpenEnumeration(dir string) Enumeration`, returning the empty `Enumeration` for an empty dir and otherwise the `Entries`/`DirUnusable`/`DirPath` triple — and have `Assembler.Open` and `enumerateThemesDir` both call it. `Assembler.Open` keeps its own `events.Enumerated` emission; doctor keeps its silent loader.

- FINDING: The theme panel redraws the page's separator rule instead of calling the page's renderer
  SEVERITY: low
  FILES: internal/tui/theme_panel_render.go:33, internal/tui/header.go:68
  DESCRIPTION: `themePanelHeaderBlock` composes the panel's rule row by hand — `headerStyle(th.Border, …).Render(strings.Repeat(headerRuleGlyph, max(width, 0)))` — which is what `headerSeparatorRule` already does (bar the zero-width fallback the panel must not take). The panel's rule exists specifically to sit in the page rule's lane, so a change to the page's glyph or token is meant to move both; as written it would move only one, and the shared-lane property the surrounding comment asserts would break silently.
  RECOMMENDATION: Split `headerSeparatorRule` into the width-resolving wrapper and a `ruleOfWidth(w int, th theme.Theme, colourless bool) string` that renders the glyph run at an exact width; have `headerSeparatorRule` call it after `headerWidthOrFallback`, and have `themePanelHeaderBlock` call `ruleOfWidth(max(width, 0), …)` directly.

- FINDING: The contrast swatch repeats a background-filled-run-of-spaces idiom five times
  SEVERITY: low
  FILES: internal/capture/swatch.go:71, internal/capture/swatch.go:81, internal/capture/swatch.go:182, internal/capture/swatch.go:185, internal/capture/swatch.go:198
  DESCRIPTION: `fillCanvas` (twice), `subtleBand` (twice) and `padBand` each write out `lipgloss.NewStyle().Background(<tint>).Render(strings.Repeat(" ", n))` inline, and `fillCanvas`'s clamp-then-pad-then-backfill loop re-implements the geometry the model's own canvas fill performs. The file already owns `onTint` for the styled-text case; the styled-blank case has no equivalent and was written out at each site.
  RECOMMENDATION: Add one `func fill(tint color.Color, n int) string` beside `onTint` and route all five sites through it. Leave the standalone `fillCanvas` loop as-is (its independence from `tui` is deliberate and documented), but express its blank and pad segments through the new helper.

- FINDING: "A theme file whose canvas is X" is composed ad hoc at each call site
  SEVERITY: low
  FILES: cmd/open_theme_construction_test.go:497, cmd/capturetool/main_test.go:434, cmd/capturetool/main_test.go:493, cmd/capturetool/swatch_test.go:31, internal/tui/theme_panel_open_test.go:103
  DESCRIPTION: Four packages stage a valid drop-in identified by its canvas value, and each spells the composition out: `themetest.Write(t, dir, base, themetest.WithValue(themetest.Lines(), "canvas", canvas))`. `internal/tui`'s variant (`writeThemeFileForTest`) instead loops `theme.TokenNames()` to paint every token the same value. `internal/themetest` is the declared owner of the fixture format ("a change to what the loader reads is one edit here"), yet this — the most-used derived fixture — lives outside it in four hand-rolled forms.
  RECOMMENDATION: Add `themetest.WriteWithCanvas(t, dir, base, canvas) string` (and, if the all-tokens-one-colour variant is genuinely needed, `themetest.MonochromeLines(value)`) and have the cmd, capturetool and tui call sites use them.

- FINDING: The capture fixture registry states every fixture name twice
  SEVERITY: low
  FILES: internal/capture/fixtures.go:126, internal/capture/fixtures.go:189
  DESCRIPTION: `FixtureByName`'s switch and `FixtureNames`'s literal slice are two hand-maintained lists of the same names (a third copy is each fixture function's own `fx.name = "…"`). The pattern predates this topic, but the topic grew it by ten `theme-panel-*` entries in each list — the largest single addition it has taken. A name added to the switch but not the slice is invisible to the swap-and-diff completeness guard, which enumerates whatever `FixtureNames` returns, so the failure mode is a quietly shrinking guard rather than a build error.
  RECOMMENDATION: Make one list authoritative — a package-level `map[string]func() *Fixture` (or an ordered slice of `{name, build}` pairs) — with `FixtureByName` looking up in it and `FixtureNames` deriving from its keys plus `ContrastValidationFixture`. Each fixture builder then takes its name from the registry entry rather than assigning `fx.name` itself.
SUMMARY: The production theme code is well consolidated overall — the ladder, the union, the resolution passes and the TUI chrome helpers are each single-sourced — but three genuine parallel implementations survive in it (the two commit paths, the slot→default pairing stated three times, and the loader's repeated Result tail). The larger and more consequential duplication is in the test surface: the "make this path unreadable" fixture was independently rewritten roughly fourteen times across four packages and has already drifted into three incompatible root policies, some of which silently make their assertions vacuous.
