AGENT: duplication
FINDINGS:
- FINDING: The 19-token synthetic swap-probe palette is authored twice, in two packages
  SEVERITY: high
  FILES: internal/capture/theme_swap_guard_test.go:117, internal/tui/restyle_repoint_test.go:49
  DESCRIPTION: `syntheticTheme(red int)` (capture) and `syntheticProbePalette(red uint8)` (tui) are the same function written independently: the same `v(i)` closure over `fmt.Sprintf("#%02X%02X%02X", red, 0x80+i, 0xC8+i)`, the same 19-field `theme.Theme` literal in the same order with the same indices, and near-identical rationale comments ("two shipped themes would not do… a token with the same value either side of the swap renders identically"). Together they are the probe palettes for the two halves of the same mechanism — the swap-and-diff completeness guard and the restyle re-point guard — so drift between them is drift between the two guards that protect the live theme swap. The failure mode is silent in exactly the way both comments warn about: `theme.Theme` is a plain struct, so adding a 20th token leaves both literals compiling with a zero-valued field, and a guard probing a zero token can neither diff nor detect a stale value. One edit is currently two edits in two packages, with nothing linking them.
  RECOMMENDATION: Move the builder into `internal/themetest` (which already exists as the shared theme-test vocabulary and is imported by both packages) as e.g. `themetest.SyntheticPalette(red)` plus the disjoint pair helper, and have both guards call it. Derive the field assignment from the canonical ordered token table rather than a hand-written 19-field literal, so a new token cannot leave either guard silently probing a zero value; keep the per-guard constants (`syntheticRedA/B`, `probeThemeBefore/After`) at their call sites.

- FINDING: The two commit-key "no other I/O" tests are a 65-line verbatim copy of each other
  SEVERITY: medium
  FILES: internal/tui/theme_panel_commit_test.go:541, internal/tui/theme_panel_commit_slot_test.go:904
  DESCRIPTION: `TestPanelEnter_NoOtherIO` and `TestPanelSlotCommit_NoOtherIO` are the same test body twice in the same package — identical doc comment (word for word, including the "internal/tui resolves no config path…" paragraph), identical prefs-file env setup, identical `writeThemeFileForTest` seed, identical `newCountingStores` + `fakeThemePersister` + `countingEnumeratorOver` wiring, an identical 10-field `Build(Deps{…})` literal, identical open/reset/opens capture, and five identical assertion blocks (seam call count with the same seven-value message, enumeration count, nil cmd, empty config dir, single themes-dir entry). Exactly four lines differ: the `theme.RawKeys` fixture, the keypress helper, the commit assertion, and the noun in the failure strings. Because the assertion messages are also duplicated, a change to what "no other I/O" means has to be applied twice and can be applied to one only.
  RECOMMENDATION: Extract one helper in the shared panel test file (`theme_testing_test.go`), e.g. `requireCommitDoesNoOtherIO(t, keys, press func(*testing.T, Model) (Model, tea.Cmd), assertCommitted func(*testing.T, *fakeThemePersister))`, holding the setup, the counters and the five assertions; leave each test as the three lines that state its own fixture and expected write.

- FINDING: "Parse this package's production .go sources" is re-implemented five-plus times for the source guards
  SEVERITY: medium
  FILES: cmd/open_theme_nomination_test.go:337, internal/tui/nomination_test.go:320, internal/tui/theme_flash_precedence_test.go:259, internal/tui/theme_source_guard_test.go:105, internal/tui/theme_panel_test.go:917, internal/theme/theme_test.go:402, internal/theme/leaf_guard_test.go:194
  DESCRIPTION: The theming work added a large family of AST source guards, and each author wrote their own enumerate-and-parse front end. `parseCmdFiles` (cmd) and `parsePackageFilesByName` (tui) are byte-identical bodies — same `os.ReadDir(".")`, same `IsDir()/.go/_test.go` skip, same `parser.SkipObjectResolution`, same two `t.Fatalf` messages — differing only in the doc comment's package name. Inside `internal/tui` alone there are then three more variants of the same scan: `themeFlashProductionFiles` (glob-based, own empty-match check), `parseProductionFiles` (named-subset), and an inline glob-parse loop inside `themeRowDelegateLiteralSites`. `internal/theme` adds two more (`themeSourceFiles` + parse loop, and `exportedSymbols`' own ReadDir+parse). The variants differ in ways that matter to what a guard covers — glob versus ReadDir, empty-match fatal versus silent, one shared FileSet versus one per file — so guards that read as siblings are quietly scoped differently, and a guard that silently matched no files would pass vacuously in three of the six. This class was already recognised and partly fixed in this same work unit: `portalbintest.GoSourceFiles` (internal/portalbintest/build.go:59) was added precisely to single-source the repo-wide walk, with the "a guard that walks its own subset is a guard narrower than its siblings by accident" rationale — but it has only one consumer, and the per-package scans never joined it.
  RECOMMENDATION: Within `internal/tui`, route `themeFlashProductionFiles`, `parseProductionFiles` and `themeRowDelegateLiteralSites` through the existing `parsePackageFilesByName` (adding an optional shared-FileSet variant, which is the only real difference). Within `internal/theme`, have `exportedSymbols` and `parseThemeSources` share one enumerator. If a cross-package home is wanted, follow the `GoSourceFiles` precedent and put the package-local parse beside it in `internal/portalbintest`, then delete `parseCmdFiles`'s copy.

- FINDING: The prefs "declines to create an absent file" test body is duplicated across two savers
  SEVERITY: medium
  FILES: internal/prefs/migration_marker_test.go:396, internal/prefs/translation_saver_test.go:161, internal/prefs/store_write_path_test.go:165
  DESCRIPTION: `TestSaveMigrationMarker_DoesNotCreateAbsentFile` and `TestSaveTranslation_DoesNotCreateAbsentFile` are the same ~28-line body twice: the same two-case table (`{"the file is absent", []string{"prefs.json"}}`, `{"the parent directory is absent too", []string{"sub","nested","prefs.json"}}`), the same `filepath.Join(append([]string{dir}, c.rel...)...)`, the same `os.Stat` / `errors.Is(os.ErrNotExist)` assertion, the same `os.ReadDir` empty-tree assertion with the same message shape, and the same `assertNoTempFiles`. Only the call under test and one extra `persisted` check differ. The same case table appears a third time in `TestSave_CreatesAbsentFile`, which asserts the opposite outcome over the identical inputs. The package already has a shared-helper home (`seedPrefsFile`, `assertUntouched`, `assertNoTempFiles`, `undecodablePrefsCases` in store_write_path_test.go), so the table and the "nothing was created" assertion belong there next to their siblings.
  RECOMMENDATION: Add `absentPathCases()` (the shared two-case table) and `assertNothingCreated(t, dir, path)` beside the existing shared prefs helpers, and reduce all three tests to the table plus their own outcome assertion — the two decliners asserting nothing was created, `TestSave_CreatesAbsentFile` asserting the written value.

- FINDING: The doctor `--fix` stale-prune fixture and assertions are copied into the theme test file
  SEVERITY: medium
  FILES: cmd/doctor_fix_theme_test.go:523, cmd/doctor_test.go:1297
  DESCRIPTION: `TestDoctorFix_ExistingRepairsUnchanged` reproduces roughly 35 lines of `TestDoctorFixPrunesStaleEntriesThenRediagnosesClean` verbatim: the same `seedHealthyStateDir` / `seedHooksJSON("sessA:0.0")` / `seedProjectsJSON(liveDir, goneDir)` / `staleDeps(..., fakeHookLister{keys: []string{"sessB:0.0"}}, ...)` fixture, and the same five assertion blocks (hooks.json no longer contains `sessA:0.0`, projects.json no longer contains `goneDir`, projects.json still contains `liveDir`, the pruned-hook breadcrumb, the pruned-project breadcrumb) with byte-identical failure messages. The new test's own subject is only what it adds — the themes dir, the stale rotated log and the advisory line. This is copy-paste across a task boundary: the two now assert the same repair contract in two places, and a change to a breadcrumb string or a prune rule has to be made twice.
  RECOMMENDATION: Extract the shared fixture and assertions into cmd's doctor test helpers — e.g. `seedStalePruneFixture(t) (deps, hooksPath, projectsPath, liveDir, goneDir)` and `assertStalePrunesApplied(t, hooksPath, projectsPath, liveDir, goneDir, out)` — and have both tests call them, leaving `TestDoctorFix_ExistingRepairsUnchanged` holding only its themes-dir seed, log sweep and advisory assertions.

- FINDING: cmd re-authors internal/themetest's theme-file fixture vocabulary and its themes-dir staging
  SEVERITY: low
  FILES: cmd/theme_test.go:613, cmd/theme_test.go:641, cmd/theme_test.go:655, cmd/doctor_theme_test.go:32, cmd/doctor_fix_theme_test.go:245, internal/themetest/theme_file.go:43
  DESCRIPTION: `internal/themetest` is documented as "the single definition of the fixture format every consumer stages files in" and exports `Lines`, `WithValue`, `WithoutKey`, `Body` and `Write`; `internal/theme` and `internal/tui` use it. The cmd theme suites instead grew a parallel set over built-in-derived lines: `themeSourceFromLines` (the same `strings.Join(lines,"\n")+"\n"` render, also open-coded at internal/theme/resolution_test.go:342), `sourceMissingTokens` (`WithoutKey` by another name), `sourceBadColours` (`WithValue` by another name) and `themeLineIndex`. Their stated justification — no hex restated in Go, immune to vocabulary change — is exactly what `themetest.Lines()` already delivers by deriving names from `theme.TokenNames()`. Separately, three themes-dir staging helpers coexist in cmd: `themesDirWith(t, files)` (doctor_theme_test.go:32), the inline map-write loop inside `fixThemeFixture` (doctor_fix_theme_test.go:245) that repeats `themesDirWith`'s body, and `seedThemesDir` (cmd/theme_test.go:116).
  RECOMMENDATION: Add a lines-to-bytes renderer to `internal/themetest` (e.g. `Render(lines []string) []byte`, the body `Body()` already computes) plus a duplicate-key helper, and have cmd's `sourceMissingTokens` / `sourceBadColours` / `sourceDuplicateKeyAt` delegate to `WithoutKey` / `WithValue` / that helper — keeping `validThemeSource` as-is, since the export tests genuinely need the built-in's own bytes. Have `fixThemeFixture` call `themesDirWith` rather than repeat its write loop.

- FINDING: The per-built-in test files carry parallel copies of the same file readers and enrolment checks
  SEVERITY: low
  FILES: internal/theme/builtins_nord_test.go:303, internal/theme/builtins_tokyo_night_day_test.go:302, internal/theme/builtins_nord_test.go:102, internal/theme/builtins_tokyo_night_day_test.go:93, internal/theme/builtins_test.go:36
  DESCRIPTION: `readNord` and `readTokyoNightDay` are the same helper twice (read the committed built-in's path, `t.Fatalf` on error, return the text) with near-identical doc comments, and `builtins_test.go` open-codes the same read twice more against `tokyoNightPath`. `TestNord_IsEnrolledInFloorChecks` and `TestTokyoNightDay_IsEnrolledInFloorChecks` are the same five-line assertion parameterised only by the slug constant. The "record lives in a `#` comment" guards (`TestNordFile_CorrectionsAndInventionsCarryComments`, `TestTokyoNightDayFile_SevenValuesCarryDerivationComments`, `TestTokyoNightDayFile_PinnedTintsCarryDerivationComments`) repeat one loop shape — assert the shipped value via `valueFor`, require a `commentBlockAbove`, require each figure/marker within it, then sweep the remaining tokens for a falsely-claimed marker — over three differently-shaped record structs, two of which (`nordJudgement` and the anonymous struct behind `sevenCheckedValues`) have identical fields. This cuts against the project's "adding a theme is adding a file" property: a fourth built-in would need a fourth copy of the reader and the enrolment test rather than being picked up by enumeration.
  RECOMMENDATION: Collapse the two readers into one `readBuiltinFile(t, slug)` beside the shared `valueFor` / `commentBlockAbove` / `declaresKey` helpers, and replace the two enrolment tests with a single table over `theme.BuiltinSlugs()` so a new built-in enrols itself. Unify the record struct (token, shipped, figures, optional marker) and lift the assert-value-then-assert-comment-figures loop into one helper the three guards call with their own records.

- FINDING: The panel's block assembly re-authors the clamp / pad-to-width / blank-fill loop
  SEVERITY: low
  FILES: internal/tui/theme_panel.go:1323, internal/tui/model.go:4429, internal/tui/model.go:4544
  DESCRIPTION: `themePanelBlock` implements the same three-step frame primitive `Model.fillCanvas` and `fillColourless` already implement: iterate the rendered lines, `break` once `len(out) == height` (the same clamp, with the same intent), pad each line out to the target width with a canvas-painted remainder, then append blank canvas rows until the block is exactly `height` tall. The panel's version legitimately adds the border/gutter prefix and the above-the-rule region split, so it is a near-duplicate rather than an exact one — but the clamp-and-fill arithmetic that guarantees "exactly h rows of exactly w cells" now exists at three sites in one package (four counting internal/capture's `swatchModel.fillCanvas`), and that invariant is one the whole composite depends on.
  RECOMMENDATION: Extract the shared primitive — "take a rendered block to exactly h rows of exactly w cells, given a per-row painter and a blank row" — and have `themePanelBlock` compose it with its prefix and header-region split, leaving the pre-existing `fillCanvas` / `fillColourless` sites to adopt it as they are next touched.
SUMMARY: The production theming code is unusually well single-sourced — the resolution ladder, the union assembly, the badge table, the panel's layout arithmetic and the prefs write path each have exactly one home, and the one production near-duplicate found (the panel's clamp/pad/fill loop) is a minor one. The duplication that did emerge is almost entirely in the test surface written across task boundaries: two independently authored 19-token synthetic probe palettes protecting the two halves of the same swap guard, a 65-line verbatim twin of the commit "no other I/O" test, five-plus re-implementations of the source-guard file parser, and copied test bodies in prefs and doctor.
