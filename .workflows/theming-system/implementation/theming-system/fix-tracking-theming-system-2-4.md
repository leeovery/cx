## Attempt 1

ISSUES:
- `/Users/leeovery/Code/portal/internal/capture/swatch_test.go:106` and `:183-192` — expectations are constructed by calling `onCanvas` / `onTint`, the production helpers under test, so both sides of every comparison move together and the assertions are tautological. Two mutations pass GREEN:
    • `onCanvas` hardcoding a canvas (`lipgloss.Color("#0b0c14")`) instead of `th.Canvas` → a light theme's labels, captions and the `border` rule are judged against a dark reference. `TestSwatch_UsesThemeOwnCanvas` — the test named for exactly this property (§13.5's wash-out risk) — does not fail. `border` is one of the four eyeball-pinned tokens and is judged solely as foreground-on-canvas, so this breaks the gate for a pinned tint.
    • `onTint` swapping Foreground and Background → every pairing renders inverted and `TestSwatchCoversForegroundOnTintPairings` still passes.
  Composition is genuinely covered (deleting the `text.tertiary` leg, or moving `state.positive` off the tint onto the canvas, both fail correctly) — the hole is confined to the shared helpers, which is where §13.1's "only viable route to seeing a visual change" is most expensive to get wrong silently.
  FIX: build the expectations inline from the tokens, matching the `internal/tui` probe pattern (`header_test.go:21`, `project_row_anatomy_test.go:75`, `canvas_paint_test.go:17`, `model_test.go:27` — all `lipgloss.NewStyle().Foreground(tok…)`) — e.g. at `:106`
  `lipgloss.NewStyle().Foreground(c.theme.TextMuted.Color()).Background(c.theme.Canvas.Color()).Render(fmt.Sprintf("%s  %s", "border", c.theme.Border.Value))`,
  and the same shape for the five `onTint(...)` expectations at `:183-192` (keeping `.Bold(true)` on the `text.on-selection` row). The reviewer validated this fix under overlay: it passes on the clean source and fails on BOTH mutations above.
  CONFIDENCE: high

NOTES:
- (a) The four-vs-three foreground count: FOUR is correct. Golden spec §13.5 (`specification.md:1709`): "Foreground-on-tint pairings, all ≥ 4.50: `text.on-selection`, `text.secondary`, `text.tertiary` and `state.positive` on `bg.selection`". The task's Do bullet listed three (a carry-over from the pre-rename band); the acceptance criterion says "every foreground-on-tint pairing §13.5 lists". §2.5's role table backs the assignment chosen (`text.tertiary` = "selected-row path", `text.secondary` = "selected-row meta").
- (c) Dropping the mode word from the title is CORRECT and required. MV spec §16.5 pins nothing about the title; §4.7 gives the vocabulary no variant concept; §13.5:1721 is explicit that light/dark is something "the *test* needs to know, not the product"; §3.2 gives `Theme` no identity field. A `LIGHT`/`DARK` word would have had to be invented by the product.
- (b) The sample-string edits are ACCEPTABLE and are in fact a fix. Rendered and measured: max 79 cells (VHS tape is 1280px @ FontSize 16 ≈ 133 cols; also fits an 80-col terminal). All four bands are now exactly `bandWidth`=56. The OLD warning sample measured 61 cells — `padBand` only pads, never truncates, so the warning band silently overflowed the other three by 5 cells at HEAD. This is a diagnostic instrument, not shipped UI copy: §14A governs panel/CLI copy, and the old string was never verbatim production text (the real warning is "the session saver is not running"). `TestSwatchBandsCoverEveryPinnedTint` now pins the equality.
- (d) Fallback resistance is genuinely proven. Both fallback mutations are caught by three and one test respectively; neither error path can silently degrade.
- (e) The zero-log assertion is TRUE and non-vacuous as to sink wiring — the positive control at `main_test.go:186-189` confirms a theme-component record IS captured. It is not, however, sensitive to the `Discard` choice: `Loader.LoadBuiltin` (`builtins.go:137-149`) deliberately never touches the event seam, so swapping `log.Discard()` for `log.For("theme")` in `newThemeLoader` also passes. The criterion as written is met and the code does pass Discard per §12.3 — but the test would not catch a future edit that drops it. To pin it: assert the loader's logger is the discard handler, or run the control through a path that does emit.
- (f) Stale VHS tapes are genuinely Phase 3's problem. `phase-3-tasks.md:21` and its acceptance criterion `:32` assign deletion of `testdata/vhs/*.png` + `*.tape` to task 3-1 as §13.2's first bounded act. Nothing in the suite reads the tapes, so no test is broken. BUT: `testdata/vhs/contrast-validation-light.tape` still passes `--appearance light`, which the swatch now ignores — it will produce a DARK capture under a filename saying light. The two in-phase visual gates (2-5 light re-derivation, 2-7 Nord) sit BEFORE that deletion, so whoever writes those tapes must not copy from these. Same for `testdata/vhs/README.md:145`, which still documents the swatch as `--appearance`-driven (Phase 10 owns that line).
- `--theme` is silently ignored for `tui.Build` fixtures this phase: `capturetool --fixture sessions-flat --theme not-a-theme` renders fine with no error. Scoped correctly per the task, but it is a one-phase instance of the "silently renders the wrong theme at a gate" shape §13.3 argues against. Phase 3 closes it when `--theme` drives everything.
- The swatch title no longer names WHICH theme is on screen — `theme.Result.Slug` is available at the call site but discarded, and the prescribed one-arg signature has no slot for it. Correct for this task; worth reconsidering when Phase 3 widens the flag to the slug-or-path form, since a path-loaded theme has no slug at all and the canvas hex becomes the only identifying mark on the frame.
- CONVENTIONS: one violation, the same finding — `internal/tui`'s established pattern is to build the probe inline from the token, precisely so the expectation is independent of the production styler. Everything else follows house style.

## Attempt 2

ISSUES:
- `internal/capture/swatch_test.go:171-190` — the four bands are asserted only by calling `selectionBand`/`attentionBand`/`subtleBand`/`borderRule` directly; nothing asserts they reach the composed `renderSwatch` output. Only their *labels* and *captions* are checked in `out`. Three separate mutations pass fully green: deleting `b.WriteString(attentionBand(th))` (`swatch.go:186`), `b.WriteString(subtleBand(th))` (`swatch.go:196`), or `b.WriteString(borderRule(th))` (`swatch.go:205`). This is exactly the acceptance criterion "the swatch's bands cover all four pinned tints", and §13.1 makes this surface the only route to seeing a theme — a band silently missing from the gate means a pinned tint never gets judged. (`bg.selection` is caught only incidentally, by the `●` glyph check.)
  FIX: extend the existing `for label, surface := range surfaces` loop in `TestSwatchBandsCoverEveryPinnedTint` with a composition assertion alongside the width check — `if !strings.Contains(out, surface) { t.Errorf("the %s band is not in the composed swatch\n--- swatch ---\n%s", label, out) }`. Using the band helper as the subject here is legitimate (its internal styling is independently pinned by the `wantStyle` assertions); the property under test is composition, not styling.
  CONFIDENCE: high

- `cmd/capturetool/swatch_test.go:16-24` — "a built-in slug pins the swatch" asserts only `m != nil`, so the `--theme` → rendered-palette wire is never checked end to end. Replacing `capture.NewContrastValidationModel(th)` with `capture.NewContrastValidationModel(theme.Theme{})` at `cmd/capturetool/main.go:114` leaves the entire `cmd/capturetool` suite green. That is acceptance criterion "`--theme <slug>` renders that built-in's tints on its own canvas" — each half is pinned, the join is not, and a wrong-variable slip here is silent.
  FIX: in the "a built-in slug pins the swatch" subtest, assert the returned model carries tokyo-night's palette — `v := m.View(); if !strings.Contains(v.Content, "CONTRAST VALIDATION — canvas #0b0c14") { … }` (`tea.View` exposes `Content string`). Derive the expected canvas from `newThemeLoader().LoadBuiltin("tokyo-night")` rather than hardcoding `#0b0c14`, mirroring how `TestResolveTheme_DefaultsToTokyoNight` sources its expectation.
  ALTERNATIVE: assert `m.View().BackgroundColor == want.Theme.Canvas.Color()` instead — a tighter single comparison, but it only covers the OSC 11 canvas and not the rendered body; the `Content` check covers both since the title carries the canvas value. Reviewer recommends the `Content` form.
  CONFIDENCE: high

NOTES:
- The prior round's fix landed and is non-vacuous. Reviewer re-ran both mutations: `onCanvas` hardcoded to a dark canvas now FAILS `TestSwatch_UsesThemeOwnCanvas/light_theme` on both the border-label and the new border-rule assertion; `onTint` fg/bg swapped now FAILS both `UsesThemeOwnCanvas` subtests and all five pairings. `wantStyle` is genuinely independent — it builds `lipgloss.NewStyle().Foreground(fg.Color()).Background(bg.Color())` directly and references no production helper, matching `internal/tui/header_test.go`'s `tokenFgSeq` convention. The added `borderRule` assertion is a real strengthening.
- `TestCaptureTool_ThemeResolutionIsSilent` cannot currently distinguish `log.Discard()` from a real component logger, because `Loader.LoadBuiltin` (`internal/theme/builtins.go:137`) emits no events on any path. The assertion is still worth keeping as a forward tripwire and the positive control keeps the sink honest — no change wanted, just don't read it as proving the `Discard` wiring.
- `testdata/vhs/contrast-validation-{dark,light}.tape` still drive the swatch with `--appearance light|dark` and now silently capture tokyo-night in both. Explicitly deferred to Phase 3 / §13.2 — flagged only so it isn't lost.
- No guard test pins acceptance criterion 6 (`swatch.go` importing neither `internal/prefs` nor `internal/tui/theme`); it holds today and is compile-visible, but nothing stops it regressing. `internal/capture` as a whole still imports `internal/prefs` via `fixtures.go`, which is correct for this phase.
- The `contrast-validation` name is in `FixtureNames()` but `FixtureByName` rejects it (it is resolved by the tool, not as a `*Fixture`). Pre-existing, and relevant later for §13.4's fixture-enumerating swap-and-diff guard.
