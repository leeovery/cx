## Attempt 1

ISSUES:

- `/Users/leeovery/Code/portal/internal/tui/model.go:290-299` — the `canvasMode` field comment is now **false** on the path this task creates. It states "It is the painted mirror of gate.appearance: every resolution (OSC 11 reply, timeout) syncs it via syncResolvedMode" and "Under a constant (or no) nomination it is that standing fallback and nothing more — no question was asked, so it must never be read as a fact about the terminal." After a conversion `loadNewlyLiveSlot` writes it directly (`theme_panel_confirm.go:229`), the gate stays on its pinned dark, and the value *is* a classified fact about the terminal. This is the one field comment that matters most: the divergence it denies is **deliberate and permanent** (a pinned gate's `resolve` always returns false, so nothing re-syncs), and the obvious "fix the drift" edit a reader would reach for is the `syncResolvedMode` route §11.4 names as the one that re-sticks a colour in the user's terminal after Portal exits. The executor updated the two neighbouring comments (`appearanceGate.appearance:82-87`, `bgReplyArrived:463-480`) to point at `retainedCanvasAnswer`, so this is an asymmetry rather than a policy.

  FIX: Amend the second paragraph to state the conversion as the third writer — that under a constant nomination it is the standing fallback *until* §9.3's mid-session conversion records the classified reply into it (`Model.retainedCanvasAnswer`), from which point it deliberately diverges from `gate.appearance`, which stays pinned; and that the divergence must be left alone rather than re-synced, because `syncResolvedMode` is where §11.4's anchor is captured. Point the reader at `loadNewlyLiveSlot`.

  CONFIDENCE: high

- `/Users/leeovery/Code/portal/internal/capture/theme_panel_fixture_test.go:394` and `:452` — the comment was changed in this diff to claim "the fake must answer from declared values on every one of its **four** methods", and the subtest is titled "every method answers from declared values with the config paths poisoned", but the body (lines 452-482) exercises only `Open`, `Reassemble` and `Resolve`. **`ResolveSlot` is never called.** The structural scans do cover the whole file, so the no-I/O property is not at risk — but this is the same shape of doc over-claim the executor corrected in `theme_panel_commit_load_test.go`, introduced by the same diff.

  FIX: Add one call inside the existing subtest — `res, err := seam.ResolveSlot(enumeration, theme.SlotLight, "nord")` — asserting `res.Slot`/`res.Requested`/`res.Resolved` and that `res.Theme` is the injected palette, before the existing `os.Stat(missing)` poison check.

  ALTERNATIVE: Narrow the two claims to "three reachable methods" and note `ResolveSlot`'s unreachability there instead. Cheaper, but it leaves the one method with no behavioural pin at all and contradicts the fake's own doc, which argues it is implemented honestly precisely so it cannot diverge from its three siblings — the reviewer recommends adding the call.

  CONFIDENCE: medium

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant — §8.4's opposite-slot-only load, the no-commit-time-directory-read refusal, §8.5's mode-matched per-slot fallback, §12.3's undeduplicated commit-time `theme: loaded` wired onto the commit entry point alone, §9.3's answer half classified from the reply already in hand with no new query/gate, and §11.4's frozen anchor are all implemented as specified. **No drift found.** ACCEPTANCE_CRITERIA: all sixteen verified independently; every one has a test that bites. ARCHITECTURE sound.
- Mutation results, all caught: routing the classification through `syncResolvedMode` (both before and after the join) — caught behaviourally (`startupCanvasHex` moved to `#E1E2E7`/`#2E3440`) and structurally, so the anchor concern is genuinely closed; `Loader.ResolveSlot` switched to a by-name directory read — caught by `TestCommitSlotLoad_NoDirectoryRead/a_stale_slot_still_resolves_from_the_retained_parse`, and the removal helper genuinely `RemoveAll`s and re-`Stat`s so it is non-vacuous; `enumerationPass` switched to `reportSlot` (open/close emitting `loaded`) — caught by task 8-10's `TestPanelClose_EventCadence` and `TestResolveNominationFrom_EmitsNoLoadedRecord`, so the 8-10 pin still holds; `Loaded` given a dedup key — caught by `TestCommitSlotLoad_EmitsLoadedOncePerConversion` plus three `internal/theme` tests; the §7.6 fatal early return removed — caught, with the failure output showing the all-empty-token `Theme` landing in the light slot (exactly the colourless-render hazard the extra test was written for); the nil-persister guard removed — the new behavioural subtest fires on all three assertions.
- **The flagged capture-at-arrival deviation is APPROVED.** `BackgroundColorMsg.IsDark()` → `uv.BackgroundColorEvent.IsDark()` → `isDarkColor` at `ultraviolet/decoder.go:1782` — unexported, nil→dark, HSL L<0.5. Re-deriving would mean a second luminance rule plus an empty-hex special case, as claimed. The reviewer walked every reachable state: reply-before-open, reply-after-open-before-conversion (the `BackgroundColorMsg` arm sits in the unconditional cross-view switch ahead of all page/panel routing, so it always lands), nil-colour reply, no reply, and reply-after-conversion. All equivalent. The only constructible divergence requires two replies where the second is nil, and `tea.RequestBackgroundColor` is issued from exactly one site (`model.go:2576`, `Init`), so it is unreachable.
- **The M5 gap is confirmed honest.** The reviewer reproduced the exact mutation shape (source first, falling through to the embedded set when the source declines) — it survives this task's tests but fails `TestResolveByName_BuiltinNeverReadsDirectory` and `TestResolution_NoStartupEagerValidation` in `internal/theme`. The ordering is genuinely covered there. The corrected comment on `TestCommitSlotLoad_UntouchedSlotIsTheShippedDefault` is now **true**: a variant that forwards the reserved-name rejection instead of falling through *does* fail that test on `requireNoFallbackLine`, which is precisely what the corrected sentence claims.
- **Seam position verified on both halves.** `persistedSlotSlug` → `themeSetting()` → `m.themeKeys` genuinely needs the mirror, and `recomputeThemePanel` reads only `m.themeKeys` + the retained enumeration, touching neither `m.canvasMode` nor `m.nomination`.
- **The retired structural guard is genuinely subsumed.** `seamGuardedOnNilPersister` / `isNilPersisterReturn` / `packageFuncBody` had exactly one call site at HEAD, so deleting them left nothing else unpinned.
- `ResolveSlot` is a genuinely separate entry point rather than a flag on `Resolve`, and the `resolutionPass` value pairs "where a slug loads from" with "at what cadence it is reported" so a later call site cannot pair them the other way round. `enumerationLoad` being the single shared `slugLoader` for both enumeration-backed passes is what makes the shared-rule-body claim structural rather than remembered.
- One reachable permutation is untested: panel open → OSC 11 reply lands → conversion. `newLoadPanelModel`'s comment reasons that a reply always precedes the open in production, but §9.3's own no-reply case is defined as "the panel opened within milliseconds of launch", which is exactly the window where a reply can land with the panel already open. Behaviour is correct (the retention arm is unconditional and ahead of all routing) and the two bracketing cases are covered — one `deliverBackgroundReply` call short of complete rather than a defect.
- `themeEventRecords` (new, `theme_panel_commit_load_test.go:172`) and `countThemeEvents` (existing, `theme_panel_open_test.go:193`) now coexist as near-duplicates; the new one is a strict superset. Harmless, but `countThemeEvents` could become `len(themeEventRecords(...))` if a later task touches those files.
- In `TestCommitSlotLoad_LoadedNamesTheFallbackSlug` the single `fallback applied` line is actually emitted by the post-commit recompute's `Resolve`, not by `ResolveSlot` — the per-process dedup then suppresses the load path's own. The test's assertion (different slugs on the same commit) holds either way and this is correct under §12.3's dedup rule, but the ordering `commitSlot → recompute → loadNewlyLiveSlot` is what determines which site emits it.
- `TestCommitSlotLoad_NonConvertingCommitIsSilent/Enter over a constant` asserts `canvasMode` unchanged against a fixture that never received a reply, so dark==dark passes trivially there. The three adaptive subtests carry the real weight (they run on `lightBg`), and the nomination/`loaded` assertions carry the constant case.
- `.tick/tasks.jsonl` carries exactly one record change (9-6, `open` → `in_progress`); no stray edits swept in.
- CONVENTIONS otherwise followed: `var _ tui.ThemeEnumerator = themeEnumerator{}` compile assertion, no `t.Parallel()`, seam-per-1-3-methods, typed slot mapping, `golangci-lint run` clean on all four touched packages, both lanes compile.

## Attempt 2

ISSUES:

- `/Users/leeovery/Code/portal/internal/tui/model.go:300-302` — "loadNewlyLiveSlot records the classified OSC 11 reply … so from that keypress on it **IS** a fact about the terminal" is **false on the no-reply path**: `retainedCanvasAnswer` returns the §8.8 dark fallback when `bgReplyArrived` is false, so nothing was classified and nothing is a fact about the terminal. This is the mirror image of round 1's blocking clause ("must never be read as a fact about the terminal"), **overcorrected past** the case §9.3 specifies and `TestCommitSlotLoad_ConversionWithNoReplyIsDark` pins.

  FIX: qualify the clause with the fallback, e.g. "… records `Model.retainedCanvasAnswer` straight into this field — the terminal's own verdict where a reply arrived, §8.8's dark no-answer fallback where none did — so from that keypress on it is the answer *in force* rather than a value nothing ever asked for." Everything else in the paragraph (the third-writer count, the pinned-gate `resolve` claim, the permanence, the `syncResolvedMode` warning) was verified accurate — **leave it as is**.

  CONFIDENCE: high

- `/Users/leeovery/Code/portal/internal/tui/theme_panel_cursor_test.go:65-67` — the stub's `ResolveSlot` doc claims "a stub-driven conversion joins a palette the fixture chose rather than a zero Theme", but the undeclared-slot branch returns `theme.SlotResolution{…}` with a **zero `Theme`**, and that branch is **live**: proved by panicking in it — `TestSlotConfirm_ConfirmsOnEitherCase/y` reaches it (the `newArrowPanelDeps` resolution declares only a `SlotConstant` record), so that conversion joins a zero palette into the nomination.

  FIX: make the claim true — answer the fallback branch with a palette the fixture chose (e.g. `s.resolution.Nomination.Select(slot == theme.SlotDark)`, or the declared constant slot's `Theme`) instead of a zero `Theme`.

  ALTERNATIVE: keep the zero-Theme fallback and reword the comment to say the declared record is used *where one exists* and an undeclared slot answers with a zero palette. Cheaper, but it leaves a stub-driven model carrying a zero palette in a live nomination slot, which is the shape `New`'s dark seed exists to keep out of the package; the reviewer recommends the first.

  CONFIDENCE: medium

- `/Users/leeovery/Code/portal/internal/tui/theme_panel_cursor_test.go:40-42` — `slotLoads` is documented as "how a suite driven off declared values observes that a NON-CONVERTING commit asked for none at all", but **no test makes that observation**; its only read is `len(seam.slotLoads) != 1` on the fatal path (`theme_panel_commit_load_test.go:1062`). Same class as round 1's second finding: a comment claiming coverage the suite does not have.

  FIX: realize the claim — assert `len(seam.slotLoads) == 0` after a non-converting commit in the stub-driven pair fixture (`newCommitPairPanelModel`, `/Users/leeovery/Code/portal/internal/tui/theme_panel_commit_slot_test.go`), which also adds a second, seam-level guard for acceptance criterion 7 alongside the log-silence one.

  ALTERNATIVE: reword to the capability form its sibling already uses ("so a split-union fixture *can* state whether the conversion's load ran"). Zero risk but adds no coverage; the reviewer recommends the assertion.

  CONFIDENCE: medium

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant — §8.4 (opposite slot only, embedded-set-first, retained-enumeration lookup, no commit-time directory read, the one load outside construction), §8.5 (mode-matched per-slot fallback), §5.8 (the panel's parse is the only source), §12.3 (undeduplicated commit-time `theme: loaded` naming the *resolved* slug; `fallback applied` naming the failed nomination), §9.3 (answer half classified off the retained reply — no new query, race or gate), §8.8 (gate resolves once; a late reply never re-themes), §11.4 (`startupCanvasHex` never re-captured), §7.6 (degrade, never escalate). ACCEPTANCE_CRITERIA: all sixteen re-verified independently. CONVENTIONS followed. ARCHITECTURE sound.
- **All three issues are documentation/test-scaffolding only. No production behaviour change is required.**
- 14 mutations run, **every one caught**, all reverted, tree byte-identical afterwards, full unit lane green: opposite-slot flip → `LoadsTheOppositeSlot`; directory read via `byNamePass` → `NoDirectoryRead`/`StaleSlot`; `reportSlot` on the re-resolution pass → `PanelClose_EventCadence` + the whole load suite; dedup on `Loaded` → `EmitsLoadedOncePerConversion` (+ theme-package guards); `Loaded(r.Requested)` → `LoadedNamesTheFallbackSlug`; gate-read in `retainedCanvasAnswer` → `ConversionUsesTheRetainedAnswer`/`ConversionIssuesNoQuery`; retention skipped while the panel is open → *only* the new late-arrival subtest (it genuinely bites); nil-persister guard removed → the rewritten `NilPersisterIsInert` subtest; `joinNomination` order flip; swallowed §7.6 fatal → `BrokenBuiltinDegrades`; `persistedSlotSlug` off `RawKeys` → `UntouchedSlotIsTheShippedDefault`; `syncResolvedMode`/`captureStartupCanvasHex` in the conversion → the §11.4 suite *and* the structural caller scan; widening the load onto `commitSlot` → the whole suite.
- The **"no production behaviour changed" claim is independently confirmed by mutation**, not diff inspection: the fix-round edits sit in comments and added test bodies, and no assertion elsewhere was moved, weakened or deleted (`git diff -- '*_test.go'` shows exactly one removed assertion — the retired `seamGuardedOnNilPersister` structural check — and its behavioural replacement bites, proven by dropping the nil-persister guard).
- The `canvasMode`/`gate.appearance` divergence is real, deliberate and **structurally permanent** — verified rather than accepted: a pinned gate's `resolve` short-circuits on `resolved()`, `arm()` is a no-op on it, and `syncResolvedMode` is reached only behind a `true` return.
- `countThemeEvents` folding into `len(themeEventRecords(...))` is behaviour-preserving at all four call sites (`theme_panel_close_test.go:465,468,476,483`, `theme_panel_open_test.go:356,365,398`) — same predicate, same walk, `len(nil) == 0`.
- The new late-arrival subtest genuinely drives reply-after-open (it asserts `!m.bgReplyArrived` before delivering) and is the *only* test covering that ordering — the retention mutation failed nothing else.
- Acceptance criterion 4 ("no commit-time directory read on any branch"): the removed-directory fixture covers the stale-slug and untouched-slot branches; the unresolvable→fallback branch is covered structurally (one `pass.load` serves nomination and fallback alike) rather than with a removed directory. Not worth an extra case.
- `splitThemeEnumerator.slotLoads` (`theme_panel_commit_recompute_test.go:458`) is recorded and never read; its doc says "can state", so it is accurate, but the field is currently unused scaffolding.
- `internal/capture` wires no `ThemePersister` anywhere, so the fake's "unreachable in a capture" claim holds; the fixture test's structural scans are file-level and cover the fourth method inherently, and the added behavioural call now runs before the `os.Stat` poison check.
- `ThemeEnumerator` is now 4 methods against CLAUDE.md's "1-3"; task-mandated and justified in-source, not a violation.
- The commit cadence is a *type* (`commitPass`/`reportSlot`) rather than a call-site decision, so a later caller cannot pair loader and reporter the wrong way round.

## Attempt 3

ISSUES:

- `/Users/leeovery/Code/portal/internal/tui/theme_panel.go:725-730` — `inForceSlot`'s doc is made **FALSE** by this task, at the **one production consumer** of the value this task redefines. It states "The answer is the gate's SINGLE resolution (§8.8), read off the model…" and "On a gate that was never armed the value is the standing dark no-answer fallback, which is the right answer for the same reason it is the right canvas." After a conversion the gate was never armed (constant → pinned) yet `m.canvasMode` is the classified reply — light on a light terminal — which is precisely the state `TestCommitSlotLoad_ConversionUsesTheRetainedAnswer` drives through `closeThemePanelForTest` and asserts.

  The executor updated the two places that **describe** the divergence (`model.go:290-312`, `appearance_gate.go:80-88`) and missed the one place that **denies** it. The failure mode this invites is concrete: a later reader trusting this doc "simplifies" the call to `m.gate.appearance` and every converted light-terminal user closes onto the dark slot.

  FIX: amend the second paragraph — keep the no-re-query rationale, and replace the never-armed sentence with the post-9-6 truth: on a gate that was never armed the value is the standing dark no-answer fallback **UNTIL** §9.3's mid-session conversion records the classified reply into `Model.canvasMode` (`loadNewlyLiveSlot` → `Model.retainedCanvasAnswer`), from which point the mode read here is deliberately **NOT** the gate's — pointing at the `canvasMode` field doc, which already carries the "leave the divergence alone" warning.

  ALTERNATIVE: drop the provenance paragraph entirely and defer to the `canvasMode` field doc. Cheaper and unfalsifiable, but it loses the "a query here would reopen the race" rationale that stops someone re-asking OSC 11 at close time; the reviewer recommends the amendment.

  CONFIDENCE: high

NOTES:

- VERDICT context: SPEC_CONFORMANCE **conformant — no drift**. §8.4 (opposite slot only; embedded-set-first; retained-enumeration lookup; no commit-time directory read; "the one theme load outside construction"), §8.5 (mode-matched per-slot fallback via the shared `resolveSlot` body), §5.8 (the panel's retained parse is the only source both passes read — `enumerationLoad` is literally one `slugLoader` shared by `enumerationPass` and `commitPass`), §12.3 (undeduplicated commit-time `theme: loaded` carrying the RESOLVED slug + slot, wired onto `Loader.ResolveSlot` alone), §9.3 (answer half classified off the reply already in hand — no new query, race or gate), §8.8 (`Init` issues the OSC 11 query unconditionally on every branch; a pinned gate's `resolve` short-circuits so nothing re-syncs), §11.4 (`startupCanvasHex` never re-captured), §7.6 (degrade before both assignments, never escalate). ACCEPTANCE_CRITERIA: **all sixteen met**, each verified against code and re-proven by mutation. CONVENTIONS followed. ARCHITECTURE sound.
- **13 load-bearing mutations re-run, all restored, every one caught**: opposite→assigned slot; widening the load onto `commitSlot` (fires `requireNoSlotLoad` at `theme_panel_commit_slot_test.go:203,228` with the exact recorded ask — **it genuinely bites**); `commitPass` reporter → `reportFallback`; dedup on `Loaded`; `canvasMode` read off `m.gate.appearance`; routing the answer through `syncResolvedMode` (caught behaviourally **and** by the caller scan); `persistedSlotSlug` off `RawKeys`; `joinNomination` members swapped; §7.6 guard removed; nil-persister guard removed; `bgReplyDark` never classified; arrival flag ignored; `commitPass` bypassing the shared `resolveNamed` ladder. Full unit lane green; `golangci-lint run` clean on all four touched packages.
- **The stub fix is genuine.** `newArrowPanelDeps` declares `ConstantNomination(target.Theme)` with only a `SlotConstant` record, so `stubThemeEnumerator.ResolveSlot`'s fallback branch **is live** (reached by `TestSlotConfirm_ConfirmsOnEitherCase` et al.) and `Nomination.Select` on a constant returns that fixture palette for either answer — non-zero. The same fix on `splitThemeEnumerator` is correct but currently dead (see below).
- The "no production behaviour changed" claim **holds as far as it is checkable here** — the production diff vs HEAD is the task's implementation plus comments (`model.go` adds only the `bgReplyDark` field and its single assignment; `appearance_gate.go` and `internal/theme/events.go` are comment-only), and current behaviour is proven by the 13 mutations rather than by the report. The reviewer could not diff against the round-1 snapshot (all rounds live in one uncommitted tree), so the claim is corroborated behaviourally, not textually.
- `/Users/leeovery/Code/portal/internal/theme/events.go:30-31,44-45` — "The other two belong to later phases and have no site yet" is **stale**: `theme: commit failed` (`cmd/theme_persister.go:76`) and `theme: appearance migrated` (`cmd/config.go:330`) both have sites now. Pre-existing (Phase 6) and outside this task's scope, but it sits two lines above the table this task edited.
- `/Users/leeovery/Code/portal/internal/tui/model.go:300-302` — "the terminal's own verdict where a reply arrived" collapses a third state the codebase treats as reachable: a **nil-colour reply arrives** and records dark, which is `IsDark`'s nil answer rather than a verdict. `retainedCanvasAnswer`'s own doc states all three cases correctly and the field points at it, so this is a summary imprecision rather than a misdirection.
- `/Users/leeovery/Code/portal/internal/tui/theme_panel_commit_recompute_test.go:458,487-503` — `splitThemeEnumerator.slotLoads` is recorded and never read, and its `ResolveSlot` is never called (both split fixtures open on shapes that raise no confirm: `newSlotSplitPanelModel` on a pair, `newSplitPanelModel` on a constant that no test converts). The doc's capability framing ("can state") is accurate, so this is unused scaffolding rather than an over-claim — but nothing exercises the fallback branch the round-2 fix was applied to there.
- `TestCommitSlotLoad_LoadedNamesTheFallbackSlug`'s single `fallback applied` line is emitted by the post-commit recompute, with the load's own suppressed by §12.3's dedup; the test says so explicitly and asserts the property (different slugs on one commit) rather than the site. Correct, and worth keeping stated if the commit ordering is ever touched.
- The 4-method `ThemeEnumerator` exceeds CLAUDE.md's "1-3 methods" guidance but is task-mandated and argued in-source. Named subtests throughout, no `t.Parallel()`, compile-time seam assertions, typed `prefs.ThemeSlot` → `theme.Slot` mapping with no third value reachable (`SaveThemeSlot` rejects out-of-range and both dispatch sites pass the two constants), exported-surface test updated (`Loader.ResolveSlot`).
- `m.canvasMode` post-conversion has exactly one production consumer (`inForceSlot`), which is what makes the answer observable at `Esc` — and that is where the one issue above sits.
