## Attempt 1

ISSUES:
- `cmd/open_theme_commit_test.go:236-245` / `:363-384` — the constant-direction binding is vacuous with respect to the very rule the task exists to bind. Verified by mutation in an isolated copy: changing `WithConstant` to `return RawKeys{Theme: slug, Light: k.Light, Dark: k.Dark}` (i.e. dropping prefs' clear entirely) leaves BOTH round-trip tests green — only the isolated literal-based tests in `internal/theme` and `internal/tui` fail, which are exactly the tests the task deems insufficient. The cause is structural, not a fixture accident: `ResolveSetting`'s `theme`-wins tiebreak makes stale in-memory slots invisible to badges AND to the row set (a file-less "ghost" slot mints no row under a constant), so a single constant commit has no observable difference. The drift becomes observable on the NEXT commit — the panel then shows `● light` on a slug the file has no key for, which is verbatim the failure mode the task names. Consequently `assertBadgesMatchPersistedKeys`'s doc claim at line 234-235 ("a mirror that drifted from the write moves a badge or drops one") is false as delivered, and the "proven non-vacuous by deliberate temporary breakage of each direction" claim does not hold for the constant direction (it does hold for a wrong-slug mirror: mutating `WithConstant` to mirror `k.Dark` fails with `the panel marks map[aurora:●], want map[sunset:●]`).
  FIX: add one cmd round-trip that commits a constant and THEN a slot in the same panel session, asserting the disk bytes and `assertBadgesMatchPersistedKeys` after each. The reviewer ran exactly this against the shipped code (passes) and against the un-cleared `WithConstant` (fails with `the panel marks map[aurora:● light sunset:● dark], want map[sunset:● dark tokyo-night-day:● light]`):
    seed `{"theme_light":"aurora","theme_dark":"<nordSlug>"}` → open → `arrowToPreviewedCanvas(roundTripChosenCanvas)` → `themePanelConstantKey` → assert `prefsOnDisk{Theme: sunset}` + badges → `themePanelDarkSlotKey` + `themePanelConfirmKey` → assert `prefsOnDisk{ThemeDark: sunset}` + badges.
    Then make the helper's doc claim true (it is the follow-on commit, not the commit itself, that surfaces a constant-direction drift). Note the ordering trap if you extend `TestThemePanelCommit_EnterRoundTripsAConstantToPrefs` in place instead: its relaunch assertion expects a constant on disk, so the follow-on must come after it or live in its own test.
  ALTERNATIVE: expose the model's keys to cmd via a test-only accessor and assert them directly against `LoadThemeKeys()`. Stronger (it binds the keys rather than a projection of them) but it punctures `internal/tui`'s encapsulation for a test, and the task explicitly sanctioned the badge route — the reviewer recommends the two-commit sequence.
  CONFIDENCE: high

- `internal/theme/setting.go:60-69` — `WithSlot`'s third-value arm is the one place the in-memory rule and the write path diverge. `prefs.SaveThemeSlot` rejects anything that is not one half of the pair and writes NOTHING (`internal/prefs/store.go:459-461`); `WithSlot(SlotConstant, slug)` — which is also `WithSlot(theme.Slot(0), slug)`, i.e. the zero value a caller who forgets to set the selector passes — is not a no-op: it clears the constant and replaces nothing, producing a keys value the write path would never have created. Unreachable from `mirrorThemeSlot` today, and pinned as intentional by the new table case, but it is newly exported API on a public token type whose export list is guarded.
  FIX: type the selector with the existing total two-valued `theme.Member` (`WithMember(m Member, slug string) RawKeys`), which makes the invalid argument unrepresentable, deletes the third-arm doc paragraph and the "a slot naming no half" test case, and gives exact parity with `SaveThemeSlot` across every representable input. `mirrorThemeSlot` keeps its shape — `if slot == prefs.SlotLight { return keys.WithMember(theme.MemberLight, slug) }` — matching the existing `oppositeThemeMember` translation in `theme_panel_confirm.go:250`.
  ALTERNATIVE: keep the `Slot` signature and make the ignored arm a genuine no-op (`default: return k`) so it cannot half-apply the rule. Cheaper, but it keeps an exported method that silently swallows one third of its argument's domain; the reviewer recommends the `Member` selector.
  CONFIDENCE: medium

COMMENT_CORRECTIONS:
- internal/tui/theme_panel_commit.go:312 — re-authored comment makes a claim about a test, which the comment discipline forbids (the guard is real — `theme_panel_commit_slot_test.go:610-637` — but the claim is falsified by any rename or move of it).
  OLD: // are the two constants, which the typed-slot source guard pins.
  NEW: // are the two constants.
- internal/theme/theme_test.go:212-213 — the added paragraph claims "the latest addition" nine lines after the existing paragraph claims the same of `InForceKeys`; both cannot be true, and the claim is falsified by the next export either way.
  OLD: // RawKeys.WithConstant and RawKeys.WithSlot are the latest addition, and they
// are exported for the same kind of reason InForceKeys was: the setting's mutual
  NEW: // RawKeys.WithConstant and RawKeys.WithSlot are exported for the same kind of
// reason InForceKeys is: the setting's mutual

NOTES:
- `WithConstant` ≡ `SaveTheme` and `WithSlot(SlotLight|SlotDark)` ≡ `SaveThemeSlot` in every representable case, including the empty-slug edge (both persist/mirror verbatim with no special case) and the already-empty-constant case (neither special-cases it); the ONLY divergence is the `SlotConstant`/out-of-range argument covered above.
- The failed-write path is confirmed untouched — both handlers still return before the mirror and the recompute on a non-nil error, and the nil-persister guard still returns before `applyCommitResult`.
- `mirrorThemeSlot` changed from a no-default switch to `if light … else dark`, so a hypothetical third `prefs.ThemeSlot` would now be mirrored INTO the dark half rather than replacing nothing. Unreachable today (the store's guard errors first) and it matches the existing `oppositeThemeMember` idiom, so no action — but the comment's "THE TWO HALVES OF THE PAIR ARE THE WHOLE OF IT" is doing more work than the code, which is really "anything not light is dark".
- `RawKeys` now has two methods with inconsistent receivers (unnamed on `WithConstant`, `k` on `WithSlot`). The unnamed form is deliberate and documented; flagged only because the naming skill asks for one receiver name per type.
- `roundTripUnionSlugs`' `append(theme.BuiltinSlugs(), …)` is safe — `BuiltinSlugs` returns a freshly allocated slice with len == cap each call.
