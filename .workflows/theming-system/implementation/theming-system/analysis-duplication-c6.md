AGENT: duplication
FINDINGS:
- FINDING: The panel's "writes nothing" proof is written twice — once for open, once for close
  SEVERITY: medium
  FILES: internal/tui/theme_panel_cursor_test.go:538-583, internal/tui/theme_panel_close_test.go:342-416
  DESCRIPTION: `TestPanelOpen_WritesNothing` and `TestPanelClose_WritesNothing` each carry the same two subtests — "a present prefs.json survives byte for byte" and "an absent prefs.json stays absent and the … directory is untouched" — and the bodies are verbatim-parallel for ~45 lines: the same `{"session_list_mode":"by-project","theme":"sunset"}` constant, the same `os.WriteFile` + `t.Setenv("PORTAL_PREFS_FILE", …)` staging, the same `writeThemeFileForTest(t, dir, "sunset.theme", "not-a-colour")` / `"#101010"` fixtures, the same read-back comparison and the same config-dir/themes-dir entry counts. The only differences are the model constructor (`themeCursorModel` vs `newClosePanelModel`), the action under test, and the noun in each error string ("opening" vs "closing"). This is textbook copy-paste across a task boundary: the open task and the close task each authored the same no-write proof in isolation. The pair will drift the moment either the prefs-file seam or the theme-file staging changes — one suite keeps proving something about a fixture the other no longer uses.
  RECOMMENDATION: Extract one helper into the existing shared home `internal/tui/theme_testing_test.go` — e.g. `requireNoPrefsOrThemesWrite(t *testing.T, verb string, act func(t *testing.T, dir string, keys theme.RawKeys))` — owning both subtests, the persisted-JSON constant, the two directory fixtures and the assertions. `TestPanelOpen_WritesNothing` and `TestPanelClose_WritesNothing` then each pass their own model constructor + action closure and the verb used in the messages.

- FINDING: The doctor `--fix` down-server regression test is cloned into the theme suite
  SEVERITY: medium
  FILES: cmd/doctor_fix_theme_test.go:560-610, cmd/doctor_test.go:1427-1490
  DESCRIPTION: `TestDoctorFix_ExistingRepairsUnchanged`'s second subtest ("the hazard guard still defers on a down server") is a near-verbatim clone of the pre-existing `TestDoctorFixDownServerPrunesProjectsButNotHooks`: identical `seedHooksJSON` / `seedProjectsJSON` staging, an identical `DoctorDeps` literal (same `StateDir`, `ServerRunning: func() bool { return false }`, `HookLister: fakeHookLister{keys: []string{}}`, `Detector`, `Resolve`) including the same inline comment, and the same three assertions (hooks byte-identical, `projects.json` no longer containing `goneDir`, `ErrDoctorUnhealthy`). Only the `ThemesDir` seam and the advisory-suffix expectations are new. The sibling subtest in the same test already routes through the shared `seedStalePruneFixture` / `assertStalePrunesApplied` helpers, which shows the shared-fixture route exists and was simply not taken here. Any change to the down-server repair contract now has to be made in two places, and the theme copy is the one most likely to be missed.
  RECOMMENDATION: Add a `downServerDeferFixture(t)` helper beside `seedStalePruneFixture` in cmd/doctor_test.go returning `(deps *DoctorDeps, hooksPath, projectsPath, goneDir string)`, plus an `assertDownServerDeferral(t, hooksBefore, hooksPath, projectsPath, goneDir)` assertion helper. Have `TestDoctorFixDownServerPrunesProjectsButNotHooks` and the theme subtest both call them; the theme subtest then only sets `deps.ThemesDir` and adds its advisory-suffix checks.

- FINDING: The stub theme-seam `Deps` / `theme.Resolution` literals are restated across four panel test files
  SEVERITY: medium
  FILES: internal/tui/theme_panel_arrow_test.go:132-151, internal/tui/theme_panel_commit_recompute_test.go:510-537, internal/tui/theme_panel_commit_test.go:106-127, internal/tui/theme_panel_commit_slot_test.go:136-162
  DESCRIPTION: Four separate fixture builders hand-roll the same two `theme.Resolution` shapes. The constant shape — `Nomination: theme.ConstantNomination(target.Theme)` plus a single `{Slot: theme.SlotConstant, Requested: cursorSlug, Resolved: cursorSlug, Theme: target.Theme}` record — appears verbatim in `newArrowPanelDeps` and in `newSplitPanelModel`. The adaptive-pair shape — a `ConstantNomination(dark.Theme)` plus `{Slot: SlotLight, Requested/Resolved: light.Slug…}` and `{Slot: SlotDark, …}` — appears verbatim in `commitPairPanelDeps` and in `newSlotSplitPanelModel`. Each site also re-assembles the same surrounding `Deps{Lister: fakeLister{}, Theme: …, ThemeSource: &fakeThemeSource{…}, ThemeKeys: …}` skeleton. Four independent statements of "what the panel's seam answers with" is exactly the drift surface `theme.Badges`/`inForceSlot` depend on: a fixture that gets `Requested` vs `Resolved` the wrong way round in one file quietly makes only that suite's badges wrong.
  RECOMMENDATION: Move `constantResolution(slug string, th theme.Theme) theme.Resolution` and `pairResolution(light, dark theme.Row) theme.Resolution` into the existing shared `internal/tui/theme_testing_test.go`, and add a `stubPanelDeps(source *fakeThemeSource, nomination theme.Nomination, keys theme.RawKeys) Deps` skeleton beside them. The four builders then differ only in the rows they list, their `enumeration`/`reassembled` priming, and whether a persister is wired.

- FINDING: `LoadFile` and `LoadPath` duplicate the read → parse → Result tail
  SEVERITY: low
  FILES: internal/theme/load.go:156-166, internal/theme/load.go:190-200
  DESCRIPTION: Both entry points end with the identical nine lines: `os.ReadFile(path)` → `return Result{}, unreadable(err)` → `parseThemeBytes(data)` → `return Result{}, rejection` → a `Result` carrying `Theme` and `Source`. Only the presence of `Slug` differs. `LoadPath`'s doc comment states "Rungs 3 to 6 are the same code LoadFile and LoadBuiltin run", which is true of rungs 4-6 (`parseThemeBytes`) but not of rung 3 — the read and its `unreadable` classification are a copy, so the comment already over-claims the sharing. A change to how a read failure is classified (or to what `Result` carries) has to land in two places while the comment says it lands in one.
  RECOMMENDATION: Extract `loadContent(path string) (Theme, []byte, *Rejection)` covering rungs 3-6, and have `LoadFile` call it after its two filename rungs (adding the slug to the Result) and `LoadPath` call it directly. The doc comment's "same code" claim then becomes literally true.

- FINDING: The two notice-band arbiters restate the flash-tier ordering
  SEVERITY: low
  FILES: internal/tui/notice_band.go:392-400, internal/tui/notice_band.go:461-468
  DESCRIPTION: `activeNoticeBand` and `activeProjectNoticeBand` both open with the identical two-arm prologue — `themeFlashClaim()` then `flashClaim()`, each returning the claim verbatim — and both carry a comment stating that the filter line sits between the two tiers. The claim predicates are already single-sourced, but the *order* between them is written twice, and that order is the load-bearing rule (a theme flash must outrank the filter line on both pages, or the failed-commit report is destroyed rather than deferred). Projects' arbiter is new in this feature; the ordering was copied into it rather than shared. A third tier added later reaches whichever arbiter the author edits.
  RECOMMENDATION: Extract `func (m Model) flashSlotClaim() (noticeBandRole, string, bool)` next to `flashClaim`/`themeFlashClaim`, holding the theme-tier-then-ordinary-flash order (and the comment explaining the filter line's position between them). Both arbiters then open with one call and keep only their own page-specific arms.

- FINDING: The nav/page keymap entries are now written out three times
  SEVERITY: low
  FILES: internal/tui/keymap.go:106-107, internal/tui/keymap.go:153-154, internal/tui/keymap.go:206-207
  DESCRIPTION: `{Key: "↑↓", HelpKey: "↑/↓", Action: "navigate", HelpAction: "Move selection"}` and `{Key: "^↑/↓", Action: "page", HelpAction: "Next / prev page"}` are byte-identical in `sessionsKeymap`, `projectsKeymap` and — added by this feature — `themePanelKeymap`. That is the third instance, which is where the Rule of Three bites: this file exists to be the single binding source, yet a copy edit to the nav glyph or its help label now needs three edits, and one descriptor silently diverging changes what the help body shows on one surface only. No comment in the file states the restatement is deliberate.
  RECOMMENDATION: Declare the pair once — e.g. `func navKeymapEntries() []keymapEntry` returning the two non-core entries — and have all three descriptors lead with `append(navKeymapEntries(), …)`. (`previewKeymap`'s page entry has a different `HelpAction` and stays as it is.)

- FINDING: The commit-key guard and idempotence assertions are duplicated across the Enter and slot suites
  SEVERITY: low
  FILES: internal/tui/theme_panel_commit_test.go:542-588, internal/tui/theme_panel_confirm_test.go:364-412, internal/tui/theme_panel_commit_test.go:507-517, internal/tui/theme_panel_commit_slot_test.go:583-593
  DESCRIPTION: Two overlaps in the same area. First, `TestPanelEnter_UnselectableRowWritesNothing` and `TestSlotConfirm_UnselectableRowAsksNothing` stage the same three subtests over the same `committableThemeSlug` guard, with identical fixture bodies — `rows := []theme.Row{arrowValidRow(t, arrowSlug(0), 0), arrowInvalidRow(arrowSlug(1))}` / `{Source: theme.SourceBuiltin, Theme: arrowPalette(t, 1)}`, `newCommitPanelModel`, `list.Select(1)`, and the same two `t.Fatalf` precondition checks — differing only in the keypress and the post-conditions. Second, `TestPanelEnter_RepeatCommitIsIdempotent` and `TestPanelSlotCommit_RepeatIsIdempotent` close with the same three assertions verbatim (message kind, nil cmd, frame unchanged), including the same message strings.
  RECOMMENDATION: Add `stageUncommittableCursor(t, kind)` (returning the model, persister and rows for the unselectable / slugless cases with their precondition checks) and `requireRepeatCommitInert(t, m, cmd, once string)` to `internal/tui/theme_testing_test.go`. Each test then keeps only its own keypress and its own outcome assertions.

- FINDING: The prefs tolerant-decode case tables are duplicated per accessor
  SEVERITY: low
  FILES: internal/prefs/theme_keys_test.go:96-144, internal/prefs/migration_marker_test.go:530-578
  DESCRIPTION: `TestLoadThemeKeys_TolerantDecode` and `TestLoadMigrationState_TolerantDecode` carry near-identical case tables of degenerate prefs.json inputs (missing file, empty file, corrupt JSON, empty object, valid JSON carrying none of the keys, keys present but empty), the same `filepath.Join(t.TempDir(), "nonexistent", "prefs.json")` / seed-file loop, the same zero-value assertion, and each is followed by the same EISDIR read-error-propagation test. The two accessors are being asserted to share one policy, so the policy is stated twice and the two tables have already diverged (one carries `a top-level array` / `a top-level string`, the other does not) — which means they no longer prove the same thing about the shared decode.
  RECOMMENDATION: Collapse to one table-driven test over the accessors — a slice of `{name string, load func(*prefs.Store) (any, error), zero any}` covering `LoadThemeKeys` and `LoadMigrationState` (and `Load`) — in the black-box `prefs_test` file, since all three accessors are exported. `internal/prefs/read_shared_test.go` already pins the shared read half; this closes the decode half the same way.

SUMMARY: The production theme code is unusually well factored — the loader ladder, the resolution passes, the badge table, the panel geometry and the restyle path each have exactly one home, and the seams are composed rather than restated. The duplication that survives is concentrated in the test suites, where separately-executed tasks each authored the same fixture staging and the same proofs: two ~45-line no-write blocks across the open/close suites, a cloned doctor `--fix` down-server test, and four hand-rolled stub-seam `Deps` literals. Three small production restatements (the `LoadFile`/`LoadPath` tail, the two notice-band arbiters' tier ordering, and the third copy of the nav keymap entries) are worth folding while the code is fresh.
