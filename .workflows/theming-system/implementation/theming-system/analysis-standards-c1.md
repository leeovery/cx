AGENT: standards
FINDINGS:
- FINDING: Production comments are written against the specification's section numbering and the plan's task/phase ids
  SEVERITY: medium
  FILES: internal/theme/load.go:117, internal/theme/builtins.go:28, internal/theme/resolve.go:93, internal/theme/badge.go:141, internal/theme/setting.go:76, internal/theme/union.go:13, internal/theme/resolution.go:29, internal/theme/events.go:44, internal/tui/theme_panel.go:124, internal/tui/theme_panel_commit.go:45, internal/tui/theme_panel_confirm.go:126, internal/tui/theme_panel_footer.go:23, internal/tui/builtin_themes.go:31, internal/tui/notice_band.go:418, cmd/doctor_theme.go:21, cmd/theme_persister.go:27, internal/prefs/store.go:467, internal/capture/fixtures.go:61, internal/capture/theme_fake.go:24
  DESCRIPTION: code-quality.md's Comments section names three things that may NEVER appear in a comment, and this implementation carries all three at scale. (1) Spec-section citations: ~2,200 `§x.y` references across 118 non-test production files, the overwhelming majority added by this feature (untouched packages carry ~194 total). (2) Workflow vocabulary: ~50 `Phase N` / `task N-M` citations inside the theming files specifically — e.g. `internal/theme/resolve.go:93` "it is what task 5-7 hands over when the themes-directory path could not be resolved", `internal/theme/builtins.go:28` "PRODUCTION constants rather than test-file literals because Phase 5 consumes them", `internal/capture/theme_fake.go:28` "Task 8-8's panel OPEN applies the theme its Resolve...". (3) The design argument itself, sometimes as verbatim spec quotation — `internal/tui/notice_band.go:418-424` embeds a block-quoted §14A paragraph, and many doc comments re-argue rejected alternatives at length rather than stating the conclusion the code needs. The rule's own stated reason is the load-bearing one here: "The comment must hold true for a reader with no knowledge of the process that produced the code, long after its artifacts are archived." The moment `.workflows/theming-system/` is archived, every `§9.13` and `task 8-9` becomes a dangling pointer into a document the reader cannot open, and the re-argued debate becomes unverifiable narration that no compiler or test checks. The density is measurably above the codebase's own norm: 56–71% comment lines in the new theming files (theme_panel.go 69%, theme_panel_commit.go 71%, resolution.go 67%, union.go 62%) versus 37–56% in comparable untouched files (restore.go 37%, grouping.go 43%, detect_inside.go 53%, session_item.go 55%).
  RECOMMENDATION: Strip `§x.y`, `Phase N` and `task N-M` tokens from production comments, restating whatever claim survives in its own terms — "the rejection ladder short-circuits at the first failure" rather than "§6.2's ladder", "the shipped fallback pair" rather than "§8.5's per-slot fallback". Delete the block-quoted spec paragraphs outright (notice_band.go:418-424 is the clearest case) and keep only the conclusion the code depends on. Where a comment exists solely to re-argue a rejected alternative, delete it; the rationale lives in the design artifacts by rule. Tests are out of scope for this cleanup only insofar as the same rule applies there too — but production is where the archived-pointer problem bites.

- FINDING: The slide-over ships outside the spec's pinned column budget and two-row header cost
  SEVERITY: low
  FILES: internal/tui/theme_panel.go:57, internal/tui/theme_panel.go:58, internal/tui/theme_panel.go:143-160, internal/tui/theme_panel.go:250-260
  DESCRIPTION: §9.1, §9.8 and §14A each state the panel's budget as a fact the rest of the design resolves against — "a fixed preferred width of ~24–30 columns", "the ~24–30 column budget", "in the panel the wording is a layout constraint as much as a copy choice — it has to fit 24–30 columns". Shipped values are `themePanelPreferredWidth = 34` / `themePanelMinWidth = 27`. Separately, §9.1 states "The header therefore costs two rows, which is what §9.8's minimum-height rule (header + footer + one row) resolves against"; the implementation derives the header cost from the page's whole header block plus its section-header block (themePanelHeaderRows), which is more than two rows and correspondingly raises §9.8's minimum-height floor. Both changes are deliberate and visually gated (a `theme-panel-chrome-revision` reference frame was exported and committed for them), and §9.8 does delegate "exact thresholds ... at implementation" — so this is drift in the spec's stated numbers rather than a defect. The consequence worth naming is that the wider panel covers more of the main footer than §9.1's explicitly "accepted" three entries, and the raised height floor makes the panel refuse to open on a slightly taller terminal than the spec's arithmetic predicts.
  RECOMMENDATION: Record the widened width pair and the derived header cost as spec amendments alongside the three already listed in §15.1, so §9.1/§9.8/§14A stop reading as the shipped contract for a reader who checks the numbers.

- FINDING: capturetool's default theme is a duplicated literal rather than the shipped-default constant
  SEVERITY: low
  FILES: cmd/capturetool/main.go:48
  DESCRIPTION: §13.3 pins the flag's default as "`tokyo-night` when the flag is omitted, matching the shipped dark default. Every capture taken without the flag depends on it." The tool declares `const defaultThemeSlug = "tokyo-night"` while `theme.DefaultDarkSlug` is exported for exactly this purpose — internal/theme/builtins.go:35 documents it as the single definition of the shipped pair, and the same file's `BuiltinSlugs()` is derived rather than restated on precisely this reasoning ("a hand-written list would be a second source of truth that silently disagrees"). A later change to the shipped dark default would leave every unflagged capture rendering a palette the product no longer defaults to, with nothing failing — the drift class the constant was introduced to close. The neighbouring `themeFileExtension` restatement is justified in-source (internal/theme keeps its extension unexported) and is not the same problem.
  RECOMMENDATION: Set `defaultThemeSlug` from `theme.DefaultDarkSlug` rather than from a literal.
SUMMARY: Spec conformance is high — the 19-token vocabulary and rename table, the six-rung rejection ladder and its reason vocabulary, the built-in files and their §7.3/§7.4 values (with §7.7's re-derivation figures recorded in-file as required), the prefs two-decode/RMW/migration rules, the resolution-and-fallback ordering, doctor's advisory class and pinned copy, export, the log catalogue, the panel's keymap scopes, all nine required panel fixtures and every §13.6 guard test all match what was decided. The findings are a pervasive code-quality.md comment violation (spec-section and task/phase citations plus quoted design argument), and two smaller drifts from spec-stated numbers and single-sourcing.
