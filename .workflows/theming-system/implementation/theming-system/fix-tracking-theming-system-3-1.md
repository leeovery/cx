## Attempt 1

ISSUES:
- `testdata/vhs/trail/` — 43 committed capture PNGs across 20 screen directories (4.8 MB) survive. Reviewer's decision: **§13.2 covers them.** They are committed capture images of a design that no longer exists (they depict the pre-rename palette), which is precisely the class §13.2 retires — "everything that exists today as an image or tape is deleted… delete today's images and tapes once at the start". The task's keep-list is enumerative (`internal/capture` fixtures, the harness, `README.md`, `reference/`) and does not include `trail/`. Critically, **nothing later in the plan is scoped to remove them**: task 10-10's acceptance is "no `*.png` or `*.tape` **from this feature** remains", and these are from the prior MV feature — so leaving them here strands them permanently while 10-10 inherits an unowned directory its inventory step does not expect.
  FIX: delete `testdata/vhs/trail/` in this task, as part of the same bounded first act.
  ALTERNATIVE: if the browsable per-task history is judged worth keeping, take and **record** that decision explicitly now (a line in `testdata/vhs/README.md`), the way task 10-10 requires for `LOCK-IN.md` — "a deliberate decision, taken and stated, not a sweep". Tradeoff: retention costs 4.8 MB of images of a superseded palette and contradicts the harness's own retention rule; deletion loses a history recoverable from git. Reviewer recommends deletion — the images are pre-rename and could not survive the token change anyway, which is §13.2's stated reason for the class.
  CONFIDENCE: medium

- `TestColourless_NoTokenReachesTheWriter` (named in the task's Tests list) was not written, and no equivalent assertion exists. `internal/tui/colourless_nocolor_test.go:114` asserts a colourless frame carries no *background* SGR (`frameHasAnyBackgroundSGR`) and `:133` asserts glyph-backed state, but nothing asserts the frame carries no *foreground* colour SGR — i.e. that no token reaches the writer at all. The substitution did not touch any `if colourless { return bare }` branch (verified in the normalised diff), so this is a missing guard rather than a live defect, but it is the assertion the named test exists to pin and it is cheap.
  FIX: add `TestColourless_NoTokenReachesTheWriter` in `internal/tui/colourless_nocolor_test.go` beside `TestColourless_FillEmitsNoCanvasBackground`, driving `colourlessTestModel(t, 90, 24).View().Content` and asserting no 24-bit colour SGR of either kind appears — a foreground counterpart to the existing `frameHasAnyBackgroundSGR` helper (scan for `38;2` / `38;5` as it already does for `48;`), so both halves of the carve-out are pinned by the same shape of check.
  CONFIDENCE: high

- 147 test failure messages now format a whole `theme.Theme` through `%v`, e.g. `internal/tui/row_style_helpers_test.go:72` (`"rowBgStyle(th=%v sel=%v col=%v)"`) and `internal/tui/session_row_anatomy_test.go:175` (`"[%v] selected row missing…"`). `theme.Theme` has no `String()`, so each prints all 19 `{name value}` pairs — roughly 500 characters of noise per failure line, where the old `theme.Mode` printed `0`/`1`. It costs nothing while green and is actively hostile on failure, in the suite that is the regression net for the remaining phases. 87 of these sites are raw `for _, th := range []theme.Theme{…}` loops with no name in hand; 22 already sit in named tables.
  FIX: replace the `%v` argument with a short label at the affected sites — either promote the raw slice loops to the named-table form the file already uses elsewhere (`{"dark", testDarkTheme(t)}`), or add a one-line `themeLabel(th theme.Theme) string` helper in `theme_testing_test.go` returning `"dark"`/`"light"` off `th.Canvas.Value` and pass that. The second is the minimal-churn option and mechanical.
  ALTERNATIVE: leave the format verbs and accept the noise. Tradeoff: zero churn now against a 500-char dump every time one of these fails for the next seven phases. Reviewer recommends the `themeLabel` helper.
  CONFIDENCE: medium

NOTES:
- The rename map was verified **exhaustively, not spot-checked**: the reviewer extracted the ordered sequence of `theme.MV.<Field>` references from every changed file at HEAD, applied the §2.4 map, and diffed positionally against the working tree. All ~110 changed files matched except 15, every one explained (comment-only refs, the deleted `previewBorderColorToken`, the collapsed `canvasHexFor` branch, the retired negative legs, the four re-expressed token tables). No token is mapped to a wrong successor anywhere.
- **The one-delta claim is independently proven.** The reviewer compared MV's 20 tokens at HEAD against the two embedded `.theme` files: all 19 surviving tokens carry byte-identical dark *and* light values (0 mismatches); only `border.footer` (dark `#20232E`) is dropped. The single edited golden confirms it empirically — one SGR run changed, all other bytes identical, `wantLight`/`wantNoColour` untouched and still passing, which also proves §7.7 moved nothing and that uppercase canonicalisation is SGR-neutral.
- **The six retired negative legs were right to retire.** All six asserted "the frame must NOT carry `border.footer`" against a token that, post-consolidation, *is* the token the frame carries — they would assert a value against itself and fail. `TestHelpModalDividerToken` replaced its leg with a strictly stronger per-line assertion. No coverage lost.
- The four re-expressed token tables keep the same role set and theme axis via `tokenNamed`, which resolves through `Theme.All()` so a vanished role fails loudly. No coverage lost.
- **The three judgement calls are all correct and correctly recorded in-source.** Token-*selecting* helpers genuinely need the theme now that a token carries its value. `canvasHexFor` becoming theme-agnostic is mandated by §11.2 and correctly stops short of §11.4's startup-hex retention (3-3's).
- Integration lane: one failure, `TestRenameRebootHook_PaneProcessKeptRunning` — confirmed unrelated and flaky (passes in isolation and on a full re-run; `internal/restore` has zero dependency on `internal/tui`/`internal/theme`).
- Three parameters became dead as a consequence of "swap the slot, don't add one": `th` is unused in `panel.go:93` (`panelFrameStyle`), `notice_band.go:137` and `:148`. In panel.go it is forwarded through four callers. Defensible as signature uniformity and invisible to lint; **worth dropping opportunistically in Phase 4's sweep rather than churning now**.
- `WithCanvasMode` is an exported option whose parameter type (`canvasAppearance`) is now unexported, so it is unusable from outside `internal/tui`. No external caller today; could be unexported if task 3-4 does not need it.
- `newPreviewModelForHelpers` (`pagepreview_helpers_test.go:22`) and the literal at `pagepreview_chrome_test.go:194` leave `th` zero, so those renders are now silently colourless where they previously rendered dark. Every consumer strips ANSI, so nothing is lost today — but they are residual instances of exactly the hazard the seeds elsewhere close.
- `loadBuiltinThemePair()` re-parses both embedded files on every `New`, and `defaultDarkTheme()` re-parses one on every `NewPreviewModel` (every `Space` keypress). Microseconds against embedded bytes, and task 3-2 replaces the holder — noted as a known property.
- `testdata/vhs/README.md`'s Contents table and `testdata/vhs/LOCK-IN.md:23` now name files that no longer exist. Both are explicitly task 10-10's to reconcile — correctly out of scope here.
- Nothing Phase 4 owns was pre-empted; the derived-style sweep and the swap-and-diff guard are untouched.

## Attempt 2

ISSUES:
- `internal/tui/theme_source_guard_test.go:83-109` (`themeReference`, specifically the `pkg.Name == "theme"` check at :98) — `TestNoPackageLevelThemeVar` does not catch the regression it is named for. It only matches a `SelectorExpr` rooted at the identifier `theme`, so it sees a `theme.`-qualified declaration but is blind to a var initialised from an **in-package** theme source. The reviewer probed five shapes against the real guard in an isolated copy:
  - CAUGHT: `var previewBorderColorToken theme.Token = defaultDarkTheme().AccentMode`
  - CAUGHT: `var previewBorderColorToken = theme.Token{Name: ..., Value: ...}`
  - **MISSED**: `var previewBorderColorToken = defaultDarkTheme().AccentMode`
  - **MISSED**: `var previewTheme = defaultDarkTheme()`
  - **MISSED**: `var previewBorderStyle = lipgloss.NewStyle().Foreground(defaultDarkTheme().AccentMode.Color())`

  Because `theme.MV` no longer exists, the three MISSED shapes are now the *only* ways `previewBorderColorToken` can be reintroduced. The task's Tests entry specifies this test as "AST scan … **catching a returning `previewBorderColorToken`**" and the AC as "`internal/tui` declares no package-level `theme.Theme` or `theme.Token` var (guard test)" — neither holds today. This matters beyond bookkeeping: Phases 4–9 all add renderers and models to this package, and §13.4's real net (the swap-and-diff guard) is explicitly out of scope here, so this guard is the only thing standing until Phase 4 lands.

  FIX: extend `themeReference` to also flag a package-scope var whose initialiser calls an in-package theme source. Pre-scan the same `centralisedColourSites(t)` file set for `*ast.FuncDecl`s whose result list contains a `*ast.SelectorExpr` with `X.Name == "theme"` — that captures `defaultDarkTheme` and `loadBuiltinTheme` — plus one transitive step for a locally declared struct type whose fields are `theme.X` (captures `loadBuiltinThemePair` via `builtinThemePair`). Then walk each package-scope `ValueSpec` initialiser for a `*ast.CallExpr` naming one of those functions, reporting at the same position with the same message. Keep the existing `theme.`-selector rule as the first arm. Add a mutation note in the doc comment recording which shapes are covered, so the next reader can tell the guard's reach without re-probing it.

  ALTERNATIVE: a one-line name-based rule — flag any package-scope var in the glob whose initialiser contains an identifier matching `(?i)theme`. It catches all three missed shapes with none of the closure machinery, but it enforces a naming convention rather than a structural property, so a future helper called e.g. `activePalette()` would slip through. Reviewer recommends the closure version: the file set is small, the scan is already parsing these files, and it stays correct as Phases 4–9 add helpers.

  CONFIDENCE: high (that the gap is real and the closure fix closes it); medium on which variant to pick.

NOTES:
- **PRIORITY CONCERN RESOLVED — the `header.go` reconstruction is VERIFIED CORRECT, zero code difference.** The reviewer mechanically transformed `git show HEAD:internal/tui/header.go` and diffed against the working tree. Token sequence positional: 6 refs at *identical line numbers* 93/103/130/192/194/206, all correctly mapped. All 7 signature slots swapped in the same position, none added. All 15 call-site arguments correct. The only difference is a deliberate 2-line `headerStyle` doc reword, which is correct — the old phrasing described a parameter that no longer exists. Independently corroborated by mutation: changing one header token fails `TestHeaderBlock_RendersWordmarkCaretSubtitleRule` on both `/dark` and `/light`.
- The suspected differing line is a doc comment and the reviewer named it: `header.go:96-97` (`headerCanvasBg`) and `:134` (`blankCanvasRow`) retain HEAD's now-stale *"for the mode"* prose, describing a removed parameter. Cosmetic only. The retained dotted names in the constants block are **not** a defect — comment prose was deliberately left alone project-wide in this diff.
- **All three fixes landed and are non-vacuous.** `trail/` deleted (131 total deletions: 87 png + 44 tape, flat + `trail/`); `reference/` (26 tracked files), `README.md`, `LOCK-IN.md` survive; zero tracked png/tape remains. The new colourless test was proven non-vacuous by three of the reviewer's own mutations, including one in `rowTokenStyle` (the delegate, not the header) — the fg half covers well beyond `headerStyle`, and the two halves are genuinely independent. `themeLabel` + 184 wraps verified by an independent `go/packages` type walk: 3770 printf calls, 9100 args, 1264 `theme.Theme`-typed idents, 184 `themeLabel` calls, **0 unwrapped**; proven non-vacuous by unwrapping one site.
- **One-delta claim re-verified independently** after the reconstruction. All 19 surviving tokens byte-identical in **both** dark and light between HEAD's MV and the two `.theme` files, so §7.7's second class is empty. Exactly one changed ANSI golden across the whole diff — the footer rule — with every other byte of that 1.2 KB expectation identical.
- Integration lane: one failure, `cmd/bootstrap TestTransientListPanesHelpers_Smoke/passes_through_unrelated_tmux_commands` — "TempDir RemoveAll cleanup: directory not empty". That subtest calls `IsolateStateForTest` + `tmuxtest.New` **without** `RegisterStateDirTeardownGuard`, i.e. precisely the teardown flake CLAUDE.md documents. Passes on re-run; the working tree changes no file under `cmd/`. Unrelated pre-existing flake.
- Five assertion messages read awkwardly — `appearance_detection_test.go:240,250` and `canvas_paint_test.go:33,43,50` test `m.canvasMode` but print `themeLabel(m.activeTheme)` and say `want testLightTheme(t)`. The *state* is consistent (`New` calls `syncResolvedMode()` after options, so `WithCanvasMode` does re-select `activeTheme`); wording only.
- `loadBuiltinThemePair()` re-parses both embedded files on every `New`, `defaultDarkTheme()` one per `NewPreviewModel` — ~600 parses across the suite, 2 in production per launch. Task 3-2 retires the holder.
- `loadBuiltinTheme` discards `found`/`rejection`, so a broken embed yields a zero `Theme`. The in-source comment reasons this against §7.6's build-time guarantee and declines an interim fallback as a second unspecified resolution policy — the right call for this task's scope.
- The `pflag` `go.mod` drift did not recur; `go.mod`/`go.sum` hashes unchanged after both lanes.
- **No Phase 4 pre-emption** (no swap entry point, no swap-and-diff guard, no wider derived-style sweep) and **no task 3-3 pre-emption** (`canvasHexFor` survives as a theme-agnostic pass-through with an in-source comment naming 3-3 as its retirement; `startupCanvasHex` not introduced).
