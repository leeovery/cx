AGENT: architecture
FINDINGS:
- FINDING: The panel→persister→prefs.json commit round trip is never composed in a test; the seam is held together by AST source guards
  SEVERITY: medium
  FILES: cmd/theme_persister_test.go:287, cmd/theme_enumerator_test.go:253, cmd/open.go:1091, internal/tui/theme_panel_commit.go:123, internal/tui/theme_panel_commit.go:282
  DESCRIPTION: The feature's central user-visible promise — press `Enter`/`d`/`l` in the panel, the choice lands in prefs.json, the next launch renders it — is verified in three disjoint pieces that never meet. `internal/tui` drives the commit keys against a fake `ThemePersister`; `cmd/theme_persister_test.go` calls `themePersister.CommitTheme` / `CommitThemeSlot` directly against a real store; and the join between them is asserted *syntactically* by two AST tests (`TestOpenTUI_ThemePersisterWiredOnlyWithAStore` counts `cfg.themePersister = …` assignments inside an `if prefsStore != nil` block; `TestOpenTUI_ThreadsThePanelConstructorSlots` counts `cfg.themeKeys` / `cfg.themeEnumerator` assignments). Nothing anywhere constructs a model with the real persister and asserts what reaches disk. The composed path crosses four packages and three type translations (`themeRowItem.Row.Slug` → `prefs.ThemeSlot` → `prefsFile.ThemeLight/ThemeDark` → `theme.RawKeys` on the next launch), and the AST guards prove none of it — they prove an assignment exists, and they break on any ordinary refactor (renaming the local `prefsStore`, extracting the wiring into a helper) without the behaviour having changed. `TestThemePanelOpen_WiredThroughBuildTUIModel` shows the pattern that is missing: it drives `t` through a real `themeEnumerator` end to end. There is no counterpart for the write half.
  RECOMMENDATION: Add one behavioural test in `cmd` that wires `newThemePersister(store)` and `newThemeEnumerator(loader)` into `buildTUIModel` against a temp prefs file, presses `t` then `Enter` (and separately `d`), asserts the resulting prefs.json keys, and feeds those keys back through `themeResolution` to assert the theme that would render on relaunch. That single test subsumes what both AST wiring guards are standing in for, and they can then be dropped rather than maintained.

- FINDING: `theme.Loader` is one type carrying four unrelated responsibilities
  SEVERITY: medium
  FILES: internal/theme/load.go:19, internal/theme/union.go:222, internal/theme/union.go:260, internal/theme/resolution.go:128, internal/theme/resolve.go:40, internal/theme/enumerate.go:59
  DESCRIPTION: `Loader` exposes ten methods spanning four distinct jobs: file parse/validate (`LoadFile`, `LoadPath`, `LoadBuiltin`), directory enumeration (`Enumerate`), panel row-model assembly (`Open`, `Reassemble`), and theme-setting resolution (`ResolveByName`, `ResolveNomination`, `ResolveNominationFrom`, `ResolveSlot`). Only the first job matches the type's name. Every consumer must hold the whole surface for a small slice of it — `cmd/theme.go` uses one method, `internal/tui/builtin_themes.go` one, `cmd/capturetool` two, `cmd/doctor_theme.go` two, the panel adapter four — and the accretion is why `LoadPath`'s receiver is unused and documented as such ("It stays a method so a caller reaches every entry point through the one Loader it holds"), which is a convenience argument, not a state one. The shared state that genuinely justifies co-location is narrow (the reserved-slug set, the `BuiltinSource` seam, the event-dedup logger) and is already injected. The union half is also the odd one out conceptually: `Open`/`Reassemble` build a *panel row model* (`Row`, `Union`, ordering, labels) rather than loading anything.
  RECOMMENDATION: Keep the package boundary (§13.3's "internal/theme owns the assembly" is satisfied either way) and split the type: leave parse/enumerate/resolve on `Loader`, and move `Open`/`Reassemble` plus `Row`/`Union`/`Badges` onto a separate value constructed from a `Loader`. The `tui.ThemeEnumerator` seam then maps one-to-one onto one type, and doctor/export/capturetool stop holding methods they must not call.

- FINDING: Light/dark is modelled four times across three packages, and the type is discarded at the `Nomination` boundary
  SEVERITY: medium
  FILES: internal/theme/nomination.go:98, internal/tui/theme_panel.go:806, internal/tui/appearance_gate.go:46, internal/theme/resolution.go:12, internal/prefs/store.go:177, internal/tui/theme_panel_confirm.go:244
  DESCRIPTION: The same light/dark axis is represented by `tui.canvasAppearance`, `theme.Slot`, `prefs.ThemeSlot` and `theme.Badge`, connected by seven hand-written mappings (`inForceSlot`, `oppositeThemeSlot`, `fallbackSlugFor`, `slotBadge`, `slotAttr`, `themeSlotAttr`, `persistedSlotSlug`) plus `joinNomination`'s positional light/dark ordering. The `prefs.ThemeSlot` copy is genuinely forced (prefs is a no-logging leaf and cannot import `theme`), but `canvasAppearance` and `theme.Slot` are both visible from the same package, and the seam between them throws the type away entirely: `Nomination.Select(dark bool)` takes an untyped boolean, so `m.nomination.Select(m.canvasMode == appearanceDarkCanvas)` flattens a named enum into a bool that the callee immediately re-interprets. A boolean parameter is exactly the shape the quality standard names, and here it sits on an exported cross-package API where the concrete type is known at design time; `joinNomination(newlyLive, loaded, assigned)` compounds it by relying on argument *position* to say which palette is light. `theme.Slot` also carries a third value (`SlotConstant`) that is meaningless as a gate answer, which is why the bool was reached for — but the fix is a narrower type, not no type.
  RECOMMENDATION: Give `Nomination` a typed selector rather than a bool — either accept `theme.Slot` and treat `SlotConstant` as "the constant" (which `Select` already does implicitly), or export a two-valued member type alongside `Nomination` — and have `AdaptivePair`/`joinNomination` take that same type instead of relying on positional light-then-dark. Once the boundary is typed, `inForceSlot`'s inline `canvasAppearance → theme.Slot` mapping becomes a single conversion at the gate rather than a rule restated per call site.

- FINDING: Panel display copy is split across two packages with no stated rule
  SEVERITY: low
  FILES: internal/theme/badge.go:56, internal/theme/badge.go:66, internal/tui/theme_panel.go:88, internal/tui/theme_panel.go:97, internal/tui/theme_panel_message.go:43
  DESCRIPTION: Every user-facing string the theme panel renders lives in `internal/tui` as a pinned constant — the `Themes` header label, `⚠ dir unreadable`, `clear constant %s?  y / n`, `⚠ couldn't save theme`, the six flashes, the footer labels — except the badge vocabulary (`●`, `● light`, `● dark`, `● both`), which lives in the leaf `internal/theme` behind `Badge.Text()`. Nothing outside the panel renders a badge: doctor renders its own slot words (`light`/`dark`/`both` in `cmd/doctor_theme.go:69`) and the log component renders its own again (`slotAttr`, `themeSlotAttr`). So the leaf now carries a rendering concern for exactly one consumer, while `Reason`'s strings in the same package are legitimately shared by three surfaces. The result is that a reviewer asking "where does the panel's copy live?" has no answer that holds.
  RECOMMENDATION: Keep `theme.Badge` and `theme.Badges()` (the derivation is genuinely a fact about the setting) but move the four glyph strings to `internal/tui`'s panel copy block beside the header label and message-slot constants, rendering the enum at the row delegate. If the strings stay, state the rule explicitly — shared-across-surfaces copy in `theme`, panel-only copy in `tui` — so the next surface knows which side it belongs on.

- FINDING: One optional seam carries a bespoke reflection-based nil guard; its fourteen siblings do not
  SEVERITY: low
  FILES: internal/tui/theme_seams.go:79, internal/tui/build.go:282, internal/tui/model.go:1046
  DESCRIPTION: `WithThemeEnumerator` routes through `liveThemeEnumerator`, which uses `reflect` to reject an interface holding a typed nil pointer. Every other optional seam in `Deps` — `Enumerator`, `Reader`, `PreviewAttacher`, `ModePersister`, `ThemePersister`, `Detector`, `AckChannel` — is gated by a plain `if deps.X != nil` in `Build` and carries the identical exposure with no guard. Neither production (`cmd/open.go` passes a struct value) nor any fixture (`internal/capture` passes a struct value) can produce the shape being defended against, so this is reflection on a DI path for a state no call site can reach, and it is inconsistent with the pattern the surrounding seams establish. The inconsistency is the cost: a reader now cannot tell whether the plain gates on the other fourteen are an oversight or a decision.
  RECOMMENDATION: Drop the reflection and gate `ThemeEnumerator` the way its siblings are gated (`if deps.ThemeEnumerator != nil`), leaving `openThemePanel`'s existing nil check as the runtime guard. If typed-nil defence is genuinely wanted, apply it once to all optional seams rather than to one.

- FINDING: Model-level theme state is sixteen flat fields on an already-oversized model.go, and Deps now carries nine capture-only seeds
  SEVERITY: low
  FILES: internal/tui/model.go:256, internal/tui/model.go:295, internal/tui/model.go:387, internal/tui/model.go:498, internal/tui/build.go:29, internal/tui/build.go:138
  DESCRIPTION: The panel's own state was correctly grouped into the `themePanel` struct, but the model-level theme state was not: `themePersister`, `themeCommitFailed`, `themeKeys`, `themeEnumerator`, `nomination`, `canvasMode`, `activeTheme`, `gate`, `startupCanvasHex`, `bgReplyArrived`, `bgReplyDark`, `flashOrigin` and three `initialTheme*` capture seeds are sixteen sibling fields spread across a 5,700-line `model.go` (up ~700 from this feature), with the invariants that bind them — `canvasMode` deliberately diverging from `gate.appearance` after a mid-session conversion, `startupCanvasHex` deliberately *not* moving with `activeTheme` — documented only in prose on the individual fields. `Deps` has correspondingly grown to 42 fields of which nine are capture-harness-only seeds (three added here), so the production wiring struct and the fixture-authoring struct are the same type with no separation between them.
  RECOMMENDATION: Group the model-level theme state into a struct beside `themePanel` (the same move that already worked for the panel) so the divergence invariants are stated once on a type rather than field by field, and group the `Initial*` capture seeds into a nested `Deps.Capture` struct so the production seam and the fixture seam are visibly distinct. Both are mechanical and neither changes behaviour.
SUMMARY: The theme package boundaries, the panel's file split, the seam idiom and the fixture coverage are all sound and consistent with the codebase's established patterns; the issues are accretion rather than misplacement. The two worth acting on are the missing end-to-end commit round trip (currently substituted by AST wiring assertions) and `theme.Loader` having grown into a four-responsibility type; the rest are localised boundary/typing tidy-ups.
