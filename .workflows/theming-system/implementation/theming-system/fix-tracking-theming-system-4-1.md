## Attempt 1

ISSUES:
- **`internal/tui/model.go:1427-1433` — the `bubbles/list` residue entry's justification "None reaches a rendered frame … the empty / no-match body is replaced wholesale" is FALSE for `Styles.NoItems`, and the value it excuses is a construction-time offender squarely inside this task's stated scope.**

  `Styles.NoItems` is assigned once by `list.New` → `DefaultStyles(true)` (`bubbles/v2@v2.1.0/list/style.go:79`) with a hardcoded `#626262`, and is rendered by `populatedView` (`list/list.go:1213`) whenever a list has zero visible items outside the `Filtering` state. On the **Projects page it is reachable and rendered**: `viewProjectList` gates the empty-body replacement on `m.projectListEmpty() && !m.commandPending` (`model.go:4592-4594`), so the command-pending + zero-projects state falls through to bubbles' own body. The reviewer drove that state through `Build` and captured the raw run:

  ```
  "\e[38;2;98;98;98mNo saved projects.\e[m                    …"
  ```

  Not hypothetical: `internal/tui/model_test.go:2346-2374` is an existing production-wiring test asserting exactly this frame renders. Task 4-3's swap-and-diff guard **cannot cover it either** — `#626262` is neither theme A nor theme B, and no fixture renders the state — so this is precisely the "residue undocumented, guard blind" outcome §11.2 warns about. It is the same defect class the executor *did* fix for `Styles.Title` ("keeping a third-party default palette — a colour belonging to no theme — out of a themed model"), except `Styles.Title` provably never reaches a frame while this one does.

  FIX: Re-point `Styles.NoItems` from the active palette on the restyle path — add it to the `canvasHelpStyles`/`colourlessHelpStyles` pair (or a sibling helper called from the same two branches of `applyCanvasMode`/`applyProjectCanvasMode`), foreground `TextMuted` over `Canvas` on the coloured path and fully unset under `colourless`, mirroring `canvasPaginationDots`/`colourlessPaginationDots`. Add a matching assertion to `TestRestylePath_RepointsListOwnedStyles` in the `probedLists` loop (`restyle_repoint_test.go:180-230`), same shape as the `Styles.HelpStyle` assertion at line 183. Then remove `NoItems` from the residue block and tighten the remaining sentence so it no longer makes a blanket "none reaches a rendered frame" claim — state the per-style reason: status bar disabled via `SetShowStatusBar(false)`; spinner never appended because `showSpinner` is false; built-in help disabled via `SetShowHelp(false)`; `Styles.Filter` genuinely dead in bubbles v2; `ArabicPagination` only fires when the dot row exceeds the list width.

  ALTERNATIVE: close the render path instead — drop the `&& !m.commandPending` gate at `model.go:4593` (or add a command-pending-aware empty body) so Portal's own `No projects yet` body replaces bubbles' wholesale, making the existing residue reason true. Arguably the better *product* outcome (it also removes bubbles' copy, which §11.1's empty-state design already replaced everywhere else), but it changes user-visible copy on a tested path and is a §11.1 empty-state decision rather than a §11.2 colour-completeness one — so it belongs in its own task. Reviewer recommends the first approach, optionally noting the copy leak as a separate follow-up.

  CONFIDENCE: high (that the claim is false and the value renders — verified empirically); medium (on which fix to take).

NOTES:
- **The package-scope sweep was re-run independently and is correct.** A `go/ast` walk over all 39 non-test files in `internal/tui`: 12 package-level var specs, zero colour-bearing under the task's own classifier. The classifier discriminated on **colour, not scope** — `nameBase` and `projectNameBase` are `lipgloss.Style` package vars correctly left unflagged because they carry only `Bold(true)`. `internal/capture` has one package var (`errFixtureFatal`), colour-free. `previewBorderColorToken` and `theme.MV` are gone from the tree.
- **Both reported construction-time offenders are real**, and the reviewer's own second AST pass (struct fields typed `lipgloss.Style` / `theme.Theme` / `theme.Token` / `color.Color` / `*Styles` across `internal/tui`, `internal/capture`, `internal/theme`, plus every once-assigned library cache) found no others beyond the `NoItems` mis-classification above.
- **All 15 per-member assertions mutation-verified by the reviewer** (via `go build -overlay`, no repo writes). Every one fails when its member is removed from the restyle path, with an isolated, correctly-attributed message. Notably: dropping only the two `l.Paginator.{Active,Inactive}Dot` re-feeds fails **exactly** those four assertions, both lists — the §11.2 trap is genuinely proven. The synthetic-palette design holds up: 19 tokens, three-decimal-digit channels so SGR cores are fixed-width and non-substring, unique within and disjoint across the pair, all 19 fields populated so no token falls through to `lipgloss.Color("")`.
- **(d) The `Styles.Title` "unset, not re-pointed" reasoning and the byte-invisible claim both check out.** `titleView` only renders `Styles.Title` when `showTitle && filterState != Filtering`, and both pages replace line 0 wholesale via `replaceHeaderLine` in every non-`Filtering` branch. Padding is preserved so `SetWidth`'s measurement is unchanged. The reviewer independently verified byte-invisibility rather than taking it on report: **all 17 non-swatch fixtures** rendered through `tui.Build` under HEAD's `model.go` vs the working tree give identical SHA-256 digests and identical lengths for every frame.
- **Documented deviation worth recording:** the acceptance criterion says every §11.2 member gets "an assertion it carries the **new** theme's colour". The `Styles.Title` assertion instead asserts it carries *no* colour. That is the right call given nothing paints the title box, and it is documented at the assertion and on `stripListTitleColours` — but it is a documented deviation, not literal compliance.
- `Styles.HelpStyle` is itself never rendered — `helpView()` is only called when `showHelp`, and both lists call `SetShowHelp(false)`. Re-pointed and asserted because §11.2 names it; no defect, but the member is dead in render, same as the residue entries.
- **Two minor imprecisions in the residue block, non-blocking:** `DefaultFilterCharacterMatch` is `Underline(true)` only (colour-free — over-inclusion, harmless), and `ArabicPagination` is not structurally unreachable — `paginationView` switches to it when the dot row is wider than the list width, i.e. above ~116 pages at 120 columns. Practically unreachable, but the blanket wording overstates it.
- **Not enumerated in the residue block:** `l.Help.Styles.*` (help.Model's own colour-bearing defaults, assigned by `help.New()` inside `list.New`) and `spinner.Model.Style`. Both unreachable for the same `SetShowHelp(false)` / `showSpinner == false` reasons already given — a completeness gap in the record rather than a defect.
- §11.1's restyle path stays O(1), no `rebuildSessionList` call, no rebuild. §13.4's "no structural guard" honoured. §11.4's `startupCanvasHex` residue entry is correct. No panel, no third `bubbles/list` instance — `list.New(` appears exactly twice in production.
- `stripListTitleColours` collapses a four-site duplication that had already drifted (the Sessions coloured branch was the odd one out at HEAD). Re-pointing `preview.th` from `applyCanvasMode` puts the preview on the same single chokepoint rather than adding a second path.
