AGENT: duplication
FINDINGS:
- FINDING: Every package hand-rolls its own AST source-guard scaffolding
  SEVERITY: medium
  FILES: cmd/open_theme_nomination_test.go:335, internal/tui/nomination_test.go:329, internal/theme/leaf_guard_test.go:196, internal/capture/theme_panel_message_fixtures_test.go:511, internal/tui/theme_source_guard_test.go:105, internal/tui/theme_source_guard_test.go:269, internal/tui/restore_source_guard_test.go:296, internal/theme/broken_builtin_test.go:387, internal/prefs/appearance_api_guard_test.go:49
  DESCRIPTION: The theming work added a large family of source guards (no-path-resolution, no-package-level-theme-var, single-log-binding, no-production-BuiltinSource, appearance-API-is-gone, ApplyTheme call-site scans, panel-discard-site scans, …). The file *enumeration* half is already consolidated in `internal/portalbintest` (`PackageGoFiles` / `GoSourceFiles` / `ProjectRoot`), but the *parse* half is re-authored per package. `cmd`'s `parsePackageFilesByName` (open_theme_nomination_test.go:335) and `internal/tui`'s `parsePackageFiles`/`parsePackageFilesByName` (nomination_test.go:329) are the same twelve-line body — enumerate, `parser.ParseFile`, key by `filepath.Base`, `t.Fatalf` on either error — and `internal/theme`'s `parseThemeSources` (leaf_guard_test.go:196) is a third copy that differs only in returning a `[]parsedThemeSource` carrying its own `token.FileSet`. They have already drifted in ways nothing forces: `cmd` and `tui` parse with `parser.SkipObjectResolution` while `internal/theme` parses with mode `0`; `cmd` shares one `FileSet` across the package while `internal/theme` mints one per file. A second, repo-wide shape is likewise triplicated — resolve the project root, enumerate, relativise, skip self/exempt paths, then read-or-parse — in internal/prefs/appearance_api_guard_test.go:49, internal/theme/broken_builtin_test.go:387 and internal/tui/restore_source_guard_test.go:296 (with internal/tui/theme_source_guard_test.go:269 holding an imports-only variant of the same walk). A third layer sits on top: the `for _, decl := range file.Decls` → `*ast.FuncDecl` → `ast.Inspect` → `*ast.CallExpr` preamble is written out at ~12 sites (cmd/open_theme_nomination_test.go:224 and :317, cmd/prefs_translation_test.go:397, cmd/doctor_persisted_theme_test.go:684, cmd/doctor_theme_union_test.go:399, internal/tui/theme_panel_commit_test.go:354, internal/tui/theme_panel_confirm_test.go:1209, internal/tui/theme_panel_close_test.go:702, internal/theme/resolve_test.go:826, internal/capture/theme_swap_guard_test.go:381). Each guard's *query* is legitimately its own; the parse-and-walk plumbing under it is not, and it is the part that silently diverges.
  RECOMMENDATION: Extend the existing test-only home — `internal/portalbintest`, already touched by this work and already the owner of the enumeration primitives — with the two shapes every guard needs: a `ParsePackageFiles(t, dir) map[string]*ast.File` (one FileSet, one parse mode, one failure message) and a `ParseTree(t, root) []ParsedFile` carrying `{RelPath, Fset, File}` for the repo-wide scans. Add one call-walk iterator beside them (`ForEachFuncCall(file, func(fnName string, call *ast.CallExpr) bool)`) and route the ~12 preambles through it. Delete `parsePackageFilesByName` from `cmd` and `internal/tui`, `parseThemeSources`/`themeSourceFiles` from `internal/theme`, `packageSourceFiles` from `internal/capture`, and `allGoFiles`/`forEachGoFile`/`parseProductionFiles`/`repoRoot` from `internal/tui`, keeping only each guard's own predicate.

- FINDING: The two-built-in subtest table is restated at 18 sites across internal/tui
  SEVERITY: medium
  FILES: internal/tui/footer_test.go:77, internal/tui/header_test.go:19, internal/tui/header_test.go:225, internal/tui/help_modal_frame_test.go:23, internal/tui/help_modal_test.go:338, internal/tui/modal_footer_test.go:179, internal/tui/multi_select_banner_test.go:32, internal/tui/pagination_dots_test.go:58, internal/tui/panel_test.go:17, internal/tui/projects_footer_test.go:82, internal/tui/projects_header_test.go:26, internal/tui/section_header_test.go:29, internal/tui/section_header_test.go:289, internal/tui/unsupported_banner_test.go:46, internal/tui/canvas_cell_background_test.go:142, internal/tui/canvas_paint_test.go:54, internal/tui/help_modal_test.go:386, internal/tui/modal_blank_screen_test.go:105
  DESCRIPTION: Converting the render tests from a light/dark appearance parameter to a whole `theme.Theme` left the same anonymous-struct table copied verbatim in 12 files (14 occurrences): `for _, tc := range []struct{ name string; th theme.Theme }{{"dark", testDarkTheme(t)}, {"light", testLightTheme(t)}} { t.Run(tc.name, …) }`. A second variant over the answer rather than the palette — `{ name string; appearance theme.Member }{{"dark", theme.MemberDark}, {"light", theme.MemberLight}}` — is copied in a further 4 files. That is ~120 lines of pure scaffolding whose only content is "run this against both shipped built-ins", and it is exactly the kind of table that goes stale in one place: the pair it enumerates is `theme.DefaultDarkSlug`/`DefaultLightSlug`, which `internal/theme` already single-sources, and `internal/theme/contrast_test.go` has already moved to auto-enumerating the embedded set for the same reason. The helpers these tables call (`testDarkTheme`/`testLightTheme`) already live in one shared place; only the loop around them was left per-file.
  RECOMMENDATION: Add two iterators to internal/tui/theme_testing_test.go beside `testDarkTheme`/`testLightTheme` — `forEachBuiltinTheme(t, func(t *testing.T, th theme.Theme))` and `forEachCanvasMode(t, func(t *testing.T, m theme.Member))`, each running the `dark`/`light` subtests itself — and rewrite the 18 sites as a single call taking the body as a closure. Nothing else about the assertions changes, and the built-in pair the suite renders against then has one declaration.

- FINDING: Doctor re-implements the slot→word mapping its own comment forbids restating
  SEVERITY: low
  FILES: cmd/doctor_theme.go:60, cmd/doctor_theme.go:277, internal/theme/resolution.go:36
  DESCRIPTION: `themeSlotLight` / `themeSlotDark` are correctly derived from `theme.Slot.AttrName()` at cmd/doctor_theme.go:60, under a comment stating that the light/dark words are read off `theme.Slot`'s own mapping "rather than restated". `persistedThemeSlotLabel` (cmd/doctor_theme.go:277) then restates the mapping anyway: its `switch key.Slot { case theme.SlotLight: …; case theme.SlotDark: …; default: "" }` is the body of `Slot.AttrName` (internal/theme/resolution.go:36) re-authored, including the "a constant has no name" empty-string arm. The two vars exist solely to feed that switch, so the derivation buys nothing — a slot added to the vocabulary would compile here and silently render as the constant's empty label.
  RECOMMENDATION: Reduce `persistedThemeSlotLabel` to the `Both` special case plus `name, _ := key.Slot.AttrName(); return name`, and delete the `themeSlotLight` / `themeSlotDark` vars (their only readers are the switch arms). `themeSlotBoth` stays as doctor's own label, since the setting has no slot for it to be named by.

- FINDING: The "`theme` component record" sink predicate is stated twice in cmd's tests
  SEVERITY: low
  FILES: cmd/open_theme_construction_test.go:674, cmd/theme_source_test.go:280
  DESCRIPTION: Both helpers walk `sink.Records()` and apply the same filter — `rec.HasAttr("component") && rec.AttrString(t, "component") == "theme"` — before diverging on what they do with the match (`themeEvents` renders each record as a `<LEVEL> <msg> k=v` line; `themeEventCount` counts records whose `Msg` matches). They are in the same package, so the shared half is directly extractable, and the duplicated half is precisely the definition of "a record this component emitted" — the thing every emission assertion in the suite rests on. `internal/tui` already made the equivalent split correctly: `countThemeEvents` (theme_panel_open_test.go:150) is `len(themeEventRecords(…))` over one walker.
  RECOMMENDATION: Add one `themeRecords(t, sink) []logtest.Record` filter in cmd, express `themeEvents` as a render over its result and `themeEventCount` as a `Msg` count over it — mirroring the `themeEventRecords`/`countThemeEvents` pairing internal/tui already uses.

- FINDING: Union-from-rows construction inlined at nine sites while a named helper exists
  SEVERITY: low
  FILES: internal/tui/theme_panel_commit_recompute_test.go:443, internal/tui/theme_panel_arrow_test.go:139, internal/tui/theme_panel_commit_test.go:113, internal/tui/theme_panel_open_test.go:87, internal/tui/theme_panel_open_test.go:552, internal/tui/theme_panel_test.go:70, internal/tui/theme_panel_cursor_test.go:331, internal/tui/theme_panel_cursor_test.go:364, internal/tui/theme_panel_cursor_test.go:412, internal/tui/theme_panel_entry_test.go:71
  DESCRIPTION: `themeRowsUnion(rows)` (theme_panel_commit_recompute_test.go:443) is the named form of "wrap hand-declared rows as the union the seam hands back", deriving `Count` and `Rejected` from the rows. Nine sibling fixtures in the same package build the same value inline instead, and they have already drifted in the field that matters: theme_panel_arrow_test.go:139 derives `Rejected` through `arrowRejectedCount`, theme_panel_open_test.go:87 and theme_panel_cursor_test.go:364 hardcode `Rejected: 1`, and theme_panel_commit_test.go:113, theme_panel_cursor_test.go:331/:412 and theme_panel_open_test.go:552 omit it entirely for unions that do contain rejected rows. Since `Rejected` is what `theme: enumerated` reports and what the panel's own assertions read, a hand-carried count is a fixture that can disagree with its own rows.
  RECOMMENDATION: Route the nine inline literals through `themeRowsUnion` (and give it the `DirUnusable` variant theme_panel_test.go:70 and theme_panel_entry_test.go:71 need, e.g. a second `themeRowsUnionUnusable` or an options-free sibling), so `Count`/`Rejected` are derived from the rows at every fixture site. `arrowRejectedCount` then has a single caller inside that helper.
SUMMARY: Production code is essentially duplication-free — a mechanical cross-file scan of the non-test surface turned up nothing beyond shared import blocks, and the one semantic near-duplicate is doctor's re-statement of `theme.Slot.AttrName`. The real duplication is in the test surface this work added: per-package AST source-guard scaffolding re-authored (and already diverging) in four packages, and the two-built-in subtest table copied across eighteen internal/tui sites.
