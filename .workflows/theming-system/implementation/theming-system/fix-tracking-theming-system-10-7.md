## Attempt 1

ISSUES:

- `.workflows/spectrum-tui-design/specification/spectrum-tui-design/specification.md:148` and the dark ratio column at lines 154–178 — §2.9's new preamble asserts "every ratio quoted is measured against that theme's own canvas", and the new column header asserts "`tokyo-night` (canvas `#0b0c14`)", but **12 of the 13 dark ratio cells are measured against pure black `#000000`, not against `#0b0c14`.** Computed with the project's own colour math (go-colorful `LinearRgb`, the same code `contrast_test.go` uses), every printed dark figure matches `#000000` to three decimals and none matches the canvas:

  | token | printed | vs `#0b0c14` | vs `#000000` |
  |---|---|---|---|
  | text.primary | 13.0 | 12.079 | 13.008 |
  | text.secondary | 9.9 | 9.236 | 9.947 |
  | text.tertiary | 6.3 | 5.882 | 6.334 |
  | text.muted | 5.0 | 4.675 | 5.035 |
  | text.subtle | 3.2 | 3.011 | 3.243 |
  | accent.primary | 9.1 | 8.430 | 9.078 |
  | accent.key | 8.3 | 7.743 | 8.339 |
  | accent.mode | 12.2 | 11.367 | 12.241 |
  | state.positive | 11.5 | 10.670 | 11.490 |
  | state.destructive | 7.9 | 7.370 | 7.937 |
  | accent.attention | 10.3 | 9.589 | 10.327 |
  | text.on-attention | 13.3 | 10.727 (on its own `bg.attention` tint) | 13.293 |

  This is the *exact* error the 2026-06-25 corrigendum fixed for the light column ("computed against pure white `#FFFFFF` … now reads vs `#e1e2e7`") — the dark half was never done. The light column is correct: all 12 light cells verified against `#e1e2e7`, and `text.on-attention`'s 5.1 against its tint; every one right to 1dp.

  Two things make this in scope rather than a benign inheritance. First, the edit **newly and explicitly asserts** the canvas reference in prose it wrote, so the file now contradicts itself where before the claim was weaker. Second, the executor's own new `text.faint` dark cell (`· 2.0`) IS canvas-referenced (1.985 → 2.0, correct) and so is now the single inconsistent member of a black-referenced column — it also replaced the old footnote's `2.1`, which was consistent with its neighbours. The executor reported the `text.subtle` 3.2-vs-3.011 gap as an isolated pre-existing inaccuracy; it is in fact one symptom of a systematic wrong reference, and leaving it is materially misleading now that the footnote pins `text.subtle` to a 3.0–4.49 *band* — 3.2 reads as 0.2 of headroom above the floor when the true margin is 0.011.

  FIX: Recompute the dark column against `#0b0c14` and update the twelve cells to (1dp): text.primary 12.1 · text.secondary 9.2 · text.tertiary 5.9 · text.muted 4.7 · text.subtle 3.0 · accent.primary 8.4 · accent.key 7.7 · accent.mode 11.4 · state.positive 10.7 · state.destructive 7.4 · accent.attention 9.6. For `text.on-attention` use **10.7** (its own `bg.attention` tint), matching how the light cell's 5.1 is already quoted and what the 2026-06-25 entry pinned for on-tint tokens. Leave `text.faint`'s 2.0 as it stands — already correct. Then add one bullet to the 2026-08-07 corrigendum, parallel in form to the 2026-06-25 light-column entry, e.g.: "**§2.9 the dark ratio column.** Superseded: a dark ratio column computed against pure black `#000000` while the table asserted `#0b0c14`. Current: every dark figure reads against the theme's own canvas `#0b0c14` (on-tint tokens against their tint) — the same correction the 2026-06-25 entry made for the light column, which never reached the dark half. **Superseded → current:** 13.0→12.1; 9.9→9.2; 6.3→5.9; 5.0→4.7; 3.2→3.0; 9.1→8.4; 8.3→7.7; 12.2→11.4; 11.5→10.7; 7.9→7.4; 10.3→9.6; `text.on-attention` 13.3→10.7 (on `bg.attention`). No hex moves; only the measured figures."

  ALTERNATIVE: drop the ratio figures from the dark column entirely and state that the authoritative numbers live in `internal/theme/contrast_test.go` and the `.theme` files — consistent with the edit's own "the values below are MV's record, not their source" reframe, and it removes a class of figure that has now silently rotted twice. Tradeoff: it loses the at-a-glance headroom a porter reads the table for, which the light column deliberately kept. The reviewer recommends the correction above; strictly smaller and keeps both columns symmetric.

  CONFIDENCE: high

- `specification.md:39` — the 2026-08-07 corrigendum's closing line states "The **§15 Paper frames and verification loop are historical and stand unedited**", but this same pass edited §15.6 (`bg.warning`→`bg.attention`, `bg.track`→`bg.subtle`, "the light borders"→"the light `border`"). Byte-compared all six §15 subsections: 15.1/15.2/15.3/15.4/15.5 identical, 15.6 not. The extension itself was correct and correctly flagged — the closing sentence just contradicts it, and this file is served as validated context, so a reader checking §15.6 finds the annotation false about its own edit.

  FIX: narrow the sentence to the frames it means, e.g. "The **§15 Paper frames stand unedited**: a frame still showing `↑↓ navigate` is a record of MV's design phase, not a defect. §15.6's light-tint checklist takes the token renames with the rest of the body."

  CONFIDENCE: high

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant on the named scope. Every correction sampled is true against the shipped code, verified independently rather than against the executor's report — the 19 tokens and their exact names/order (`internal/theme/theme.go` `fields()`), all 14 renames plus the 5 unchanged plus `border.footer` retiring into `border`; `Token{Name,Value}` with a no-argument `Color()` and no `ColorFor`/`theme.Mode` anywhere; contrast measured against each theme's own canvas (`contrast_test.go` enumerates `BuiltinSlugs()` and uses `th.Canvas.Value` as every reference); the `text.subtle` 3.00–4.49 and `text.faint` >1.0/<3.00 bands matching `TestTextSubtleBand`/`TestTextFaintDecorativeBand`; all 19 dark + 19 light hexes verbatim against the built-ins with only `#20232E` gone; Nord's `#2E3440`; §8.1's single-tone `border` (`panel.go` `renderJoinedPanel(… borderToken …)`), correctly recorded as stale before this feature (`panel.go`: "dropped in task 3-4" — spectrum-tui-design's own implementation).
- §3.4's Sessions footer verified against `sessionsKeymap()` — Core order ⏎ · / · ␣ · s · x · t · m · ?, exactly the pinned row; `m` Action "multi" with HelpAction "Multi-select mode"; the non-core set is exactly ↑↓, ^↑/↓, n, r, k, q. §6.3's Projects footer verified against `projectsKeymap()` — ⏎ · x · e · / · t · ?, with `? help` right-aligned.
- §3.4's degrade description verified: `fitLeftCluster`/`fitClusterToWidth` drop trailing entries with `…`, never wrap or truncate a label; `assembleRightAnchoredRow` rung 3 keeps the anchor, rung 4 renders an empty w-cell row. Lockstep filtering is one call-site filter (`sessionsHelpKeymap`/`projectsHelpKeymap`) consumed by both the render and the height budget.
- §2.6's appearance deletion verified: the enum/API is gone (`appearance_api_guard_test.go` walks the tree); `prefs.go` keeps `Appearance string` "read and preserved, NEVER parsed" for the downgrade pin. "Query issued regardless of the setting's shape" matches `model.go` `Init`'s own comment; single-resolution and constant-skips-the-gate match `appearance_gate.go`.
- §11's precedence, §12.1's Preview `←`/`→` (`handlePreviewKey` binds KeyLeft/KeyRight), the theme-picker scope (`themePanelKeymap`'s six keys + `themePanelConfirmKeymap`'s y/n), §2.9's "glob-based guard with no exemption" (`colour_literal_guard_test.go` is a glob, no allowlist), and §2.8's "three built-ins / flat key = value / `#RRGGBB` only / full replacement, no merge / syntactic only" all verified against `builtins/`, `lex.go` and `validate.go`.
- §14.5 / §2.6's AdaptiveColor claim verified: it is in `charm.land/lipgloss/v2/compat`, and the theming spec's "neutral, not supporting evidence" reading is what the corrigendum reproduces.
- **The three deliberate extensions each check out.** (a) §2.6's Mechanism bullet carried the identical AdaptiveColor claim item 11 corrects in §14.5 — correcting one and not the other would have left the falsehood in the more-read section. (b) §15.6's "the light borders" plural is the same border collapse the rename bullet covers, and §15.6 was not in the byte-unchanged set. (c) §13.2's slide-over entry is needed for §12.1's new "(slide-over, §13.2)" cross-reference to resolve, and matches theming §9 and `internal/tui/theme_panel.go`.
- The executor's self-corrected `text.faint` figures are right: 1.985→2.0 dark and 1.626→1.6 light, both against the correct canvas. Its two reported observations are also right — `↑↓ navigate` legitimately survives in §7.1's *filter* footer (`filter_footer.go` binds `↑↓` "navigate" in the list-active cluster), and the footer degrade drops from the right of the left cluster with a `…` rather than truncating labels.
- **Three surviving "mode-matched" references** — line 63 ("The canvas is mode-matched, not pure black"), line 341 ("cleared to the owned canvas (mode-matched — §1, …)") and line 559 ("(mode-matched — §8.1)"). §1's heading and body dropped that framing, so the parentheticals now point at sections that no longer carry the term, and under a single named theme the canvas is not mode-matched at all. Non-blocking: the substantive claim each makes is still true, line 63 is scoped to MV's own direction (MV genuinely ships as a pair), and `internal/theme/theme.go` still uses the phrase in its `Canvas` field comment. Worth tidying if the file is reopened.
- Minor asymmetry the dark-column fix resolves: `text.on-attention`'s light cell is quoted against its tint while its dark cell is quoted against a canvas (currently black) — the two halves of one row use different references.
- §13.3 ("`?` is bound on every page") reads correctly only because §13.2 now classifies the theme picker as a slide-over rather than a page; that ordering dependency is fine as written but worth knowing it exists.
- Protocol conformance: the four steps respected in order (status check → in-place edit → corrigendum extension → re-index); the corrigendum matches the file's established grouped form (approved deviation from the one-liner template); the two pre-existing blocks unchanged apart from the single approved path pointer; the owning manifest untouched and still `completed`. Re-index verified to have replaced the chunks — the store serves the 2026-08-07 text (§16 and §3 queried back verbatim), no stale-token chunk survives. Nothing committed.
- `go test -count=1 ./...` passes except a `TestTailScrollback_PerformanceBudget` timing flake (5.89ms vs 5ms) that passes on re-run; no Go file was touched. `go vet ./...` clean; `golangci-lint run` 0 issues; `gofmt -l` reports only the three known pre-existing offenders.
