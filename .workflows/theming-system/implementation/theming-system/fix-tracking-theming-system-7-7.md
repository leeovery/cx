## Attempt 1

ISSUES:

- `/Users/leeovery/Code/portal/cmd/doctor_fix_theme_test.go:223-227` — the `fixThemeFixture` doc comment claims the `appearance` key is "what makes `prefs.json is byte-identical` evidence that doctor read it through the non-migrating variant". **That is false in this test binary**: `cmd`'s `TestMain` replaces `persistTranslation` with a no-op (`cmd/testmain_isolation_test.go:84`), and `loadPrefsStore` performs its entire write inside that function (`cmd/config.go:268`).

  The reviewer proved it — swapping `resolveDoctorDeps` to the migrating `loadPrefsStore()` leaves `TestDoctorFix_ThemeStateUntouched` **fully green**, `theme_migrated` subtest included; only 7-5's source guard and `TestLoadPrefsStore_SingleProductionCaller` fail. The assertion is real for *this* task's claim (a direct `SaveMigrationMarker()` in `runDoctorFix` is caught), but it cannot distinguish the two prefs loaders, so the stated rationale is wrong in a codebase where these comments are the design record.

  FIX: call `syncPersistTranslation(t)` (`cmd/prefs_translation_persist_test.go:37`) at the top of `fixThemeFixture`. The fixture's `{"session_list_mode":"by-tag","appearance":"light"}` then genuinely rewrites under a migrating read (`translateAppearance("light")` is non-empty, no theme keys, no marker → `SaveTranslation` writes slug + marker), which makes the existing comment literally true and gives the byte-compare teeth against a loader swap.

  ALTERNATIVE: leave the fixture and re-scope the comment to what the assertion actually proves — that `runDoctorFix` performs no prefs write of its own — naming `TestPersistedThemeAdvisory_UsesNonMigratingRead`'s source-guard subtest as the owner of the non-migrating-read claim. Cheaper, but it keeps the weaker test; the reviewer recommends the first, since it is one line and converts a false claim into real coverage.

  CONFIDENCE: high

NOTES:

- SPEC_CONFORMANCE conformant. §12.2's "the theme scan runs on the `--fix` path too, and its advisories and the `· <M> advisories` suffix appear there" is satisfied by the single production edit at `cmd/doctor.go:336`; §12.2's "read-only, with no `--fix` action" holds — `runDoctorFix` (`cmd/doctor.go:377-383`) gained no step; §5.5's never-creates-or-seeds is asserted; §12.3's Discard-backed loader is unchanged and pinned; §14A copy in the fixtures matches the spec table verbatim.
- ACCEPTANCE_CRITERIA all met, each verified independently rather than from the report: reverting line 336 to `nil` fails **all eight** new tests (the executor's TDD claim checks out); hoisting into a shared variable (behaviourally identical) fails **only** the source guard, so the guard earns its place; a `--fix` that deletes rejected `.theme` files, seeds a non-theme file + chmods, creates the absent themes dir, prints a `Pruned invalid theme:` breadcrumb, calls `SaveMigrationMarker`, or logs through a real `theme` logger is caught by the corresponding test in each case; removing `"doctor"` from `skipTmuxCheck` fails the exemption test while its probe control still passes; the post-repair-only exit contract is intact and an advisory-driven exit is caught. The second collect is genuinely fresh — an inline call evaluated at line 336, after `runDoctorFix` (315) and after the second `runDoctorDiagnosis` (318).
- TEST_COVERAGE adequate apart from the overclaimed guard above. Vacuity guards are real for two of the three named: the 8-`⚠`/12-`⚠` counts genuinely gate assertions that would otherwise pass against a dead scan, and the non-exempt probe genuinely proves the orchestrator seam is live.
- CONVENTIONS followed. No `t.Parallel`; package-level `*Deps` injection with `t.Cleanup`; all fixtures under `t.TempDir` + `t.Setenv`; no tmux, no daemon, no built binary (correctly unit-lane); the probe-command idiom matches `cmd/bootstrap_orchestrator_test.go:69-80`; the AST source guard matches the `parseCmdFiles` idiom used by `TestLoadPrefsStore_SingleProductionCaller`; every helper reused rather than re-declared. `go test ./...` green; `-race` green on the doctor/prefs suites; vet, gofmt, `golangci-lint` (0 issues) clean.
- ARCHITECTURE sound. One-line production change with no new seam, no new state, no new call graph. The two-pass double scan is spec-mandated and cheap (read-only, fresh Discard-backed loader per call, so the per-instance dedup set stays owned). The Phase-8 boundary is unaffected — the union rule the panel must agree with lives in `assembleThemeAdvisories`, untouched.
- **The source guard's cardinality pin is right, not brittle.** It is the only thing that separates "re-collected" from "hoisted" — no output assertion can, since nothing `--fix` does changes the scan's answer (the reviewer confirmed the hoist mutation is invisible to all seven other tests). The `len(renders) != 2` and `len(call.Args) != 3` checks are `Fatalf`s with self-explaining messages, and the loop already rejects a third `nil`-passing render, so the count pin only adds "a third render must be re-examined by a human". It does reject a legitimate refactor that assigns a *fresh* collect per render to a local; the failure message points straight at the requirement, so this is acceptable.
- `TestDoctorFix_RemainsBootstrapExempt`'s first assertion is byte-identical to `TestDoctorRegisteredInSkipTmuxCheck` (`cmd/doctor_test.go:446-450`). Harmless, but the behavioural half is where the new information is; the duplicated map lookup could go.
- `TestDoctorFix_ExistingRepairsUnchanged` substantially re-covers `TestDoctorFixPrunesStaleEntriesThenRediagnosesClean` / `TestDoctorFixDownServerPrunesProjectsButNotHooks` / `TestDoctorFixLogSweepNeverDrivesExit`. The added dimension (repairs running *with* the theme surface present) is what the task asked for, so justified duplication rather than over-testing.
- A strictly stronger end-to-end proof of the re-collect was available and not taken: `deps.HookLister` is called from inside `runDoctorFix`, i.e. between the two renders, so a fake lister with a filesystem side effect could show the second report reflecting a mid-run directory change through a real `Execute`. The task prescribed the function-level + source-guard pair instead, and the executor followed it; noted only as a future option if the AST guard ever becomes a refactor tax.
- The two new comment blocks are accurate and mutually consistent; `doctor_theme.go:89-91`'s "nothing between the two passes can change its answer" is scoped by its own trailing clause, so it does not contradict `doctor.go:333-335`.
