## Attempt 1

ISSUES:

- `CHANGELOG.md:21` — the detection bullet asserts a change that did not happen in this release, and contradicts the file's own history. Portal has detected via the OSC 11 terminal background since detection first shipped (commit `4f103b5e`, 2026-06-19, released in 0.8.0), and **`CHANGELOG.md:157` already told users exactly that**: "Portal detects the terminal background via OSC 11 on first paint and matches it." `git log --all -S ModeLightDark` shows the OS-colour-scheme mechanism appears only in research/discussion/spec artifacts — never in shipped code — so "instead of following the operating system's colour scheme" describes a state Portal was never in. `tea.RequestBackgroundColor` / `tea.BackgroundColorMsg` are still the mechanism (`internal/tui/model.go:2695`, `:2781`; `appearance_gate.go`). The "not the OS colour scheme" phrasing is §1.4/§8.7's *rejected alternative* (DEC 2031), i.e. internal decision residue — which is why `docs/theming.md:428` correctly states it descriptively ("Detection asks the terminal what colour its background is… It does not read the operating system's light/dark setting") while a `🔧 Changed` bullet cannot.

  FIX: state what actually changed — detection's *subject*, not its *signal*. Replace with:

      Detection now chooses between your two themes rather than between two fixed
      canvases. It asks the terminal what colour its background is, over OSC 11 — not
      the operating system's light/dark setting; the two routinely disagree, and the
      terminal is the one Portal is drawn inside.

  This is true, keeps the bullet in `🔧 Changed`, and still satisfies the "detection is described as following the terminal background" criterion.

  ALTERNATIVE: keep it purely descriptive and drop the transition verb — "Which theme a `theme_light` / `theme_dark` pair renders follows the terminal's background colour (OSC 11), not the operating system's light/dark setting…" — and move it up into `✨ Added` beside the pair. Tradeoff: no false change claim either way, but `✨ Added` reads oddly for behaviour that predates the release, and the criterion's home for it is the upgrade note. The reviewer recommends the FIX.

  CONFIDENCE: high (on the fact; the wording is one of several valid repairs)

- `CHANGELOG.md:14` — the export example is a copy-pasteable command that fails for the audience it targets. `portal theme export nord > ~/.config/portal/themes/nord-mine.theme` requires the themes directory to exist, and Portal never creates or seeds it (`cmd/config.go:384-387`: "It returns a path and nothing more — it never creates, seeds or stats the directory… which is why the documented drop-in workflow starts with `mkdir -p`"). The reviewer reproduced it against a built binary in a fully isolated env: `/bin/sh: …/themes/nord-mine.theme: No such file or directory`, `exit=1`; with `mkdir -p` first it exits 0 and the file starts `# Nord — https://www.nordtheme.com/`. `docs/theming.md:305-309` publishes the two-line form and calls the `mkdir -p` "part of that workflow, not an omission", and `cmd/theme.go:27-29` says the same — so the entry reintroduces precisely the trap the docs task was written to close.

  FIX: make the example the documented two-line form, or fold the mkdir in: `mkdir -p ~/.config/portal/themes && portal theme export nord > ~/.config/portal/themes/nord-mine.theme`.

  ALTERNATIVE: drop the inline command entirely and end the bullet at "…as a starting point for your own." — the entry already points at `docs/theming.md`, and no other CHANGELOG bullet in the file carries a redirect. Shorter and cannot rot, but loses the at-a-glance workflow. The reviewer recommends the FIX.

  CONFIDENCE: high

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant on §12.5's three mandated elements — the new setting + three built-ins pointing at `docs/theming.md` (`:11-12`), the automatic translation with the exact mapping and an explicit "nothing you need to do" (`:19`), the retained-for-downgrade / not-kept-in-sync key (`:20`). §1.4's three exclusions honoured (no `unset`, no `--theme`, no `portal theme list` anywhere in the entry). §14.2's footer revision reported (`:22`). 8 of 9 acceptance criteria met; the detection criterion is satisfied in its first clause but framed as a transition from a state that never existed.
- Heading form / position / no `Unreleased`: met. `## [0.11.0] - 2026-08-07` inserted above `## [0.10.5] - 2026-07-27`; the v0.10.5 tag is dated 2026-07-27, matching its entry, so the date convention holds. Minor bump is right for additive-feature-plus-translated-setting. The diff is a pure insert at `@@ -5,6 +5,22 @@` — nothing below the new entry moved.
- **The backtick-vs-link deviation is justified.** `CHANGELOG.md:32` (0.10.4) writes `` `docs/custom-terminals.md` `` in backticks, and the only two markdown links in the entire file are the boilerplate Keep-a-Changelog / SemVer links in the header. A markdown link would have been the file's first inline doc link. The criterion says "linked"; the spec says "pointing at" — the entry points at it and the file exists. Satisfied in substance.
- **The "first time you open the picker" claim (`:19`) is correct** — checked specifically. `loadPrefsStore()` (the migrating variant) has exactly one caller, `openTUI` at `cmd/open.go:959`, and `openTUI` is reachable only via the two picker routes (`-f/--filter`, `cmd/open.go:243`; no-target, `:281`). Every pin/positional/burst path bypasses it, and `portal doctor` deliberately uses `loadPrefsStoreNoMigrate` (`cmd/doctor.go:224`, `cmd/doctor_theme.go:195`). So it is not "at prefs load on every launch" in the user-visible sense — prefs load *is* picker construction. The one-shot is marker-gated (`theme_migrated`, `prefs/store.go:563`), so "the first time" holds.
- Mapping verified exactly: `cmd/config.go:353-362` `dark`→`DefaultDarkSlug`, `light`→`DefaultLightSlug`, everything else `""`; `internal/theme/builtins.go:35-36` pins those to `tokyo-night` / `tokyo-night-day`. `auto`→nothing, and `theme/setting.go:116-119` defaults empty keys to the `tokyo-night-day` / `tokyo-night` pair — so `:19`'s "the shipped default is already the light/dark pair it meant" is exact.
- Retained-key claim verified: `Appearance` is read at `prefs/store.go:371` and written nowhere (`grep '\.Appearance = \|Appearance:'` across prefs/cmd/tui returns only that read); the struct field carries an explicit do-not-delete rationale at `store.go:123-131`, and `SaveTranslation` writes only `Theme`/`ThemeLight`/`ThemeDark`/`ThemeMigrated`.
- Built-in slugs and light/dark classification verified against the three files in `internal/theme/builtins/`: `tokyo-night` ("Portal's dark built-in"), `tokyo-night-day` ("Portal's light built-in", canvas `#e1e2e7`), `nord` ("Portal's second dark built-in", canvas `#2E3440`). `:12` is correct.
- "Re-read every time the theme selector opens" verified: `openThemePanel` calls `themeEnumerator.Open` (`theme_panel.go:580`) → `Loader.Open` → `Enumerate` (`union.go:222-224`), one directory read per open; `Reassemble` (the recompute path) does no I/O by contract.
- Doctor's three advisory classes all exist and all three listed cases are really covered: `themesDirUnreadableFormat` (`doctor_theme.go:55`, `:350`), the file-rejection frames (`:54`, `:57-59`, `:388-417`), and the persisted-slug frame (`:61`, `:298-309`).
- `t` is dispatched on both pages (`model.go:3274` Projects, `:4078` Sessions) through one shared handler; `Enter`/`d`/`l` dispatch as described (`theme_panel.go:1250-1262`); live preview on cursor move is `previewSelectedThemeRow` → `ApplyTheme` (`:1380-1386`); `↑↓` is genuinely still in `?` help (`keymap.go:112` keeps the entry with `Core` dropped, and `help_modal.go:141-151` renders every non-RightAligned entry).
- Footer bullet is accurate but slightly loose: only the **Sessions** footer loses `↑↓ navigate` — the Projects nav entry was already non-core (`keymap.go:146-149`), so the revision is a no-op there. The trailing "the Projects footer gains `t theme` too" disambiguates, but a reader could infer Projects lost arrows as well. Non-blocking; if the bullet is being edited anyway, "The Sessions footer drops…" removes the ambiguity for free.
- Full unit lane `go test -count=1 ./...` fully green (all 34 packages, `internal/state` green first pass). `go vet ./...` exit 0; `golangci-lint run` → 0 issues; `gofmt -l` lists only the three pre-existing offenders, and no Go file was touched. `git diff HEAD --numstat` is `CHANGELOG.md 16/0` plus a 1-line `.tick/tasks.jsonl` status flip.
- Isolation for the binary run held: real `~/.config/portal` unchanged (prefs.json still 2 Aug 14:55, no `themes/` created); the only writer to the real state dir during the review was the developer's live daemon (pid 68742, `version=0.10.5`).
