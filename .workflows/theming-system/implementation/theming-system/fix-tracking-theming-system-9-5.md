## Attempt 1

ISSUES:

- `internal/tui/theme_panel_commit.go:161` — the new third `committableThemeSlug` guard site (`handleSlotCommitKey`'s constant branch) is **untested**. Replacing it with `slug, _ := committableThemeSlug(...); (&m).raiseSlotConfirm(slug, slot)` leaves the **entire package suite green**. This is the one of the three guard sites whose failure mode is worst: an unguarded raise asks "clear constant X?" about an empty slug, and `y` then calls `commitSlot("", slot)` — clearing the user's constant *and* writing an empty slot value, which is precisely the silent loss §9.2's confirm exists to prevent. The identical guard at the `Enter` site is tested (`theme_panel_commit_test.go:624`, `TestPanelEnter_UnselectableRowWritesNothing`), so the coverage is inconsistent rather than deliberately omitted.

  FIX: Add a subtest to the confirm suite mirroring `TestPanelEnter_UnselectableRowWritesNothing`'s shape — open over a constant, `m.themePanel.list.Select(<unselectable index>)` directly (bypassing the anchor and the arrow skip, exactly as that test does), press `d`, and assert `m.themePanel.message.Kind == themeMessageNone` and `m.themePanel.pending == themeSlotConfirm{}`. Include its "a selectable row does raise" positive control so the refusal is a guard rather than a dead arm. A second subtest for the selectable-row-with-empty-slug shape (`{Source: theme.SourceBuiltin, Theme: arrowPalette(1)}`) matches the existing precedent.

  CONFIDENCE: high

- `internal/tui/theme_panel_confirm.go:220-222` — `themeConfirmAnswer`'s documented modifier contract is both **untested** and, in the direction it actually bites, **inverted**. Removing the mask entirely (`return strings.EqualFold(msg.Text, letter)`) leaves the whole package suite green, so nothing pins it. More importantly the stated rationale is inaccurate: a `Ctrl-Y`/`Alt-N` carries **empty `Text`** in this stack (bubbletea `key.go:298-302`; ultraviolet's `printMod := keyMod <= ModShift || keyMod == ModCapsLock || ...` gates text population), so those never match on the `EqualFold` alone and the mask contributes nothing there. The mask's only reachable effect is rejecting `{Code:'y', Text:"Y", Mod:ModCapsLock}` — a caps-lock user's answer, which §9.2 plainly intends to accept. (Neither is live today: Portal never sets `KeyboardEnhancements`, so bubbletea requests disambiguate-only and uppercase arrives as the legacy byte with `Mod == 0` — but the matcher was written for the enhanced case, so it should be right for it.)

  FIX: Widen the forgiveness to the lock/shift modifiers that can accompany a printable answer — `msg.Mod&^(tea.ModShift|tea.ModCapsLock) == 0` — and correct the comment to say that ctrl/alt combos are excluded by carrying no `Text` rather than by the mask. Then pin it: add `ctrl+y` (`{Code:'y', Mod:tea.ModCtrl}`) and `alt+n` rows to the swallow table (their positive control being that the unmodified letter *does* resolve), and a `capslock+y` case to `TestSlotConfirm_ConfirmsOnEitherCase`.

  ALTERNATIVE: Keep the mask as-is and only add the swallow-table rows plus a comment correction, accepting the caps-lock rejection as out of scope. Cheaper and behaviour-preserving, but it leaves a documented answer key dead for caps-lock users on enhanced terminals — the reviewer recommends the widening, since the whole reason for the bespoke matcher was to stop exactly that class of silent dead key.

  CONFIDENCE: medium

- `internal/tui/theme_panel_confirm.go:147-151` and `169-174` — `confirmSlotAssignment` branches on `commitSlot`'s error, but `commitSlot` returns `nil` for **two** different outcomes: a successful write and the **nil-persister early return** (`theme_panel_commit.go:222-224`), which returns *before* the mirror and the recompute. So on a nil persister (capturetool, fixtures) `loadNewlyLiveSlot` fires with un-mirrored keys and no recompute — directly contradicting its own doc comment, "IT RUNS AFTER commitSlot's own mirror and recompute rather than between them", which is the exact invariant 9-6's author will build the load on. Today it is inert (empty body), but 9-6's load reads `m.themeKeys` to decide which slug the opposite slot names; under this path those keys still hold the constant, so it would resolve the wrong slot and emit a spurious commit-time `theme: loaded` (§8.4/§12.3) for a write that never happened. This is also the "a caller with nothing to do" shape the executor's own reasoning rejects for putting the seam on `commitSlot`.

  FIX: Make the seam fire only on an actual write. Simplest: in `confirmSlotAssignment`, guard the tail on the persister — `if m.themePersister == nil { return }` before `loadNewlyLiveSlot`, with a note that a nil persister is inert rather than committed (the `commitConstant`/`commitSlot` comments already state that rule, so this inherits vocabulary rather than inventing it). Update `loadNewlyLiveSlot`'s comment to say it runs after a write that landed, not merely after a non-error return.

  ALTERNATIVE: Have `commitSlot` report whether it wrote (`(wrote bool, err error)`, or a `errNoPersister` sentinel the caller discriminates), which makes the distinction available to every future consumer rather than re-derived at one call site. Stronger and more honest, but it touches the shared path both `Enter` and the pair-`d`/`l` route through, so it is a wider edit than this task needs — the reviewer recommends the guard, and notes the sentinel as the shape to reach for if 9-7 needs the same distinction.

  CONFIDENCE: medium

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant; ACCEPTANCE_CRITERIA all met (all twelve walked against code and tests); CONVENTIONS followed; ARCHITECTURE sound apart from the seam-placement concern above.
- Five load-bearing mutations were re-run independently by the reviewer and all were caught: M4 (Esc arm ahead of the confirm arm), M9 (drop `applyThemePanelListStyles` from `setThemePanelMessage`), M11 (discard the failure error), M12 (`closeThemePanel` retains `pending`/`message`), M16 (cancel moves the cursor). Removing shift-forgiveness also fails both `shift+y` and `shift+n` subtests.
- The swallow table's positive controls are **genuine**. Every row's control is `t.Fatalf`-guarded, so a control that did not fire would fail the test rather than pass vacuously. `Ctrl+↑`/`Ctrl+↓` run at `arrowPagingTermH` with `requireArrowPanelPageSize(base, 2)` asserted first; `?`/`k`/`x`/`m`/`/` use `closedControl` asserting `modalHelp` / `modalKillConfirm` / `PageProjects` / `MultiSelectActive()` / `SettingFilter()`; the second `d`/`l` rows use `openControl` asserting the pending assignment is raised with the matching slot, and the live case then asserts the pending slot stays `SlotLight`.
- **Judgement call 1 (forced-close cancel) is genuinely exact.** Traced `resizeThemePanel` → `closeThemePanel` → `applyInForceTheme(enumeration)` → `m.themePanel = themePanel{}`. `pending` and `message` are both fields of `themePanel`, so both are zeroed by the single whole-struct discard. One correction to the executor's rationale: `panelDiscardSites` matches only the empty `themePanel{}` composite literal, so an explicit `m.themePanel.pending = themeSlotConfirm{}` in `resizeThemePanel` would **not** have tripped that guard — the guard argument is wrong, but the core "structural and a no-op" argument stands and the call is right.
- **Judgement call 2 (9-6 seam ordering) holds.** `recomputeThemePanel` derives rows from `Reassemble(enumeration, themeKeys)` and badges from `Resolve(enumeration, setting)` — neither consults the nomination the 9-6 load would produce, so the recompute has no dependency on the load. The load needs the mirrored keys, which `commitSlot` sets before the recompute, so both orderings satisfy it. Nothing renders in between. Defensible and recorded in-source — but it exposes the nil-persister issue above.
- The Kitty claim checks out. `isRuneKey` is `msg.Mod == 0 && msg.Text == ch` (`internal/tui/keys.go:35`), and `ultraviolet`'s decoder test pins exactly `KeyPressEvent{Code:'a', ShiftedCode:'A', Text:"A", Mod:ModShift}`, so `isRuneKey` would indeed miss that encoding. The bespoke matcher is justified in principle.
- `Ctrl-C` ordering is correct. `keyIsCtrlC` is the first arm of `updateThemePanel`, ahead of `m.themePanel.confirming()`, and `updateSlotConfirm`'s `default` swallows everything, so nothing leaks past.
- The single-write claim is pinned properly (`len(persister.slugs) == 1` counts both seam methods, plus `requireSlotCommits` asserts zero constant commits). Both seams are genuine empty-bodied no-ops with AST caller guards pinning one route in each. `committableThemeSlug` is behaviour-preserving at the two pre-existing call sites (the extracted condition is character-identical). The confirm arm cannot be reached with the panel closed — `updateThemePanel` is gated on `m.themePanel.open` at `model.go:2995`.
- The two superseded 9-3 assertions were **genuinely superseded, not weakened**. `message.Kind != themeMessageNone` → `!= themeMessageConfirm` is the landed behaviour, and the byte-identical-frame assertion could not survive a keypress that legitimately raises a message row and swaps the footer. Both properties are recovered at finer grain in the new suite. Net coverage is higher, not lower.
- `confirming()` derives liveness from `message.Kind`, which is the right single-source call, but it means the invariant "confirm live ⇒ `pending` set" rests on `raiseThemePanelConfirm` having exactly one production caller rather than on the type. It does today, and `themePanelSeamCallers` is already the idiom for pinning that structurally if it is ever worth doing.
- There is no `TestThemePanelDescriptorDispatchParity` — the panel scope (and now the confirm scope) has no descriptor↔dispatch guard arm, despite §9.12 saying `keymap_dispatch_guard_test` covers them and §9.2 extending that to `y`/`Y`/`n`/`N`. That gap predates this task (the panel scope landed in Phase 8) and no task in the plan claims it; flagged so it is not lost.
- `setThemePanelMessage`'s re-size runs `applyThemePanelListStyles` unconditionally, which would `SetSize` a zero `list.Model` if ever called with the panel closed. Unreachable today (every writer is panel-scoped) — worth remembering when 9-7 wires the failed-commit line, since that message's lifetime is the one specified to outlive a keypress.

## Attempt 2

ISSUES:

- `internal/tui/theme_panel_confirm.go:247` — `themeConfirmAnswer`'s mask omits `ModNumLock`, so `y`/`n` are **dead keys** on an encoding the vendored decoder demonstrably produces.

  **Ruling on the executor's open question: the trace is correct; IN SCOPE and blocking.** Evidence, read directly from `ultraviolet@v0.0.0-20260525132238/decoder.go`:
  - 1448: `key.Text = ""` for any `key.Mod > ModShift` on the Kitty CSI-u path.
  - 1474–1478: `keyMod := key.Mod` then `keyMod &^= ModNumLock`, commented "Remove these lock modifiers from now on since they don't affect the text" — stripped from a **local copy only**, so `key.Mod` keeps `ModNumLock`.
  - 1480/1504–1518: `printMod` is computed from the stripped copy, and with `keyMod == 0` the repopulation sets `key.Text = string(key.Code)`.

  So `{Code:'y', Text:"y", Mod:ModNumLock}` is a shape the decoder emits, and Portal's mask refuses it. That is precisely the silent dead key the bespoke matcher exists to prevent, and precisely the argument that justified adding `ModCapsLock` in round 1 — leaving two of the three lock modifiers forgiven is the one position defensible on neither ground.

  **The executor's second argument does NOT fully hold.** "On every encoding the decoder can produce, the mask is behaviour-equivalent to the `EqualFold` alone" is true only for Portal's *current* flags, not for the decoder: decoder.go:1463's associated-text arm does `key.Text += string(rune(code))` in paramIdx 2, which the parse loop reaches **after** the paramIdx 1 clear at 1448 — so `{Text:"y", Mod:ModCtrl}` is producible whenever `ReportAssociatedText` is on. Portal never sets `KeyboardEnhancements` (bubbletea `cursed_renderer.go:845` ORs flags=1 unconditionally), so it is unreachable today, but that is a one-field opt-in away. That makes the mask genuinely **load-bearing rather than defensive-only** — which is the reason to complete its lock set rather than delete it. Confirmed behaviourally: removing the mask entirely leaves the suite green (mutation M-G survived), and narrowing it back to shift-only is killed by the `capslock+y` row (M-H).

  FIX: widen to `return msg.Mod&^(tea.ModShift|tea.ModCapsLock|tea.ModNumLock) == 0 && strings.EqualFold(msg.Text, letter)`; add `confirmYesNumLock = tea.KeyPressMsg{Code: 'y', Text: "y", Mod: tea.ModNumLock}` to the press vars and a `{name: "numlock+y", press: confirmYesNumLock}` row to `TestSlotConfirm_ConfirmsOnEitherCase` (`internal/tui/theme_panel_confirm_test.go:372-397`); and amend the "THE LOCK KEYS ARE FORGIVEN ALONGSIDE SHIFT" paragraph (`theme_panel_confirm.go:231-236`) to name **both** locks and cite the local-copy strip at decoder.go:1478 as the reason num lock reaches `key.Mod` while the text survives. Optionally sharpen the "belt-and-braces" sentence at 238-245 to name the associated-text arm as the encoding the mask actually guards, since that is what makes it worth keeping.

  ALTERNATIVE: delete the mask and match on `EqualFold` alone. Tradeoff: strictly simpler and provably green today, but it forfeits the ctrl/alt refusal the moment `ReportAssociatedText` is ever enabled — at which point `Ctrl-Y` would confirm a destructive constant-clear. NOT recommended; the widening is the same one line and keeps the guarantee.

  CONFIDENCE: high

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant (§9.2's three-input resolution, key-exclusivity, `Esc`-cancels-not-closes, `Ctrl-C`-stays-live, swallow-everything; §9.8's silent forced-close cancel; §8.2's `theme`-wins confirm copy; §8.4/§9.3's seam placement; §14A's pinned strings). ACCEPTANCE_CRITERIA: all twelve met, independently re-verified. CONVENTIONS followed. ARCHITECTURE sound.
- Mutation battery: 13 mutations run independently. All six of the executor's are killed, including the three called out — M-A (confirm-site `committableThemeSlug` guard deleted) killed by `TestSlotConfirm_UnselectableRowAsksNothing`; M-E (nil-persister guard removed) killed by `TestSlotConfirm_NilPersisterIsInert`'s structural half; M-F (guard hoisted ahead of the resolve) killed by its behavioural half (the confirm never comes down). Seven further reviewer mutations also killed: `y` resolving without committing, `Esc` dropped from the cancel arm, the confirm arm disabled in `updateThemePanel`, the raise not setting the message, `setThemePanelMessage` not re-sizing, the failure seam call dropped, and `resolveSlotConfirm` not clearing `pending`. Two survivors, both benign: M-G (mask removal — see ISSUES) and M-M (substituting the live cursor slug for the captured pending slug), which is behaviour-equivalent by construction since the confirm swallows every cursor-moving key. The capture is a routing-independence guarantee, correctly documented as such at `themeSlotConfirm`; no test is owed for it.
- `seamGuardedOnNilPersister` genuinely bites and is the right idiom — it is the only assertion possible against an empty seam body, and the package already carries the same structural-guard shape (`themePanelSeamCallers`, and CLAUDE.md's `canvasHexFor` and `internal/log` owner guards). It matches on the field name rather than the receiver, so a receiver rename does not unpin it. One contrived false negative: it takes the *first* `if x.themePersister == nil { return }` anywhere in the body, so a guard placed inside the error branch would satisfy it without guarding. Not worth defending now; when 9-6 gives the seam a body, retire the structural half for a behavioural one.
- The `raiseSlotConfirmForTest` → `raiseSlotConfirmForTestAt` refactor is behaviour-preserving at every call site. All seven callers (`theme_panel_confirm_test.go` 384, 416, 503, 558, 675, 726, 1055) run on the confirm fixture whose cursor `newSlotConfirmModel` arrows onto `slotConfirmTarget()`, so the delegated pending value is identical, and both round-1 assertions (`cmd == nil` fatal, `requireConfirmLive`) are retained verbatim in the new base. No assertion weakened.
- Guard placement is right. `loadNewlyLiveSlot` is the only statement on the success tail, so the early return skips nothing else; `commitSlot` (`theme_panel_commit.go:221-231`) owns the mirror and the recompute internally and returns before both on the nil-persister path, so the confirm still comes down and the standing footer still returns.
- The 9-6 seam sitting after `commitSlot` leaves 9-6 everything the plan requires: `mirrorThemeSlot` carries the opposite slot's raw value through untouched, and the plan's "pre-commit keys carried a non-empty `Theme`" discriminator is the caller identity, pinned by `themePanelSeamCallers(t, "loadNewlyLiveSlot") == ["confirmSlotAssignment"]`. `recomputeThemePanel` consumes only the raw keys and the retained enumeration.
- The forced-close cancel is implemented structurally (`closeThemePanel` discards the panel struct whole) rather than as the plan's literal "clear the pending state before calling `closeThemePanel`". Same observable effect, one fewer drift site, reasoning recorded at `theme_panel.go:898-907`, and `TestSlotConfirm_ForcedCloseCancels` asserts the exact §14A geometry flash so "no confirm-specific flash" is genuinely pinned. **Endorsed.**
- `setThemePanelMessage`'s re-size is beyond the task's literal "Do" but is a real defect the raise would otherwise ship. Correctly placed at the writers so 9-7's persistent failed-commit line inherits it, and it bites (M-J killed by `TestSlotConfirm_ResizesTheListForTheSwappedLayout`). Good judgement.
- The swallow table's positive controls survive the `answerControl` addition. The ctrl+y/alt+n rows pin the `EqualFold` refusal (both press shapes carry no `Text`), not the mask — which the test header states honestly at `theme_panel_confirm_test.go:57-63`. The other thirteen rows keep their own genuine controls.
- Minor, non-blocking: `theme_panel_message.go:140` has a blank line between the doc block and `func (m *Model) setThemePanelMessage`, detaching it from godoc. There is precedent in the package (`model.go:3368`, `internal/restore/logger_nil.go:16`) and both gofmt and golangci-lint pass, but unlike the file-narrative blocks this one is plainly a doc comment.
- Minor, non-blocking: `theme_panel.go:1071` still reads "over a constant, until task 9-5's confirm lands — writes nothing". Still true, but the clause is now stale.
- Minor, cosmetic: `requireConfirmFooter`/`requireStandingFooter` shadow the builtin `copy` (`theme_panel_confirm_test.go:213, 232`). Not flagged by this repo's lint set.
- Dispatch-guard coverage of the panel and confirm scopes is task 9-10 (`phase-9-tasks.md:669`), correctly out of scope here despite §9.2's mention.
- Hygiene: `go test -count=1 ./internal/tui/` green, `go build ./...` green, `gofmt -l` clean, `go vet` clean, `golangci-lint run ./internal/tui/...` 0 issues. Working tree restored byte-identical after mutation testing.

## Attempt 3

ISSUES:

- `internal/tui/theme_panel_confirm.go:224-229` (and the same claim at `internal/tui/theme_panel_confirm_test.go:46-50`) states: *"a legacy terminal sends the byte, which decodes as the rune `Y` with NO modifier, while under the Kitty keyboard protocol the same keypress arrives as the base rune `y` carrying ModShift"*. **The vendored decoder contradicts this.** `parseUtf8` lowercases uppercase ASCII and **adds** `ModShift` (`decoder.go:1098-1102`), so both encodings converge. Driven through the real decoder:

  ```
  bytes "Y":                    Code='y' ShiftedCode='Y' Text="Y" Mod=1   // ModShift — not Mod=0
  kitty y+shift (ESC [121;2u):  Code='y'                 Text="Y" Mod=1
  ```

  The shape the comment calls "the legacy encoding" — `{Code:'Y', Text:"Y", Mod:0}`, the `confirmYesUpper`/`confirmNoUpper` press vars — is **not produced by this stack at all**. The derived sentence is wrong in the *understating* direction: pinning the dispatch to `isRuneKey` would leave `Y` dead **everywhere**, not "on every terminal negotiating the richer protocol" / "half the terminals Portal runs in". That is the same silent-dead-key class round 2 blocked on, and a maintainer trusting "legacy sends `Y` with no modifier" could conclude `ModShift` is only a Kitty concern and narrow the mask.

  FIX: Correct both paragraphs to what the decoder does — an uppercase answer reaches Portal as the **base rune `y` carrying `ModShift` with the shifted text `"Y"` on both paths** (legacy byte via `decoder.go:1098-1102`, Kitty via the modifier param), which is exactly why `isRuneKey`'s `Mod == 0` requirement would kill `Y`/`N` on every terminal rather than on a subset. Keep the `confirmYesUpper`/`confirmNoUpper` rows (they pin the `EqualFold` case-insensitivity, and the case-sensitivity mutation kills them), but relabel them as the defensive `Mod == 0` shape rather than "the legacy encoding", and let `confirmYesShift`/`confirmNoShift` carry the "this is what a terminal actually sends" note.

  CONFIDENCE: high

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant (§9.2's three-input resolution, key-exclusivity, `Esc`-cancels-not-closes, `Ctrl-C`-stays-live, footer substitution, §8.2's one-atomic-write rule, §9.8's silent forced-close cancel, §8.2's `theme`-wins hand-edit case, §14A's verbatim copy — two spaces, spec line 1798). ACCEPTANCE_CRITERIA: all twelve re-verified independently. CONVENTIONS followed. ARCHITECTURE sound.
- Mutation battery: nine mutations run independently, all reverted byte-identically with hashes verified — dropping the third `committableThemeSlug` guard, taking the pending slug from `themeKeys.Theme` instead of the cursor, leaving `pending` uncleared on resolve, discarding the commit error instead of routing to 9-7's seam, removing the nil-persister guard ahead of `loadNewlyLiveSlot`, ordering the confirm arm after `Esc`, ordering `Ctrl-C` after the confirm arm, dropping `applyThemePanelListStyles` from `setThemePanelMessage`, and case-sensitive matching. **Every one killed.**
- The mask narrowings: M-N (→ `Shift|CapsLock`) is killed by `numlock+y` and by no other test in the package; M-O (→ `Shift|NumLock`) by `capslock+y` and no other. No row passes vacuously — the case-sensitivity mutation kills `Y`, `shift+y`, `capslock+y`, `N`, `shift+n`.
- The two **rewritten** paragraphs are accurate clause by clause, line citations included: the clear is `key.Text = ""` at 1448 inside the `if key.Mod > ModShift` at 1445-1449; the local-copy strip is 1474-1478; `printMod` is computed from the copy at 1480; repopulation is 1504-1518 with the `keyMod == 0` arm at 1506. Empirically, `ESC [121;129u` (num lock) decodes to `{Code:'y', Text:"y", Mod:128}` and `ESC [121;65u` (caps lock) to `{Code:'y', Text:"Y", Mod:64}` — the round-2 finding and its fix are both real. The associated-text claim holds too (`key.Text += …` at 1461-1464 sits at `paramIdx` 2, past the `paramIdx` 1 clear), as does `keyboardEnhancementsFlags` (`cursed_renderer.go:845-860`) and Portal's zero `KeyboardEnhancements` references.
- Removing the modifier mask entirely survives the whole suite — consistent with both comments' explicit "belt-and-braces / what excludes `Ctrl-Y` and `Alt-N` today is the Text" framing. Empirically `ctrl+y` → `Text:""`, `alt+n` → `Text:""`. Honest, not a gap.
- The three free notes were taken cleanly: the doc block is attached to `setThemePanelMessage` with no blank line, the stale "until task 9-5's confirm lands" clause now reads correctly, and the `copy` → `footer` rename preserves the identical expression and assertions. No behaviour or assertion weakened.
- All three round-1 fixes still bite — verified by mutation, not by inspection.
- The confirmed path runs 9-6's seam **after** `commitSlot`'s recompute, where the task text reads "seam … then 9-2's recompute". The in-source reasoning is sound (the recompute derives rows/badges from raw keys + retained enumeration and consumes nothing the load produces; the load needs the mirrored keys), and reordering would mean forking `commitSlot`. Non-blocking, but 9-6 should confirm it still holds when the body lands.
- The failure path's early return ahead of `loadNewlyLiveSlot` has no structural pin, unlike the nil-persister path (`seamGuardedOnNilPersister`). Undetectable behaviourally while the seam is empty; worth a companion guard when 9-6/9-7 land.
- Test-harness fragility (not this task's defect): `slotConfirmPanelText` → `renderRecomputePanel` → `renderThemePanel` nil-derefs inside `bubbles/list` when handed a **closed** panel, so under the "confirm arm after `Esc`" mutation the binary panics and aborts the remaining package tests instead of failing cleanly. Production is guarded (`overlayThemePanelOnContent` returns early when `!open`).
