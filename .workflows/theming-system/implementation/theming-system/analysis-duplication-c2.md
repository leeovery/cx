AGENT: duplication
FINDINGS:
- FINDING: The "load an embedded built-in in a test" helper is written seven times across five packages
  SEVERITY: medium
  FILES: internal/tui/theme_testing_test.go:36, internal/tui/model_test.go:24, internal/capture/theme_panel_fixture_test.go:725, internal/capture/theme_panel_fixture_render_test.go:370, cmd/open_theme_nomination_test.go:185, cmd/capturetool/main_test.go:627, internal/theme/resolution_test.go:43
  DESCRIPTION: Seven independently-authored helpers do the identical thing — `theme.NewLoader(nil).LoadBuiltin(slug)` (or `NewSilentLoader()`), Fatal on `!found`, Fatal on `rejection != nil`, return `result.Theme`. Three pairs are byte-identical bodies with near-identical doc comments (`builtinPalette` / `builtinTheme` in internal/capture; `testBuiltinTheme` / `darkBuiltinTheme` in internal/tui; `builtinThemeForTest` in cmd). Cycle 1's task 11-13 already collapsed two of these inside `package capture_test` (`darkBuiltin`, `nordBuiltin`) but could not reach `builtinTheme`, because it lives in `package capture` and the extracted helper landed in `package capture_test` — the same in-directory package split separates `testBuiltinTheme` (`package tui`) from `darkBuiltinTheme` (`package tui_test`). The split is exactly why the duplication keeps regenerating: there is no importable home, so each package pair mints its own. Two of the seven (`darkBuiltinTheme`, `builtinForTest`) have already drifted — they collapse `!found` and `rejection != nil` into one failure message, so they cannot tell "the slug names no built-in" from "the shipped file is broken", which is precisely the discrimination the build-time guarantee exists to surface.
  RECOMMENDATION: Add `Builtin(t *testing.T, slug string) theme.Theme` to the existing test-only `internal/themetest` package (already imported by cmd, cmd/capturetool, internal/tui and internal/theme's external test package for `Lines`/`Write`), with the two-way failure discrimination kept. Optionally add the two shipped-default wrappers `DefaultDark(t)` / `DefaultLight(t)` for the ~dozen call sites that only want the pair. Delete all seven local helpers and re-point their callers; `internal/tui`'s `testDarkTheme`/`testLightTheme` and `internal/capture`'s `darkBuiltinTheme`/`lightBuiltinTheme` become one-line wrappers over the shared helper. No import cycle: `themetest` imports `internal/theme`, and every consumer above is either a different package or an external `_test` package.

- FINDING: The "SGR parameter run from a rendered probe" derivation is written eight times, six of them in one package
  SEVERITY: medium
  FILES: internal/tui/header_test.go:19, internal/tui/header_test.go:36, internal/tui/edit_modal_test.go:687, internal/tui/active_theme_test.go:93, internal/tui/active_theme_test.go:57, internal/tui/canvas_paint_test.go:15, internal/tui/session_row_anatomy_test.go:52, internal/tui/model_test.go:38, internal/capture/theme_swap_guard_test.go:212, internal/capture/theme_panel_fixture_test.go:740
  DESCRIPTION: Eight helpers (plus two inline copies) implement the same three lines: render a one-cell probe through a lipgloss style, find `'['` and `'m'`, slice out the parameter run. `tokenBgSeq` (header_test.go:36) and `bgSeq` (edit_modal_test.go:687) are byte-identical bodies in the SAME package — nothing separates them but the file they sit in. `sgrForegroundCore` (active_theme_test.go:93) is the same body again with a raw hex instead of a token, and `tokenFgSeq` (header_test.go:19) the same with a token foreground. `canvasSeq` (canvas_paint_test.go:15) and `selectionBgParams` (session_row_anatomy_test.go:52) are the same derivation returning two further shapes of the result (full `ESC[…m` prefix; params via TrimPrefix/TrimSuffix), and `assertActiveTheme` (active_theme_test.go:57) and `editFieldFocused` (model_test.go:38) inline it again. Six of the ten sites are `package tui`, so nothing structural prevents sharing. The drift is already visible in the failure messages and in the three different return shapes for one derivation, which is what makes a call site pick the wrong one.
  RECOMMENDATION: Put one `sgrParams(t *testing.T, style lipgloss.Style) string` in `internal/tui`'s shared test-helper file (theme_testing_test.go, `package tui`) returning the bare parameter run, plus the two thin wrappers the call sites actually want — `tokenFgSeq(t, tok)` and `tokenBgSeq(t, tok)` over it. Delete `bgSeq`, fold `sgrForegroundCore` into `sgrParams(t, lipgloss.NewStyle().Foreground(lipgloss.Color(hex)))`, express `canvasSeq` as `"\x1b[" + sgrParams(...) + "m"` and `selectionBgParams` as `sgrParams(...)`, and inline-replace the two probe copies in `assertActiveTheme` / `editFieldFocused`. In `internal/capture`, collapse `backgroundSGR` into `sgrParameterRun` the same way (the package split means capture keeps its own single copy).

- FINDING: Three near-identical ~28-line `NoConfigAccess` fixture tests, plus two hand-rolled valid-theme-file bodies that bypass internal/themetest
  SEVERITY: medium
  FILES: internal/capture/theme_panel_fixture_test.go:628, internal/capture/theme_panel_remaining_fixtures_test.go:595, internal/capture/theme_panel_message_fixtures_test.go:654, internal/capture/theme_panel_fixture_test.go:669, internal/capture/theme_panel_remaining_fixtures_test.go:627
  DESCRIPTION: `TestPanelFixture_NoConfigAccess`, `TestPanelFixture_RemainingFramesNoConfigAccess` and `TestPanelFixture_MessageFramesNoConfigAccess` each carry the same body: TempDir, `MkdirAll(dir/portal/themes)`, write `decoy-drop-in.theme`, `t.Setenv` of `XDG_CONFIG_HOME` / `PORTAL_THEMES_DIR` / `PORTAL_PREFS_FILE`, render the frame, assert the frame does not contain `"decoy-drop-in"`, then assert the config dir holds exactly one entry named `portal`. The three differ only in which fixture-name list they range over and which of three render entry points they call (`driveToPanel(fx.Deps(...))`, `panelFixtureFrame(...)`, `fx.ModelAt(...).View().Content`). Two of the three are in the same package (`capture_test`) and already share `validThemeFileBody`, so nothing prevents sharing the setup either. Separately, `decoyThemeFile` (theme_panel_fixture_test.go:669) and `validThemeFileBody` (theme_panel_remaining_fixtures_test.go:627) are identical loops over `theme.TokenNames()` emitting `name = #123456` — a third and fourth implementation of the valid-theme-file body that cycle 1 single-sourced into `themetest.Lines()`, reached from every other package. The two panel-fixture-name enumerations are a related copy: `panelFixtureNames()` (theme_panel_fixture_test.go:45, ten names) restates the union of `capturePanelFixtureNames()` + `remainingPanelFixtureNames()` + `messagePanelFixtureNames()` (theme_panel_remaining_fixtures_test.go:66), so adding a panel fixture needs both lists edited.
  RECOMMENDATION: Extract `stageDecoyConfig(t) (configDir string)` (TempDir + decoy write + the three `t.Setenv`s) and `requireConfigUntouched(t, configDir, frame string)` (the decoy-absent + one-entry assertions) into the `capture_test` helper file, and have the two `capture_test` tests become a range over their names calling those two plus their own render. Replace `validThemeFileBody` and `decoyThemeFile` with `[]byte(strings.Join(themetest.Lines(), "\n") + "\n")` — or add `themetest.Body()` returning exactly that — so the valid-file shape has the one home cycle 1 gave it. Derive `panelFixtureNames()` from `allPanelFixtureNames()` (or from `capture.FixtureNames()` filtered on the `theme-panel-` prefix, as `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet` already does) so the set is stated once. The third test lives in `package capture` and cannot import the `capture_test` helpers; move it to `capture_test` if nothing in it needs unexported access, otherwise leave it as the one intentional copy.

- FINDING: The "which of prefs.json's three theme keys are in force" rule is implemented twice, in two packages
  SEVERITY: medium
  FILES: internal/theme/union.go:383, cmd/doctor_theme.go:259
  DESCRIPTION: `inForceValues` (union.go) and `persistedThemeNominations` (doctor_theme.go) are independent implementations of one rule with three identical clauses: run `ResolveSetting`; under a constant yield the constant alone; otherwise yield only the slots with a NON-EMPTY raw value; and collapse the case where both raw slots name the same value. They differ only in how they render the collapse — the union yields one deduped value, doctor yields one nomination labelled `both`. Both files' own comments record that the two must agree ("Doctor's persisted line makes the identical call for the identical reason, so the two surfaces cannot disagree about which slug is live" — union.go:369-372), which is the tell: an invariant stated in prose across a package boundary is exactly the shape that drifts. A change to the tiebreak, to the empty-slot rule, or to the collapse has to be made in both, and nothing fails if only one moves — the panel would list a row doctor does not report, or the reverse.
  RECOMMENDATION: Export one selector from `internal/theme` — e.g. `InForceKeys(keys RawKeys) []InForceKey` where `InForceKey` carries the persisted `Slug` and the `Slot`s it occupies (constant / light / dark / both) — and single-source the three clauses there. `inForceValues` becomes a map over its `Slug` field; `persistedThemeNominations` becomes a map over the pair, converting `Slot` to its rendered label through the existing `theme.Slot.AttrName` / `themeSlotBoth`. No behaviour changes and both surfaces keep their own rendering.

- FINDING: The theme-panel test fixtures repeat two model-construction sequences across nine files
  SEVERITY: medium
  FILES: internal/tui/theme_panel_geometry_test.go:381, internal/tui/theme_panel_chrome_test.go:47, internal/tui/theme_panel_arrow_test.go:174, internal/tui/theme_panel_behaviour_test.go:159, internal/tui/theme_panel_commit_test.go:66, internal/tui/theme_panel_cursor_test.go:37, internal/tui/theme_panel_close_test.go:56, internal/tui/theme_panel_commit_recompute_test.go:49, internal/tui/theme_panel_entry_test.go:100
  DESCRIPTION: Two distinct ~15-20 line sequences are each written out several times. (1) THE OPEN SEQUENCE — `Build(deps)`, assign `termWidth`/`termHeight`, `applySessions(...)`, `applySessionListSize(...)` (sometimes `applyProjectListSize`), optionally assert the content region resolved, `pressThemeKey`, `t.Fatal` unless `themePanel.open` — appears in `newGeometryPanelModel`, `newChromePanelModel`, `newArrowPanelModelAt`, `behaviourPanelAt` and `openCommitPanel`. `newGeometryPanelModel` and `newChromePanelModel` are the closest pair: same `newArrowPanelDeps` source, same `geometryTerm` dims, same three-session seed shape, same two list sizings, same two content-region Fatal guards, same open assertion; `newChromePanelModel` adds one width assertion and its own session names. (2) THE CONSTRUCTION-TIME RESOLUTION SEQUENCE — `theme.ResolveSetting(keys...)`, `loader.ResolveNomination(setting, dir)`, Fatal on error, then `New(fakeLister{}, WithThemeEnumerator(countingEnumeratorOver(loader, dir)), WithThemeKeys(keys), WithThemeNomination(resolution.Nomination), WithCanvasMode(...))` — appears verbatim in `themeCursorModel` and `newClosePanelModel`, and `behaviourNomination` (behaviour_test.go:130) is a third variant of the same resolve-then-Fatal step. All are `package tui`, so nothing structural separates them.
  RECOMMENDATION: Extract two helpers into the package's shared theme test-helper file. `openPanelForTest(t, m Model, contentW, contentH int) Model` — assigns the dims via `geometryTerm`, seeds the standard session set, sizes both lists, asserts the content region, presses `t` and asserts `themePanel.open` — which `newGeometryPanelModel`, `newChromePanelModel`, `newArrowPanelModelAt`, `behaviourPanelAt` and `openCommitPanel` then call, keeping only their own Deps and their own extra assertions. And `newDirBackedPanelModel(t, dir string, keys theme.RawKeys, mode canvasAppearance) (Model, *countingThemeEnumerator)` for sequence (2), which `themeCursorModel` and `newClosePanelModel` reduce to one call each (close_test adding only its sink-bound loader).

- FINDING: Two declared-value ThemeEnumerator test doubles survive in package tui
  SEVERITY: low
  FILES: internal/tui/theme_enumerator_fake_test.go:19, internal/tui/theme_panel_open_test.go:48
  DESCRIPTION: `fakeThemeEnumerator` and `recordingThemeEnumerator` are both `package tui`, both answer all four seam methods from declared values with no filesystem, and both record what they were asked; their `ResolveSlot` bodies are byte-identical (`SlotResolution{Slot: slot, Requested: slug, Resolved: slug}`) and their `Reassemble` bodies differ only in the split-union branch. The only real difference is WHICH calls each records: the fake counts `reassembles` and records `settings` and `slotLoads`, the recorder counts `opens` and records `keys`. Cycle 1's task 11-9 collapsed two other fakes into `fakeThemeEnumerator` for exactly this reason and left this third one behind, so the package now has one configurable fake plus a second fake that is a strict subset of it bar two counters.
  RECOMMENDATION: Move `opens` and `keys` onto `fakeThemeEnumerator` (incremented/appended in its existing `Open`), re-point `theme_panel_open_test.go` and `theme_panel_entry_test.go`'s `newEntryEnumerator` at it, and delete `recordingThemeEnumerator`. Leave `countingThemeEnumerator` and `behaviourEnumerator` alone — both EMBED the production `theme.DirEnumerator` and are deliberately not declared-value fakes, and `fixtureThemeEnumerator` (theme_seams_test.go, `package tui_test`) is unavoidable across the package split.

- FINDING: The `.theme` extension is restated outside internal/theme in two packages
  SEVERITY: low
  FILES: cmd/capturetool/main.go:55, internal/capture/fixtures.go:747, internal/capture/fixtures.go:1035, internal/capture/fixtures.go:1070, internal/capture/fixtures.go:1084, internal/capture/fixtures.go:1257, internal/capture/fixtures.go:1264, internal/theme/name.go:12
  DESCRIPTION: `internal/theme` keeps `themeExtension = ".theme"` unexported, so every other package that needs it restates it: `cmd/capturetool` declares its own `themeFileExtension` constant (with a comment acknowledging the restatement), and `internal/capture/fixtures.go` carries twelve inline `".theme"` literals composing fixture filenames. The extension is a published user-facing contract (it is what `docs/theming.md` and the drop-in workflow print), so it is a magic string with more than one home rather than a package-private detail.
  RECOMMENDATION: Export it as `theme.FileExtension` (keeping the unexported alias inside the package if the existing call sites read better that way). `cmd/capturetool` then drops its own constant and `isThemePath` reads the shared one; `internal/capture/fixtures.go` composes its filenames as `slug + theme.FileExtension`, which also removes the risk of a fixture filename disagreeing with the slug it declares beside it.
SUMMARY: The production theme code is tightly consolidated — cycle 1's extractions hold and the only production duplication left is the "keys in force" selection rule implemented once in internal/theme and once in cmd/doctor. The residue is concentrated in test scaffolding: a built-in-loading helper written seven times, an SGR-probe derivation written eight times (six in one package), three near-identical no-config-access fixture tests that also re-implement the theme-file body cycle 1 moved into internal/themetest, and two repeated panel-model construction sequences. Most of it regenerates because the natural home is across an in-directory `package X` / `package X_test` split, which is what makes internal/themetest the right destination for the cross-package pieces.
