AGENT: duplication
FINDINGS:
- FINDING: Three independently authored panel-frame parsers in the capture panel-fixture tests
  SEVERITY: medium
  FILES: internal/capture/theme_panel_fixture_render_test.go:63, internal/capture/theme_panel_message_fixtures_test.go:87, internal/capture/theme_panel_remaining_fixtures_test.go:127, internal/capture/theme_panel_remaining_fixtures_test.go:142, internal/capture/theme_panel_remaining_fixtures_test.go:160
  DESCRIPTION: Three separate task-authored test files in `package capture_test` each built their own parser for the same rendered slide-over frame, and all three implement the identical primitive: split the frame on newlines, `strings.Cut(line, panelBorder)` to take the panel side, `ansi.Strip` it, tokenise with `strings.Fields`, and fatal with the stripped frame when nothing matched. `panelRows` returns a label→{cursor,label,badge} map; `panelLinesOf` returns an ordered []panelLine; `uniquePanelLine` (behind `panelLineWith` / `panelRowLine`) returns one matching line and its index. Worse than the boilerplate is that the ROW-SHAPE RULE is stated twice: both `panelRows` (render_test:76-80) and `panelRowLine` (remaining:152-156) independently re-implement "drop the leading `▌` field, then the first remaining field is the label". That rule is a fact about theme_row.go's composition, and two copies of it can disagree the moment the cursor column changes — in tests whose entire job is to catch such a change. The three parsers also disagree today on whether they strip ANSI before matching (`panelRows`/`uniquePanelLine` do; `panelLinesOf` keeps a raw copy alongside), so an assertion's meaning depends on which file it happens to live in.
  RECOMMENDATION: Extract one panel-frame projection into a shared file in `package capture_test` (e.g. `panel_frame_test.go`): `panelLines(t, frame) []panelLine` carrying `raw`/`visible`, plus one `fields()` accessor that owns the drop-the-cursor-bar rule. Re-express `panelRows`, `panelLineIndex`, `panelLineWith` and `panelRowLine` as thin projections over it; delete the three private line loops and the second copy of the cursor rule.

- FINDING: Five near-identical fixture-registration/guard tests partitioned only by task subset
  SEVERITY: medium
  FILES: internal/capture/theme_panel_fixture_render_test.go:381, internal/capture/theme_panel_fixture_render_test.go:399, internal/capture/theme_panel_message_fixtures_test.go:606, internal/capture/theme_panel_message_fixtures_test.go:626, internal/capture/theme_panel_remaining_fixtures_test.go:562
  DESCRIPTION: Three task-authored files each declare their own "is this fixture registered and guarded?" tests over their own slice of the panel fixture set. `TestPanelFixture_RegisteredInBothRegistries` and `TestPanelFixture_MessageFramesRegistered` are byte-identical bodies (same two assertions, same error strings) differing only in which name accessor they range over; `TestPanelFixture_UnderTheGuard` and `TestPanelFixture_MessageFramesUnderTheGuard` are likewise byte-identical; `TestPanelFixture_AllRegistered` folds the same three claims into one loop over the third subset. The partition is an artefact of which task added which fixture, not a behavioural distinction — the claims are identical for every `theme-panel-*` fixture. `allPanelFixtureNames()` (remaining:65) already exists as the union of the three subsets, and `registeredPanelFixtureNames()` (render:104) already derives the set from the registry, so the consolidated form is already reachable. The same partition-by-task shows in six parallel name accessors: `panelFixtureNames` (fixture_test:45, package capture), `capturePanelFixtureNames` (render:358), `messagePanelFixtureNames` (message:44), `remainingPanelFixtureNames` (remaining:47), `allPanelFixtureNames` (remaining:65) and `registeredPanelFixtureNames` (render:104) — with the `"theme-panel-"` prefix literal restated between the last two.
  RECOMMENDATION: Replace the five tests with one table-free test ranging over `registeredPanelFixtureNames()` asserting resolvable + enumerated + guarded, keeping `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet` (remaining:594) as the separate exact-set claim it already is. Keep the per-task name slices only as inputs to `allPanelFixtureNames()`; single-source the `theme-panel-` prefix so the two registry derivations read one constant.

- FINDING: The "abort on undecodable prefs.json" contract re-authored per saver, with the case table already drifted
  SEVERITY: medium
  FILES: internal/prefs/store_write_path_test.go:132, internal/prefs/migration_marker_test.go:440, internal/prefs/translation_saver_test.go:372, internal/prefs/theme_savers_test.go:351
  DESCRIPTION: Four newly-authored test files — all `package prefs`, all already sharing `seedPrefsFile` / `readRaw` / `assertUntouched` — each restate the corrupt-prefs.json case table and the seed→save→assert-untouched loop for their own saver. The tables have already diverged, which is exactly the failure mode: `store_write_path_test.go` carries four cases (truncated object, trailing comma, junk, unterminated-with-real-values) and pushes the zero-byte and top-level-array shapes into two separate tests (`:169`, `:263`); `migration_marker_test.go` and `translation_saver_test.go` each carry five (adding zero-byte and top-level array, dropping the unterminated-with-real-values case); `theme_savers_test.go` covers the contract with one inline literal. Only two of the four assert the decoder's error is returned verbatim as `*json.SyntaxError`. Since `mutate` is the single write-path chokepoint every saver routes through, these are five statements of one contract that can no longer be read as one — a corruption shape added to any one table leaves the other savers unproven against it.
  RECOMMENDATION: Add one `undecodablePrefsCases()` table beside the existing shared helpers in `store_write_path_test.go`, holding the union of the six shapes, and have all four abort tests range over it. Keep each test's saver-specific assertions (persisted=false for SaveTranslation, no-error-variance for the marker) in the loop body; drop the three restated tables and fold `TestSave_AbortsOnEmptyFile` / `TestSave_TopLevelTypeMismatchAborts` into the shared table so the store's own coverage is not the outlier.

- FINDING: Module-root resolution re-implemented twice beside the shared helper one of the guards already uses
  SEVERITY: medium
  FILES: internal/tui/theme_source_guard_test.go:259, internal/theme/broken_builtin_test.go:441, internal/prefs/appearance_api_guard_test.go:49
  DESCRIPTION: Three newly-authored repo-wide source guards each need the module root. `internal/prefs/appearance_api_guard_test.go` calls the existing shared `portalbintest.ProjectRoot()`; `internal/tui/theme_source_guard_test.go:repoRoot` and `internal/theme/broken_builtin_test.go:themeRepoRoot` each hand-rolled the same ~18-line walk (Abs(".") then loop upward stat-ing for `go.mod`, fatal at the filesystem root), down to the same failure wording "could not locate the module root (no go.mod above the package dir)". Three executors met the same need; one found the shared helper, two did not. `portalbintest` is not integration-tagged (its own tests are untagged and it is already consumed from the unit lane), so both hand-rolled copies are removable with no lane change.
  RECOMMENDATION: Delete `repoRoot` and `themeRepoRoot` and call `portalbintest.ProjectRoot()` at both sites, wrapping its error in the caller's `t.Fatalf` as the prefs guard already does.

- FINDING: Repo-wide .go source walk duplicated four ways with divergent exclusion rules
  SEVERITY: medium
  FILES: internal/tui/theme_source_guard_test.go:279, internal/tui/restore_source_guard_test.go:295, internal/theme/broken_builtin_test.go:392, internal/prefs/appearance_api_guard_test.go:58
  DESCRIPTION: Four newly-authored guards each walk the whole repo for `.go` files with their own `filepath.WalkDir` closure, and the walks no longer agree on what they walk. Three skip every dot-directory (`strings.HasPrefix(d.Name(), ".")`); `internal/prefs/appearance_api_guard_test.go` instead skips only `.git`, `vendor` and `node_modules`, so it descends into `.workflows`, `.claude` and `.github` while the others do not. The two inside `internal/tui` are the sharpest case: `forEachGoFile` and `allGoFiles` sit in the SAME package (`tui_test`), and their walk bodies are line-for-line identical — same dir-skip, same `.go` suffix test, same "walk %s: %v" fatal — differing only in what they do per file (parse imports-only versus collect the path). The in-source comment at restore_source_guard_test.go:293 justifies the second *parse mode*, which is real, but the *enumeration* needs no second copy. As these guards are the enforcement layer for "this identifier/import must not exist anywhere", a divergence in which directories are walked is a silent hole in whichever guard is narrower.
  RECOMMENDATION: Within `tui_test`, re-express `forEachGoFile` as a loop over `allGoFiles(t, root)` so one walk feeds both. Across packages, promote the walk beside `portalbintest.ProjectRoot` (e.g. `portalbintest.GoSourceFiles(root)`) with one stated exclusion rule, and point all four guards at it so "what the repo-wide guards cover" is a single decision.

- FINDING: cmd's tree fingerprint re-implements portaltest's snapshot/diff helper
  SEVERITY: low
  FILES: cmd/theme_test.go:488, internal/portaltest/fingerprint.go:62
  DESCRIPTION: `snapshotTree` walks a directory and returns path → "mode + sha256" so three cmd tests (cmd/theme_test.go:601, cmd/doctor_theme_test.go:540, cmd/doctor_fix_theme_test.go:280) can assert a command created, deleted or rewrote nothing. `internal/portaltest` already exports exactly that capability — `SnapshotStateDir` (Lstat-based, sha256-hashed, symlink-aware) plus `DiffFingerprints` / `FormatDelta`, whose whole documented purpose is "compare two snapshots at caller-chosen points" — and the cmd test package already imports `portaltest` elsewhere. The hand-rolled version is also weaker on the properties the assertions care about: it uses `entry.Info()` rather than Lstat, so a file-to-symlink swap is invisible to it, and a `maps.Equal` failure prints two whole maps where `FormatDelta` names the changed path.
  RECOMMENDATION: Replace `snapshotTree` + `maps.Equal` with `portaltest.SnapshotStateDir` + `DiffFingerprints`, reporting via `FormatDelta`; delete the local walk.

- FINDING: The log-sink vacuity subtest duplicated verbatim across two cmd test files
  SEVERITY: low
  FILES: cmd/theme_test.go:474, cmd/doctor_theme_test.go:498
  DESCRIPTION: Both files close their "this command emits no `theme` records" test with a byte-identical nine-line subtest that constructs a `logtest.Sink`, installs it, emits one `theme.NewEventLogger(log.For("theme")).Rejected(...)` and requires exactly one captured record — the vacuity guard proving the capture harness would have seen an emission. Same package, same helper stack, same wording of the failure. A third silent-path test (cmd/doctor_fix_theme_test.go:427 `TestDoctorFix_EmitsNoThemeRecords`) makes the same silence claim and would want the same guard, so the pattern is on its way to a third copy.
  RECOMMENDATION: Extract `requireSinkCapturesThemeEvent(t)` (or fold it into a shared `assertNoThemeRecords(t, run func())` that runs the command, asserts zero records and then proves the sink live) into one cmd test file; call it from all three silent-path tests.

- FINDING: The dark/light theme pair restated as a literal at 102 assertion sites
  SEVERITY: low
  FILES: internal/tui/theme_testing_test.go:139, internal/tui/theme_testing_test.go:144, internal/tui/edit_modal_test.go, internal/tui/rename_modal_test.go, internal/tui/session_row_anatomy_test.go
  DESCRIPTION: `for _, th := range []theme.Theme{testDarkTheme(t), testLightTheme(t)}` appears 102 times across 31 files, all in `package tui`. The two accessors are already single-sourced (theme_testing_test.go), but "the pair a render assertion walks" is not — it is a slice literal re-typed at every site. The helper file's own comment states the reason the accessors exist ("a per-site load would make the suite's notion of 'the dark theme' re-derivable in several hundred places"); the same argument applies one level up to the pair. It is mechanical rather than risky, which is why it is low: no site can silently diverge in meaning, only in membership if a third built-in ever needs enrolling.
  RECOMMENDATION: Add `themePair(t) []theme.Theme` beside `testDarkTheme`/`testLightTheme` and range over it. Worth doing opportunistically rather than as a sweeping edit.

- FINDING: The paginated fixture's union restates the base row set instead of deriving it
  SEVERITY: low
  FILES: internal/capture/fixtures.go:745, internal/capture/fixtures.go:1255
  DESCRIPTION: `themePanelPaginatedUnion` re-declares the same four base rows `themePanelUnion` declares — the one drop-in plus the three built-ins, in the same display order, with the same `Source` tags — before appending its synthetic drop-ins. Every other derived panel fixture in this file composes from its base (`themePanelConfirmFixture` from the constant frame, `themePanelNarrowFixture` and `themePanelPaginatedFixture` from the adaptive-pair frame), and the file's own comments lean on those pairs being controlled. The same holds for the retained parse: `themePanelPaginatedEntries` re-states the shared drop-in entry that `themePanelEnumeration` already builds. A built-in added to the embedded set changes one of the two lists and not the other.
  RECOMMENDATION: Build the paginated union by appending the synthetic rows to `themePanelUnion().Rows` (re-deriving Count), and the paginated entries by appending to `themePanelEnumeration().Entries`, so the base set is declared once.
SUMMARY: The production code is clean — the new `internal/theme`, `internal/tui` panel and `cmd` sources single-source their rules well (one parse ladder, one resolution body, one restyle sequence, one canonical token table), and a mechanical scan of the 35 new production files surfaced no repeated logic at all. Every finding is in the test/harness layer, where separate task executors independently authored the same scaffolding: three panel-frame parsers and five near-identical fixture-registration tests in `internal/capture`, four restatements of the prefs abort-on-undecodable contract whose case tables have already drifted apart, and repo-walking guard machinery re-implemented two-to-four times — twice beside a shared helper a sibling guard already uses.
