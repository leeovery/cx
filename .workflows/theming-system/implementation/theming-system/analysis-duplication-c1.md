AGENT: duplication
FINDINGS:
- FINDING: The per-list canvas/colourless restyle sequence is now written three times
  SEVERITY: high
  FILES: internal/tui/model.go:1752 (applyCanvasMode), internal/tui/model.go:1867 (applyProjectCanvasMode), internal/tui/theme_panel.go:1146 (applyThemePanelCanvasMode)
  DESCRIPTION: All three functions run the identical six-step sequence on a `list.Model` — `SetDelegate(...)`, `canvas|colourlessHelpStyles`, `canvas|colourlessNoItemsStyle`, `canvas|colourlessPaginationDots`, `Styles.TitleBar` background/unset, `Styles.Title = stripListTitleColours(...)` — each with its own `if m.colourless` fork, so each step appears six times in total. The session/project pair pre-existed at two copies; this feature added the third (the panel) and added the `NoItemsStyle` step to all three. Drift is already visible: the panel arm omits the shared `listTitleBarStyle(...)` geometry wrapper the other two apply, and orders the `Title` strip before the fork rather than inside each branch. This is the exact hazard §11.2 names (a surface silently keeping the previous theme's cached `bubbles/list` styles), and it now runs on every arrow keypress in the panel — a fourth list, or a fifth cached style, has three places to be remembered.
  RECOMMENDATION: Extract one `applyListCanvasMode(l *list.Model, delegate list.ItemDelegate, th theme.Theme, colourless bool)` in model.go carrying the whole sequence including the colourless fork, and have all three call sites reduce to a delegate expression plus (where it genuinely differs) the `listTitleBarStyle` wrapper passed as an explicit argument or applied by the caller. Keep the panel's `if !m.themePanel.open` guard at its own call site.

- FINDING: Theme-file authoring helpers are re-implemented in five test files across four packages
  SEVERITY: high
  FILES: internal/theme/load_test.go:613 (themeLines) / :641 (withValue) / :653 (withoutKey) / :660 (writeTheme), cmd/capturetool/main_test.go:627 (themeFileLines) / :638 (withTokenValue) / :650 (withoutTokenLine) / :658 (writeThemeFile), internal/tui/theme_panel_open_test.go:202 (writeThemeFileForTest), internal/tui/theme_row_test.go:405 (writeThemeFile), cmd/open_theme_construction_test.go:651 (writeThemeFile)
  DESCRIPTION: `cmd/capturetool/main_test.go`'s four helpers are a verbatim copy of `internal/theme/load_test.go`'s four, down to the doc comments ("renders a complete, valid theme file: one `key = value` line per §2.4 token, in table order, each carrying a distinct value") and the `#abcd%02x` value generator — only the identifiers differ. Three further independent "write a valid 19-token theme file into a temp dir" writers exist (two of them inside package `tui`, which could share directly). Six copies of the fixture format means a loader-format change has six places to follow, and the `tui` pair already diverges pointlessly on file mode (0o644 vs 0o600).
  RECOMMENDATION: Add a test-only helper package `internal/themetest` following the established `logtest` / `spawntest` / `transienttest` convention, exposing `Lines()`, `WithValue(lines, key, value)`, `WithoutKey(lines, key)` and `Write(t, dir, base string, lines []string) string`. Collapse all six sites onto it; at minimum collapse the two same-package `tui` writers into one immediately.

- FINDING: The panel's vertical footer row duplicates the help modal's key-column row algorithm
  SEVERITY: medium
  FILES: internal/tui/theme_panel_footer.go:107 (themePanelFooterRow), internal/tui/help_modal.go:157 (helpModalRow)
  DESCRIPTION: Both compose the same row: render `helpKeyGlyph(e)` in an accent token, measure it, pad the key column to a fixed width with `headerCanvasBg(...)`, render a canvas-backed gap, then render the action label in a text token, and `JoinHorizontal` the four segments. The two bodies differ only in the key token (destructive-aware vs always `AccentKey`), boldness, label token, the column-width constant, and a trailing `headerPadRight`. The copy relationship is acknowledged in-source (theme_panel_footer.go:35 references `helpKeyColumnWidth`), which is precisely the copy-paste-with-a-comment shape that drifts.
  RECOMMENDATION: Extract a shared `keyColumnRow(glyph, label string, keyTok, labelTok theme.Token, columnWidth int, gap string, th theme.Theme, colourless bool) string` (natural home: help_modal.go or a small row helper beside `renderKeyHint` in modal_footer.go) and have both callers supply their tokens, width and gap. The panel keeps its `headerPadRight` wrap at its own call site.

- FINDING: Four near-identical `ThemeEnumerator` test doubles in package tui, two of which re-implement the production adapter
  SEVERITY: medium
  FILES: internal/tui/theme_panel_cursor_test.go:77, internal/tui/theme_panel_commit_recompute_test.go:487, internal/tui/theme_panel_open_test.go:77 (realThemeEnumerator), internal/tui/theme_seams_test.go:49 (loaderThemeEnumerator), cmd/theme_enumerator.go:28 (themeEnumerator)
  DESCRIPTION: Two problems in one seam. (1) `stubThemeEnumerator.ResolveSlot` and `splitThemeEnumerator.ResolveSlot` are the same ~15-line body — record the ask, error-out, scan `resolution.Slots` for the slot, else synthesise from `Nomination.Select(slot == SlotDark)` — in the same package; the second's comment even says "see stubThemeEnumerator.ResolveSlot". Their `Open`/`Reassemble`/`Resolve` arms are likewise near-identical. (2) `realThemeEnumerator` and `loaderThemeEnumerator` are two hand-written copies of the production `cmd.themeEnumerator` adapter — the same four one-line delegations to `theme.Loader.Open / Reassemble / ResolveNominationFrom / ResolveSlot` — so the adapter that must not drift exists in three places, one of them unreachable from the other two by import.
  RECOMMENDATION: (1) Collapse the two same-package fakes into one configurable fake in a shared `theme_testing_test.go`-style file (fields for the opened union, the reassembled union, the resolution, an injectable error, and the recorded asks); the recompute suite's split-union case is a field, not a second type. (2) Move the four-method loader delegation into `internal/theme` as an exported adapter (e.g. `theme.DirEnumerator{Loader, Dir}`), have `cmd.newThemeEnumerator` return it, and delete both test re-implementations.

- FINDING: Package cmd builds each invalid-theme fixture class twice
  SEVERITY: medium
  FILES: cmd/doctor_theme_test.go:98 (sourceMissingTokens) / :112 (sourceBadColours) / :131 (sourceDuplicateKeyAt), cmd/theme_test.go:647 (missingTokenSource) / :634 (badColourSource) / :624 (duplicateKeySource)
  DESCRIPTION: Three pairs of helpers in the same package produce the same three §6.2 fixture classes from the same `themeKeyLines` / `themeSourceFromLines` primitives. Each pair differs only in parameterisation — the doctor set takes the key(s)/line to break, the export set hardcodes "the first key" / "drop line 0". A change to what makes a file `bad colour` (say the value domain widening) has to be made in two families that already share their inputs.
  RECOMMENDATION: Keep the parameterised trio (`sourceMissingTokens` / `sourceBadColours` / `sourceDuplicateKeyAt`), move them beside `themeKeyLines`/`themeSourceFromLines` in cmd/theme_test.go, and delete `missingTokenSource` / `badColourSource` / `duplicateKeySource`, calling the parameterised forms with the keys those tests want.

- FINDING: docs/theming.md's example theme is an unguarded verbatim copy of the shipped dark built-in
  SEVERITY: medium
  FILES: docs/theming.md:90-124, internal/theme/builtins/tokyo-night.theme, internal/theme/docs_guard_test.go:144 (TestThemingDocExampleThemeIsValid)
  DESCRIPTION: The doc's example block reproduces all 19 values of `tokyo-night.theme` byte-for-byte (only the header comment differs) and asserts the identity in prose: "These are the values of Portal's dark built-in." The guard test only checks the example parses as a *valid* theme, not that it still equals the built-in — so any value that moves in the `.theme` file (the spec's own §7.7 re-derivation contemplates exactly that) leaves the doc silently claiming to be the built-in while showing different colours.
  RECOMMENDATION: Either make the guard assert the example block equals `theme.BuiltinBytes(theme.DefaultDarkSlug)` modulo its header comment, or drop the values from the doc and point at `portal theme export tokyo-night` (which §12.1 exists to provide) with a short excerpt rather than the full palette.

- FINDING: The silent theme loader is constructed by hand at three call sites
  SEVERITY: medium
  FILES: cmd/theme.go:120, cmd/doctor_theme.go:112, cmd/capturetool/main.go:161
  DESCRIPTION: `theme.NewLoader(theme.NewEventLogger(log.Discard()))` — the "diagnose without emitting" loader — is written out identically three times, twice within package cmd. A fourth surface (or a change to how silence is expressed once `EventLogger` gains state) has three sites to find. Compounding it, two different packages now each define a `newThemeLoader()` with different bodies (cmd/open.go:760 emits through `themeLogger`; cmd/capturetool/main.go:160 discards), so the name no longer identifies the behaviour.
  RECOMMENDATION: Add one constructor for the silent form — `theme.NewSilentLoader()` in internal/theme, or a package-level `newSilentThemeLoader()` in cmd if it should stay a cmd concern — and route all three sites through it. Rename capturetool's `newThemeLoader` to match what it builds.

- FINDING: The slot → "light"/"dark" mapping is written twice, with a third literal set in doctor
  SEVERITY: medium
  FILES: internal/theme/events.go:267 (slotAttr), cmd/theme_persister.go:109 (themeSlotAttr), cmd/doctor_theme.go:69-73 (themeSlotLight/Dark/Both), internal/tui/theme_panel_confirm.go:244 (oppositeThemeSlot) / :259 (persistedSlotSlug), internal/tui/theme_panel_commit.go:310 (mirrorThemeSlot)
  DESCRIPTION: `theme.slotAttr` and `cmd.themeSlotAttr` are the same function — a light/dark switch returning the attr name plus a "named" bool — differing only in which of the two parallel slot enums they take (`theme.Slot` vs `prefs.ThemeSlot`). Both feed the same `theme` log component's `slot` attr, so the vocabulary a reader greps for is defined in two places; doctor then declares the same two strings again as its own constants. The two parallel enums are themselves the root: five small conversion helpers across tui/cmd exist only to move between them.
  RECOMMENDATION: Give `theme.Slot` a single exported name mapping (e.g. `func (s Slot) AttrName() (string, bool)`) and have `cmd.themeSlotAttr` convert `prefs.ThemeSlot` to `theme.Slot` and delegate, with doctor's `themeSlotLight`/`themeSlotDark` sourced from it. Separately consider whether `prefs.ThemeSlot` can be dropped in favour of `theme.Slot` (the persister's only need is light/dark), which would delete `oppositeThemeSlot`/`mirrorThemeSlot`'s conversion halves outright.

- FINDING: Three built-in-palette loaders and three SGR-probe helpers in one test package
  SEVERITY: low
  FILES: internal/capture/theme_panel_fixture_render_test.go:375 (builtinPalette) / :90 (panelFixtureFrame), internal/capture/swap_harness_test.go:144 (darkBuiltinTheme) / :208 (lightBuiltinTheme) / :223 (bgSeq), internal/capture/theme_panel_remaining_fixtures_test.go:186 (fgSeq) / :108 (panelFrameAt), internal/capture/theme_swap_guard_test.go:212 (sgrParameterRun)
  DESCRIPTION: All eight are in package `capture_test`. `builtinPalette(t, slug)`, `darkBuiltinTheme(t)` and `lightBuiltinTheme(t)` are the same ten-line "LoadBuiltin, Fatal on not-found, Fatal on rejection" body with the slug varying. `bgSeq` and `fgSeq` are the same eight-line SGR probe as the general `sgrParameterRun`, specialised to Background/Foreground. `panelFixtureFrame` and `panelFrameAt` are the same body, one hardcoding the harness size — its doc even opens "It exists beside panelFixtureFrame". (A fourth built-in loader lives in package `capture`, fixture_test.go:725, and a fifth in internal/tui/theme_testing_test.go:36 — cross-package, so covered by the `internal/themetest` recommendation above.)
  RECOMMENDATION: Keep `builtinPalette(t, slug)` and define the dark/light helpers as one-line calls to it; define `bgSeq`/`fgSeq` as one-line calls to `sgrParameterRun` with the appropriate style; define `panelFixtureFrame(t, fixture, palette)` as `panelFrameAt(t, fixture, palette, harnessWidth, harnessHeight)`.

- FINDING: The panel fixtures build the same enumeration and slot set two ways
  SEVERITY: low
  FILES: internal/capture/fixtures.go:762 (themePanelEnumeration), internal/capture/fixtures.go:985 (themesDirPath) / :995 (themePanelDirEntry) / :1001 (themePanelDirEnumeration), internal/capture/fixtures.go:804 (themePanelAdaptivePairFixture), internal/capture/fixtures.go:1234 (themePanelPaginatedFixture)
  DESCRIPTION: `themePanelEnumeration` hand-assembles the `Entry`+`Enumeration` shape that `themePanelDirEntry`/`themePanelDirEnumeration` exist to build, and re-writes the themes-directory path as a string literal twice although `themesDirPath` is declared for exactly that ("shared so no fixture invents a second, different-looking one"). Separately, `themePanelPaginatedFixture` repeats `themePanelAdaptivePairFixture`'s keys, two-record `themeSlots`, cursor and `captureKeys` verbatim — the two differ only in the union and enumeration — while every other derived panel fixture correctly starts from a base fixture and overrides.
  RECOMMENDATION: Redefine `themePanelEnumeration` as `themePanelDirEnumeration(themePanelDirEntry(themePanelDropInSlug+".theme", themePanelDropInSlug))` and delete the two path literals. Build the paginated fixture from `themePanelAdaptivePairFixture()` and override only `name`, `themeUnion` and `themeEnumeration`, as the narrow / commit-failed / min-height fixtures already do.

- FINDING: prefs.Store's tolerant and strict reads duplicate their read prologue
  SEVERITY: low
  FILES: internal/prefs/store.go:212 (readFile), internal/prefs/store.go:264 (readFileStrict)
  DESCRIPTION: The two reads share an identical eight-line prologue — `os.ReadFile`, ENOENT → `(zero, false, nil)`, other error → `(zero, false, err)`, then `json.Unmarshal` into a `prefsFile` — and differ only in what they do with a decode error. §8.9 makes the difference load-bearing, so the two must not merge, but the shared half being copied means an ENOENT/permission policy change has two homes.
  RECOMMENDATION: Extract `func (s *Store) readBytes() ([]byte, bool, error)` carrying the read and its ENOENT branch, and have both decoders start from it; each keeps its own `json.Unmarshal` error policy, which is the only thing that should differ.

- FINDING: The panel's floor and body arithmetic each sum the same four chrome components
  SEVERITY: low
  FILES: internal/tui/theme_panel.go:279 (themePanelMinHeight), internal/tui/theme_panel.go:1459 (themePanelListSize)
  DESCRIPTION: Both add `themePanelHeaderRows() + themePanelDirRowHeight(...) + <message rows> + themePanelFooterHeight(...)`, one with the floor's fixed one-row message and standing scope, the other with the live message height and live scope. The components are single-sourced individually, but the *set* of components is not: a fifth panel chrome element (or dropping one) must be added to both sums, and a floor that stops matching the render is precisely the "opens a broken frame" failure `themePanelFloor`'s own doc forbids.
  RECOMMENDATION: Extract `themePanelChromeRows(dirUnusable bool, messageRows int, footer []keymapEntry) int` returning the sum, and have `themePanelMinHeight` call it with `themePanelFloorMessageRows` + the standing scope and `themePanelListSize` call it with the measured message height + the live scope.

- FINDING: setSuccessFlash re-states setFlash's body rather than composing with it
  SEVERITY: low
  FILES: internal/tui/model.go:2493 (setFlash), internal/tui/model.go:2530 (setSuccessFlash), internal/tui/model.go:2519 (setThemeFlash)
  DESCRIPTION: `setSuccessFlash` repeats all four of `setFlash`'s statements (`flashGen++`, `flashText`, `flashOrigin = flashOriginDefault`, `resyncPageLayouts()`) to change one field, while the sibling `setThemeFlash` — added by this work — correctly composes (`m.setFlash(text); m.flashOrigin = flashOriginTheme`). The duplication predates this feature, but this feature added the new `flashOrigin` field to *both* copies, which is the drift the copy invites: a fourth flash-state field will need the same double edit.
  RECOMMENDATION: Redefine `setSuccessFlash` as `m.setFlash(text); m.flashKind = flashSuccess`, matching `setThemeFlash`'s shape, so the flash-raising sequence has one home.

SUMMARY: The theming work is well factored inside internal/theme, but three consolidation gaps stand out: the panel's list-restyle arm is a third hand-written copy of a sequence §11.2 identifies as the completeness risk, the panel's vertical footer re-implements the help modal's key-column row, and the test surface re-implements theme-file authoring, built-in loading and the ThemeEnumerator seam once per file across four packages. Most findings are mechanical extractions of existing code with no behaviour change; the docs/theming.md example and the two slot-name mappings are the two places where an unguarded copy can silently start lying.
