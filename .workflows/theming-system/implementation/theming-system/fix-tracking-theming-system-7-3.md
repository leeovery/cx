## Attempt 1

ISSUES:

- `/Users/leeovery/Code/portal/cmd/open_theme_nomination_test.go:30-32` — **the newly-added justification for widening the exemption is false.** The reviewer wired `collectThemeAdvisories(&DoctorDeps{ThemesDir: d})` into the `portal open` exec branch (`open.go`'s `openResolved` SessionResult arm, right before the connector handoff) and **the entire cmd suite stayed green**. The source half cannot see it (`doctor_theme.go` is exempt, and `collectThemeAdvisories` is not one of the three tracked local helpers at `:195`); the runtime half cannot see it either, because doctor's loader is `Discard`-backed by design, so the poisoned-directory read emits zero `theme` records.

  **The exemption decision itself is fine** — the rationale genuinely applies (`collectThemeAdvisories` has exactly one production caller, `doctor.go:275`), it is minimal (one file, and removing it flags exactly one function: `collectThemeAdvisories calls theme.NewEventLogger`), and nothing previously caught is now uncaught. The identical hole pre-exists for `cmd/theme.go` (the reviewer confirmed `resolveThemeSource` wired into the same branch also passes), so this is a widening in kind, not a weakening — **but the sentence claiming a backstop that does not exist is new**, and 7-4/7-5/7-6 all grow that same exempt file.

  FIX: make the claim true rather than deleting it — add `"collectThemeAdvisories": true` to the `local` map at `open_theme_nomination_test.go:195`. Validated by the reviewer: the clean tree stays green (`doctor.go`'s call sits inside `doctorCmd`'s composite-literal `RunE`, which is not an `*ast.FuncDecl`, so it is not recorded), and with the scan wired into open the guard fails with `open.go: openResolved calls collectThemeAdvisories`. Then reword `:30-32` to say the SOURCE half backstops the exemption via the tracked-helper list, and that the runtime half does not (a `Discard`-backed helper writes no record).

  ALTERNATIVE: leave the guard as-is and only correct the comment to state the exemption rests on the file being unreachable from `open` — cheaper, but leaves the regression uncaught by either half and leaves `theme.go` equally unprotected. The reviewer recommends the fix above; it costs one map entry and closes the class.

  CONFIDENCE: high

- `/Users/leeovery/Code/portal/cmd/doctor.go:212-214` — **the best-effort `themesDirPath()` resolution is the single line connecting this feature to production, and nothing pins it.** Deleting it (doctor then never scans the user's real themes directory) leaves `go test ./cmd/` fully green. Every scan test either injects `deps.ThemesDir` directly or asserts the *unresolvable* path yields nothing, so both halves pass with the production resolution gone.

  FIX: add a subtest to `TestThemeAdvisories_ReachTheDoctorReport` (`cmd/doctor_theme_test.go:641`) that seeds a broken drop-in, sets `t.Setenv("PORTAL_THEMES_DIR", dir)`, runs `runDoctorCmd(t, healthyDoctorDeps(t))` with **no** `ThemesDir` override, and asserts the advisory line appears — keeping the existing override-based case so the `doctorDeps.ThemesDir` override arm stays pinned too. Validated by the reviewer: passes on the clean tree, fails when the resolution line is deleted.

  CONFIDENCE: medium — the analogous `loadHookStore`/`loadProjectStore` resolutions in the same function are equally unpinned, so this is raising the bar rather than fixing a convention break; it is worth it here because the line is the whole feature's production wiring and the test costs ~10 lines.

NOTES:

- SPEC_CONFORMANCE conformant. §14A copy verbatim and single-sourced (`cmd/doctor_theme.go:22-23`): `⚠ theme %s: %s — %s` and `⚠ themes directory unreadable: %s`. §5.5's three directory states map correctly onto `Enumerate`'s two returns (`doctor_theme.go:75-86`): unusable → one pinned line and no per-file lines (Enumerate yields no entries there, including the ENOTDIR "regular file where a directory belongs" arm produced by `internal/theme/enumerate.go:217`); absent → nothing at all; usable → per-rejection lines. §12.3 honoured: `theme.NewEventLogger(log.Discard())` (`doctor_theme.go:44`) — mutating it to a real logger is killed by `TestThemeAdvisories_EmitsNoThemeRecords`. §6.2 one-reason-per-file preserved by delegating the ladder entirely to Phase 1; §12.2's exit-code exclusion untouched, an advisory-only run exits 0 (`cmd/doctor_theme_test.go:641`).
- Filename reasons genuinely deferred, not half-handled: the reviewer seeded `Nord.THEME`, `UPPER.theme` and a reserved `tokyo-night.theme` alongside a broken drop-in — only the owned reason produced a line.
- ACCEPTANCE_CRITERIA all met: the four owned reasons, the OS-error-exactly-once rule, valid/non-`.theme` silence, slug+`fromPrefs:false` identity, absent/unusable/unresolved directory arms, one-reason-per-file, zero log records, and the read-only tree+prefs assertions. AC 9's "still returns so 7-5's producer runs" is structurally satisfied — the skip sits in `scanThemesDirectory` (`doctor_theme.go:71-73`), not in `collectThemeAdvisories`.
- Killed mutations: dropped `slug`; `fromPrefs: true`; detail via `Rejection.Error()` (double-prefix); real logger instead of `Discard`; first render reverted to `nil`; dropped `ThemesDir` override.
- CONVENTIONS followed — no `t.Parallel`, `*Deps` seam extended per the existing per-field nil/empty idiom, hermetic temp dirs, root-skip + chmod cleanup on the 0000 fixtures, vacuity guards throughout (`requireDeniedRead`, the "sink captures a theme event" subtest, the snapshot-size guard), copy single-sourced as consts. `gofmt`/`go vet`/`golangci-lint` clean; full unit lane green.
- ARCHITECTURE sound — `collectThemeAdvisories` → `scanThemesDirectory` → `themeFileAdvisory` → `rejectionDetail` splits exactly along the seams 7-4/7-5/7-6 extend, and nothing re-derives Phase 1's ladder or detail formats. Existing doctor tests needed no change because `TestMain` poisons `PORTAL_THEMES_DIR` to an absent path, which the silent-absent arm handles.
- Two surviving mutations that are behaviour-equivalent, not defects: (a) making the `default:` arm at `doctor_theme.go:121-124` emit the generic frame survives the suite — no fixture exercises `bad name`/`reserved name`, which 7-4 owns per the phase boundary; (b) deleting the `dir == ""` guard at `doctor_theme.go:71-73` survives, because `os.Stat("")` returns ENOENT and `Enumerate` treats it as the absent arm. The guard is correct defensive intent (it also makes "no Enumerate call" literal per the Do-item) — keep it.
- `rejectionDetail`'s `Err` fallback (`doctor_theme.go:139-141`) is unreachable today: `internal/theme/load.go:263` sets `Detail: err.Error()` alongside `Err`. The task text mandated the fallback, and the doc comment is honest about the dual carriage, so this is fine — just untestable.
- AC 10's "a `logtest.Sink` installed for the whole run" is exercised at producer level, not across a full `portal doctor` Execute. Adequate today (the scan is doctor's only theme-touching code), but 7-5 adds a prefs read to the same function — worth promoting the sink assertion to the Execute-level test then.
- Guard-comment nit for whoever fixes issue 1: the parenthetical "on a bootstrap-exempt verb" is true but does no work for this guard — reachability from `open`, not bootstrap exemption, is what carries the exemption.
