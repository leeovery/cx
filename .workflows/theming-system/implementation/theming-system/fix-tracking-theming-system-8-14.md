## Attempt 1

ISSUES:

- `internal/tui/help_modal_m_suppression_test.go:133-155` (and the file header claim at `:12-13`) — `TestSessionsFooter_NeverListsMultiSelect` still asserts "`m` is a non-Core descriptor entry, so the condensed Sessions footer never lists it". **§14.1 makes that false**: `m` is Core and the footer *does* render `m multi` on a supported terminal. The assertion is now **vacuous in all three of its cases** — it greps for the lowercase substring `multi-select`, which the footer can no longer contain in any state (supported → `m multi`; unsupported → entry filtered). The executor correctly tombstoned the analogous tautology (`TestPanelEntry_LeavesDescriptorsUnfiltered`) but left this one asserting the superseded rule.

  FIX: Re-point it at the new contract rather than deleting it — the file's value is its identity-shaped matrix. Keep the three cases, add the supported/unsupported split, and assert on `"m multi"`: ghostty ⇒ the footer carries `m multi`; `appleTerminalIdentity()` and `spawn.Identity{}` ⇒ it does not. Rename to something like `TestSessionsFooter_ListsMultiOnlyWhenFunctional`, and correct the header comment at `:12-13` (which states the same superseded "never lists `m` (non-Core)" claim), citing §14.1/§14.3.

  ALTERNATIVE: Delete it with a tombstone comment pointing at `TestFooterRevision_BlockedMultiKeyFilteredInLockstep`, mirroring the treatment given to `TestPanelEntry_LeavesDescriptorsUnfiltered`. Cheaper, but loses the per-identity matrix (the NULL/remote shape in particular), which the new test does not reproduce — the re-point is recommended.

  CONFIDENCE: high

- `internal/tui/footer_revision_test.go:266,280` — **the Projects help half of the lockstep claim does not exercise the wiring this task introduced.** It asserts against `helpModalBody(tc.keymap(blocked), …)`, i.e. the test composes the filtered slice itself. The wiring is `model.go:4875` (`renderHelpModalOnClearedCanvas(m.projectsHelpKeymap(), …)`, changed from `projectsKeymap()`); **reverting that one line leaves the entire suite green.** The footer half of the same subtest is asserted correctly through the composed path (`Model.renderProjectsFooterForFilterState`), so the inconsistency is within one test.

  FIX: In the `projects` case, drive the help assertion through the render path — set `m.modal = modalHelp` on the blocked model and assert `ansi.Strip(m.viewProjectList())` (or `m.View().Content`) does not contain `Theme picker`, with the unblocked control asserting it does. The `helpModelProjects` fixture in `help_modal_test.go:33-42` shows the established shape for a Projects-page help model. Doing the same for the Sessions leg is optional — that wiring predates this task.

  CONFIDENCE: high

NOTES:

- **§14.3's width arithmetic does not survive the cell count.** By the executor's own measurement (`footer_test.go:16-17`, which matches the reviewer's independent glyph count) the full Sessions row is 80 + 1 spacer + 6 anchor = **87 content cells**, i.e. ~91 terminal columns with the `Hinset=2` gutters. §14.3 asserts it fits at the reference mock's 86 columns *with spare* after shortening to `m multi`; in cells it misses by one, so at exactly 86 the `m multi` entry degrades to `· …`. Nothing in the implementation can fix this (§14.2 pins the copy), §14.4 handles it gracefully, and the vhs capture width (1280px) is far wider, so no visual gate is affected. **Flagged because the shortening decision's whole justification was headroom at that width** — a spec-record inaccuracy, not an implementation defect.
- **Contextual filter footers now lose their whole cluster at once.** They have no left-cluster fitter (pre-existing, explicitly out of scope), so under the inverted rule they jump straight from "full cluster + anchor" to "anchor alone" — for `filterAppliedFooterEntries` (~41 cells) that is every width below ~48. Net improvement (the old rule let them *overflow* the row below 41 cells), documented in the assembler comment, and consistent with §14.4's "`? help` is never dropped" — but a `fitFilterCluster`-style fitter on `filterFooterRow` would make them degrade one entry at a time like the standard footer. Reasonable follow-up, not this task.
- `renderSessionsFooter` and `renderProjectsFooter` are now byte-identical one-line delegations with identical signatures, so neither encodes page identity any more and nothing prevents passing the Projects entries to the Sessions renderer. The executor's reasoning (task pins the signature; collapsing churns ~10 files) is sound; worth revisiting if a third page-named footer appears.
- **Stale comment counts left behind by the amendment**: `sessions_footer_switch_view_test.go:22` says "all six Core keys" (now eight incl. `?`); `projects_footer_test.go:23-25` says the footer renders "EXACTLY `⏎ new session` · `x sessions` · `e edit` · `/ filter`" and its assertion list omits `t theme`, so a `t`-drop regression would not fail a test named `…CopyExact` (it is caught by `TestFooterRevision_ProjectsPinnedCopy` and `TestProjectsKeymap`).
- No test pins the *deliberate non-filtering* of `t` in the too-short region — `themeKeyBlocked()` gaining a `|| !fitsFloor` leg would pass everything. The old `TestPanelEntry_LeavesDescriptorsUnfiltered` nominally covered those states but only against the static descriptors, so nothing was lost in the fold; it was never covered.
- `TestFooterRevision_HelpAnchorSurvivesNarrowing`'s contiguous monotonic sweep runs on Sessions only; `TestFooterRevision_LabelsAreNeverTruncated` covers Projects at every width but ignores the anchor it splits off. Both pages share the assembler and `TestFooters_RouteThroughSharedAssembler_NarrowDegradeIdentical` pins the shared rung, so this is thin rather than absent.
- **SPEC_CONFORMANCE conformant.** §14.2's two rows match the spec file **byte-exactly** (the reviewer extracted both from `specification.md` and diffed them against the test constants: identical, including the `·` separators and glyphs). §14.1's Core promotions/demotions, §14.3's `m multi` short label + lockstep filter + shared budget, §14.4's inverted ladder, §9.10's `t` block predicate (`themeKeyBlocked() == m.colourless`, the same predicate `themePanelEntry` gates on), and §15.1's amendment framing are all honoured. The deliberate non-filtering of a narrow/short region (space shortage, not capability absence) correctly follows §9.10's stated contrast.
- **ARCHITECTURE sound.** The four-rung ladder is correct at every boundary — traced against `fitLeftCluster`'s budget (`w-rightWidth-1`, clamped ≥0): rung 2 is taken for every `w ≥ rightWidth+1`, rung 3 only at `w == rightWidth` exactly, rung 4 below. `fitClusterToWidth` is untouched and drops whole entries only, so mid-word truncation is structurally impossible; the 1→120 two-page sweep confirms exactly two rows and exact width at every step. `headerPadLeft` is a faithful mirror of `headerPadRight`. Putting the filter at the call site and keeping the descriptors nullary is the right seam and keeps the dispatch guard honest.
- **Checks that came back clean**: the `t` dispatch probes are genuinely non-vacuous (`TestKeymapDispatchGuard_ThemeKeyProbe` proves unwired ⇒ no-op, wired ⇒ open, on both pages, and `assertDescriptorDispatchParity` demands a probe for every non-`RightAligned` descriptor key in both directions); the empty-state footers (`empty_states.go:152,161`) select only `n`/`x`/`/`/`?` so they need no filter; `filter_footer.go:157` reads the static descriptor only to source the anchor, which filtering never touches; every footer render **and** budget site now resolves against the filtered slice (`model.go:1847,1894,5011,5167`); and `TestThemePanel_OverlayCutsMidLabel`'s 100 → 86 move is genuine — at 100 the cut column lands on the space at index 70 (immediately after `t theme`), which trips the test's own "case is not exercised" guard, while at 86 it lands inside `x proj`.
- **CONVENTIONS followed** — no `t.Parallel()`, table-driven with named subtests, `golangci-lint run ./internal/tui/...` 0 issues, `go build ./...` clean, full unit lane green (`internal/state`'s `TestTailScrollback_PerformanceBudget` tripped once under load and passes on rerun — unrelated, no state files touched).
