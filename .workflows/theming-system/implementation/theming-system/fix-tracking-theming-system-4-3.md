## Attempt 1

ISSUES:
- `internal/capture/theme_swap_guard_test.go:178-187` (`sgrParameterRun`) — **silent failure mode #3 is not closed.** The representation derivation has no assertion of its own, and nothing in the package fails if it is wrong. Mutation-proven: changing `return probe[start+1 : end]` to `probe[start+2 : end]` (so every form becomes `8;2;R;G;B` / `4;2;...`) leaves the **entire `./internal/capture` package green**:
    - assertion 1 passes vacuously (it is a negative — the wrong form is never found, so nothing "survives");
    - assertion 2 passes vacuously (`:527-537` only errors when the A-observed and B-observed sets *disagree*; a wrong form empties **both**, so the check is symmetric and silent);
    - the canary at `:464` passes because it searches for the hardcoded `truecolorForegroundIntroducer` literal rather than a derived form.

  The doc comment at `:148-153` states the opposite — *"assertion 2 fails loudly against a wrong representation, so one derivation means one of the two assertions is always load-bearing on it"* — which is a load-bearing safety claim that is empirically false. §13.4 itself says of the wrong-representation risk *"Assertion 2 is the backstop that would fail loudly, which bounds the exposure"*; as implemented, assertion 2 is a symmetric disagreement check and provides no such backstop.

  This matters disproportionately: this guard is the feature's central completeness mechanism, and a silently-wrong derivation makes every later phase's completeness claim vacuous.

  FIX: add a well-formedness/round-trip assertion on the derivation, self-contained in 4-3 and not overlapping 4-4's assertion 3. E.g. a `TestThemeSwapGuard_TokenFormsAreWellFormed` (or a subtest of `TestSyntheticThemes_AllValuesUnique`) that, for every token form from `tokenForms(t, a)` and `tokenForms(t, b)`, asserts `strings.HasPrefix(form.fg, "38;2;")`, `strings.HasPrefix(form.bg, "48;2;")`, and that the three trailing decimal parameters parse back to exactly `rgbComponents(t, tok.Value)` — reusing the existing `rgbComponents` helper at `:434`. That deterministically catches both the off-by-one slice above and a truncating variant. **Then correct the `:148-153` comment** to state what actually backstops the derivation.

  ALTERNATIVE (cheap addition, not a replacement): tie the canary to the derivation — change `TestThemeSwapGuard_RenderIsTruecolor` (`:459`) to assert each A-frame carries at least one *derived* A form (`carriesRun(before, form.fg) || carriesRun(before, form.bg)` for some `form` in `aForms`) rather than the bare `"38;2;"` literal. It additionally catches a profile divergence between the probe render and the frame render, but it is a weaker pin on the derivation itself (a form wrong in a way that still matches *something* on the frame would slip through) and is a per-fixture rather than per-derivation property. Reviewer recommends the round-trip fix as primary and this as a cheap addition; both is ~12 lines and closes the mode from both ends.

  **Relying on 4-4's assertion 3 to close it is NOT acceptable** — it would leave 4-3's guard depending on a later task for its own soundness, contradicting the file's explicit self-sufficiency claim.

  CONFIDENCE: high on the diagnosis (mutation-proven); medium on the exact fix shape (two valid approaches above).

NOTES:
- **(a) The reviewer's own independent break, a different site from the executor's**: removed `m.preview.th = m.activeTheme` (`internal/tui/model.go:1523` — the §9 preview's whole-palette copy). The guard failed on **`preview-screen` only**, naming `text.primary`, `text.muted`, `accent.key`, `accent.mode` (foreground runs) and `canvas` (background run). Correct token *and* correct fixture, no false positives on the other 16.
- **(c) The enumeration is real.** Adding a live `zz-probe-fixture` to `fixtures.go` produced a `TestThemeSwapGuard_NoStaleValueSurvives/zz-probe-fixture` subtest with **zero** test-file edits. Removing the probe from `FixtureNames()` while leaving its `case` fired `TestFixtureRegistry_ByNameCasesMatchFixtureNames` with both lists printed.
- **(f) Assertion 2 is genuinely a union and catches a site rendering nothing.** Zeroing `BgAttention` and `StateDestructive` inside `ApplyTheme` (so those sites render nothing only after the swap) fired: `state.destructive renders under theme A (on [loading-error sessions-multi-select-preflight-abort]) but no theme-B value … appears on any fixture`, and the same for `bg.attention` on `[projects-command-pending sessions-inline-flash]`. Assertion 1 correctly stayed green, so the two assertions are genuinely complementary. The union is accumulated across all fixtures before the per-token check, never per fixture.
- **(d) Palette verified independently.** Reds 110/210 disjoint, greens 129–147, blues 201–219 — 38 unique, every component three decimal digits. The reviewer instrumented every A- and B-frame across all 17 guarded fixtures: **zero** 24-bit SGR runs outside the two synthetic palettes, and **zero** non-truecolor SGR parameters of any kind. No fixture content (canned scrollback, session names, third-party defaults) can collide with a synthetic value.
- **Union coverage measured: 19/19 tokens under A and 19/19 under B.** No token is currently rendered on zero fixtures, so the union assertion is non-vacuous for every one of the 19 today, and 4-4's assertion 3 will pass against the current fixture set.
- The `BackgroundColor` split is genuinely necessary: caching the canvas at first `View` fails all 17 `ViewBackgroundColourFollowsSwap` subtests while `NoStaleValueSurvives` stays green.
- **(b) Blind spot found — spec-acknowledged, covered elsewhere, not a task defect.** No fixture renders a filter-active frame, so §11.2's explicitly-named "both filter inputs" cached styles are outside this guard. Proven: short-circuiting `styleFilterInput` after its first call leaves `./internal/capture` fully green, while `internal/tui/restyle_repoint_test.go` fails loudly with six named assertions. §13.3 states directly that "a missing fixture is a blind spot the guard structurally cannot report", and the structural sibling covers it. Same reasoning for modal chrome (no modal fixtures) — though `internal/tui` holds no package-level cached `lipgloss.Style` or `theme.Token` after 4-1's sweep, so nothing there caches.
- **(b) Second vacuity probe, correctly out of scope.** Mis-pointing a *per-frame* token (`loading_view.go:371-372`, `bg.subtle` → `bg.selection`) keeps the guard green, because it changes the A-render and the B-render identically so the token renders nowhere under either. That is precisely 4-4's assertion 3 and the phase boundary assigns it there.
- **Non-vacuity floor is cross-test, not per-test.** The `len(resolved) == 0` fatal lives only in `TestThemeSwapGuard_EnumeratesRegistry` (`:312`). Running e.g. `-run TestThemeSwapGuard_NoStaleValueSurvives` in isolation against an empty registry would pass with zero subtests. Same-file/same-package makes this low-risk.
- **(e) All three flagged decisions are sound.** (1) Reusing `harnessWidth`/`harnessHeight` from `swap_harness_test.go` is right — a second declaration in the same test package would let the two guard files render at silently different sizes. (2) Importing `internal/tui` is already done by the sibling, and `sameColour` comparing resolved RGBA rather than `color.Color` identity is the correct robustness choice. (3) Omitting a union-size floor is correct — that is 4-4's assertion 3.
- **Minor duplication inside `capture_test`.** "FixtureNames minus swatch" is derived three times — `buildBackedFixtureNames()` (`swap_harness_test.go:34`) and inline at `theme_swap_guard_test.go:315` and `:343` — and `bgSeq` (`swap_harness_test.go:174`) duplicates `sgrParameterRun`. No correctness risk; consolidating would match the project's single-source convention.
- **`swapModel` (`:284`) hand-mirrors `RenderSwapRender`.** Only the latter's construction counts are AST-pinned by `TestRenderSwapRender_MutatesASingleModel`; a future change to the harness seam could leave `swapModel` drifted. Low risk given the explicit comment, but a harness-side `SwapModel` returning the model would remove the second copy.

## Attempt 2

ISSUES:
- `internal/capture/theme_swap_guard_test.go:390-399` — `fixtureByNameCases` collects **only** `*ast.BasicLit` string literals from the case clauses and **silently ignores any other expression form**. A `case` written as a named constant is therefore invisible to the drift check, while the test's own doc comment at `:350-356` claims the invariant absolutely: *"A fixture present in the switch but ABSENT from the slice is invisible to an enumerating guard … Absence reads as coverage, which is exactly the shape §13.4 warns about."*

  Demonstrated against a real on-disk copy: adding `case zzProbeFixture:` (with `const zzProbeFixture = "zz-probe-fixture"`) returning a real fixture, and deliberately omitting it from `FixtureNames()`, leaves the **entire `./internal/capture` package green** while `FixtureByName("zz-probe-fixture")` renders a screen nothing ever swaps.

  Not hypothetical: `fixtures.go:231` already references a fixture name as a constant (`ContrastValidationFixture`) inside `FixtureNames()`, so a constant-cased `case` is a natural next step. It is also **the same category as the issue this fix round just closed** — a mechanism whose comment claims more than it delivers.

  FIX: make the scan fail loudly on anything it cannot read, in the `ast.Inspect` loop at `:390-399` — replace the `if ok && lit.Kind == token.STRING` silent skip with a `t.Fatalf` on the negative:

  ```go
  for _, expr := range clause.List {
      lit, ok := expr.(*ast.BasicLit)
      if !ok || lit.Kind != token.STRING {
          t.Fatalf("%s: FixtureByName carries a non string-literal case expression (%T) — this scan reads literals only, so such a case is invisible to the drift check above", catalogue, expr)
      }
      unquoted, err := strconv.Unquote(lit.Value)
      if err != nil {
          t.Fatalf("unquote case literal %s: %v", lit.Value, err)
      }
      cases = append(cases, unquoted)
  }
  ```

  The reviewer applied exactly this to a scratch copy and verified both halves: it fires on the constant-cased fixture with `non string-literal case expression (*ast.Ident)`, and the whole package is green against unmodified production code.

  ALTERNATIVE: resolve identifier cases by scanning the file's `const` declarations and substituting the value, which would *permit* constant-cased fixtures rather than forbidding them. More faithful to the stated invariant but adds a symbol table to a test whose catalogue is 100% literal today, and it fails open if the constant is declared in another file. The Fatal is the better trade — it closes the hole completely, is four lines, and directs a future author to either use a literal or extend the scan deliberately. Reviewer recommends the Fatal.

  CONFIDENCE: high

NOTES:
- **The prior round's fix is genuinely closed**, verified by three independent mutations through `go test -overlay` (no repo file touched): `probe[start+2 : end]` fails **only** `TestThemeSwapGuard_TokenFormsAreWellFormed`, on all 38 forms across both palettes, with every scanning assertion and the canary staying green; the drop-a-channel truncation fails only the well-formedness test with the exact reported diagnostic; a digit-truncating variant (`end-1`) fails both.
- **(b) The executor's correction about the canary is accurate and the comments now match the mechanism.** `carriesRun` matches on substring, so `8;2;110;129;201` is still found inside the frame's `38;2;110;129;201` — the canary genuinely cannot catch the off-by-one. `tokenForms`' doc, the file-header failure-mode-3 bullet and the canary's doc all claim only what is empirically true. If anything the canary doc slightly **under**-claims: it is also the sole assertion that fires when `carriesRun` is stubbed to always-false, and when `tokenForms` returns empty — both "the scan finds nothing" vacuities it closes on its own. Under-claiming is the safe direction.
- **(c) The reviewer could not make a genuinely un-re-pointed site stay green.** Its own independent break, different from the prior round's: it simulated the §11.2 once-assigned-cache class exactly — `ApplyTheme` snapshots and restores `sessionList.Paginator.ActiveDot/InactiveDot` and the two dot styles around `applyCanvasMode`, so the site is themed under A at construction and not re-pointed on swap. The guard failed on `sessions-paged` only, naming `text.faint` and `accent.primary` (foreground) and `canvas` (background) — correct tokens, correct fixture, no false positives on the other 16. It also measured the observed union directly: 16/19 tokens seen as foregrounds, 5/19 as backgrounds, union 19/19 under both A and B with byte-identical per-fixture counts, so there is no systematic blindness to either form.
- **(d) Re-confirmed.** Adding a live `zz-probe-fixture` to both lists produced an 18th subtest with zero test-file edits. Name-without-a-case fires both the registry Fatal and the AST test. Case-without-name fires the AST test. Promoting the swatch to a resolvable fixture fires the converse subtest. The union assertion is a genuine cross-fixture union accumulated before the per-token check.
- **(e) No production code changed.** `git diff HEAD -- internal/ cmd/ main.go` is empty; only the new test file plus workflow bookkeeping.
- **Non-vacuity floor sits above the colourless filter, not below it.** `TestThemeSwapGuard_EnumeratesRegistry:328` Fatals on an empty `registryFixtures`, but `guardedFixtures` (`:287`) has no equivalent. If every fixture reported colourless, all four per-fixture guard tests run **zero** subtests and pass, and the union test passes vacuously too. Reaching that state requires 17 independent fixture edits, each individually spec-sanctioned, so it is not blocking — but a one-line `if len(fixtures) == 0 { t.Fatal(...) }` in `guardedFixtures` would close it without pinning an excluded count (which the comment at `:247-254` is right to avoid). **Worth doing alongside the fix above since it is the same shape of hole one layer down.**
- **A site rendering a colour belonging to no theme is out of the guard's reach**, by construction. The reviewer's first break attempt (removing `canvasPaginationDots` from the coloured branch outright) left the dots carrying bubbles/list's own defaults in *both* frames; assertion 1 saw no A survivor and assertion 2 was masked by the union. Correct behaviour for §13.4 — the colour-literal guard and `restyle_repoint_test.go` own that class — worth knowing when reading a future green run.
- Carried over, still low risk: `swapModel` (`:300-305`) hand-mirrors `RenderSwapRender` and only the latter is AST-pinned; "FixtureNames minus swatch" is derived three times; `bgSeq` duplicates `sgrParameterRun`.
- The four per-fixture guard tests each re-run `RenderSwapRender` over all 17 fixtures (~4.3s for the guard, 17s for the package). Acceptable for this lane; the separation buys per-assertion diagnostics. Noting only because the cost scales linearly as the panel fixtures land.

## Attempt 3

ISSUES: (both close with ONE edit in the same per-fixture loop)

- `internal/capture/theme_swap_guard_test.go:633-660` — **a single element on a single screen rendering colourless after the swap passes the whole suite green.** Demonstrated: with `RenderSwapRender`'s after-frame stripped of theme B's `state.positive` foreground run for `sessions-flat` only, `go test ./internal/capture` is `ok`. Assertion 1 is a negative so a colourless site reads as clean; assertion 2 is a union so the other 16 fixtures rendering `state.positive` mask it; the truecolor canary only inspects the **before** frame; `ViewBackgroundColourFollowsSwap` reads the declarative background, not Content. The stronger whole-frame version passes too — stripping *all* colour from `sessions-unsupported-null`'s after-frame leaves the suite `ok`.

  **This is a live failure shape for this codebase, not a hypothetical**: `theme.Token.Color()` resolves a zero-value Token through `lipgloss.Color("")` to the **no-colour sentinel rather than erroring** (`internal/theme/theme.go:46-48`), so a cached style re-pointed at a partly-populated theme renders nothing and this guard says fine. It is also precisely what §13.4 says assertion 2 exists to catch ("a site that renders **nothing** rather than merely stale"). Relatedly, the comment at `:626-627` claims flatly that this test "catches a site that renders NOTHING after the swap" — **the same doc-overclaims-the-check shape the previous round fixed on `fixtureByNameCases`**.

  FIX: in the per-fixture collection loop (`:639-647`), hoist the two `observedTokens` calls into `underA`/`underB` and add a per-fixture symmetry assertion before folding them into the union maps — `slices.Sorted(maps.Keys(underA))` must equal `slices.Sorted(maps.Keys(underB))`, erroring with the fixture name and both sets. **Keep the existing union loop unchanged** (§13.4 names the union form, and the per-fixture check does not subsume the "every token appears somewhere" claim). Update the `:626-627` comment to say the union catches a token no fixture renders under B and the per-fixture check catches a screen that loses a token it rendered under A.

  The reviewer verified this is exact and non-flaky today: over all 17 guarded fixtures the A-observed and B-observed sets are identical (counts 5–14 per fixture, `equal=true` for every one), and no render path branches on colour value (`grep` for luminance/is-dark logic in `internal/tui` finds only `appearance_gate.go`, resolved at construction under `ConstantNomination`). With the assertion added, the real code stays green and both probes fail with the offending fixture and token named: `fixture sessions-flat: renders [… state.positive …] under A but […] under B`.

  ALTERNATIVE: a weaker per-fixture floor (`len(underB) > 0`) closes only the whole-screen case and misses the single-element case, which is the more likely bug. **Not recommended** — same cost, strictly less coverage.

  CONFIDENCE: high

- `internal/capture/theme_swap_guard_test.go:230-238, 637-647` — **`observedTokens` has no non-vacuity floor, so a bug in it silently empties assertion 2 entirely.** Demonstrated: replacing the body with `return seen` (always empty) leaves `go test ./internal/capture` fully `ok`. The file's own reasoning at `:158-163` explains why assertion 2 cannot backstop a wrong *form derivation* and pins that from outside with `TokenFormsAreWellFormed` — but that test pins `tokenForms`, not the collection helper, so `observedTokens` sits outside every pin. The canary uses `carriesRun` directly and never routes through `observedTokens`, so it does not cover it either. **Same shape as both previously-fixed holes**: a derivation silently producing an empty set with nothing reporting it.

  FIX: same edit as above — add `if len(underA) == 0 { t.Errorf("fixture %s: no theme-A token observed at all …", fx.Name()) }` in the per-fixture loop. Verified: with the always-empty `observedTokens`, this fails once per fixture; on the real code it is green.

  CONFIDENCE: high

NOTES:
- **Both fixes from round 2 are verified landed and effective.** The constant-cased `case` hole was reproduced on a real on-disk rsync'd copy (the AST scan reads `fixtures.go` from disk, so `-overlay` cannot reach it): it now fails with `fixtures.go: FixtureByName carries a non string-literal case expression (*ast.Ident)`, and reverting the `t.Fatalf` to a silent `continue` returned the package to fully green with that fixture resolvable and unenumerated. The `guardedFixtures` floor was reproduced both directions: forcing `Colourless()` → `true` makes all four per-fixture consumers Fatal; deleting the floor makes all four pass green having run zero subtests.
- **Everything else is bracketed.** `carriesRun` always-false is caught by the truecolor canary; empty `tokenForms` is caught by the canary and the 38-value pin; empty `FixtureNames()` is caught by the `EnumeratesRegistry` floor, the AST `len(cases)==0` Fatal and the `guardedFixtures` floor; `theme.TokenNames()` vs `All()` name-set drift is structurally impossible (one `fields()` table) and pinned by `internal/theme/theme_test.go`.
- Re-confirmed independently: the enumeration is real (adding `zz-new-screen` to both lists produced a new subtest with zero guard edits); the skip's converse fires (promoting the swatch to a resolvable case failed both that subtest and the AST drift check); the derivation round-trip fires (`start+2` failed `TokenFormsAreWellFormed` on every token, both palettes); the render size is pinned once via `harnessWidth`/`harnessHeight` and behaviourally anchored by 4-2's captured-state table.
- **No production code changed.** `git diff HEAD -- internal/ cmd/ main.go` is empty.
- `guardedFixtures`' Fatal message (`:299`) says "every enumerated fixture reported colourless" but also fires when `FixtureNames()` itself is empty or swatch-only. It still fails loudly and the sibling `EnumeratesRegistry` floor names that case precisely — wording only.
- `swapModel` (`:312-317`) duplicates `RenderSwapRender`'s render-swap sequence and is not covered by 4-2's structural one-model pin. It is only used for the declarative-background assertion, which does not depend on cache population, so a drift there is currently harmless — worth a line in its doc comment at most.
- The `fixtureByNameCases` AST scan reads only `FixtureByName`'s own body, so a future `default:` delegating unknown names to a helper registry would be a third list invisible to the drift check. Not present today and out of scope; noted because the doc comment states the two-list invariant absolutely.
- `excludeColourless` being generic over a `colourReporter` interface is the right call — it lets the true branch be driven by `stubFixture` while the guard instantiates it with `*capture.Fixture`, so the tested function is the used function.

## Attempt 4

ISSUES: (comment-only — the guard mechanism is verified sound on every dimension)

- Three comment passages were **falsified by this round's own fix** and now under-claim the guard. `internal/capture/theme_swap_guard_test.go:52-54`, `:160-164`, `:500-506` each assert that assertion 2 "passes as silently as assertion 1" on a wrong derivation because "it errors only when the A-observed and B-observed sets DISAGREE". The emptiness floor added at `:679-681` makes that false.

  Verified: an always-false `carriesRun` and a mis-sliced `sgrParameterRun` each fail `TestThemeSwapGuard_EveryBValuePresentInUnion` **as well as** the canary — so `:504`'s "leaves the whole guard green" is now demonstrably wrong (two and three tests fire respectively).

  This is the same claim-versus-behaviour mismatch shape the three prior rounds each turned on, in a file whose entire value is an exact claim→check mapping.

  FIX: Correct all three to distinguish the two halves of assertion 2 — its *symmetry* comparison still cannot judge the form (a wrong form empties both sets symmetrically, so they still agree), but its *emptiness floor* does catch a wrong derivation; it just reports "nothing observed on this screen" rather than identifying the malformation. Keep the stated reason for `TestThemeSwapGuard_TokenFormsAreWellFormed`: it is the only *positive* check that round-trips each form back to its own token's channels and so names *which* part of the derivation is wrong; the floor only reports the symptom. At `:504` replace "leaves the whole guard green" with the accurate consequence (the canary and the union's emptiness floor both fire, but neither identifies the derivation as the cause).

  CONFIDENCE: high

NOTES:
- **(c) One more vacuity sweep — NO FIFTH HOLE FOUND.** The reviewer walked every derivation, filter and scan in the file and probed each: `FixtureNames()` (three independent floors), `registryFixtures` (Fatal on unresolvable; a name/`Name()` mismatch fails the enumeration comparison), `excludeColourless` (floored; operates on a fresh slice each call so `slices.DeleteFunc`'s in-place mutation is harmless), `tokenForms` (canary + union floor), `sgrParameterRun` (round-trip pin; an empty run fails loudly rather than silently — it is found everywhere, not nowhere), `carriesRun` (always-false → two tests; always-true → assertion 1 storms; the fixed-width 3-digit invariant makes cross-token substring confusion impossible since all runs are equal-length and all 38 values unique), `observedTokens` (now floored, and it is the *only* consumer — no second path), `fixtureByNameCases` (floored + fails-loud on non-literals; `default:` correctly contributes nothing), `theme.TokenNames()` (unfloored *here*, but pinned by `internal/theme`'s own tests, and losing it would only silence the subsumed union), `theme.Theme.All()` (floored by the 38-value assertion — a 20th or removed token Fatals there), `guardedFixtures` (floored).
- **(a) All three probes reproduced and firing.** Probe 1 (strip theme B's `state.positive` fg from `sessions-flat`'s after-frame — 2 real occurrences, confirmed before trusting the silence): pre-fix silent, current guard FAILs naming the fixture and both sets, diff exactly `state.positive`. Probe 2 (always-empty `observedTokens`): pre-fix silent, 17 failures now. Probe 3 (whole-frame strip on `sessions-unsupported-null`): pre-fix silent, FAILs with `[…] under A but [] under B`.
- **(b) BOTH executor corrections verified and both are right.** *Screen × role*: stripping *one* of the two `state.positive` sites on `sessions-flat` is silent under the current guard — exactly the residual the comment admits. *Subsumption*: the proof holds both directions, and the reviewer confirmed it empirically with the one token appearing on exactly one fixture — stripping `text.on-attention` from `sessions-inline-flash` fires symmetry *and* the union together, while probes 1 and 3 fire symmetry with the union silent. The union genuinely cannot fail alone. All 17 guarded fixtures symmetric, counts 5–14.
- **(d) Re-confirmed.** Enumeration floors fire (`FixtureNames()` reduced to the swatch → three separate Fatals); the skip's converse fires (repointing `ContrastValidationFixture` at `"sessions-flat"` → the converse assertion *and* the AST test); the AST two-list test fires both directions — run **on disk**, since the scan reads the real `fixtures.go` and `-overlay` cannot reach it: an extra `case "sessions-ghost"` fires the drift check, and a constant-cased case Fatals with round 2's message; the derivation round-trip pin fires on both a mis-sliced run and a wrong introducer.
- **(e) No production code changed** — `git diff --stat -- '*.go'` is empty. Full unit lane green, `golangci-lint` 0 issues, five consecutive runs deterministic.
- §11.3's declarative background is covered separately, as §13.4's frame scan structurally cannot see it — confirmed empirically: freezing `v.BackgroundColor` in `model.go:4096` fails *only* `TestThemeSwapGuard_ViewBackgroundColourFollowsSwap`; all four scan assertions stay green.
- `TestThemeSwapGuard_TokenFormsAreWellFormed` ranges over `tokenForms(...)` with no length floor, so an empty return passes *that test* vacuously. Not a hole — double-covered by the canary and the new union floor (both verified firing). Worth knowing if the canary is ever weakened.
- `sameColour` (`:323`) has no pin; an always-true implementation would make `ViewBackgroundColourFollowsSwap` vacuous. Low risk (eight local lines, `==` on RGBA components) and the test is demonstrably non-vacuous today — it is the sole catcher of a frozen declarative background. Not blocking; the one remaining unfloored comparison helper.
- **Coverage is size-robust**: the A-union covers **19/19 tokens** at 120×40 and still 19/19 at 100×24, 80×20 and 60×12. The render-size pin is not a hidden coverage cliff, and task 4-4's assertion 3 should be satisfiable on today's fixture set without adding fixtures.
- **Useful for 4-4**: `text.on-attention` is carried by exactly one fixture (`sessions-inline-flash`), and `bg.attention`, `bg.subtle`, `state.destructive` by two each — the thin edge of the union.
- `ApplyTheme` is a pointer receiver on an addressable local in both `RenderSwapRender` and `swapModel`, so the swap takes. `t.Fatalf` calls all occur on the test goroutine (including inside the `ast.Inspect` closure), so `Goexit` is safe.
