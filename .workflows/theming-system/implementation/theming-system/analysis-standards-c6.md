AGENT: standards
FINDINGS:
- FINDING: Production comments assert what tests prove and make cardinality claims — both explicitly forbidden by code-quality.md
  SEVERITY: medium
  FILES: internal/theme/load.go:26-30, internal/theme/load.go:207, internal/theme/resolve.go:31-33, internal/theme/builtins.go:145-147, internal/theme/builtins.go:56, internal/theme/builtins.go:161, internal/theme/union.go:183, internal/theme/union.go:267, internal/theme/theme.go:86-87, internal/tui/theme_panel.go:26, internal/tui/theme_panel.go:193, internal/tui/theme_panel.go:544-549, internal/tui/theme_panel.go:726-727, cmd/theme_persister.go:22-23, cmd/open.go:789
  DESCRIPTION: code-quality.md's "Never in a comment" list bars two categories outright: "Claims about tests — what a test pins, catches, or proves. A renamed test or moved assertion turns the claim into a confident lie" and "Cardinality claims — 'the single caller', 'the only site that…'. Falsified by ordinary additive change far from the comment." The theme surfaces carry both throughout. Test claims: `load.go:28` ("a source guard keeps it out of production code, where NewLoader's own construction is exempt"); `resolve.go:32` and `builtins.go:146` ("build-time validation of the embedded set is what makes it unreachable / exists to make impossible"); `union.go:267` ("Always valid — build-time validation of the embedded set makes that true"); `theme_panel.go:193` ("a paginating fixture exercises the dots so the swap-and-diff guard is not blind at that site"); `theme_panel.go:726` ("identical before and after a swap, which the swap-and-diff guard structurally cannot see"); `theme_panel.go:26` ("keeps the colour-literal guard and the swap-and-diff guard satisfied"); `theme_panel.go:546-548` describes what today's fixture set is built from ("fixtures are built from an invalid drop-in and an unreadable themes directory"), which the next fixture added falsifies. Cardinality claims: `load.go:207` ("the only implementation of it"); `theme.go:87` ("there is deliberately no second list of token names anywhere in production code"); `theme_persister.go:22` ("the emission site for `theme: commit failed`, which otherwise has none"); `open.go:789` ("the ONE construction-time theme read"); `builtins.go:56`/`161` and `union.go:183` (claims about which package can reach the built-in lookup). Note this is a narrower charge than comment volume generally — code-quality.md explicitly *permits* rationale and a rejected alternative, and commit e30939b2 already stripped spec-section/phase citations. The residue is the two categories that carry no exception. These are load-bearing invariants, but the guarantee lives in a test whose name and location are free to move; the comment then states something false in the file a future reader trusts most.
  RECOMMENDATION: Rewrite each to state the invariant the code needs without naming its enforcer or its cardinality — "a built-in is always valid" rather than "build-time validation makes that true"; "the merge belongs to prefs; this type resolves nothing about the record" rather than "the emission site, which otherwise has none"; "the panel's list caches its styles at open, so the restyle path must re-point them" rather than citing the swap-and-diff guard's blindness. Where a comment exists solely to point at a test (`theme_panel.go:193`, `load.go:28`), delete it — the fixture and the guard already encode the relationship.

- FINDING: Slice accumulators left nil where golang-code-style mandates explicit initialisation, inconsistently within the same feature
  SEVERITY: low
  FILES: internal/theme/union.go:357, internal/theme/enumerate.go:67, internal/theme/setting.go:237, cmd/doctor_theme.go:252, cmd/doctor_theme.go:372
  DESCRIPTION: `.claude/skills/golang-code-style` states "Slices and maps MUST be initialized explicitly, never nil", with `make([]T, 0, len(ids))` when capacity is known. The new theme code applies this in some places and not others: `lex.go:51` (`pairs := []Pair{}`), `validate.go:81` (`offenders := []string{}`), `validate.go:118` (`missing := []string{}`), `union.go:305`/`306`, `enumerate.go:108` and `doctor_theme.go:192`/`281` all initialise explicitly, while the five sites above declare `var xs []T` and return a nil slice on the empty path. The consequence is confined (none of these is JSON-serialised or written to), so this is a consistency deviation rather than a defect — but it means the same package returns `[]Entry{}` from one path and nil from another for the identical "nothing found" answer.
  RECOMMENDATION: Initialise the five accumulators explicitly, preallocating where the bound is known (`rows := make([]Row, 0, len(keys))` in `persistedRows`, `entries := make([]Entry, 0, len(dirEntries))` in `Enumerate`, `inForce := make([]InForceKey, 0, 2)` in `InForceKeys`, and the two `advisories` slices against their loop bounds).

- FINDING: CLAUDE.md's capture-harness rewrite omits the `reference/` retention carve-out the implementation introduced and depends on
  SEVERITY: low
  FILES: CLAUDE.md:27, testdata/vhs/README.md, internal/tui/keymap.go:83
  DESCRIPTION: Spec §13.2 pins a bounded act — "Everything that exists today as an image or tape is deleted" — and §12.6 requires CLAUDE.md's visual-capture-harness entry be corrected to say captures are scaffolding cleared at sign-off. Both were done, but the implementation also kept ~30 committed PNGs under `testdata/vhs/reference/` and documented an exemption for them in `testdata/vhs/README.md` ("A reference frame is the design the code was built against… Keep them, and keep pointing comments at them"). That exemption is a reasonable divergence — the frames are design exports rather than renders of the code, and production comments now point at them (`keymap.go:83` cites `testdata/vhs/reference/sessions-help-modal-mv.png`) — but it is recorded only in the harness README. CLAUDE.md, which an implementing agent reads first, states the rule without the carve-out: "The images and the tapes that produce them are scaffolding… cleared out after sign-off rather than living in the repo." A future contributor following CLAUDE.md alone would delete the reference set at sign-off and orphan every comment pointing into it.
  RECOMMENDATION: Add the carve-out to CLAUDE.md's capture-harness paragraph — one clause distinguishing `testdata/vhs/*.png|*.tape` (scaffolding, cleared at sign-off) from `testdata/vhs/reference/*.png` (committed design exports, kept, and pointed at by production comments) — so the two documents state one rule.

SUMMARY: The implementation is an unusually faithful rendering of the specification — the 19-token vocabulary and its table order, the six-rung rejection ladder and its short-circuit, the slug/extension rules, the union and its one-slug-one-row dedup and sort tie-break, the per-slot mode-matched fallback and its embedded-set-first ordering, the panel's commit/confirm/outstanding-failure state machine, the prefs RMW split-decode and migration gating, the doctor advisory class and summary, the log event catalogue with its per-event dedup keys, and every pinned §14A string all match. No spec conformance gaps were found; the three findings are project-standards deviations (comment claims code-quality.md forbids, nil slice accumulators, and a CLAUDE.md/README disagreement about capture retention).
