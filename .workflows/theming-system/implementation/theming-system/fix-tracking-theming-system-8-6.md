## Attempt 1

ISSUES:

- `internal/tui/theme_panel.go:92-94` — "The panel's own delegate … is not cached at all: it re-derives per frame from the previewed theme through the single construction point `Model.themeRowDelegate`." This is **false as implemented**: the delegate is assigned once by `newThemePanelList` and only re-pointed by 8-9's `SetDelegate` and 8-11's resize path. `renderThemePanel` has no `Model` and never touches the delegate, so a `th` that differs from the delegate's own `Theme` silently renders themed chrome over stale rows. The related claim at `:215-216` ("every chrome surface is painted from it per frame, with nothing cached") reads as covering the rows too. This file is the design record for §11.2's self-declared worst case of the cached-style class, so a false caching claim here is the one comment that must not be wrong.

  FIX: Reword `:92-94` to state what the code does — the delegate holds `Theme`/`Colourless`/`Width` as fields, is built once through `Model.themeRowDelegate` at construction, and is **re-pointed** through that same single site by task 8-9's restyle and task 8-11's resize (never rebuilt) — and add one clause at `:215-216` scoping "nothing cached" to the chrome this function renders, naming the delegate as the surface `th` does *not* reach.

  ALTERNATIVE: Honour §11.2 literally by extracting the composite literal into a free `newThemeRowDelegate(th, colourless, inner)` that `Model.themeRowDelegate` wraps, and calling it from `renderThemePanel` (`p.list.SetDelegate(...)` on the copy). That keeps exactly one composite-literal site, so `TestThemePanel_DelegateHasASingleConstructionPoint` stays green, and makes a missed re-point structurally impossible. Tradeoff: it partly pre-empts 8-11's explicitly-assigned resize re-point and adds a per-frame `SetDelegate`/`updatePagination` cost on a per-keypress surface, which is why the reviewer recommends the comment fix and leaving the mechanism as the plan sequenced it.

  CONFIDENCE: high (that the comment is wrong); medium (on which of the two mechanisms the phase should end up with)

- `internal/tui/theme_panel_test.go:526-544` and `:637-649` — the overlay and composed-frame assertions compare `ansi.Strip`ped text only, so the criteria "leaves every base cell to the left of the panel byte-identical" and "the fill never paints over any panel cell" are pinned at **glyph** level. A composite that flattened the base's canvas behind the panel — the live-preview premise — or that recoloured panel cells would pass unchanged.

  FIX: In `TestThemePanel_OverlayDoesNotRelayoutTheBase`, compose over a *canvas-painted* base (e.g. rows rendered through `headerCanvasBg(th,false)`) and assert with the existing `scanCellBackgrounds` helper that every cell left of the cut still carries the base's bg params and every cell from the cut rightwards carries the panel's — the same cell-level precision `TestThemePanel_BodyIsCanvas` already uses.

  CONFIDENCE: medium

NOTES:

- **Both flagged judgement calls check out.** (a) Overlay placement: the two pinned instructions genuinely conflict — applying `X(contentW - panelWidth).Y(0)` against a full-terminal base lands the panel `Hinset` columns left and `Vinset` rows high. Compositing inside `fillCanvas` after the content-region fill and before the gutter inset satisfies both intents; the per-line backfill/pad loop has already completed so no fill cell can touch a panel cell, and the panel's right edge is the content region's right edge where `? help` and the header hint actually sit. The closed-panel path returns the content string untouched and the `[]string`→join→split round-trip is lossless, so byte-identity holds and is asserted. (b) The painter is necessary, not papering over a layout bug — verified directly against the pinned `ultraviolet` build: `renderLine` buffers `EmptyCell`s and drops them if nothing follows, so trailing unstyled cells are discarded (probe: a 40-cell composite came back 31 cells wide). The bare cells are real — `list.View()`'s `JoinVertical` pads short sections with plain spaces and `headerPadRight` then sees a row already at `inner`. Reusing the surface-agnostic `backfillCanvasBackground` is the same fix `fillCanvas` already applies for the same reason. No double-painting: the transform only touches SGRs that reset bg to default, panel rows are never seen by `fillCanvas`, and the border cell is styled outside the painter.
- **The two `//nolint:unused` directives are legitimate**, verified rather than assumed: `golangci-lint run --default=none --enable=nolintlint --enable=unused ./internal/tui/...` reports 0 issues, i.e. `nolintlint` does not flag them as unnecessary, so `unused` really does fire on both fields. They are forward declarations of a struct shape the task pinned.
- Copying `themePanel` by value into `renderThemePanel` and `SetSize`ing the copy is safe (`SetSize`/`updatePagination` mutate only value fields, and `updatePagination` restores `Paginator.Page` from the index), so `View()` stays pure.
- **Forward risk for 8-7/8-9, not a defect here:** `newThemePanelList` leaves `Styles.ActivePaginationDot`/`InactivePaginationDot` at bubbles' hardcoded `#847A85`/`#979797`/`#DDDADA` defaults (`bubbles/v2@v2.1.0/list/style.go:89-95`). Portal's `canvasPaginationDots`/`colourlessPaginationDots` are not applied at construction, and 8-9 re-points them only inside `applyCanvasMode`, so between open and the first arrow a paginating panel draws non-token dots — and under colourless they would emit a foreground hue. `TestThemePanel_EveryChromeSurfaceIsATokenLookup` and `TestThemePanel_Colourless` both use non-paginating fixtures, so neither exposes it. Carry into 8-7 (theme the dots at open) or 8-9 (§11.2's paginating fixture).
- `renderThemePanel` sizes only its own copy of the list. 8-7 must still `SetSize` the model's list at open (8-11 already requires it on resize), or `Ctrl+↑/↓` will page against a `PerPage` of 1 while the render shows the correct page — the render would stay coherent, so the divergence is silent.
- Under `colourless` an open panel's rows end in unstyled pad, so the compositor trims them and `insetColourless` emits ragged-width rows. Visually inert (no canvas is painted under `NO_COLOR`) and unreachable once 8-13's §9.10 block lands; noted only so nobody later asserts exact frame widths on a colourless open-panel frame.
- The panel is `contentH` tall inside the gutter rather than `termH` flush to the terminal edge. The reviewer judges that correct — it matches Portal's frame idiom and §9.1's "covers … the right end of the footer" — but it is the one thing the Paper frames could disagree with, so it belongs on the visual gate at 8-15/8-16.
- All 14 named tests exist under the specified names, plus two justified extras. The page-2 test correctly guards itself ("the list did not page: the page-2 assertion proves nothing"); the mid-label test guards that the cut actually lands inside a word; the geometry table covers paginating and non-paginating body heights. `go build`, `go test ./...`, `go vet`, `gofmt -l`, `golangci-lint run` all re-run independently and clean.

## Attempt 2

ISSUES:

- `internal/tui/theme_panel_test.go:658` — "It also pins the layer ORDER." is **false**, and the executor verified it false in their own fix-round report while stating they had removed it; **the sentence is still in the file**. Reproduced independently: rewriting `fillCanvas` to composite the panel *before* the per-line fill leaves the entire `internal/tui` suite green. In this file's own idiom "pins" means "the test asserts" (cf. `:201`, `:320`, `:601`), so a maintainer reading this would believe a reorder is caught. It is the same defect class the previous round blocked on — a claim in the design record about a guard that does not exist.

  FIX: Drop the "pins the layer ORDER" sentence and state what the test actually asserts — that every panel cell survives verbatim in the composed frame at the content region's right edge, gutter offset included, in glyphs and in paint, which is the criterion's observable ("the fill never paints over any panel cell"). Then record, in the same paragraph, why the ORDER itself is not assertable here: `themePanelPainter` paints every panel cell, so the fill's backfill (which only rewrites bg-reset SGRs) and its trailing pad (which only strips background-less spaces) have nothing to touch — the shipped order is defence-in-depth, and it is load-bearing only on the colourless path, which §9.10 blocks and which the ragged-width note already records.

  ALTERNATIVE: Add a colourless composed-frame test that would bite on a reorder (it would — `fillColourless`'s trim reaches the panel's unstyled pad). **Rejected as a recommendation**: it would pin behaviour on a path §9.10 makes unreachable and whose current output is already a shape nobody ships, so it would encode a defect as a contract. Prefer the comment fix.

  CONFIDENCE: high

NOTES:

MUTATION EVIDENCE (reviewer-run via `go test -overlay`, no repo files touched — confirmed by `git status`):

| Mutation | Result |
|---|---|
| `overlayThemePanel`: base stripped of SGR | **caught** at `theme_panel_test.go:587` (cell-level paint compare) |
| `overlayThemePanel`: panel stripped of SGR | **caught** at `:567` / `:702` |
| `overlayThemePanelOnContent`: panel painted with a different canvas (glyphs + widths identical) | **caught** at `:717` — the composed-frame paint assertion genuinely bites |
| `fillCanvas`: composite the panel **before** the fill (coloured path) | **suite stays green** — the executor's observation is correct |

- **Prior issue 1 is properly fixed.** `theme_panel.go:93-105` now describes the mechanism as implemented (fields on a value, one construction site at `Model.themeRowDelegate`, re-pointed by 8-9/8-11 via `SetDelegate`), and `renderThemePanel`'s doc at `:226-231` correctly scopes "nothing cached" to the chrome it renders while naming the list's rows as the surface `th` does not reach. No free `newThemeRowDelegate`, no per-frame `SetDelegate` — the mechanism is unchanged, as recommended.
- **Prior issue 2 is properly fixed and independently verified to bite** under three separate mutations (see table).
- **The contested acceptance criterion is met.** "The panel is the last layer composed, so the outer full-terminal canvas fill never paints over any panel cell" IS satisfied and IS tested at its observable level. No new test is needed — the *mechanism* half is unpinnable rather than untested, for the reason given in the ISSUE's FIX.
- **Residual inconsistency, out of this task's diff:** `internal/tui/theme_row.go:59-60` (committed under 8-4) still opens with "Theme is the PREVIEWED palette, and it is re-derived per frame rather than cached", which is the wording 8-6 just corrected. Its next clause self-corrects ("the panel re-points the whole delegate through one construction site on each restyle and each resize"), so it is not flatly false, but the two files now describe the same mechanism in opposite terms. Worth aligning whenever 8-9 touches that file.
- **Carry-forward (re-confirmed, now measured):** a paginating panel renders `bubbles`' hardcoded pagination dots — `38;2;151;151;151` / `38;2;60;60;60` — identically under both built-ins, so that chrome surface does not re-theme; under `colourless` it emits foreground hues. `newThemePanelList` applies neither `canvasPaginationDots` nor `colourlessPaginationDots`, and `applyCanvasMode` (§11.2's "same restyle path as the main list") does not yet cover the panel's instance. AC 13 and `TestThemePanel_Colourless` both pass only because their fixtures do not paginate. §11.2 assigns this to the restyle path, so it belongs to 8-7 (theme the dots at open) or 8-9 — but it must land **before** §13.4's paginating fixture (8-15) or the swap-and-diff guard will be blind at exactly the site §11.2 adds it for.
- `TestThemePanel_OverlayCutsMidLabel` deliberately stays glyph-level over an unpainted base. That is correct — it is a layout assertion, not a paint one — and it guards itself against a word-boundary cut.
- The panel is `contentH` tall inside the gutter rather than flush to the terminal edge. Matches Portal's frame idiom and §9.1's "covers … the right end of the footer", but it remains the one thing the Paper frames could disagree with; belongs on the 8-15/8-16 visual gate.
- SPEC/ARCHITECTURE otherwise conformant and sound: §9.1's shape and token table, §9.5's pinned-chrome directory row with its 16-column copy, §9.8's width-ladder ends and measured-remainder layout, the no-animation record and the composite-don't-re-lay-out rationale are all present and match. The overlay's placement inside `fillCanvas` is the only position satisfying both "last layer" and "right edge = content region's right edge". `themePanelPainter` is necessary, not papering over a layout bug. `renderThemePanel` taking `p` by value keeps `View()` pure. Both `//nolint:unused` directives re-verified necessary via `--enable=nolintlint --enable=unused`. `golangci-lint run` 0 issues; colour-literal glob guard covers the new file and passes; no new token.

## Attempt 3

ISSUES:

- `internal/tui/theme_panel.go:237-247` — **the footer is silently truncated at floor heights.** `bubbles/list` has a hard minimum rendered height of 3 rows (item + blank + paginator) regardless of the height it is given, but `themePanelListSize` budgets `max(height-reserved, 1)`. Whenever the body budget is 1 or 2 the list overshoots and `themePanelBlock`'s clamp (`if len(out) == height { break }`, line 345) drops that many rows off the **BOTTOM** — i.e. off the footer.

  Measured: at the no-dir/no-msg floor (h=7) the footer renders only `⏎ set theme` / `d set as dark`; `l set as light` and `esc close` are **gone**. `esc close` is absent at h=7 and h=8, present only from h=9.

  Concrete in-band path: on an 11-row terminal (contentH=9) raising §9.2's confirm shrinks the budget to 2 and eats `esc close` — precisely the "one row short at exactly the moment a message appears" failure §9.8 exists to prevent. It also poisons task 8-11's floor arithmetic, which is specified to read `themePanelHeaderRows + themePanelFooterHeight(...) + 1 (+1)`.

  It further falsifies the claim at `theme_panel.go:196-199` ("Each subtrahend is MEASURED off the renderer that produces it … the reserved rows are by construction the rows that render and the two can never drift") — true of the dir row, message and footer, **false of the list body**.

  FIX (recommended): clamp the list block to its budget before appending, e.g. in `renderThemePanel` take `strings.Split(p.list.View(), "\n")[:bodyRows]` when it overshoots, so the clamp can never reach the footer. The reviewer verified this exact change keeps the whole `internal/tui` suite green and restores all four footer rows at every floor case. Add a named test (`TestThemePanel_FooterSurvivesAtTheFloor`) asserting the last footer entry and, with a message set, the message slot are both present at each of the four floor heights — the case `BlockGeometry` structurally cannot see. Correct the "can never drift" sentence at `theme_panel.go:196-199` to name the list body as the block whose height is **clamped** rather than measured.

  ALTERNATIVE (not chosen): raise `themePanelMinBodyRows` from 1 to `bubbles/list`'s real minimum of 3 and let 8-11's floor inherit it. Honest about the library's constraint and needs no clamp, but it silently redefines §9.8's floor from "one row" to "three rows" — a spec-facing change the spec does not authorise — and makes the panel refuse on terminals where it could degrade. The clamp keeps §9.8's floor literally true and degrades the paginator row (chrome) rather than the keymap.

  CONFIDENCE: high (defect), medium (choice of fix)

NOTES:

- **SPEC_CONFORMANCE**: drift is confined to the above — §9.8's floor ("header + footer + one row + one message row") is unrealisable by this renderer. Everything else conformant: left-border-only chrome, all surfaces token-resolved (no `#0C0C16`/`#2B3050`), two-row countless header, dir row as pinned chrome, message slot unreserved-when-empty, composite-not-re-lay-out, no animation, third `bubbles/list` instance noted. §9.1's "may wrap to two rows at minimum width" is implemented as always-truncate — permissive language, documented in-source, fine.
- **ACCEPTANCE_CRITERIA**: the layout criterion is not met at the floor (footer renders 2 of its 4 rows). All 13 other criteria verified met, most by killing mutations — border/label/dir tokens, overlay X offset, inner-width arithmetic, message reservation, painter removal, dir-row ordering, missing `SetSize` all killed.
- **TEST_COVERAGE gap that let this through**: no test asserts the footer or message slot **survives** at the floor. `TestThemePanel_BlockGeometry` scans every height from the floor upward but asserts only total height and per-line width, so it stays green while the footer is eaten (confirmed green both with and without the defect).
- **Round-2 finding verified fixed.** The rewritten comment at `theme_panel_test.go:654-678` is true in every clause tested: "It does NOT pin the layer ORDER" — rebuilding `fillCanvas` to composite before the fill leaves the whole suite green; "the frame's bytes shift elsewhere, but not one panel cell moves" — frame sha/len differ (5152 vs 5718) while every panel cell's glyph AND background params are byte-identical across the two orders; "the panel's pad IS background-less [under colourless] … current output is ragged-width" — 21 of 24 frame rows are short of `termW` under the shipped order, 0 under the reordered one. The executor's correction of the reviewer's suggested "no assertion could" is independently confirmed and correctly scoped.
- **The sweep was real.** 12 of the ~27 "pins/guards/proves/asserted" sites spot-checked by mutation or by reading the mechanism (border/label/dir tokens, painter, overlay offset, inner width, message reservation, dir-row position, missing `SetSize`, the AST single-construction-site guard, the colour-literal guard's identical scoping, §9.8/§11.2 spec restatements). All true as written except the one below.
- `theme_panel_test.go:415-416` — "It also pins the sizing contract: the list is fed the panel's INNER width" is one step looser than the assertion: the test pins `themePanelListSize`'s returned tuple, not the pass-through to `SetSize`. Feeding `inner±2` leaves the suite green. Non-blocking because the fed width is currently unobservable in output (the delegate carries its own `Width` by design — `theme_row.go:71-74` — and the paginator row is left-aligned, not centred to list width), so there is no behaviour to guard. Worth tightening the verb, or pinning it with a recording delegate that captures `m.Width()` in `Render`.
- Unasserted-but-correct: the message slot's `text.secondary` token (swapping it to `text.primary` survives) and the header label's `Bold(true)` (removing it survives). Neither is claimed as guarded, neither is an acceptance criterion, and §9.1's slot contenders are Phase 9's.
- `internal/tui/model.go:4287` composites the panel on the colourless branch too, where the compositor strips the panel's background-less trailing pad and leaves a ragged frame. Unreachable per §9.10 and documented at `theme_panel_test.go:670-675`, but the wiring is live on that path; worth a one-line in-source note at the call site (or skipping the overlay under colourless) so a future reader does not treat it as a working path.
- **ARCHITECTURE otherwise sound**: the overlay's placement between the fill and the gutter inset is correct and independently verified; the per-block canvas painter is necessary (neutering it kills three tests); `themePanelListSize` is a pure theme-free function so 8-11's floor arithmetic can read it; `renderThemePanelFooter`'s entries-as-parameter shape and the documented "thread the live scope through BOTH" warning are good forethought for Phase 9. The one structural flaw is that **the list body is the only block whose rendered height is budgeted but never measured.**
- **Carry-forward, unchanged**: pagination dots are not themed at construction (`newThemePanelList` applies neither `canvasPaginationDots` nor `colourlessPaginationDots`); belongs to 8-7 or 8-9, must land before 8-15's paginating fixture. `theme_row.go:59-60`'s "re-derived per frame" wording is now inconsistent with 8-6's corrected text; align when 8-9 touches it. The panel being `contentH` tall inside the gutter is judged correct but belongs on the 8-15/8-16 visual gate.
