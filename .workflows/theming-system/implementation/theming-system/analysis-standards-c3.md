AGENT: standards
FINDINGS:
- FINDING: Theme panel width ladder sits outside the specified column budget and is proportional rather than staged
  SEVERITY: low
  FILES: internal/tui/theme_panel.go:46-47, internal/tui/theme_panel.go:210-212, internal/tui/theme_panel.go:22-24, internal/tui/theme_row.go:130, internal/tui/theme_panel_message.go:225
  DESCRIPTION: §9.1 and §9.8 pin the slide-over at "a fixed preferred width of ~24–30 columns", and §14A treats that range as the layout constraint every pinned panel string is written against ("it has to fit 24–30 columns"). The implementation uses themePanelMinWidth = 27 / themePanelPreferredWidth = 34, and themePanelWidthFor computes min(max(contentW/2, 27), 34) — a proportional half-of-content rule clamped at both ends, rather than the preferred-width-then-staged-shrink shape §9.8 describes and justifies ("A fixed width is predictable to lay out against"). The code has silently re-baselined the spec's number: three separate comments now restate the budget as "~27–34 columns" (theme_panel.go's header, theme_row.go's compose, theme_panel_message.go's no-band note), so the divergence is recorded only as a contradiction of the spec it is derived from. Consequences are small but real — the panel covers ~4 more columns of the previewed page at wide terminals (the §9.1 trade-off explicitly weighed against preview value), it refuses entry at content widths 24–26 that the spec's floor would have degraded into, and the panel's width now changes on every resize step between ~54 and ~68 terminal columns. §9.8 does delegate "exact thresholds ... at implementation", which is why this is low rather than higher, but the delegation was for the degradation steps, not for the preferred-width band the copy decisions rest on.
  RECOMMENDATION: Either bring the ladder inside the decided band (preferred ≤ 30, minimum ~24, with a staged shrink between them) or record the widened 27–34 range as a deliberate amendment to §9.1/§9.8/§14A rather than leaving it asserted only in code comments that contradict the spec.

- FINDING: Panel cursor row omits the bold the "shipped selection treatment" is defined to include
  SEVERITY: low
  FILES: internal/tui/theme_row.go:112-116, internal/tui/theme_row.go:253-256, internal/tui/session_item.go:19, internal/tui/session_item.go:495-499
  DESCRIPTION: §9.1 pins the panel's cursor row as "the shipped selection treatment (`▌` + tint + white bold name), so the panel's list reads as the same kind of list as Sessions". The Sessions delegate renders the selected name through nameBase (lipgloss bold) with text.on-selection; the panel's delegate renders its label through rowToken with an explicitly empty base style, and its comment states the opposite of the spec's requirement — "Panel rows carry no non-colour attribute of their own, so there is no base style to pass". The tint, the `▌` and the token are all correct; only the weight differs, so the panel's cursor row reads slightly lighter than the Sessions cursor row it was specified to mirror.
  RECOMMENDATION: Pass the shared bold base (the same style the Sessions delegate uses for the selected name) when the panel row is selected, so the three elements of the shipped selection treatment are all reproduced — or amend §9.1 if the unbolded label was a deliberate outcome of the visual gate.

- FINDING: §13.2's bounded pre-feature capture deletion was not performed
  SEVERITY: low
  FILES: testdata/vhs/reference/ (24 pre-feature PNGs, e.g. kill-confirm-modal-mv.png, loading-mv.png, sessions-modern-vivid-v2.png, edit-modal-*.png, filtering-*.png), internal/capture/fixtures.go:376, internal/capture/fixtures.go:528, internal/capture/fixtures.go:559, internal/capture/fixtures.go:770, internal/capture/fixtures.go:823, internal/tui/keymap.go:81, internal/tui/loading_view.go:17
  DESCRIPTION: §13.2 draws the retention rule and puts two bounded acts in scope: "Everything that exists today as an image or tape is deleted — the committed reference PNGs and the VHS tapes that produce them ... delete today's images and tapes once at the start, clear this feature's own once at sign-off." The first act did not happen: two dozen pre-feature Modern Vivid reference PNGs are still committed, including ones the spec says "could not survive the token rename and the theme split without a full recapture" (the light/dark MV frames, the modal frames). They are additionally cited by name from production comments in internal/capture/fixtures.go, internal/tui/keymap.go and internal/tui/loading_view.go, so the surviving set reads as the durable visual-regression baseline §13.2 states does not exist — and this feature's own new citations (fixtures.go:770 and :823, the two theme-panel frames) will become dangling the moment the sign-off clearance runs.
  RECOMMENDATION: Perform the start-of-feature deletion §13.2 scopes (the pre-feature reference PNGs and any tapes), and drop the "mirrors testdata/vhs/reference/<name>.png" citations from the fixture doc comments — a comment pointing at scaffolding the retention rule deletes cannot stay true, and code-quality.md bars artifact references in comments for exactly this reason.

- FINDING: New test-only package `internal/themetest` is absent from CLAUDE.md's internal-package inventory
  SEVERITY: low
  FILES: internal/themetest/builtin.go, internal/themetest/theme_file.go, CLAUDE.md
  DESCRIPTION: This feature adds a new internal package, `internal/themetest` (Builtin / DefaultDark / DefaultLight, plus the theme-file writer helpers), consumed from cmd, cmd/capturetool, internal/tui and internal/capture test files. CLAUDE.md's internal-packages table documents every other test-only helper package explicitly, and each carries the "production code must not import" rule in writing (portaltest, logtest, spawntest, restoretest / tmuxtest / portalbintest / transienttest). §12.6 makes CLAUDE.md this feature's own responsibility on the grounds that "CLAUDE.md is what an implementing agent reads first", and all seven named corrections were made correctly (the theme row, the tui row, config path resolution, the bootstrap-exempt set, the prefs row, the 18-component logging count, the capture-harness section) — but the new package was not added, so its test-only contract rests solely on the `*testing.T`-first signature convention with nothing written down.
  RECOMMENDATION: Add a `themetest` row to CLAUDE.md's internal-packages table alongside the other test-only helpers, stating its exports and the production-must-not-import rule.
SUMMARY: Conformance to the specification is unusually close — the rejection ladder and its short-circuit order, the reason vocabulary and pinned copy for every surface, the two-state setting with `theme`-wins, per-slot mode-matched fallback, embedded-set-before-directory ordering, prefs RMW with the split tolerant/strict decodes and the retained raw `appearance`, the `theme` component's closed event and attr catalogue with its emission-by-injected-logger rule, the panel's union/badge/sort/confirm/commit-failure state machines, the startup-canvas-hex anchoring for RestoreTerminalBackground, and every spec-mandated guard test all match what was decided. The four findings are peripheral: a panel width ladder that drifted outside the decided column band (and is proportional rather than staged), a missing bold on the panel cursor row, the un-performed start-of-feature capture deletion §13.2 puts in scope, and a new test-only package missing from CLAUDE.md's inventory.
