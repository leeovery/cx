## Attempt 1

ISSUES:
- `internal/theme/builtins_test.go:151-159` — `TestBuiltins_AreEnrolledInFloorChecks` is a TAUTOLOGY. `embeddedThemes(t)` (`contrast_test.go:378-399`) builds its map from exactly `theme.BuiltinSlugs()` and `t.Fatalf`s on any load failure, so on return its key set IS `BuiltinSlugs()`. Ranging over `BuiltinSlugs()` and testing membership in that map makes the `t.Errorf` on :156 unreachable. Proved both by construction and empirically in a scratch copy: adding a fourth built-in passes; the only way to make the test fail is to break a built-in file, at which point the failure comes from `embeddedThemes`'s own fatal — which every other floor test already gets for free.
  The task's own Tests bullet ("confirm the enrolment test fails naming its slug") is unsatisfiable against this shape, and the report offers the PASSING case as evidence. Two tests that COULD fail (a deleted `nord.theme` / `tokyo-night-day.theme` errored naming the slug at HEAD) were replaced by one that cannot, and the doc comment on :137-150 claims a pin the code does not provide.
  The property itself is not lost — `TestThemeAppearanceTableCoversEveryEmbeddedTheme` (`light_pins_test.go:131`) genuinely fails on an unenrolled built-in, confirmed with a fourth theme file — but this test contributes nothing.
  FIX: range over the INDEPENDENTLY DERIVED set instead of the same one: `for _, slug := range committedBuiltinSlugs(t)` (already in the same file, `builtins_test.go:163`, reading the committed directory). The loop then compares an on-disk read against the loaded floor set, so a built-in that never reaches the floor set — e.g. because the `//go:embed builtins/*.theme` pattern was narrowed — fails naming its slug. Adjust the doc comment's second paragraph to say the expectation is read off the committed directory, so the comment stops claiming more than the code does.
  ALTERNATIVE: assert set equality — `slices.Equal(committedBuiltinSlugs(t), slices.Sorted(maps.Keys(enrolled)))` — which additionally catches a STALE enrolment (a slug measured that is no longer committed). Slightly less precise failure output; the per-slug loop is recommended because the task asks for a failure naming the slug.
  CONFIDENCE: medium

NOTES:
- CORRECTION to the report: the "got stronger" claim about the eyeball-pin guard is OVERSTATED. At HEAD an entirely absent comment block already failed (`Contains("", anchor)` and `Contains("", marker)` both false → two `t.Errorf`s); the reviewer ran that mutation against a HEAD checkout to confirm. The change makes it a `t.Fatalf` with a clearer message — better diagnostics, not new detection. The second "stronger" claim (the seven re-derivation records now pass through the sweep, vacuously today) IS accurate as stated.
- No per-built-in claim was weakened. The reviewer diffed the record literals (figure lists verbatim-identical for all 13 records), diffed the full `-v` subtest inventory HEAD vs now, and independently falsified all eight surviving claim classes against a scratch copy: shipped-value drift, missing marker, false marker (sweep), missing figure, missing anchor, and an entirely deleted comment block each fail under the correct subtest name. The four eyeball-pinned values a generic loop cannot re-derive survive as data, including the lower-case anchor `#28243a`.
- The marker sweep is now derived from the records rather than a fixed list. Today's coverage is identical, but if every record of a given marker were ever deleted from the Go table, that marker would silently stop being swept — a small loss of resistance to test-side hollowing, not a loss of a current claim. Worth a line in the sweep's doc if cheap.
- The report's honesty about the two hand-edit tests is ACCURATE and was reproduced exactly: dropping a fourth `.theme` into `builtins/` auto-enrolled it in 25 contrast subtests, and precisely two tests demanded a hand edit, both failing loudly by name — `TestThemeAppearanceTableCoversEveryEmbeddedTheme` (light/dark table) and `TestRowOrder_TotalAndDeterministic` (canonical union order).
- Placement, not a deviation (the task directed it): the shared scaffolding sits in `builtins_tokyo_night_day_test.go`, so a Nord guard failure is reported at that file. Worth moving the shared block to `builtins_test.go` in a later sweep.
- `assertDerivationRecords`'s `markedSet string` is a sentinel-shaped parameter (`""` from the seven-values guard). Reads fine given the doc comment; noted in case a later guard makes the pattern grow.
