## Attempt 1

ISSUES:
- The located-vs-unreadable fold branch (the executor's flagged judgement call #2) has **zero test coverage**. `cmd/theme.go:147-149` (`return nil, unreadableRejection(err)`) and the whole of `unreadableRejection` at `cmd/theme.go:193-195` are uncovered — confirmed with `go test ./cmd -coverprofile`, which reports `theme.go:147.16,149.3 → 0` and `theme.go:193.54,195.2 → 0` across the entire package. This is the only new branch in the diff with no assertion. A regression that reverted line 148 to `return nil, err` would emit `failed to determine home directory: $HOME is not defined` as the user's whole message — a fifth, unpinned sentence on a surface §14A closes at four — and every existing test would still pass. It is conspicuously absent from the executor's own mutation list, which covers every other decision in the diff.
  FIX: Add a subtest to `TestThemeExport_UnreadableFrame` in `cmd/theme_test.go` (after the `an unreadable directory` case at :942) named `a themes directory that cannot be located`. Reuse the fixture already used twice in the file (`theme_test.go:352-358` and `:903-909`): `t.Setenv` `PORTAL_THEMES_DIR`, `XDG_CONFIG_HOME` and `HOME` to `""`, assert `themesDirPath()` errors so the subtest cannot go vacuous, capture that error, then `requireExportRefusal(t, execThemeExport(t, "nord-lee"), "theme nord-lee could not be read: "+err.Error())`. Deriving the expected text from `themesDirPath()`'s own error (rather than hard-coding `$HOME is not defined`) keeps it platform-neutral, matching how `osReadError` sources the OS wording in the sibling subtests. The reviewer verified this passes against the current implementation.
  CONFIDENCE: high

NOTES:
- Copy verified character-by-character against §14A (spec lines 1835-1838). All four frames exact; the three content reasons render §6.2's labels verbatim. `unreadable` is a separate frame, not folded. Exit-1-for-every-class and control-strip-at-the-read-site both honoured.
- **(b) The `Lstat` judgement call is CORRECT, verified empirically.** `os.ReadFile` on a dangling symlink returns ENOENT with `os.IsNotExist == true`; `os.Stat` likewise; only `os.Lstat` returns nil. An `os.IsNotExist`-over-the-read-error implementation would report `no theme named` for a dangling link, contradicting §5.6 and the task's own list. Non-ENOENT `Lstat` failures correctly fall through to `unreadable`. TOCTOU exists in principle but the worst outcome is one wrong diagnostic sentence in a read-only CLI.
- **(c) The located-vs-unreadable fold is the RIGHT CALL** (its lack of coverage is the issue above, not the decision). The resulting frame is `theme nord-lee could not be read: failed to determine home directory: $HOME is not defined` — names the theme, stays inside §14A's four, carries the system's reason verbatim. §5.5's "a theme made unreachable by an unusable directory carries the reason `unreadable`" covers an unlocatable directory as naturally as an unreadable one.
- **(d) The `-nord` handling is verified and the substitute is acceptable.** Bare `-nord` yields pflag's `unknown shorthand flag: 'n' in -nord` and never reaches `RunE`. Asserting through `--` is right; `DisableFlagParsing` would be scope expansion and would break `--help`. §12.1 already puts arg-parsing failures outside the four frames.
- **(f) The exit-1 structural assertion is genuinely equivalent.** `main.classify` has exactly four arms; `requireOrdinaryError` rules out three, and `main_test.go` pins the fourth end-to-end. The residual is a future fifth arm, which the helper's doc comment names explicitly.
- **(e) `StripControl` is correct for every shape the spec names, with one documented-but-untrue edge.** Probed against SGR, truecolor SGR, OSC-8 hyperlinks, DCS/Sixel, private-mode, a trailing lone ESC, a bare `ESC[`, bare NUL/BS/TAB/CR/LF/DEL and C1 NEL — all stripped clean. The exception is the **8-bit C1 CSI**: `"31mnord"` → `"31mnord"`; `ansi.Strip` does not consume the C1 form, so the control-pass removes only U+009B and leaves printable `31m` — precisely the residue the doc comment at `internal/theme/name.go:92-95` claims the composition prevents. Reaching it requires a UTF-8-encoded C1 byte in argv. Not worth code, but the doc comment slightly overstates.
- `StripControl` does not remove Unicode format characters (Cf) — U+200B ZWSP and U+202E RTL-override pass through, since `unicode.IsControl` covers Cc only. Out of scope: §9.5 names newline, tab and ANSI escape, and §5.2's reject-never-normalise rule means the charset check refuses them anyway.
- An argument that is *entirely* control characters strips to `""`, rendering `theme  is not valid: bad name` with a double space. §14A pins no empty-slug case, so this is cosmetic and correct-by-the-letter, but untested and a reader might mistake it for a formatting bug.
- **Forward-looking for Phase 5**: `internal/theme` transitively imports `internal/log` (via `events.go`). CLAUDE.md records that `internal/prefs` is a deliberate leaf that must not import the logging machinery (avoids an import cycle with `internal/tui`). So §9.5's prefs-slug strip site must sit in `cmd` or `internal/tui`, not inside `internal/prefs`. Worth carrying into the Phase 5 task rather than discovering at implementation.
- `exportRefusal` uses `%s` on `rejection.Detail` rather than `%w` on `rejection.Err`. Given `Detail == Err.Error()` the rendered text is identical, and it matches the `Rejection` doc's contract. Deliberate and fine.
