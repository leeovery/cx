AGENT: standards
FINDINGS:
- FINDING: Production comments restate the specification's design argument at a volume the quality standard forbids
  SEVERITY: medium
  FILES: internal/tui/theme_state.go:1-220, internal/tui/theme_seams.go:5-54, internal/theme/load.go:10-107, internal/theme/resolution.go:119-152, internal/theme/resolution.go:359-404, internal/theme/union.go:200-285, internal/theme/badge.go:3-97, internal/theme/setting.go:57-95, internal/tui/theme_panel.go:563-673, internal/tui/theme_panel_commit.go:10-33, internal/tui/theme_panel_commit.go:321-377, internal/tui/theme_panel_confirm.go, internal/prefs/store.go:514-574, cmd/doctor_theme.go:10-59, cmd/config.go:188-325
  DESCRIPTION: code-quality.md's Comments section is explicit that a comment may not carry "the design argument … State the conclusion the code needs, not the debate; the reasoning lives in the project's design artifacts", and that a comment must earn its place. Across this topic's production files the comment block IS the specification prose, re-narrated: rejected alternatives ("Rejected: mutable package state", "A first arithmetic answer … was rejected at a visual gate"), the debate behind a choice, and multi-paragraph restatements of what the code below plainly does. Comment-to-line ratios in the new production files are 56–85% (theme_state.go 186/220 = 85%, theme_seams.go 50/60 = 83%, theme_panel_commit.go 308/432 = 71%, load.go 205/289 = 71%, theme_panel.go 1224/1745 = 70%, setting.go 80/115 = 69%, badge.go 120/176 = 68%, resolution.go 326/493 = 66%, union.go 319/504 = 63%, doctor_theme.go 278/457 = 61%, prefs/store.go 364/625 = 58%) against 30–50% in the comparable pre-existing subsystems (spawn/burst.go 108/225, notice_band.go 332/579, restore/restore.go 79/209, state/capture.go 144/497, cmd/doctor.go 408/884). Single doc comments run 40–60 lines (resolveSlot, SaveTranslation, recomputeThemePanel, Loader, ResolveSetting). Every one of those claims is unchecked by compiler or test, so the volume is maintenance liability: the spec is archived after sign-off, but these paragraphs stay and go stale on the first behavioural edit.
  RECOMMENDATION: Trim the topic's production comments to what code-quality.md admits — why, a warning about deliberate-looking-wrong code, opaque-what, and a doc comment stating what/inputs/outputs/errors. Delete rejected-alternative narration and the re-argued rationale (they live in the specification), collapse the multi-paragraph doc comments to the conclusion the code needs, and aim at the surrounding codebase's density rather than the spec's. Keep the small number of genuine trap warnings (the startupCanvasHex freeze, the canvas-echo guard, the fallback-slug coupling, the two-decodes split in prefs), which are exactly the class the standard preserves.

- FINDING: Comment classes the quality standard names as never-allowed appear in new production code
  SEVERITY: low
  FILES: internal/tui/theme_row.go:54, cmd/theme_persister.go:48, internal/tui/theme_panel_footer.go:20, internal/tui/theme_panel_confirm.go:309, internal/tui/theme_panel_commit.go:324-325, internal/tui/theme_panel.go:314, internal/tui/theme_panel.go:567, internal/theme/union.go:225
  DESCRIPTION: code-quality.md lists two classes as "Never in a comment" that this topic's production code carries. (1) Claims about tests — "anywhere in cmd is what TestThemeComponent_BoundOnceInCmd exists to catch" (cmd/theme_persister.go:48), "the exact drift class keymap_dispatch_guard_test exists to guard" (theme_panel_footer.go:20), "is the parity keymap_dispatch_guard_test's contract exists to hold" (theme_panel_confirm.go:309); a renamed or moved test turns each into a confident lie. (2) Cardinality claims — "so nothing consumes it today" (theme_row.go:54, the standard's own verbatim example of what not to write), "IT RUNS ON A SUCCESSFUL COMMIT AND NOWHERE ELSE … and nothing else does" (theme_panel_commit.go:324-325), "it has TWO callers by design" (theme_panel.go:314), "THE READ HAPPENS HERE … AND NOWHERE ELSE" (theme_panel.go:567), "The read happens here and NOWHERE ELSE on this path" (union.go:225) — all falsified by ordinary additive change far from the comment.
  RECOMMENDATION: Delete the test-name references (the guard tests are discoverable by name; the comment adds nothing the code needs) and re-word the cardinality claims into the invariant they are protecting without counting call sites — e.g. "a commit that did not land must not recompute" instead of "runs on a successful commit and nowhere else".

- FINDING: Workflow vocabulary (spec-section citations, task ids) remains throughout the topic's test comments
  SEVERITY: low
  FILES: internal/theme/name_test.go:118, internal/theme/enumerate_test.go:350, internal/theme/theme_test.go:123, internal/capture/theme_swap_guard_test.go:626, internal/tui/theme_panel_behaviour_test.go, internal/tui/theme_panel_confirm_test.go, internal/tui/theme_panel_commit_load_test.go
  DESCRIPTION: code-quality.md forbids "any workflow vocabulary — task ids, phase numbers, spec-section citations" in comments, and states the reason: the comment must hold true for a reader with no knowledge of the process that produced the code, long after its artifacts are archived. Production files were swept clean of `§` citations by the phase-11 task, but the topic's test files carry them densely (theme_panel_behaviour_test.go 71 occurrences, theme_panel_confirm_test.go 69, theme_panel_commit_load_test.go 66, theme_panel_commit_slot_test.go 56, theme_panel_commit_failure_test.go 54, capture/theme_panel_message_fixtures_test.go 52, and so on), plus three task-id references ("Enumeration (task 1.7)", "from task 1.8", "from task 1.3"). The standard draws no production/test distinction, and the spec these citations point at is archived at sign-off, so `§9.5` will name nothing a future reader can open. Pre-existing test files carry a handful of the same citations, so this is an amplification of an existing pattern rather than a new one.
  RECOMMENDATION: Replace the citation with the rule it names ("pins the one-slug-one-row union rule" rather than "pins §9.4") and drop the three task-id references outright. If the project has consciously scoped the no-citations rule to production comments, record that decision so the next cycle does not re-raise it.

- FINDING: The panel's column band is wider than the specification's stated ~24–30 columns, with no spec record
  SEVERITY: low
  FILES: internal/tui/theme_panel.go:62-63, internal/tui/theme_row.go:140
  DESCRIPTION: §9.1, §9.5 and §9.8 all state the slide-over as "a fixed preferred width of ~24–30 columns", and the four-element row-composition priority and its truncation floor are argued against that budget ("The elements compete for a fixed ~24–30 columns"). The implementation ships themePanelPreferredWidth = 34 / themePanelMinWidth = 27, ~13% above the stated top end, and the row delegate's own comment restates the budget as "~27–34 columns". The in-source note gives the reason (at 30 columns badges crowded the right edge and `set as light` ran to the border), which reads like a visual-gate outcome rather than drift — but §9.8's "exact thresholds are pinned at implementation" is written about the degradation ladder's steps, not about the preferred width itself, and the accepted cost recorded in §9.1 (how much of the main footer the overlay covers) was reasoned at the narrower band. Nothing else in the implementation depends on the old number, so this is a documentation divergence rather than a defect.
  RECOMMENDATION: Confirm the widened band was accepted at the human visual gate and, if so, surface it as a spec amendment (§9.1/§9.5/§9.8's "~24–30" and the footer-coverage cost) rather than leaving the spec and the code stating different budgets. If it was not gated, bring the preferred width back inside the specified band.
SUMMARY: The implementation tracks the specification unusually closely — the rejection ladder and its short-circuit order, the two-state setting and its theme-wins tiebreak, per-slot mode-matched fallback, badge derivation, the union's one-slug-one-row rule and sort tie-break, the panel's entry conditions, key exclusivity, confirm and outstanding-failure state machine, the event catalogue and its dedup rules, prefs' two-decode write path and marker semantics, doctor's advisory class and pinned copy, export, capturetool's --theme, and every pinned user-facing string all match. The findings are standards rather than conformance: production comments carry the specification's design argument at 56–85% of file lines (against 30–50% in comparable existing subsystems), with a handful of the never-allowed test-claim and cardinality classes, workflow citations left in the topic's test comments, and one undocumented widening of the panel's column band.
