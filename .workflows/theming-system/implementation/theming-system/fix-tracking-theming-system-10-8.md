## Attempt 1

ISSUES:

- `.workflows/cli-verb-surface-redesign/specification/cli-verb-surface-redesign/specification.md:309` — the rewritten lead-in reads "The authoritative catalog of **Portal-health checks** — **the class that drives the exit code**", and the list it introduces ends (`:316`) with "host terminal detected + supported". The code contradicts this: `checkHostTerminal` returns `checkInfo` (`cmd/doctor.go:502-512`), `doctorUnhealthy` counts only `checkFail`/`checkUnknown` (`cmd/doctor.go:808-815`), and `doctorCheckCounts` puts `checkInfo` in *neither* `<N>` nor `<T>` (`cmd/doctor.go:836-849`, pinned by `TestDoctorSummary_InfoLineOutsideCounts`). The artifact then contradicts itself three times below — `:337` ("distinct from the informational host-terminal line"), `:341` ("*outside* the pass/fail set"), `:343` ("count toward neither"). A reader querying "what drives doctor's exit code" is served a seven-item list whose last item does not. The pre-edit wording ("the set `doctor` inspects") was true of that list; the new gloss is not. **This falsification was introduced by the correction itself.**

  FIX: narrow the gloss so the carve-out survives. Replace

      The authoritative catalog of **Portal-health checks** — the class that drives the exit code (planning implements the concrete probe per check):

  with

      The authoritative catalog of **Portal-health checks** — the class the exit code is drawn from, its informational host-terminal line excepted (planning implements the concrete probe per check):

  Optionally tighten the table row at `:332` the same way (`| **Portal-health checks** — the catalog above, bar its informational host-terminal line |`), or drop the `— the catalog above` gloss there so the row stays the class-level statement §12.2 makes rather than a claim about every listed member.

  ALTERNATIVE: leave `:309` alone and instead qualify the catalog bullet at `:316` (`host terminal detected + supported — informational only, outside the pass/fail set (see …)`). This puts the truth where the falsified item actually is, but edits a bullet outside the correction's named scope and reads as a second statement of the `:341` carve-out; the `:309` fix is one clause and touches only text this task already rewrote. Reviewer recommends the `:309` fix.

  CONFIDENCE: high (the falsification), medium (which of the two spots to edit)

NOTES:

- VERDICT context: SPEC_CONFORMANCE conformant to theming §12.2 / §14A / §15.1 and to the protocol — edit-in-place, corrigendum beneath the title, re-index. Owning unit `cli-verb-surface-redesign` is `completed` and its manifest untouched. Nothing committed. All acceptance criteria met except the internal contradiction above.
- Old "0 iff every check passes" wording survives ONLY inside the corrigendum quote. Two classes named with markers, `⚠` glyph-backed and non-exit-code-driving (`:328-333`). Reason recorded in the spec's own terms (`:336`). Summary in both forms + ` · <M> advisory|advisories` + M=0 suppression + described as a line the report **gains** (`:342`), byte-exact against `doctorSummaryLine` (`cmd/doctor.go:871-881`). `<N>`/`<T>` Portal-health only, `<M>` counts lines (`:343-344`). Host-terminal bullet (`:341`) byte-identical — no hunk in the diff; new class sits beside it (`:337`). `--fix` both passes, exit solely post-repair (`:340`). Class open-ended, theme as "first member, not its definition" (`:337`). Five corrigendum entries, protocol one-line form, dated 2026-08-07. Re-index live — a knowledge query returns the corrected body; the old sentence appears in the store only inside the corrigendum quote, no stale chunks.
- **Code disagreement independently verified — the executor's call was right, and its framing slightly overstated.** §14A is *silent* on info/not-evaluable checks, not in conflict with the code: it says only that `<N>`/`<T>` count Portal-health checks and that advisories never fold in. `doctorCheckCounts` (`cmd/doctor.go:836-849`) excludes `checkInfo` and `checkNotEvaluable` from both counts, counts `checkPass` toward both, and `checkFail`/`checkUnknown` toward the total only. `TestDoctorSummary_NotEvaluableOutsideCounts` (`cmd/doctor_summary_test.go:95-108`) produces exactly "6 checks passed" with exit 0 over a seven-check catalog with one not-evaluable — i.e. the spec's illustrative "6 of 7 … beside exit 0" is precisely the state the code avoids. Writing the spec to the code added the precision §14A lacks; it did not overrule it.
- **Every other new sentence fact-checks clean against the code**: summary forms and no-singular-carve-out (`doctorSummaryLine`, `TestDoctorSummary_NoSingularCarveOut`); ` · 1 advisory` / ` · 2 advisories` and M=0 suppression (`pluralCount`, `if m := len(advisories); m > 0`); `<M>` = length of the post-union slice, so it counts lines after the one-slug-one-line dedup (`assembleThemeAdvisories` / `persistedSlugs`, `cmd/doctor_theme.go:152-186`); advisories in **both** `--fix` renders, freshly re-collected (`cmd/doctor.go:301,336`, guarded structurally by `TestDoctorFix_ScanReRunForSecondPass`'s AST check and behaviourally by `TestDoctorFix_AdvisoriesInBothPasses` / `_SuffixInBothSummaries`); exit driven solely by `doctorUnhealthy(postResults)`; trailing-block ordering after the whole catalog including the host-terminal line (`renderDoctorReport`, `TestDoctorSummary_IsTheLastLine`).
- **Sweep correction (b) `portal theme export` row verified**: `themeCmd`/`themeExportCmd` carry no `Hidden` and are registered on `rootCmd` (`cmd/theme.go:157-160`) — public; `skipTmuxCheck["theme"] = true` (`cmd/root.go:73`) — bootstrap-exempt; it writes `result.Source` (the file's bytes) to `cmd.OutOrStdout()` and resolves built-ins *and* drop-ins via `ResolveByName`; one-member group. Row is accurate.
- **Sweep correction (c) `themes/` verified**: `cmd/uninstall.go` contains no `os.Remove`/`RemoveAll`/`WriteFile`/`AtomicWrite` at all, and `themesDirPath()` resolves `XDG config base/portal/themes` (`cmd/config.go:392-403`), so the directory genuinely sits alongside the enumerated files and is untouched.
- **The item left uncorrected was rightly left.** Completion is registered only for `open`'s bare positional, `--session`, `--alias` (`cmd/open.go:1188-1196`) and `kill`'s positional (`cmd/kill.go:62`); `themeExportCmd` has no `ValidArgsFunction`. The Tab Completion table (`:239-247`) therefore matches the shipped surface exactly — nothing is falsified, and adding a theme-slug slot would invent a decision no spec makes. Correct restraint.
- Corrigendum entry 2 truncates the original mid-bold (drops ", non-zero if anything remains unhealthy or unfixable") with no ellipsis. The quoted fragment is still verbatim and the correction accurate; non-blocking.
- Corrigendum entries 4 and 5 record completeness gaps rather than falsified claims (entry 4 has no quotable original claim and describes the omission instead). Acceptable under the protocol's "the live file is current truth" framing; worth knowing they are updates, not refutations.
- The Bootstrap Exemption section (`:386-394`) still names only `doctor` and `uninstall` while the new table row calls `theme` bootstrap-exempt. Not falsified — the section says "exempts a set (including `state`)" and never claims to enumerate it — so no change is required.
- `go test -count=1 ./...` all green (34 packages); `go vet ./...` clean; `golangci-lint run` → 0 issues; `gofmt -l .` reports only the three pre-existing out-of-scope offenders. No Go file touched by the diff. No "was X, now Y" residue in any section body.
- **Outstanding for the orchestrator**: protocol step 4 requires a commit **scoped to the owning unit** — `engine.cjs commit cli-verb-surface-redesign -m "specification(cli-verb-surface-redesign): corrigendum from theming-system"` — separate from tasks 10-7 and 10-9. Must not fold into an `impl(theming-system)` commit.
