AGENT: standards
FINDINGS:
- FINDING: The §2.4 token rename was applied to code but not to the render layer's own comments — ~275 references to the eleven retired token names survive across 25 in-scope files
  SEVERITY: medium
  FILES: internal/tui/section_header.go:14, internal/tui/section_header.go:37, internal/tui/section_header.go:317, internal/tui/notice_band.go:28, internal/tui/notice_band.go:45, internal/tui/notice_band.go:46, internal/tui/notice_band.go:47, internal/tui/header.go:33, internal/tui/header.go:35, internal/tui/header.go:37, internal/tui/session_item.go:25, internal/tui/session_item.go:31, internal/tui/session_item.go:176, internal/tui/destructive_confirm.go:13, internal/tui/destructive_confirm.go:30, internal/tui/edit_modal.go:17, internal/tui/delete_modal.go:20, internal/tui/rename_modal.go:105, internal/tui/build.go:164, internal/capture/fixtures.go:531, internal/capture/fixtures.go:1321, internal/tui/loading_view.go, internal/tui/pagepreview.go, internal/tui/filter_footer.go, internal/tui/help_modal.go, internal/tui/kill_modal.go, internal/tui/footer.go, internal/tui/project_item.go, internal/tui/modal_footer.go, internal/tui/empty_states.go, internal/tui/filtering_no_matches.go, internal/tui/sessions_flash.go, internal/tui/panel.go, internal/tui/modal.go, internal/tui/keymap.go, internal/tui/model.go
  DESCRIPTION: §2.4 retires eleven token names (`accent.violet`, `accent.blue`, `accent.cyan`, `accent.orange`, `state.green`, `state.red`, `text.strong`, `text.muted-bright`, `text.detail`, `text.dim`, `bg.warning`, `bg.track`, `text.on-warning`, `border.separator`, `border.footer`) and §1.3/§2.3/§12.4/§15.3 make the new names a public contract a theme author writes against. Every Go field, every `.theme` key and every call site was migrated correctly — but the doc comments that describe which token each surface paints in were not. `header.go:33` still says the caret is `accent.violet`; `section_header.go:14` says `Sessions` is `accent.cyan` and the count `state.green`; `notice_band.go:45-47` maps all four band roles to retired names; `edit_modal.go:17` names `border.separator`, the token §2.2 explicitly consolidated away; `build.go:164` and `fixtures.go:531` describe the flash as sitting on the `bg.warning` tint with a `text.on-warning` message. A maintainer or agent reading these files learns a vocabulary that no longer exists and would author a drop-in theme against it — with no compiler, guard or test catching it (the docs guard covers `docs/theming.md`, not source comments). code-quality.md is explicit: "When a change makes a nearby comment false, fix it in the same edit." §13.5 adds the docs guard precisely because this feature "found the MV spec's '2-tone border' claim stale against the implementation purely by chance. Same drift class, same subsystem" — and the same drift has been re-created one layer down, at 25× the volume.
  RECOMMENDATION: Sweep the in-scope `internal/tui` and `internal/capture` files for the eleven retired names and rewrite each comment to the §2.4 replacement (`accent.violet`→`accent.primary`, `accent.blue`→`accent.key`, `accent.cyan`→`accent.mode`, `accent.orange`→`accent.attention`, `state.green`→`state.positive`, `state.red`→`state.destructive`, `text.strong`→`text.secondary`, `text.muted-bright`→`text.tertiary`, `text.detail`→`text.muted`, `text.dim`→`text.subtle`, `bg.warning`→`bg.attention`, `bg.track`→`bg.subtle`, `text.on-warning`→`text.on-attention`, `border.separator`/`border.footer`→`border`). Each site is a mechanical substitution against the field the adjacent code already reads. Consider whether the existing `colour_literal_guard_test.go` glob machinery can be extended to fail on a retired name appearing anywhere under `internal/tui` — the rename is a one-way breaking change and a guard is what stops the next one drifting the same way.

- FINDING: doctor's `bad name` extension advisory asserts the slug portion is legal without having checked it
  SEVERITY: low
  FILES: internal/theme/name.go:148, cmd/doctor_theme.go:440
  DESCRIPTION: §14A pins two distinct `bad name` lines and gives the reason for the split: the extension-casing message is "a distinct message because the slug portion is already legal, and sending the user to fix the one thing that is fine is exactly the misdirection §9.4 and §12.1 discriminate against elsewhere." `SlugFromFilename` checks the extension first and short-circuits, so a file whose stem is ALSO illegal (`My Theme.THEME`, `Nord.THEME`) is reported as `BadNameExtension` and doctor prints "extension must be lowercase .theme". The user renames to `My Theme.theme`, re-runs doctor, and is told the slug is wrong — a two-step correction on a surface whose whole justification is naming the single fix. The line's premise ("the slug portion is already legal") is unverified at the point it is claimed.
  RECOMMENDATION: In `SlugFromFilename`, report `BadNameExtension` only when the stem also satisfies `ValidSlug`, and fall through to `BadNameSlug` otherwise — the extension message then means exactly what §14A says it means, and the general slug message stays the answer for a name that is wrong in more than one way. The ladder order and the single `bad name` reason class are unchanged.

- FINDING: CLAUDE.md's keymap-revision claim is falsified by the panel's third `pinArrowOnlyNav` call site
  SEVERITY: low
  FILES: CLAUDE.md, internal/tui/theme_panel.go:406
  DESCRIPTION: §12.6 requires CLAUDE.md be corrected because "it is what an implementing agent reads first" and leaving entries stale would have them "actively misdescribing the subsystem under construction". The seven named entries were all updated correctly. But the "Modern Vivid TUI → Keymap revision" bullet still states `pinArrowOnlyNav` "strips them from the live `bubbles/list` keymap on **both** Sessions and Projects" — `newThemePanelList` now calls it on a third list, and the comment there records that the pin is load-bearing for the panel specifically (the v2 default binds `l` and `d` to NextPage, colliding with the panel's own commit keys). The same bullet's render-structure inventory enumerates every modal and chrome file in `internal/tui` and omits the slide-over entirely, so the feature's largest new surface has no entry in the architecture doc an implementing agent reads first.
  RECOMMENDATION: Amend the `pinArrowOnlyNav` clause to name all three lists (Sessions, Projects and the theme slide-over), noting the `l`/`d` collision as the panel-specific reason. Adding the slide-over to the render-structure inventory alongside the modals is a one-line addition in the same bullet and closes the omission.

- FINDING: Spec-section citations in new test comments contradict code-quality.md's comment rules
  SEVERITY: low
  FILES: internal/tui/footer_revision_test.go:1, internal/tui/keymap_dispatch_guard_theme_test.go:1, internal/prefs/migration_marker_test.go:29, cmd/prefs_translation_persist_test.go:23, internal/tui/theme_panel_commit_failure_test.go:1, internal/tui/theme_panel_behaviour_test.go:1
  DESCRIPTION: code-quality.md forbids "any workflow vocabulary — task ids, phase numbers, spec-section citations" in comments, on the grounds that "the comment must hold true for a reader with no knowledge of the process that produced the code, long after its artifacts are archived". 149 in-scope test files carry ~1,181 `§`-prefixed citations (e.g. `§8.1's omit-empty-values rule`, `§12.3's catalogue`, `§8.9's read-modify-write`) that will be unresolvable once the specification is archived. Production files are clean — the implementers stripped the pre-existing `§2.7` citations from `footer.go` while editing it, which is the right instinct applied inconsistently. Noted at low severity because this is an established codebase convention rather than a new practice: 202 test files already carried it before this feature. Flagging it so the inconsistency is a decision rather than an accident.
  RECOMMENDATION: Either restate each citation as the rule it names (`the omit-empty-values rule`, `the read-modify-write merge`) so the comment survives the spec's archival, or accept the convention explicitly and drop the production-side stripping so the two halves of the codebase agree. Not worth a broad rewrite of the pre-existing 202 files either way.

SUMMARY: The implementation is an unusually faithful reading of the specification — the rejection ladder and its short-circuit order, the two-state setting and its `theme`-wins tiebreak, per-slot mode-matched fallback, the union's one-slug-one-row rule and sort tie-break, badge derivation from `Requested`, the panel's key-exclusivity, confirm nesting and outstanding-failure state machine, the RMW/strict-decode split in `prefs`, the retained startup canvas hex, doctor's advisory class and summary, `theme export`, the `theme` component catalogue, the built-in files including §7.7's chroma re-derivation record, and all seven CLAUDE.md corrections all match what was decided. The one systemic gap is documentation-side: the §2.4 rename — the feature's headline public contract — was never carried into the render layer's own comments, leaving ~275 references to retired token names across 25 files. Three smaller items follow: an unverified premise in doctor's extension advisory, one falsified CLAUDE.md claim, and spec-section citations in new tests.
