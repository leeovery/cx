## Attempt 1

ISSUES:

- `/Users/leeovery/Code/portal/cmd/open_theme_construction_test.go:470-472` — the "constructs no TUI" proof does not hold. The undrained-sink proxy only proves execution did not reach `stageBootstrapWarningsOnModel` (`cmd/open.go:1049`), which is the statement *after* `m := buildTUIModel(...)` (`cmd/open.go:1048`).

  The reviewer verified this by mutation, with zero repo writes, via `go test -overlay`: moving the fatal's `return err` to sit between `buildTUIModel` and `stageBootstrapWarningsOnModel` constructs a full `tui.Model` on the fatal path, and **the entire unit lane still passes**, including `TestOpenTUI_FatalBeforeModelConstruction`. The criterion the orchestrator promoted onto this task from task 5-6 is therefore unproven — the test admits the implementation it forbids.

  FIX: add a pre-construction tripwire alongside the existing sink assertion, using the cmd package's existing `recordingCommander` (`cmd/uninstall_test.go:36`) instead of `stubCommander`: build the client as `tmux.NewClient(rc)` and assert `len(rc.Calls) == 0`. On the correct implementation the fatal returns before the `if tmux.InsideTmux() { client.CurrentSessionName() }` block at `cmd/open.go:1042-1047`, which is the statement immediately preceding construction. The reviewer ran this both ways under overlay: it passes on the current tree and fails on the mutant with `tmux commander saw [[display-message -p #{session_name}]]`. Keep the sink assertion too — the pair then brackets construction on both sides. Add an explicit `t.Setenv("TMUX", ...)` so the tripwire does not silently depend on `TestMain`'s poison making `InsideTmux()` true.

  ALTERNATIVE: an AST guard over `openTUI` asserting the `themeNomination` call and its `return err` precede the `buildTUIModel` call — the package already has the machinery (`parseCmdFiles`/`themeCallSites`). It pins the criterion literally rather than by consequence, but it is brittle to refactors and reads as a structure assertion rather than a behavioural one. The reviewer recommends the `recordingCommander` tripwire; it is behavioural, uses an existing helper, and was empirically confirmed to discriminate.

  CONFIDENCE: high

NOTES:

- SPEC_CONFORMANCE conformant. §8.4 construction-time load: `themeNomination` (`cmd/open.go:781-808`) does prefs keys → `theme.ResolveSetting` → `Loader.ResolveNomination(setting, themesDir)`; one palette under a constant, two under a pair, verified end-to-end by the canvas-value fixtures in `TestConstruction_ReadBudget`. §8.4 resolution ordering (embedded first, directory never touched for a built-in) is proven behaviourally: the seeded themes dir carries a shadow `nord.theme` with a different canvas and the built-in constant still paints the embedded Nord. §8.5 fallback + §6.3 keep-the-persisted-name byte-compare `prefs.json`. §12.3 cadence pinned with exact, ordered event assertions (failure-then-palette). §8.8's "Dies" row honoured exactly. §10.5 single call site: `loadPrefsStore()` has exactly one production caller (`cmd/open.go:940`), inside `openTUI`. §12.3 exec-path win: source guard + a real `portal open <session>` under a mode-0000 themes dir asserting zero `theme` records. Phase-8 deferral of `rawKeys` respected.
- ACCEPTANCE_CRITERIA: every criterion met except "constructs **no** TUI on that path" (the issue above). Met: constant-from-prefs paints from frame one; pair selects by gate (dark/light/timeout); resolve-once with late reply still populating `originalBg`; constant wins over stale slots with the slots provably unread; typo'd slug falls back with exact events and a byte-identical `prefs.json`; read budget (built-in 0 / drop-in 1 / pair 2, no `ReadDir` — enforced by a mode-0111 dir with a self-vacuity tripwire); appearance API gone + raw field round-trips; `themesDirPath` failure and nil store both degrade to the shipped pair; `NO_COLOR` loads both and emits two `loaded` lines; startup canvas hex from the *selected* member (read through `restore.go`'s echo guard, discriminating in both directions); exec path emits zero `theme` records; capturetool untouched.
- TEST_COVERAGE otherwise adequate and unusually precise: assertions are exact/ordered rather than substring (`assertThemeEvents` uses `slices.Equal`), fixtures are loud-before-silent (the poisoned-dir test proves records exist to be seen before asserting none), and vacuity tripwires guard both the unlistable dir and the unresolvable-path case.
- The two "asserted through consequence" substitutions are **sound**, because the mechanism is pinned one layer down and the cmd layer pins the missing half. "Init issues no timeout tick" — `internal/tui/nomination_test.go:65` `TestNomination_ConstantSkipsDetectionAndWait` calls `assertNoTimeoutTick` and asserts the gate is resolved *and unarmable* for any constant built through `Build`; the cmd test proves `"theme":"nord"` yields a constant. "the dark member is selected under `NO_COLOR`" — `internal/tui/nomination_test.go:200` `TestNoColor_LoadsBothAndSelectsDark` asserts `activeTheme == dark` and the light member still held; the cmd test adds both-loaded + two `loaded` lines + gate-skipped-not-pending. Composition covers both criteria.
- CONVENTIONS followed. `OpenDeps.ThemeLoader` / `buildThemeLoader` mirror `OpenDeps.AckWriter` / `buildAckWriter` line for line (`cmd/open.go:437-443` vs `753-758`); production carries nil — grep confirms nothing outside tests ever sets it, so production behaviour is provably unchanged. Single `log.For("theme")` binding retained; `TestThemeComponent_BoundOnceInCmd` still guards it. No `t.Parallel`, package-level seams restored via `t.Cleanup`, no real tmux/daemon touched, chmod cleanups registered after `t.TempDir` so they run first (LIFO). `go build`, `go vet ./...`, `go vet -tags integration ./...`, `go test ./...` all clean; `golangci-lint run ./cmd/... ./internal/prefs/...` → 0 issues.
- ARCHITECTURE sound. `themeNomination(prefsStore, loader)` takes both collaborators as parameters, so every construction test drives the production function without touching globals; only the `openDeps` lookup is hidden behind `buildThemeLoader`. The two failure shapes are correctly separated: `themesDirPath()` error → empty dir (degrade), `ResolveNomination` error → returned (fatal) — exactly §8.5 vs §7.6. Injecting the concrete `*theme.Loader` rather than an interface is right: `Loader.BuiltinSource` is the seam `internal/theme` declares for precisely this state, so no new abstraction was invented.
- capturetool byte-identity verified independently rather than taken on report: `go list -deps ./cmd/capturetool` yields 18 internal packages, of which only `internal/prefs/store.go` is touched by this diff, and the touch is pure deletion of symbols nothing in that graph references (confirmed by grep and by the new tree-wide guard). An empirical pty render comparison was attempted but produced only 146-byte control-sequence output with no real frame, so it is not evidence either way; the structural argument is the stronger proof and it holds.
- The exec-path criterion has three clauses; the test covers two. "Performs no prefs read" is not asserted — the poisoned-themes-dir + zero-`theme`-records assertion cannot see a prefs read that does no theme work. It holds today because `loadPrefsStore()` has exactly one production caller, but once Phase 6 puts the migration *write* behind `loadPrefsStore`, a stray exec-path call becomes a config mutation on every `x <target>`. A `loadPrefsStore` call-site guard mirroring `themeCallSites` would be cheap insurance; not required by this task.
- `prefs.json` is opened twice per process (`Store.Load` for the mode, `Store.LoadThemeKeys` for the theme) through one `*prefs.Store`. That is exactly the shape the task's Do list prescribes, so it is conformant — flagged only because §8.9's RMW work in Phase 6 will make the "one instance, one snapshot" reading matter.
- `internal/prefs/appearance_test.go` was deleted wholesale; diffed against HEAD, every one of its four tests exercised only the deleted API. The cross-field-blanking behaviour it covered is still covered by 5-1's `TestSave_PreservesRawAppearance`. No coverage lost. The one line removed from `TestPrefsFile_DeclaresNoMigrationMarker` was the `SaveAppearance` half; necessary and correct.
- `TestPrefs_AppearanceAPIIsGone` correctly excludes the on-disk field and correctly does not collide with `internal/tui`'s unrelated `canvasAppearance` family.
- CLAUDE.md's `prefs` row still documents the deleted `Appearance` enum — owned by task 10.6, out of scope here.
- Seam-shape alternative worth recording but not worth changing: a package-level `newThemeLoaderFunc` var (the `openTUIFunc`/`openPathFunc`/`openSessionFunc` precedent) would avoid widening the exported `OpenDeps` surface for a test-only field. The chosen `AckWriter` precedent is equally established in this package.
