TASK: theming-system-16-1 — Single-Source The Panel's Stub Theme-Seam Fixtures (tick-a95043, severity medium, source: duplication)

ACCEPTANCE CRITERIA:
- No `theme.Resolution` literal remains in `internal/tui`'s panel test files; all four builders construct through the two shared helpers.
- The four builders no longer restate the `Lister`/`Theme`/`ThemeSource`/`ThemeKeys` skeleton.
- `Requested` and `Resolved` are set in exactly one place per shape.
- Every existing panel arrow / commit / recompute / slot-commit assertion still holds.
- `go test ./internal/tui` passes.

STATUS: complete

SPEC CONTEXT:
Spec §13.3 commits the panel's theme enumeration to an injectable seam ("production wires the real implementation, fixtures fake it") returning the finished §9.4 union, and §14's test table makes the panel-behaviour suite the primary cover for the badge derivation table, the §9.2 commit recompute and the identity-anchored cursor — all of it driven through that seam. The correctness of those suites therefore rests entirely on the shape the *fake* seam answers with: `theme.Badges` keys on `SlotResolution.Requested` (internal/theme/badge.go:24,33) while the panel's cursor anchors on `Resolved` (internal/tui/theme_panel.go:196). A per-file fixture that transposed the two would still open on a valid row while badging the wrong slug — a silent, per-suite divergence. Single-sourcing the stub shape is the structural answer to that, and matches the plan's existing consolidation home (`internal/tui/theme_testing_test.go`).

IMPLEMENTATION:
- Status: Implemented (commit bb13afd4)
- Location:
  - `internal/tui/theme_testing_test.go:204-214` — `constantResolution(slug string, th theme.Theme) theme.Resolution` (constant nomination + single `SlotConstant` record, `Requested`/`Resolved` both from `slug`).
  - `internal/tui/theme_testing_test.go:216-224` — `pairResolution(light, dark theme.Row) theme.Resolution` (`theme.AdaptivePair(light.Theme, dark.Theme)` + `SlotLight` then `SlotDark`, each from its own row).
  - `internal/tui/theme_testing_test.go:226-233` — `stubPanelDeps(source *fakeThemeSource, nomination theme.Nomination, keys theme.RawKeys) Deps` (the `Lister`/`Theme`/`ThemeSource`/`ThemeKeys` skeleton).
  - All four named builders re-pointed: `newArrowPanelDeps` (theme_panel_arrow_test.go:79-87), `commitPairPanelDeps` (theme_panel_commit_test.go:56-65), `newSplitPanelModel` (theme_panel_commit_recompute_test.go:342-357), `newSlotSplitPanelModel` (theme_panel_commit_slot_test.go:78-93). Each now differs only in its rows, its `enumeration`/`reassembled` priming and whether a persister is attached (`deps.ThemePersister = persister` after the skeleton).
  - Two further sites adopted the constant helper beyond the four the task named: `theme_panel_cursor_test.go:232,262,301` and `theme_panel_entry_test.go:41`. Same mechanical change, same outcome — not scope creep.
- Notes:
  - Step 5 ("the shared derivation is the correct one") was genuinely exercised: all four original pair literals wrote `Nomination: theme.ConstantNomination(dark.Theme)` on a resolution whose slots are a light/dark pair. `pairResolution` now returns `theme.AdaptivePair(light.Theme, dark.Theme)`, which is what the real loader produces for pair keys (internal/theme/resolution.go:53-58, nomination.go:34). Behaviourally inert in these fixtures: the only production reader of `Resolution.Nomination` is `applyCommittedSetting` (theme_panel_commit.go:126 → `themeState.nomination`); no pair-fixture suite asserts on `themeState.nomination` (the only such assertions live in `nomination_test.go`, `theme_panel_commit_load_test.go` and `theme_panel_confirm_test.go:752-763`, none of which use the pair builders), and every pair suite asserts on `themeState.active`, which `applyCommittedSetting` deliberately never touches. `AdaptivePair(...).Select(MemberDark)` and `ConstantNomination(dark.Theme).Select(...)` both yield `dark.Theme`, and these fixtures all run on the standing dark no-answer fallback, so the open/arrow/commit paths resolve identically. The change is strictly more honest and observably neutral.
  - AC bullet 1 holds in substance but not to the letter: two `theme.Resolution` literals survive in the package's panel tests, and neither is a copy of either shared shape — `theme_panel_open_test.go:365-375` is a *fallback* record (`Requested: "nord"`, `Resolved: DefaultDarkSlug`, `FellBack: true`, `Reason: ReasonNotFound`), the one shape whose whole point is that the two fields differ, so it is inexpressible through `constantResolution` by construction; and `theme_panel_commit_recompute_test.go:396` is the zero `theme.Resolution{}` that is itself the input under test (the loader's answer alongside a fatal). Manufacturing a third helper for a single fallback site would be the over-abstraction the task explicitly guards against. No drift.
  - No assertion and no test name changed — verified against the full diff of bb13afd4; only construction moved (the cursor-test hunks are literal-to-helper swaps plus gofmt realignment).
  - Helper shape complies with step 7: three plain functions, no options struct, no variadic functional options.

TESTS:
- Status: Adequate
- Coverage:
  - The two pinning tests the task asks for exist: `TestConstantResolution_IsTheConstantStubShape` (theme_testing_test.go:235-254) asserts the CONSTANT nomination, that it carries the given palette, and `reflect.DeepEqual` on the single `SlotConstant` record with `Requested == Resolved == "aurora"`; `TestPairResolution_IsTheAdaptiveStubShape` (256-277) asserts the non-constant nomination, both `Select` members, and the light-then-dark slot pair each carrying its own row's slug and palette. Together they pin exactly the properties a per-file copy could get wrong (transposition, slot order, wrong palette per slot).
  - The four builders' downstream suites are large and unchanged: arrow (theme_panel_arrow_test.go), commit + commit-failure + commit-slot + close-report + confirm (via `commitPairPanelDeps` / `newCommitPairPanelModel`, ~15 call sites), recompute (`newSplitPanelModel`, 5 sites), slot-split (`newSlotSplitPanelModel`, 2 sites). Badge, cursor-anchor and clamp assertions in those suites are what make the helpers load-bearing rather than cosmetic — e.g. `TestPanelRecompute_ResolveErrorKeepsBadges` (recompute:382) and `requireBothBadge` (slot:95) both read badges derived from `Requested`.
  - The task's third test bullet (temporarily transpose a row in one caller, confirm the badge assertion fails, revert) is a one-off manual proof that leaves no artefact; it cannot be verified from the tree. Reading the path confirms it would hold: transposing `light, dark := rows[1], rows[0]` at a pair call site flips every `Requested` the badge map is keyed on, which `requireBothBadge` / `requireBadge` assert on directly.
- Notes: Not over-tested — two helpers, two pinning tests, no redundant per-field duplication beyond the single `DeepEqual` plus the nomination accessors (which `DeepEqual` on `Slots` cannot cover, since the nomination is a separate field with unexported state). No new mocking or setup was introduced; the change is net-negative in fixture lines (89 removed, 125 added, of which 98 are the shared file including its two tests).

CODE QUALITY:
- Project conventions: Followed. Test-only helpers live in `_test.go` files in the package under test; `themetest` accessors are used for palettes rather than package-level theme vars (consistent with the file's "Functions, not package-level vars" rule at theme_testing_test.go:304-313); no `t.Parallel()`; helpers that cannot fail take no `*testing.T`, matching the file's existing split.
- SOLID principles: Good. Each helper has one job (one shape each, plus the Deps skeleton); the builders retain their own distinguishing state. No abstraction was invented for its own sake.
- Complexity: Low. Three straight-line constructors, no branching.
- Modern idioms: Yes. Nothing to modernise here; composite literals and value returns are correct for the shape.
- Readability: Good. The builders now read as "these rows, this priming, this persister", which is exactly what differs between them.
- Issues: None. The one comment added (theme_testing_test.go:202-203) is accurate against the code it describes — `theme.Badges` does key on `Requested` (badge.go:24,33) and the cursor anchor does take `Resolved` (theme_panel.go:196) — though it is compressed; see the note below.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/tui/theme_testing_test.go:202-203 — the comment's conclusion ("so a transposed shape still resolves") states the failure mode too obliquely to act as the warning it is meant to be. Replace the two lines with: "// Requested and Resolved come from one argument deliberately: badges key on the\n// former and the cursor on the latter, so a fixture that transposed them would\n// still open on the right row while badging the wrong slug — silently."
- [quickfix] internal/tui/theme_panel_commit_test.go:64 and internal/tui/theme_panel_commit_slot_test.go:90 — the two pair builders still hand `stubPanelDeps` a construction-time nomination of `theme.ConstantNomination(dark.Theme)` while their keys say `{Light, Dark}` and the seam now answers with `AdaptivePair`, so one fixture describes two different settings. Hoist the resolution (`res := pairResolution(light, dark)`) and pass `res.Nomination` to `stubPanelDeps` instead. `Select(MemberDark)` yields `dark.Theme` under either shape and the fixtures never arm the gate, so the suites should be unaffected — but it touches fixture state feeding ~17 call sites, so it belongs in the pipeline rather than applied on the spot.
- [quickfix] internal/tui/theme_testing_test.go:226 — `stubPanelDeps` takes the concrete `*fakeThemeSource`, which caps its reach at the stub-seam family; three further fixtures assemble the identical `Lister`/`Theme`/`ThemeSource`/`ThemeKeys` skeleton by hand because their source is a different fake: `newLoadPanelModel` (theme_panel_commit_load_test.go:66), `behaviourPanelAt` (theme_panel_behaviour_test.go:66) and the nil-persister fixture at theme_panel_confirm_test.go:740. Widen the parameter to the `ThemeSource` interface and route those three through it (each then adds `deps.ThemePersister = …` as the four builders now do). The task specified the concrete type, so this is a deliberate follow-on rather than a miss.
