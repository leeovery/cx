TASK: theming-system-12-7 — Trim The Topic's Production Comments To The Quality Standard (severity medium, sources: standards)

ACCEPTANCE CRITERIA:
1. No production comment in the topic's files narrates a rejected alternative or restates the design debate.
2. No production comment names a test.
3. No production comment counts call sites or consumers, or asserts "nowhere else"/"nothing else does".
4. The four named trap warnings survive intact (startupCanvasHex freeze, canvas-echo guard, fallback-slug coupling, prefs two-decodes split).
5. Comment-to-line ratio in each swept file materially reduced and broadly in line with comparable existing subsystems.
6. No non-comment line changes; `go build ./...` and `go test ./...` behave identically.

STATUS: complete

SPEC CONTEXT: The task is standards-sourced, not spec-sourced — it applies `.claude/skills/workflow-implementation-process/references/code-quality.md` ("Comments"), which bans the design argument, claims about tests, cardinality claims, and restated adjacent code, and preserves why/warning/opaque-what/API-doc comments. The specification touches the same principle only for theme *files* (§ around line 509: "MV's inline erratum comments are deleted, not ported… a comment recording *why* a hex differs is duplicated history"), which is consistent with the sweep's direction. No spec behaviour is in scope — this is a comment-only change.

IMPLEMENTATION:
- Status: Implemented (comment-only sweep); outcome complete at HEAD, with criterion 5 and a residue of criterion 2/3 phrasing finished by later work (see Notes).
- Location: commit `c69101ca` — all 16 named production files touched: `internal/tui/theme_state.go`, `theme_seams.go`, `theme_panel.go`, `theme_panel_commit.go`, `theme_panel_confirm.go`, `theme_panel_footer.go`, `theme_row.go`; `internal/theme/load.go`, `resolution.go`, `union.go`, `badge.go`, `setting.go`; `internal/prefs/store.go`; `cmd/doctor_theme.go`, `cmd/config.go`, `cmd/theme_persister.go`. Net -815 lines, all comment/blank.
- Criterion 6 verified exactly: every `+`/`-` line in the commit's Go diff is either blank or a whole-line `//` comment (a trailing-comment edit on a code line would have surfaced as a code line and did not). No behavioural risk; build/test behaviour is unchanged by construction.
- Criterion 1 verified at HEAD: no production comment across `cmd`, `internal/tui`, `internal/theme`, `internal/prefs` narrates a rejected alternative or re-argues a design debate. The remaining "rejected" occurrences are all the theme domain's own rejection-ladder vocabulary (`internal/theme/name.go:12`, `lex.go:58`, `union.go:64`, `enumerate.go:83`), not design history.
- Criterion 2 verified at HEAD: no production comment names a test. All three called-out sites are gone — `cmd/theme_persister.go` no longer mentions `TestThemeComponent_BoundOnceInCmd`, `internal/tui/theme_panel_footer.go` and `theme_panel_confirm.go` no longer mention `keymap_dispatch_guard_test`; each was removed by this commit itself, not by later work. The only `//`-comment hits on `_test`/`Test…` repo-wide are in test-helper packages (`internal/tmuxtest`, `internal/transienttest`, `internal/restoretest`, `internal/log`) naming their own exported helpers.
- Criterion 3 verified at HEAD: no comment counts call sites or asserts "nowhere else"/"nothing else does". All five called-out sites were removed by this commit (`theme_row.go`'s "so nothing consumes it today", `theme_panel_commit.go`'s "RUNS ON A SUCCESSFUL COMMIT AND NOWHERE ELSE", `theme_panel.go`'s "TWO callers by design" and "THE READ HAPPENS HERE … AND NOWHERE ELSE", `union.go`'s "NOWHERE ELSE on this path"), and each was re-worded into the invariant it protects (e.g. `theme_panel_commit.go:93-96` "Landed writes only: badges and nomination derive from the raw keys, so recomputing after a failed write would move the `●` off what is persisted"). Two surviving phrasings are prohibitions rather than counts, which the task explicitly sanctions: `internal/theme/load.go:103` ("no caller may lex or validate on its own") and `internal/tui/theme_panel_confirm.go:69` ("applyCommittedSetting is its single owner").
- Criterion 4 verified at HEAD — all four trap warnings intact: startupCanvasHex freeze (`internal/tui/theme_state.go:59-62`, `model.go:893-896`, `model.go:902-904`), canvas-echo guard (`internal/tui/restore.go:10-19`), fallback-slug coupling (`internal/theme/builtins.go:9-11`), prefs two-decodes split (`internal/prefs/store.go:123-124` tolerant load / `142-144` strict write, plus the `166-167` write-time re-read rationale).
- Criterion 5 — met at HEAD, not by this commit alone. Post-commit ratios were 43–76% (e.g. `theme_state.go` 110/144, `theme_panel.go` 930/1450, `load.go` 147/230), i.e. a real but modest 4–8 point reduction that still sat well outside the 30–50% guide the task set. The in-band state came from later work in the same effort: the repo-wide `chore/comment-strip` branch (`fee1927d`…`ab0d1583`) and, for the residual near-cardinality phrasing this sweep left ("the ONE construction-time theme read", "the only place it runs", "the only implementation of it"), plan task 16-4 (`735daeeb`), which re-touched `theme_persister.go`, `load.go`, `union.go` and `theme_panel.go`. At HEAD the swept files sit at 11–37% (`theme_row.go` 11%, `theme_panel.go` 18%, `prefs/store.go` 14%, `doctor_theme.go` 15%, densest are the new `internal/theme` API files at 24–37%, which is proportionate to their exported doc-comment surface). Comparable existing subsystems dropped in the same repo-wide sweep and now sit at 4–16%, so the topic reads slightly denser than its neighbours but in the same band and squarely inside the standard's "why/warning/API doc" allowance.
- Notes: the surviving comments are consistently the admitted classes — order-matters warnings (`theme_panel.go:136-138`, `resolution.go:192`, `store.go:266-265`), deliberate-looking-wrong warnings (`badge.go:19-21` "Keyed on Requested, never Resolved", `theme_row.go:100-102` "accent.primary, never state.positive"), and exported doc comments stating what/inputs/outputs/errors (`load.go:68-73` LoadFile's rung contract, `setting.go:62-66` ResolveSetting). Spot-checked claims hold against the code: `theme_panel.go:94-95` (floor read with `dirUnusable` false at line 100, re-checked with the real flag at 129), `theme_panel.go:82-83` (v2 keymap collision — `pinArrowOnlyNav` applied at 90), `theme_state.go:55-56` ("New seeds this with the dark built-in" — `model.go:822`), `theme_panel_footer.go:20-23` (height measured off the rendered block with a zero width/theme). No stale or falsified comment found in the swept files.

TESTS:
- Status: Adequate (no new tests expected or warranted).
- Coverage: A comment-only sweep has no behaviour to assert; the task's own micro acceptance is diff-shaped (suite unchanged + `git diff --stat` review). I verified the diff-shaped criterion directly by reading the commit: zero non-comment lines changed, so the pre-existing unit and integration lanes are the baseline and are untouched by construction. Existing structural guards over comment content remain in place and unaffected (`internal/tui/retired_token_guard_test.go:70` TestNoRetiredTokenNameInComments; `internal/theme/docs_guard_test.go`).
- Notes: No over-testing — nothing was added. Adding an automated guard for the banned comment classes would be a regex over prose with high false-positive cost; its absence is not a gap here.

CODE QUALITY:
- Project conventions: Followed. Comment-only change, so `CLAUDE.md`'s architecture and test-isolation rules are untouched. `.golangci.yml` runs the standard set plus `modernize`; the staticcheck ST10xx doc-comment-form checks are not part of golangci-lint v2's default staticcheck selection, so no doc-comment prefix is lint-enforced and nothing in the sweep can trip the linter.
- SOLID principles: N/A (no code changed).
- Complexity: Low — unchanged.
- Modern idioms: N/A.
- Readability: Good, and materially improved. The 40–60 line doc comments named in the task are gone: `resolveSlot` is now 3 lines (`resolution.go:167-169`), `SaveTranslation` 4 lines (`store.go:262-265`), `recomputeThemePanel` 4 lines (`theme_panel_commit.go:93-96`), `Loader` 2 lines (`load.go:10-11`), `ResolveSetting` 5 lines (`setting.go:62-66`). Two small residues are noted below: three comment blocks that now float unattached after the imports, and four doc comments that open with an identifier other than the one they document.
- Issues: None blocking.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/tui/theme_panel.go:12-13 — the sentence "Composited over the page rather than replacing it: modals blank the page to the canvas, so a modal picker would preview nothing." sits after the imports with a blank line before the `const` block, so it documents nothing, and the compositing it explains lives in `theme_panel_render.go`. Move it verbatim onto the `themePanel` type declaration (line 58) so it hangs off the thing it explains.
- [do-now] internal/tui/theme_panel_footer.go:8 — "Vertical because a horizontal keymap cannot fit the panel's column budget." is likewise orphaned (blank line, then the const). Fold it into `renderThemePanelFooter`'s doc comment at line 16 as its first sentence: "renderThemePanelFooter renders the panel's keymap vertically — a horizontal keymap cannot fit the panel's column budget. Entries are a parameter because the confirm scope substitutes a shorter footer. Over-wide rows are returned unpadded, never truncated."
- [do-now] internal/tui/theme_panel.go:82, internal/tui/theme_panel.go:43, internal/tui/theme_state.go:18, cmd/doctor_theme.go:207 — each doc comment opens with a different identifier's name than the function it precedes ("pinArrowOnlyNav is load-bearing:" on `newThemePanelList`, "dimNone is unreachable here" on `themePanelForcedCloseFlash`, "IsDark is nil-safe" on `terminalReplyFrom`, "BadNameNone is unreachable" on `badNameAdvisoryLine`), so gopls hover renders a foreign name as the function's doc. Re-word each to lead with the documented name while keeping the same claim, e.g. "newThemePanelList pins arrow-only navigation: the v2 DefaultKeyMap binds `l` and `d` to NextPage, colliding with the panel's commit keys."
- [do-now] internal/prefs/store.go:296 — "Indirection so a test can count writes; production never reassigns it." is a claim about test behaviour that a rewritten test falsifies. Replace with: "Substitutable so the write can be intercepted without a filesystem; production never reassigns it."
- [do-now] internal/theme/setting.go:100 — "so the two halves cannot be paired anywhere else and drift" is a residual scope claim that ordinary additive change falsifies. Replace the sentence with: "Every surface needing a single slot's slug goes through it, so the tiebreak and the default substitution stay paired in one place."
