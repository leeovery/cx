TASK: theming-system-12-5 — Collapse the three no-config-access fixture tests and the re-implemented theme-file body and fixture-name lists (tick-35ce5b, severity medium, source: duplication)

ACCEPTANCE CRITERIA:
1. The decoy staging and the config-untouched assertions each exist once in `capture_test`; at most one test retains its own copy, and only if it provably needs unexported access.
2. No package re-implements the valid-theme-file body; every site routes through `internal/themetest`.
3. The panel fixture-name set is stated once; `panelFixtureNames()` derives from it rather than restating it.
4. All three tests still assert the same thing: no config read, no themes-dir read, one entry in the config dir.

STATUS: complete

SPEC CONTEXT:
The claim these tests carry is spec-anchored, not invented by the tests. Specification §13.3 (line 1645) states that the panel fixtures' declared theme data "is fixture data, not config discovery, so §7.1's import guard is untouched — the same reasoning that lets `--theme <path>` take an explicit input"; §7.1 (line 508) states the `internal/capture` no-real-config import guard is preserved; line 196 pins that the loader takes the themes directory as an injected value and never resolves it, which is what makes the embedded set reachable from the capture harness with no path at all. The tests under review are the *runtime* half of that static guard: render every panel fixture with `XDG_CONFIG_HOME` / `PORTAL_THEMES_DIR` / `PORTAL_PREFS_FILE` pointed at an observable temp tree holding a decoy drop-in, and prove nothing was read and nothing was written. The task changes only where that claim is stated, not what it claims.

IMPLEMENTATION:
- Status: Implemented (all four criteria met; criterion 1 met on its stronger branch)
- Location: commit `49d87e4f`; current state in
  - `internal/capture/theme_panel_fixture_render_test.go:36` (`stageDecoyConfig`), `:53` (`decoySlug`), `:55` (`requireConfigUntouched`), `:70` (the single `TestPanelFixture_NoConfigAccess`), `:26` (`registeredPanelFixtureNames`)
  - `internal/capture/theme_panel_fixture_test.go:24` (`panelFixtureNames` now registry-derived), `:34` (`panelFixturePrefix`)
  - `internal/capture/theme_panel_remaining_fixtures_test.go:304` (`TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet` re-pointed at `registeredPanelFixtureNames()`)
  - `internal/capture/theme_panel_message_fixtures_test.go` (`TestPanelFixture_MessageFramesWireNoThemePersister` — the message frames' distinct persister claim, retained)
  - `internal/themetest/theme_file.go:39` (`Body()`), `:44` (`Render()` — 12-5 introduced this as unexported `body`; a later task exported it), `:114` (`Write` re-pointed through it)

  Criterion 1 — met, and exceeded. `stageDecoyConfig` / `requireConfigUntouched` / `decoySlug` exist exactly once, in `capture_test`. **Zero** tests retain a copy: the `package capture` original was not left as "the one intentional copy" but moved wholesale, which the task's step 5 named as the preferred branch. That was the correct call — the old body used only `FixtureByName` and `fx.Deps` (both exported) plus the local `driveToPanel` drive, so nothing forced it to stay in-package, and no test scaffolding had to be exported from production code to reach it.

  The executor went one step further than the task's step 2 (which asked for two rewritten per-file tests): the three tests became **one** test ranging over `registeredPanelFixtureNames()` — the registry-derived `theme-panel-*` set. Verified this loses nothing: the three old lists covered 2 + 5 + 3 = 10 names, and the registry-derived set is exactly those 10 (pinned by `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet` against `allPanelFixtureNames()`, which composes the same three per-file slices). It also closes the enrolment hole the task was aimed at — a new panel fixture is now covered by the no-config claim with no test edit at all.

  Criterion 4 — met. Both halves of the claim survive verbatim (decoy absent from `ansi.Strip(frame)`; config dir holds exactly one entry named `portal`), and the env staging is unchanged (all three vars, with `PORTAL_PREFS_FILE` deliberately outside the staged `portal/` tree so a prefs write shows up as a second entry). The one non-shared claim in the deleted set — the message frames' `deps.ThemePersister == nil` — was **not** folded away; it survives as its own test with its reasoning intact. No claim was lost or weakened.

  Render-entry-point change, checked deliberately: the moved test switched from `driveToPanel(t, fx.Deps(...))` (fixed 120×40, synthetic `t` keypress) to `panelFixtureFrame` → `fx.ModelAt` (`internal/capture/harness.go:22`). Both construct through `tui.Build(f.Deps(th))` and drive to an open panel, so the config-access surface is identical; `ModelAt` additionally renders at the fixture's declared `RenderSize`, i.e. the geometry the frame is actually captured at. Not a weakening. I checked the two frames where narrowing could theoretically hide a leaked decoy row (`theme-panel-narrow`, `theme-panel-paginated`): an unbadged decoy row has ~19 columns of label budget at the minimum panel width (`theme_row.go:83` `compose`), so `decoy-drop-in` (13) does not truncate, and the paginated fixture's synthetics are `vivid-NN` (`fixtures.go:610`), which sort after `decoy-drop-in`, so a leak would land on page 1. No blind spot.

  Criterion 2 — met. `decoyThemeFile` and `validThemeFileBody` are both deleted (no references remain anywhere in the tree). The one staging site routes through `themetest.Body()`, and `themetest.Write` was re-pointed through the same renderer, so the file shape has exactly one home. The one apparent counter-example is not one: `internal/theme/docs_guard_test.go:454` (`exampleThemeText`) emits `name = #123456` lines, but it lives in `package theme` — which `themetest` imports, so reaching it is structurally impossible — and it builds a *docs example block* over caller-supplied names, a different artefact from the loader fixture body. `cmd/theme_test.go:70` (`validThemeSource`) uses `theme.BuiltinBytes`, not a hand-rolled body. `themetest.Body()` also earns its keep beyond this one site: `internal/theme/load_test.go:590` byte-compares loader `Source` against it, which `Write` (path-returning) could not serve.

  Criterion 3 — met. `panelFixtureNames()` is now a registry filter rather than a ten-name literal, so the two `package capture` structural checks (`TestPanelFixture_FourInputs`, `TestPanelFixture_UnionIsProductionAssembled`) span the catalogue automatically. The literal set is stated once, in `allPanelFixtureNames()`, and is now reached by exactly one assertion — the exact-set claim — which is the shape the follow-on task 13-1 deliberately preserved. Adding a panel fixture is registry + one list entry, with every class-wide assertion picking it up unaided.

- Notes: The two extracted helpers now have a single caller each, because the three consumers they were extracted for collapsed into one. I read this as fine rather than as a dead abstraction — they name the two halves of the claim (stage the observable decoy / assert nothing touched it), which is exactly what a reader of a 6-line test body needs, and `stageDecoyConfig`'s three `t.Setenv`s are the part most likely to be quietly copied wrong by the next such test. No action.

  Later-phase interaction accounted for: task 13-1 (`80775a8c`) subsequently created `panel_frame_test.go` as the shared `capture_test` scaffolding file and moved the `"theme-panel-"` prefix constant there; the repo-wide comment sweeps (`915e7fcb`, `d939ae76`) stripped the doc comments 12-5 wrote on these helpers. Neither is drift — both are the plan intentionally revising this task's surface, and the outcome this task was for survives both.

TESTS:
- Status: Adequate
- Coverage: This task is a test refactor, so the relevant coverage is (a) the refactored claims and (b) the new helper's own contract. (a) `TestPanelFixture_NoConfigAccess` (`theme_panel_fixture_render_test.go:70`) makes both halves of the claim over all 10 panel fixtures — strictly more coverage than the three tests it replaces, since it now enumerates from the registry. (b) `themetest.Body()` gained `TestBody_IsTheBytesWriteStages` (`internal/themetest/theme_file_test.go:220`), which pins the one thing that matters — `Body()` is byte-for-byte what `Write` stages — by writing a file and comparing, so a divergence between the two renderers fails loudly rather than silently letting a decoy-staging consumer drift from the fixture format.
- Notes: Would the tests fail if the thing broke? Yes on both halves: a fixture that reached production discovery would list `decoy-drop-in` (verified the row would be neither truncated nor paginated off — see Implementation), and any prefs/config write lands as a second entry in the asserted directory. The vacuous-pass risk of a registry-derived range (empty set ⇒ zero subtests ⇒ green) is covered by `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet`, which fails on any inequality against the hand-written 10-name union — the two tests are complementary by design, not redundant.

  One over-test in the `themetest` file (added after this task, when `body` was exported as `Render`): `TestBody_IsRenderOfLines` (`theme_file_test.go:245`) asserts `Body() == Render(Lines())` against `func Body() []byte { return Render(Lines()) }` — a restatement of the one-line body, and already implied by `TestBody_IsTheBytesWriteStages` plus `TestRender_IsTheBytesWriteStages`. Non-blocking, noted below.

  The task's two manual verifications (break a fixture's config isolation and confirm each surviving test fails; add a fixture name and confirm one edit propagates) are by their nature not reproducible from the committed tree — I judged them structurally instead: the decoy mechanism's failure mode is traced above, and the single-edit property follows from `panelFixtureNames` / `registeredPanelFixtureNames` both deriving from `FixtureNames()`.

CODE QUALITY:
- Project conventions: Followed. `internal/themetest` remains test-only and stdlib+`theme`-only (no `internal/log`), per its CLAUDE.md role; `Body()` is described in the package's own vocabulary. The unit-lane rule is respected — nothing here spawns a daemon or execs a binary. The helpers stay inside `_test.go` files in `package capture_test`, so no test scaffolding was exported from production code (the task's explicit prohibition).
- SOLID principles: Good. Each helper does one thing (stage / assert), and the claim's two halves stay separable. `themetest` keeps sole ownership of the fixture file format — `Body()` and `Write` render through one function, so a format change is one edit, which is the whole point of the extraction.
- Complexity: Low. No branching added; the collapsed test is a single range.
- Modern idioms: Yes — `strings.HasPrefix` filter over the registry, named return on `stageDecoyConfig` used as documentation, `t.Helper()` on both helpers and `t.Fatalf` vs `t.Error` chosen correctly (fatal on staging/IO failure, non-fatal on claim failure, so all subtests report).
- Readability: Good. `decoySlug` single-sources the literal that both the write and the assertion depend on — the previous copies restated `"decoy-drop-in"` in two places per test, which is exactly the kind of split a false green hides in.
- Comment accuracy: No stale or false comments in the changed code. `themetest.Body()`'s comment ("the same bytes Write puts on disk") is exactly what `TestBody_IsTheBytesWriteStages` proves. The capture-side helpers carry no comments at all after the later repo-wide comment sweeps, which is consistent with the surrounding file.
- Issues: None blocking.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [quickfix] internal/capture/theme_panel_fixture_render_test.go:44 — replace the `os.WriteFile(filepath.Join(themes, decoySlug+theme.FileExtension), themetest.Body(), 0o644)` block (and its 2-line error check) with `themetest.Write(t, themes, decoySlug+theme.FileExtension, themetest.Lines())`. `Write` already owns the join, the render, the fatal-on-error and the single `fixtureMode`; this site is the only place in the tree that stages a `.theme` at a second mode literal (`0o644`), and `themetest`'s stated reason for one mode is that no test should depend on a permission difference that says nothing about what it is testing. `Body()` is unaffected — it retains its independent consumer at `internal/theme/load_test.go:590`.
- [quickfix] internal/themetest/theme_file_test.go:245 — delete `TestBody_IsRenderOfLines`. It asserts `Body() == Render(Lines())` against a one-line body that *is* `Render(Lines())`, and the claim is already implied by `TestBody_IsTheBytesWriteStages` (:220) and `TestRender_IsTheBytesWriteStages` (:232), both of which anchor on the observable bytes on disk rather than on the implementation's shape.
- [idea] internal/capture/theme_panel_fixture_test.go:24-34 and internal/capture/panel_frame_test.go:14 — the `"theme-panel-"` literal and its registry-filter loop now exist once per test package: `panelFixturePrefix` + `panelFixtureNames()` in `package capture`, `panelFixtureNamePrefix` + `registeredPanelFixtureNames()` in `capture_test`. The two packages cannot share an unexported identifier, so closing this means deciding whether to export the constant from the `package capture` test file (e.g. `PanelFixturePrefix`, declared in a `_test.go` so nothing reaches the production build) and have the external test package read it through `capture.` — or to accept the split as the cost of the two-package test layout. Note the divergence is self-reporting today: the two derivations feed `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet`, so a prefix change that reached only one of them fails loudly rather than silently shrinking a guard.
