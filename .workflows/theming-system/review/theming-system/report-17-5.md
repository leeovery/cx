TASK: theming-system-17-5 — Give internal/themetest Ownership Of The Deny-Read And Canvas-Valued Fixtures

ACCEPTANCE CRITERIA:
1. `rg 'Chmod\(.*0o000' --glob '*_test.go'` returns only `internal/themetest`.
2. No test-local helper named for unreadable/denied/poisoned paths survives in `internal/theme`, `cmd`, `cmd/capturetool`, `internal/tui` or `internal/prefs`.
3. Exactly one root policy exists in the tree, and every former inline site now skips under root rather than asserting vacuously.
4. `themetest.WithValue(themetest.Lines(), "canvas", …)` appears at no call site outside `internal/themetest`.
5. The unit and integration lanes stay green, and the `unreadable` rung's coverage is unchanged in substance at every former site.

STATUS: complete

SPEC CONTEXT:
The `unreadable` rung is first-class product behaviour, not incidental: it is one of the six rejection-ladder rungs (spec §6.2 line 451), the reason a theme made unreachable by an unusable directory carries (line 384), the discriminator export refuses on (line 1428), the `theme: directory unusable` WARN's trigger (line 1488), the pinned `⚠ dir unreadable` panel row (lines 1107–1112), and doctor's verbatim-OS-error line (line 1863). It "covers every read failure, not only permissions" (line 397). So the fixture behind it must stage a *real* OS denial and must distinguish denied from absent — which is exactly what this task centralises. The task itself is analysis-remediation (duplication), not a spec behaviour; the spec's stake is that the rung's coverage must not weaken.

IMPLEMENTATION:
- Status: Implemented (all seven Do items).
- Location:
  - `internal/themetest/deny.go:17` `DenyRead`, `:30` `DenyDir`, `:39` the shared `deny` (chmod 0o000 → verify → return the denial), `:57` `restoreMode` (prior-mode capture + `t.Cleanup` restore, registered *before* the chmod so a failed chmod still restores, and after `t.TempDir`'s RemoveAll so LIFO ordering un-blocks teardown).
  - Single root policy at `deny.go:49-51`: read succeeds at mode 0000 ⇒ `t.Skipf`, documented on `DenyRead` (`:14-16`) and inherited by reference on `DenyDir` (`:28-29`).
  - Do item 3 folded: the verified OS error is the return value, so callers assert "denied, not absent" on it (`cmd/doctor_theme_test.go:115`, `cmd/theme_test.go:768,918`, `internal/theme/resolve_test.go:198`, `internal/theme/load_test.go:449`).
  - `internal/themetest/theme_file.go:87` `MonochromeLines`, `:99` `LinesWithCanvas`, `:104` `WriteWithCanvas` (expressed as `Write(t, dir, base, LinesWithCanvas(canvas))`, so the write- and lines-level forms cannot drift).
  - Migration: 45 `DenyRead`/`DenyDir` call sites across `internal/theme` (7 files), `cmd` (5 files), `internal/prefs/read_shared_test.go`; ~110 canvas/monochrome fixture sites across `internal/theme`, `internal/tui`, `cmd`, `cmd/capturetool`. Commit `3d3081cf` (37 files, +541/−341).
  - All seven named helpers are gone: `writeUnreadableTheme`, `unreadableDir`, `unreadableThemeFile`, `unreadableThemesDir`, `requireDeniedRead`, `poisonThemesDir`, the `deniedDir` closure, `skipUnlessModeBitsDeny`, and `internal/tui`'s `writeThemeFileForTest` (verified by name grep — zero hits).
  - Import direction preserved (Do item 7): `internal/theme`'s tests consuming `themetest` remain in the external `theme_test` package; themetest → theme only.
- Notes:
  - Criteria 2 and 4 pass cleanly (verified by grep: no surviving fixture-staging helper of that shape; no `WithValue(Lines(), "canvas", …)` outside `internal/themetest`).
  - Criteria 1 and 3 were recorded as unreachable-as-written in the task's fix-tracking, and that judgement is sound as far as it goes: `deny.go` is not a `_test.go` file so the grep can never return `internal/themetest`, and the tree carries ~20 pre-existing `os.Geteuid()` guards in `internal/state`, `internal/project`, `internal/spawn`, `internal/tui/preview_adapter_test.go` and `cmd/state_hydrate*` that are unrelated to themes and out of this task's scope.
  - Within `internal/theme` one root policy survives at `resolve_test.go:511` (`searchOnlyDir`, mode `0o111` — read bit dropped, search bit kept). `DenyDir` structurally cannot express it, it has one call site, and the fixture is a different thing rather than a duplicate policy. Accepted deviation.
  - Two theming-authored sites the Do list did not enumerate were left inline and are the one real residue: `cmd/prefs_translation_test.go:200` (no root guard at all — vacuous under root) and `internal/prefs/store_write_path_test.go:206` (its own `t.Skip` policy). Both are expressible via `DenyRead`. Non-blocking; see notes.
  - Placement tension (inherited from the task's direction, not a defect): the deny helpers hold no theme content, so a shared root policy is reachable only by packages willing to import the theme fixture package. `internal/prefs`'s test now imports `themetest` (hence `internal/theme`) for a generic filesystem fixture. No cycle, and prefs stays a leaf in production.

TESTS:
- Status: Adequate.
- Coverage:
  - `internal/themetest/deny_test.go` covers all three contract clauses for both helpers: genuinely unreadable while staged (`:13`, `:45` — re-reads the path after the call), the return value is the denial and not a not-exist error (`:24`, `:57` — compared verbatim against a live read, plus `requireDenial` at `:78` pinning `fs.ErrPermission` and negating `fs.ErrNotExist`), and prior-mode restoration (`:35`, `:68` — staged inside a subtest so cleanup fires, then the outer test asserts the original `0o640`/`0o750` is back; deliberately non-default modes, so a restore-to-a-hardcoded-mode bug would fail).
  - `internal/themetest/theme_file_test.go:152` proves `WriteWithCanvas` produces a file the loader accepts, whose canvas is the requested value, and whose other tokens are `Lines()` unchanged (whole-token-slice comparison, not just the canvas). `:132` pins `LinesWithCanvas` at the lines level (order + every non-canvas value); `:174`/`:189` cover `MonochromeLines` for both the one-value invariant and loader acceptance.
  - Migrated sites retain their assertions verbatim — diff-checked across `cmd/theme_test.go`, `cmd/doctor_theme_test.go`, `cmd/doctor_persisted_theme_test.go`, `cmd/open_theme_nomination_test.go`, `cmd/theme_source_test.go`, `internal/prefs/read_shared_test.go`. The only substantive behaviour change is the prescribed one: `cmd` sites that previously fatalled on a mode-0000 read succeeding now skip. No site loses meaning, because the helper verifies the denial before returning, so a chmod that did not take can no longer produce a vacuous pass.
  - One near-miss worth recording as verified-not-weakened: `cmd/theme_test.go:808` previously used `requireDeniedRead` (which rejected ENOENT) on `nope.theme` inside a denied directory and now uses the local `osReadError` (which accepts any error). Under a mode-0000 directory the OS reports EACCES, not ENOENT, and `DenyDir` has already proven the denial, so the discrimination is not lost.
- Notes: Not over-tested — six deny cases and four fixture cases is one test per contract clause, with no redundant restaging. Not under-tested for the helpers themselves. Tests were assessed by reading; no suite was executed.

CODE QUALITY:
- Project conventions: Followed. `*testing.T`-first parameters structurally confine both helpers to `_test.go` consumers, matching the `portaltest`/`themetest` test-only convention in CLAUDE.md. Both new helpers call `t.Helper()`. Fixture mode stays the package's single `fixtureMode` constant. No production import of `themetest`.
- SOLID principles: Good. `deny` is the one policy; `DenyRead`/`DenyDir` differ only in the injected read closure, so the root policy, the denial verification and the mode restoration exist once. `WriteWithCanvas` is defined in terms of `LinesWithCanvas`, so the write- and lines-level forms cannot diverge.
- Complexity: Low. `deny` is a three-branch switch; `restoreMode` is linear.
- Modern idioms: Yes — closure-injected read, `t.Cleanup` over defer, `t.Skipf`/`t.Fatalf` with the path in the message.
- Readability: Good. Every doc comment states *why* (mode restoration is justified by TempDir teardown; the skip is justified by the fixture being impossible). The comment corrections from the prior fix round landed: `deny.go:14-16` no longer claims tree-wide uniqueness, `:29` says "the same root policy", and the duplicated inline comment at `cmd/theme_source_test.go:191` is gone.
- Issues: The `os.IsNotExist` branch at `deny.go:51-52` is unreachable in practice — `restoreMode`'s `os.Stat` fatals first on an absent path (and follows a dangling symlink, so that fatals too). It is defensive, correctly worded, and paired with a contract pin in the test; not worth changing.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [quickfix] cmd/prefs_translation_test.go:200-203 — replace the inline `os.Chmod(path, 0o000)` + `t.Cleanup` pair with `_ = themetest.DenyRead(t, path)`. This site has no root guard at all, so under root the subtest "it returns a bound store against an unreadable file" passes while the file is plainly readable: `loadPrefsStoreNoMigrate` succeeds either way, and the `LoadThemeKeys` zero-keys assertion at :228 holds regardless because the seeded `{"appearance":"dark"}` declares none of the three theme keys. It is theming-feature-authored (added by task 6-5) and is the exact "silently vacuous under root" shape this task set out to eliminate.
- [quickfix] internal/prefs/store_write_path_test.go:206-213 — route through `themetest.DenyRead` (the file's sibling `read_shared_test.go` already imports it, same package, no cycle). Today it carries its own `t.Skip` root policy, making it a second copy of the policy inside a package this task explicitly covered. Keep the existing explicit `os.Chmod(path, 0o644)` at :224 before `assertUntouched`; the helper's cleanup restore is then a harmless no-op.
- [quickfix] internal/theme/enumerate_test.go:53-55 — delete the `os.ReadDir` re-read and its `t.Fatalf`. `themetest.DenyDir` on the line above has already performed and verified that read (skipping if it succeeded), so the block can never fire; it is the "assert by re-reading the path" pattern Do item 3 retired.
- [quickfix] cmd/theme_test.go:568 and internal/theme/resolve_test.go:615 — two `osReadError` helpers with the same contract (fail if the read succeeds, else return the error) now exist in two packages, both serving the read-a-file-inside-an-already-denied-directory case that `chmod` cannot reach. Add a `ReadError(t *testing.T, path string) error` companion beside the deny pair in `internal/themetest/deny.go` and delete both locals (call sites: cmd/theme_test.go:768,808 and internal/theme/resolve_test.go:288).
- [do-now] internal/themetest/theme_file.go:1-4 — the package doc enumerates three capabilities and no longer describes the package; it now also owns the tree's deny-read fixture and is consumed from `internal/prefs`. Replace "it authors `.theme` fixture files, loads the embedded built-ins by slug, and builds the synthetic probe palettes a swap guard diffs between." with "it authors `.theme` fixture files, loads the embedded built-ins by slug, builds the synthetic probe palettes a swap guard diffs between, and stages a path unreadable under one root policy."
- [idea] internal/themetest/theme_file.go:104 — decide whether the monochrome fixture deserves the write-level convenience its canvas sibling has. `themetest.Write(t, dir, base, themetest.MonochromeLines(v))` is spelled out at ~60 call sites (all of `internal/tui/theme_panel_*_test.go`, `internal/tui/builtin_themes_test.go`, `internal/tui/theme_testing_test.go`), whereas the canvas form got `WriteWithCanvas` for far fewer. A `WriteMonochrome(t, dir, base, value)` would restore the symmetry, at the cost of a ~60-site sweep for no behaviour change.
