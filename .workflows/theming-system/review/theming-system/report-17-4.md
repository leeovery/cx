TASK: theming-system-17-4 — Make the theme slot seam state what it does and single-source the slot collapse (tick-3e59e9, severity medium, sources: architecture, duplication)

ACCEPTANCE CRITERIA:
1. `ResolveSetting(keys)` followed by `Setting.Slug(slot)` appears in exactly one place outside `internal/theme`'s own tests: `SlugForSlot`.
2. `tui.ThemeSource` has four methods, the fourth returning `error` only, named for the act.
3. No call site in the tree discards a `SlotResolution`.
4. The `theme: loaded` emission still fires exactly once per confirmed slot commit, on the same path and with the same slug and slot attrs.

STATUS: complete

SPEC CONTEXT:
Spec §8.4 (line 802-809) specifies the mid-session constant → adaptive conversion: the slot the user assigns needs no read (the §5.8 enumeration already holds its parse), but the *opposite* slot becomes live and must be loaded at commit time. "This is the one theme load that happens outside construction, so it emits `theme: loaded` at commit rather than at construction — the catalogue's cadence column (§12.3) accounts for it." §12.3 (line 1485) pins the emission contract: INFO, one line per nominated theme at construction, "**Also fires at commit time** for the one load that happens outside construction: the newly-live opposite slot on a constant → adaptive conversion (§8.4)", carrying `slug` and `slot`. §12.3 (line 1495) pins that emission is controlled by an injected logger, not by the loader deciding. The spec names no seam method — the `ThemeSource` shape is a planning artifact, so this task's rename is free of spec constraints as long as the cadence and attrs are unchanged.

IMPLEMENTATION:
- Status: Implemented (commit 62a7c974), all 9 `Do` steps present.
- Location:
  - `internal/theme/setting.go:97-104` — `SlugForSlot(keys, slot)`, documented as the one collapse (tiebreak + shipped-default substitution).
  - `internal/theme/dir_theme_source.go:31-39` — `LoadSlot(enumeration, slot, keys) error`, body `_, err := e.Loader.ResolveSlot(enumeration, slot, SlugForSlot(keys, slot))`.
  - `internal/tui/theme_seams.go:11-16` — the seam now declares `LoadSlot(e, slot, keys) error`; interface doc rewritten (lines 8-10) to state the method resolves the named slot and records the load.
  - `internal/tui/theme_panel_confirm.go:66-77` — vacuous `if _, err := …; err != nil { return }` replaced by `_ = m.themeState.source.LoadSlot(...)`; the comment states why the error is not actionable and records the rejected alternative ("applyCommittedSetting is its single owner, and a second writer would put two sources of truth behind one screen"). `adoptRetainedReply()` ordering preserved.
  - Fakes updated: `internal/capture/theme_fake.go:49-53`, `internal/tui/theme_seams_test.go:31-34`, `internal/tui/theme_source_fake_test.go:56-61` (the last routes through `theme.SlugForSlot`). The "the same collapse the production adapter runs" comments are gone repo-wide (grep: zero hits).
  - `internal/theme/theme_test.go:159,230` — exported-surface list updated (`DirThemeSource.LoadSlot`, `SlugForSlot`); `Loader.ResolveSlot` retained unchanged at line 184 as required.
- Criterion-by-criterion:
  1. Satisfied and *structurally guarded*. `grep ResolveSetting` outside `internal/theme` tests finds three sites: `cmd/open.go:501` (reads `raw`, never `Slug`), `internal/tui/theme_panel.go:201` (`themeSetting()` — callers read `.IsConstant` only: `theme_panel_commit.go:74`, `theme_panel_behaviour_test.go:643`), and `internal/theme/setting.go:102` (`SlugForSlot` itself) plus `InForceKeys` (reads `Constant`/raw). `grep '\.Slug('` outside `internal/theme` finds nothing. The new AST guard `internal/theme/slug_collapse_guard_test.go:19` enumerates every repo `.go` file and asserts the pairing appears only in `internal/theme/setting.go:SlugForSlot`.
  2. Satisfied — `internal/tui/theme_seams.go:11-16`: `Open`, `Reassemble`, `Resolve`, `LoadSlot`; the fourth returns `error` only and is named for the act (load), not the mechanism (resolve). All four implementations in the tree (`theme.DirThemeSource`, `capture.fakeThemeSource`, `tui_test.fixtureThemeSource`, `tui.fakeThemeSource`) were migrated in the same commit; `internal/capture/theme_fake.go:20` keeps the compile-time `var _ tui.ThemeSource` assertion, and `internal/tui/theme_seams_test.go:36-41` keeps both.
  3. Satisfied in the sense the task intends. The only surviving `_, err :=` on a `SlotResolution` is inside `DirThemeSource.LoadSlot` itself (`dir_theme_source.go:37`) — the narrowing point, with a doc comment saying why the record is not carried ("the palette belongs to the nomination Resolve hands back, and a second copy of it would be free to disagree"), and the task's `Do` step 4 prescribes exactly this body. Strictly read, the criterion sentence is one discard short of literal; that discard is the deliberate mechanism, not an oversight, so I judge it met. No consumer outside the package receives-and-drops a record any more.
  4. Satisfied. Path unchanged: `confirmSlotAssignment` → `loadNewlyLiveSlot` → `source.LoadSlot` → `Loader.ResolveSlot` → `commitPass` (`resolution.go:108-110`) → `reportSlot` (`resolution.go:193-197`) → `events.Loaded(r.Resolved, r.Slot)`. `Resolve` still routes through `enumerationPass` → `reportFallback`, which emits no `loaded`. Attrs are produced by the same `events.Loaded` call as before.
- Notes: no dead code left behind — `memberForSlot`, used by the deleted fake body, is still consumed by `internal/tui/theme_panel_commit_load_test.go:171`. No duplicate declarations introduced in `package theme_test` (`calledName` / `collapsingFuncsIn` / `exemptFromCollapseGuard` are unique). The new guard uses the untagged, stdlib-only `sourceguardtest` + `portalbintest.ProjectRoot`, so it runs in the unit lane like the repo's other ~20 source guards. No markdown/doc reference to the old seam method survives outside historical planning artifacts.

TESTS:
- Status: Adequate (with one weak test that is redundant rather than a gap; see notes).
- Coverage vs the task's three test bullets:
  - Bullet 1 (`LoadSlot` emits `theme: loaded` with the collapsed slug for an unset slot; `Resolve` emits none): `internal/theme/dir_theme_source_test.go:39-62` drives `{Light:"nord"}` + `SlotDark`, asserts exactly one `loaded` record carrying `slug=DefaultDarkSlug` and `slot=dark`; `:64-80` asserts `Resolve` emits zero. Chosen input makes the assertion non-vacuous (the recorded slug can only come from the substitution). `internal/theme/dir_theme_source_test.go:11-35` table-pins `SlugForSlot` across set/unset/tiebreak/control-stripped cases, gated by `requireDistinctDefaults` so a swapped fallback map cannot hide.
  - Bullet 2 (exactly one `theme: loaded` per confirmed slot commit): `requireLoadedLine` (`internal/tui/theme_panel_commit_load_test.go:120-133`) fails on `len != 1` and checks both attrs; it is driven by the conversion suites (`:519`, `:585`), with negative controls at `:477` (plain `Enter` emits none) and `:497` (failed write emits none).
  - Bullet 3 (all implementations answer the same slug): `internal/tui/theme_slot_collapse_test.go:11-43` compares the production adapter's emitted slug against the recording fake's recorded slug for one set of raw keys. The two non-recording fakes (`capture.fakeThemeSource`, `tui_test.fixtureThemeSource`) can no longer answer a slug at all — after the narrowing they perform no collapse — so they are covered structurally (compile-time `ThemeSource` assertions) rather than by comparison. That is the honest post-change reading of the bullet.
  - Retargets from the task's step 7 are all present and correct: `internal/capture/theme_panel_fixture_test.go:445-450`, `internal/tui/theme_seams_test.go:99-123`, `internal/tui/theme_panel_commit_load_test.go:559` (now `loader.ResolveSlot(enumeration, slot, theme.SlugForSlot(keys, slot))` — the shared rule body, which is what that test's "the two share one rule body" assertion needs).
- Notes: the guard test is the strongest piece of new coverage — it is a source guard precisely because a second collapse would compile and answer correctly on the day it is written. One redundancy and one now-hollow test are listed below; neither leaves a property unpinned, because `internal/theme/dir_theme_source_test.go:39` and `internal/tui/theme_slot_collapse_test.go:11` both pin the adapter's slot-and-slug behaviour through the emitted record.

CODE QUALITY:
- Project conventions: Followed. `internal/tui` still emits no `theme` component (the seam takes raw keys and the collapse stays in `internal/theme`); the `theme` log component's emission sites are unchanged; the test-only guard helpers come from `sourceguardtest`/`portalbintest` per CLAUDE.md; no `t.Parallel()`; no test spawns a daemon or binary, so the unit lane is respected.
- SOLID: Good. Interface segregation improved — the seam now advertises only what the single production caller consumes, and the seam's name matches its effect. The nomination keeps one writer (`applyCommittedSetting`), explicitly recorded at `theme_panel_confirm.go:68-71`.
- DRY: Good. The four-way duplicated `ResolveSetting`+`Slug` pairing collapses to `theme.SlugForSlot`, and the guard makes the deduplication load-bearing rather than a snapshot.
- Complexity: Low. `SlugForSlot` is two lines over existing primitives; `LoadSlot` is two lines; the removed guard-with-no-body reduces a branch.
- Modern idioms: Yes. `cmp.Or` reused via the existing `Setting.Slug`; explicit `_ =` on the deliberately-ignored error rather than a bare call, which reads as intent and keeps `errcheck`-class linters honest.
- Readability: Good. The comments explain the *why* (why only the error, why the palette is not carried, why the error is not actionable, why the rejected alternative was rejected) rather than restating the code, and carry no task/phase/spec-section references.
- Issues: one inherited over-broad claim in the rewritten interface doc block (see `[do-now]` below). No security or performance surface — this is a pure-in-memory resolution against a retained enumeration; `LoadSlot` performs no I/O.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/tui/theme_seams.go:7 — the doc block says "No method reads the filesystem", which the code falsifies for `Open`: `DirThemeSource.Open` (`internal/theme/dir_theme_source.go:14-16`) calls `Assembler.Open`, which calls `Loader.OpenEnumeration(themesDir)` and lists the themes directory (`internal/theme/enumerate.go:58-65`). The intended claim is the one `union.go:113-116` states ("Call it on open and Reassemble for everything after, so no later step reads the directory"). Replace the sentence with: "No method after Open reads the filesystem: a further parse of the same slug could disagree with the row on screen."
- [quickfix] internal/tui/theme_seams_test.go:99-123 — `TestThemeSourceLoadsASlotFromTheRawKeys` no longer tests what its name says. After the narrowing, the only property it pins about the seam is that `LoadSlot` returns nil; every assertion below it runs against a *fresh* `loader.ResolveSlot(...)` call the test makes itself, so it would stay green if `LoadSlot` loaded the wrong slot, the wrong slug, or nothing at all. Either give it teeth (build the source over a capture logger — `theme.NewLoader(theme.NewEventLogger(logger))` instead of `NewSilentLoader` — and assert the single `theme: loaded` record's `slug`/`slot` after the `LoadSlot` call, dropping the second resolution), or delete it as redundant: `internal/theme/dir_theme_source_test.go:39-62` and `internal/tui/theme_slot_collapse_test.go:11-31` already pin that property through the emitted record.
- [quickfix] internal/capture/theme_panel_fixture_test.go:448-450 — the `theme.SlugForSlot(keys, theme.SlotLight) != "nord"` assertion exercises `internal/theme`, not the capture fake (the fake ignores its arguments entirely), inside a sub-test whose subject is "every method answers from declared values with the config paths poisoned". The collapse is already table-pinned in `internal/theme/dir_theme_source_test.go:11-35`. Delete the three lines and keep the `LoadSlot` nil-error + poisoned-dir assertions, which are the fake's actual contract. (Noted for completeness: the task's step 7 prescribed this assertion, so this is a refinement of the plan rather than a deviation from it.)
