TASK: theming-system-17-11 — Let A Capture Fixture Declare Its Render Size (tick-060df6)

ACCEPTANCE CRITERIA:
- `theme-panel-narrow` and `theme-panel-min-height-message` render differently from their base fixtures with no external instruction, from both the guard and `capturetool`.
- A fixture that declares no size renders exactly as it does today at the caller's size.
- The swap-and-diff guard renders each fixture at its declared size and stays green.
- No render size is stated only in a `.tape`.

STATUS: complete

SPEC CONTEXT:
- §13.3 (specification.md:1628-1637) mandates the two frames this task is about — "The narrow degraded panel, and the panel at its minimum height with a message live — §9.8's floor is defined as header + footer + one row + one message row, and that arithmetic is only observable on a frame that renders it" — and states the failure mode the task attacks: "A missing fixture is a blind spot the guard structurally cannot report: §13.4 enumerates whatever fixtures exist, so absence reads as coverage." A fixture that renders byte-identically to its base is the same blind spot wearing a name.
- §13.4 (:1651) pins that the guard "enumerates the harness's fixture set; it never names fixtures", so a duplicate frame inflates the coverage list without adding coverage.
- §13.2 (:1592-1600) is why the geometry could not stay in a `.tape`: images and tapes are scaffolding cleared at sign-off; only the Go fixture definitions and the harness are permanent.
- §9.8 (:1170-1188) defines both geometries the fixtures now declare — the preferred/minimum width ladder and the header + footer + one row + one message row height floor, with thresholds "pinned at implementation".

IMPLEMENTATION:
- Status: Implemented, matching the plan's five Do steps.
- Location:
  - `internal/capture/fixtures.go:21-26` — unexported `width, height int` on `Fixture`, documented as "The render size the fixture requires, per dimension, where its frame IS a geometry rather than content at whatever size the caller has. Zero takes the caller's."
  - `internal/capture/harness.go:22-25` — `ModelAt` runs `w, h = f.RenderSize(w, h)` before the `tea.WindowSizeMsg`, so every Go-side consumer honours a declaration without its own branch; `:45-56` `RenderSize` (per-dimension substitution, zero = caller's); `:58-68` `PinRenderSize` (rewrites a live resize, passes every other message through).
  - `internal/capture/fixtures.go:485-495` — `theme-panel-min-height-message` declares `tui.ThemePanelMinWidthTerminal()` × `tui.ThemePanelFloorTerminalHeight()`; `:583-592` — `theme-panel-narrow` declares the width only, so its height stays the caller's.
  - `internal/tui/theme_panel_geometry.go:73-85` — the two derivations the fixtures read (`themePanelPreferredAffordance - 1 + 2*Hinset`, `themePanelMinHeight(themePanelKeymap(), false) + 2*Vinset`). No bare numbers reached the fixture file; step 3's "derived from the panel geometry constants" is met.
  - `cmd/capturetool/main.go:56, 70-80` — `tea.WithFilter(renderSizeFilter(fixture))`; the filter resolves the fixture once and delegates to `PinRenderSize`, falling back to a pass-through for the swatch (no `*Fixture`).
  - Prose replaced as step 4 asked (`fixtures.go:485-488`, `:583-586`); both now state the declared size and what it exercises rather than instructing a capture operator.
- Step 5 (confirm both consumers route through `ModelAt`): satisfied. The guard reaches every render through `RenderSwapRender` → `ModelAt` (`theme_swap_guard_test.go:122, 260, 277, 297, 342`) over the enumerated registry (`:95-119`), and `capturetool` is the live path via the filter. I checked the wider capture package for sizing sites that bypass it: `theme_panel_fixture_test.go:545-563` (`panelModel`/`driveToPanel`) still hardcodes 120×40, but it takes `tui.Deps` rather than a `*Fixture` and its callers are `theme-panel-paginated` / `theme-panel-adaptive-pair`, neither of which declares a size — inert today, noted below.
- Notes:
  - Derivations check out against the code they read: `insetRegion` subtracts `2*inset` (`model.go:2760-2764`) with `Hinset = 2`, `Vinset = 1`, so `ThemePanelMinWidthTerminal()` = 63 is the widest terminal whose content region (59) is below the 60-column preferred affordance, and `ThemePanelFloorTerminalHeight()` = 10 leaves 8 content rows, exactly §9.8's floor. `internal/tui/theme_panel_geometry_test.go:65-88` pins both as *ends* (one column wider takes the preferred width; one row shorter refuses with `dimHeight`), which is what stops the two helpers from being restated literals.
  - The re-captured PNGs confirm the declarations produce the intended frames: `testdata/vhs/theme-panel-narrow.png` shows the stepped-down panel with both badges retained and `tokyo-night…` truncated rather than wrapped; `testdata/vhs/theme-panel-min-height-message.png` shows exactly the floor — 2-row header, one list row (`nord ● dark`), the `⚠ couldn't save theme` row, and the intact 4-row standing footer.
  - Both tapes were brought onto the declared 63 columns and their now-false "a fixture cannot resize itself" / "narrow the window to about 54 columns" prose removed, and `testdata/vhs/README.md:222-230` gained the authoring clause ("If the frame *is* a geometry, declare the terminal size on the fixture rather than only in the tape"), so the next author following the documented procedure does not reintroduce the drift.
  - No drift from the plan. Later phases did not supersede this mechanism — 17-12's registry work and the 17-13/14/15 commits left `RenderSize`/`PinRenderSize` and both declarations intact.

TESTS:
- Status: Adequate (one small redundancy).
- Coverage, against the three tests the task asked for:
  1. Differs-from-base — `internal/capture/fixture_render_size_test.go:14-38`: a base map (`narrow`→`adaptive-pair`, `min-height-message`→`commit-failed`) and an assertion that the rendered frames differ at the harness size. This is the exact property the task exists to create, and it is stated as data rather than prose.
  2. Declared-vs-caller — `:40-70` (declared dimensions honoured, the narrow fixture's *height* still the caller's, a no-declaration fixture at the caller's size) plus `:72-86`, which enumerates the whole registry and asserts only the two geometry fixtures deviate — so a stray future declaration is caught rather than silently accepted. The `capturetool` half is covered separately at `cmd/capturetool/render_size_test.go:13-59` (including the swatch's pass-through) with `:61-67` pinning that non-resize messages are untouched and `:69-77` pinning the filter is actually passed to `tea.NewProgram` (a source-text guard — the only way to assert wiring without running a program, and consistent with this repo's guard idiom).
  3. Frame-is-the-geometry — the substantive assertions are not tautological, because the constants the pin-tests compare against are themselves pinned to behaviour: `internal/tui/theme_panel_geometry_test.go:65-88` fixes both helpers to the ladder's ends, `internal/capture/theme_panel_remaining_fixtures_test.go:175-191` asserts the narrow fixture's *panel* renders at the ladder's stepped-down width (measured off the base fixture at both ends), and `theme_panel_message_fixtures_test.go:193-205, 247-252` asserts the floor frame's arithmetic (message row adjacent to the footer, and the panel refusing one row below).
- Notes:
  - The consequential edit at `internal/capture/swap_harness_test.go:60-62` is a correct tightening, not a weakening: `● light` moves from `present` to `absent` because the floor's body is one list row. It is also the tripwire that fires if the fixture ever silently reverts to the caller's size.
  - `theme_panel_message_fixtures_test.go:196-202` and `theme_panel_remaining_fixtures_test.go:177-181` correctly re-point the "one row shorter refuses" and "ladder ends" measurements onto the *base* fixtures, which declare no size — the only way to keep asking for a size once the derived fixture pins its own. Sound.
  - Nothing here is over-tested beyond the one subsumed subtest noted below.
  - I did not execute the suite (reading only, per my remit), so "the guard stays green" is verified by reading: the guard's every entry point routes through `ModelAt`, the one assertion the narrowed/shortened frames invalidate was updated in the same commit, and the guard's per-fixture assertions (A/B token-set parity, no stale run) are size-independent while its aggregate coverage assertion still has the full-size base fixtures.

CODE QUALITY:
- Project conventions: Followed. Geometry is derived from the panel's own constants rather than restated (the codebase's standing preference), the harness stays the single sizing chokepoint, and the `internal/capture` no-real-config import guard is untouched (the helpers come from `internal/tui`, not config).
- SOLID principles: Good. `RenderSize` is one substitution rule with two adapters over it — `ModelAt` for offline consumers, `PinRenderSize` for the live message stream — so neither consumer branches on fixture identity and a third consumer inherits the behaviour.
- Complexity: Low. Two guard clauses and a type switch.
- Modern idioms: Yes. Zero-value-means-caller's avoids an option type for a two-field concern; the `tea.WithFilter` seam is the idiomatic Bubble Tea v2 interception point.
- Readability: Good. The field comment states the rule at the declaration site, and both fixture comments now say what the frame exercises instead of instructing a human.
- Issues: None blocking. Minor: two doc comments say "plus the page gutter" where the code adds both gutters; `RenderSize` is exported with no consumer outside its package; `ThemePanelMinWidthTerminal` reads as a near-synonym of an existing test constant meaning the opposite end of the ladder.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/tui/theme_panel_geometry.go:75 and :82 — both comments end "plus the page gutter the content region is inset by" / "plus the page gutter", while the code adds `2*Hinset` and `2*Vinset`. Replace with "plus the page's gutters on both sides, which the content region is inset by" (line 75) and "plus the page's gutters on both sides" (line 82).
- [quickfix] internal/capture/harness.go:48 — unexport `RenderSize` to `renderSize`: its only callers are `ModelAt` (`:24`) and `PinRenderSize` (`:66`) in the same package, and the exported live-path entry point (`PinRenderSize`, used by `cmd/capturetool/main.go:79`) is unaffected.
- [quickfix] internal/tui/theme_panel_geometry.go:76 — rename `ThemePanelMinWidthTerminal` to `ThemePanelSteppedDownTerminalWidth` (6 call sites). As it stands it reads as "the minimum terminal width the panel needs", which is what `minimumPanelTermWidth = 28` already means at `internal/capture/theme_panel_remaining_fixtures_test.go:32` — and since this task both constants are in scope in that same file while naming opposite ends of the ladder.
- [quickfix] internal/capture/theme_panel_fixture_test.go:545-563 — route the two fixture-backed callers of `driveToPanel` (`:174` paginated, `:332` and the `panelModel` caller at `:266`) through `fx.ModelAt` / `panelFixtureFrame` instead of the hardcoded 120×40 in `panelModel`, leaving the synthesised-`Deps` callers (`:238`, `:247`) as they are. Inert today (neither fixture declares a size) but it is the one remaining sizing site that cannot honour a declaration.
- [quickfix] internal/capture/fixture_render_size_test.go:63-69 — the "a fixture that declares no size renders at the caller's" subtest is fully subsumed by `TestFixtureRenderSize_DeclaredOnlyByTheGeometryFixtures` (`:72-86`), which asserts the same for every non-geometry fixture including `sessions-flat`; delete the subtest.
- [idea] internal/capture/fixtures.go:468-469 (`theme-panel-confirm`) and :556-559 (`theme-panel-dir-unreadable`) — decide whether to enrol these size-dependent frames in the new mechanism. Their capture geometry still lives outside the fixture (`messagePanelTermWidth = 54` at `theme_panel_message_fixtures_test.go:27`, `dirUnreadablePanelTermHeight = 16` at `theme_panel_remaining_fixtures_test.go:34`), so `go run ./cmd/capturetool --fixture theme-panel-confirm` at an ordinary terminal renders the preferred width rather than the minimum-width wrap the comment says the frame is for, and the dir-unreadable frame renders one page rather than the second page its `Ctrl+↓` needs. Out of this task's literal scope (their data differs from their bases, so they are not the duplicate-frame problem), but the same class and the mechanism now exists.
- [idea] cmd/capturetool/main.go:74-80 — decide how to tell a human that the live terminal is smaller than a fixture's declared size, where the pinned frame now silently wraps or clips (the tapes' "resize your window" instruction used to cover this by hand). Placement is the open question: `main.go:46-47` deliberately writes warnings before the program starts so they land on the primary screen, while the size is only known once the first resize reaches the filter.
