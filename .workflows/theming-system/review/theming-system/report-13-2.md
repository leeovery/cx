TASK: theming-system-13-2 — Single-Source The Prefs Abort-On-Undecodable Case Table

ACCEPTANCE CRITERIA:
1. One case table exists; the three restated tables and the inline literal are gone.
2. Every saver routing through `mutate` is proven against all six shapes.
3. The decoder error-class assertion runs for every saver on every shape that carries one.
4. `assertUntouched` runs for every case/saver pairing — the file is byte-identical after each aborted save.
5. `go test ./internal/prefs` passes.

STATUS: complete

SPEC CONTEXT:
Spec §8.9 / line 925 defines the write-path rule this table states: "`prefs.json` present but unusable — malformed JSON or an I/O failure — aborts the write; it never becomes an overwrite." The abort exists because the tolerant load decode turns a stray comma into a zero-value struct with no error, so a writer merging into it "would erase `session_list_mode`, `theme_migrated`, every untouched theme key and the retained raw `appearance` in a single `s` keypress". Line 906 names the savers that inherit the rule (`SaveTheme`, `SaveThemeSlot`, `SaveMigrationMarker`, `SaveTranslation`, alongside the mode saver), and line 929 confirms the migration write "inherits only the abort half". Line 925 also carves out the non-abort side — "Unrecognised *values* in a syntactically valid file are not 'unusable'" — which the sibling `TestSave_UnrecognisedValueIsNotUnusable` and `TestSave_WrongTypedFieldDoesNotAbort` still hold. So the contract under test is genuinely one rule with five inheritors, exactly the shape that justifies a single table.

IMPLEMENTATION:
- Status: Implemented (exceeds the plan's stated scope, additively)
- Location:
  - `internal/prefs/store_write_path_test.go:103-150` — `undecodableErrClass` enum, `undecodableCase` type, `assertErr` method, `undecodablePrefsCases()` table, sited beside the existing shared `seedPrefsFile`/`readRaw`/`assertUntouched` helpers as the task specified.
  - `internal/prefs/store_write_path_test.go:184-200` — `TestSave_AbortsOnUndecodable` (replaces `TestSave_AbortsOnMalformedJSON`, absorbing the folded `TestSave_AbortsOnEmptyFile` and `TestSave_TopLevelTypeMismatchAborts`).
  - `internal/prefs/migration_marker_test.go:328-343` — `TestSaveMigrationMarker_AbortsOnUndecodable` ranges over the table.
  - `internal/prefs/translation_saver_test.go:263-281` — `TestSaveTranslation_AbortsOnUndecodable` ranges over the table.
  - `internal/prefs/theme_savers_test.go:301-320` — `TestThemeSavers_InheritWritePathRules/abort-on-undecodable` nests `themeSaverCases()` × `undecodablePrefsCases()`, replacing the single inline literal.
  - Commit `7bb25397`.
- Notes:
  - The table holds NINE shapes, not the six the task enumerated. The extra three are the top-level bare string / number / boolean shapes that the deleted `TestSave_TopLevelTypeMismatchAborts` covered for `Save` alone; folding them into the table was required by Do item 7 ("do not weaken any existing claim") and is strictly additive. The union is correct.
  - Saver coverage is complete and verified against the source rather than assumed. `internal/prefs/store.go` has exactly five `mutate` call sites — `Save` (:212), `SaveTheme` (:221), `SaveThemeSlot` (:237), `SaveMigrationMarker` (:253), `SaveTranslation` (:267) — and `themeSaverCases()` (`theme_savers_test.go:421-442`) expands `SaveTheme` + `SaveThemeSlot` light/dark, so all five reach the table. No saver is left out.
  - The four abort-test files are all `package prefs` (white-box), which is what permits sharing the unexported helper; `theme_keys_test.go` and `store_test.go` are `package prefs_test` and correctly do not participate (the pre-existing comment at `store_write_path_test.go:14-15` already explains why `seedPrefsFile` is duplicated across that boundary).
  - Documentation (Do item 3) was satisfied at implementation time — commit `7bb25397` added "undecodablePrefsCases is the single statement of the shapes a save aborts on… a shape added here is proven against all of them at once". A later commit, `fee1927d` ("chore(comments): strip internal/prefs to the code-quality standard", 864 comment lines → 122, removing "code-describing summaries on unexported identifiers"), removed it as part of a deliberate package-wide sweep. Per the review context's note on later intentional revision, this is a superseding standard rather than drift on this task, and is NOT raised as a finding — re-adding the comment would fight that decision. The retained comment at `:107-109` still explains the non-obvious half (the empty-`Field` discriminator).
  - `migration_marker_test.go:423` retains an inline `[1,2]` literal, but it belongs to `TestLoadMigrationState_TolerantDecode` (:411) — the *tolerant read* contract, where corruption is absorbed to a zero record, the opposite claim. That table also holds valid-JSON shapes (`{}`, a populated object), so it is a different case set and correctly left separate. Likewise `translation_saver_test.go:298`'s single `{` inside `TestSaveTranslation_ReportsPersisted` is one representative row in a persisted-reporting outcome matrix, not a restatement of the abort table. Neither is residual duplication.

TESTS:
- Status: Adequate
- Coverage: The abort-on-undecodable contract is now stated once and exercised 5 savers × 9 shapes = 45 pairings, each asserting (a) a non-nil error, (b) the decoder's error class, and (c) `assertUntouched` (byte-identical file + no leftover `.atomic-` temp files, via `assertNoTempFiles`). Spec §8.9's non-abort side stays covered separately and unweakened by `TestSave_UnrecognisedValueIsNotUnusable` (:264) and `TestSave_WrongTypedFieldDoesNotAbort` (:232), so the change did not blur the abort/absorb boundary.
- Notes:
  - Acceptance criterion 3 is met and the change is strictly *strengthening*, which I verified against the pre-change code rather than taking on trust. Before: only two of four files asserted the error class, and `Save`'s zero-byte case asserted only "some error". After: `c.assertErr` runs on every pairing, and the top-level branch preserves the deleted test's `typeErr.Field == ""` discriminator assertion verbatim. No claim from either folded test was lost — the empty-file case kept `assertUntouched` and *gained* a class assertion; all four top-level shapes and their `Field` check carried over intact.
  - Not over-tested. The 45 pairings are the product of a real contract (one write-path chokepoint, five inheritors) rather than redundant variations, and the per-saver loop bodies hold only their own claims, per Do item 6: `persisted=false` at `translation_saver_test.go:273-275`, plain no-error-variance for the marker at `migration_marker_test.go:335-338`, `assertUntouched` everywhere. Setup is a temp file per case — no mocking.
  - Behaviour, not implementation detail: the assertions are on the returned error and on the file's bytes. The white-box package placement is justified and pre-existing (the file header documents it), not introduced here.
  - Criterion 5 (`go test ./internal/prefs` passes) — assessed by reading, per my no-execution constraint. The package compiles: all four abort files are `package prefs`, and I confirmed `encoding/json` and `errors` still have live references in each of the three files that dropped their local class assertions (`migration_marker_test.go` json:3/errors:1, `translation_saver_test.go` json:1/errors:2, `theme_savers_test.go` json:2/errors:1), so no import was orphaned by the deletions. The shapes themselves are correct against `encoding/json`: the five syntax shapes (including the zero-byte file, which `readBytes` reports present with empty bytes) yield `*json.SyntaxError`, and the four top-level shapes yield `*json.UnmarshalTypeError` with an empty `Field`, which is precisely the branch `readFileStrict` (`store.go:155-161`) does not absorb.
  - The task's two throwaway verification procedures (temporarily add a seventh shape; deliberately break a saver-specific assertion) leave no artefact by design and are unverifiable after the fact. The table's structure makes the first hold by construction — all four call sites range over the same function — and the nine-shape union is itself evidence that shapes were added centrally and propagated.

CODE QUALITY:
- Project conventions: Followed. Table-driven subtests with `t.Run`, `t.Helper()` on the assertion method's path, `t.TempDir()` for isolation, no `t.Parallel()` (correct — CLAUDE.md forbids it), unit-lane and hermetic (no tmux, no daemon, no built binary), so no `//go:build integration` tag is warranted. `internal/prefs` remains a leaf: the test adds only `encoding/json`/`errors`, no `internal/log` import, so `leaf_guard_test.go` stays satisfied.
- SOLID principles: Good. The case type owns its own error-class assertion (`assertErr`), so adding a shape carries its expectation with it instead of forcing every call site to branch — this is what lets the four savers keep identical loop bodies. Open/closed in the intended sense: extension is a new row.
- Complexity: Low. `assertErr` is a two-arm switch; each abort test is a single loop.
- Modern idioms: Yes. `errors.As` for typed-error extraction (correct — not a type assertion), the `iota` enum for the class discriminator, and the function-returning-slice table form already established in this file by `absentPathCases()`.
- Readability: Good. Case names read as prose in test output ("a top-level bare boolean"), and `undecodableErrClass` names the distinction that was previously implicit in which file you happened to be reading.
- Issues: None blocking. One robustness gap in `assertErr` and one out-of-scope structural gap, both below.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/prefs/store_write_path_test.go:121-135 — `assertErr`'s `switch c.errClass` has no `default` arm, so a future third `undecodableErrClass` constant used in the table would silently run zero error assertions across all four savers. That is the precise failure mode this task exists to eliminate, reintroduced one level up. Add as the final arm: `default:` / `t.Fatalf("undecodableCase %q declares errClass %d with no assertion — extend assertErr", c.name, c.errClass)`. Unreachable for all nine current cases, so it cannot change the present result.
- [idea] internal/prefs/store.go:212,221,237,253,267 vs the four abort tests — all five current `mutate` callers are enrolled in the table, but nothing structurally enrolls a *sixth*: a new saver added to `store.go` can ship with no abort test and no failing test to say so, leaving the "no saver can silently fall out of coverage" outcome true only for today's set. The repo already uses source guards for exactly this class of drift (`colour_literal_guard_test.go`, `keymap_dispatch_guard_test.go`, and `sourceguardtest`'s shared AST primitives, which `PackageGoFiles` + `ForEachFuncCall` would serve directly). Whether the enumeration is worth its own maintenance cost, and whether it keys on `mutate` call sites or on an exported-`Save*` naming rule, are open design calls — hence an idea rather than a concrete edit.
