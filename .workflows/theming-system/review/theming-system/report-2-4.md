TASK: theming-system-2-4 — Re-Point The Contrast Swatch Surface At A Loaded Theme Via --theme

ACCEPTANCE CRITERIA:
1. `capturetool --fixture contrast-validation` with no `--theme` renders `tokyo-night`.
2. `--theme <slug>` renders that built-in's tints on **its own** canvas (a light theme judged against `#e1e2e7`).
3. `--theme not-a-theme` exits non-zero with a message naming the slug, and renders nothing.
4. A `--theme` naming an embedded file that fails validation exits non-zero carrying the §6.2 reason (asserted through an injected loader / synthetic built-in FS).
5. The swatch's bands cover all four pinned tints (`bg.selection`, `bg.attention`, `bg.subtle`, `border`) and every foreground-on-tint pairing §13.5 lists, including `state.positive` on `bg.selection`.
6. `swatch.go` imports neither `internal/prefs` nor `internal/tui/theme`.
7. Resolving a theme in `capturetool` produces zero records through a `logtest.Sink` — `log.Discard` is what the loader is handed.
8. `internal/capture`'s no-real-config invariant holds (no `internal/xdg`, no `cmd`, no prefs read) and `cmd/capturetool`'s import guard still passes.
9. Every other fixture still renders exactly as before through `--appearance`. *(Superseded — see below.)*

STATUS: complete

SPEC CONTEXT:
§13.3 requires `capturetool` to gain `--theme` **replacing** `--appearance`, defaulting to `tokyo-night` (the shipped dark default), with invalid input a hard error carrying the §6.2 reason and a non-zero exit — never a fallback, because "silently rendering the wrong theme at a visual gate is precisely the failure this tool exists to prevent". It explicitly names the contrast-validation swatch as re-pointed too: it deliberately does not route through `tui.Build` and is the surface that satisfies the §7.5 / §13.5 human eyeball gate for a new light theme's pinned tints. §13.5 fixes the four eyeball-pinned tints as `bg.selection` / `bg.attention` / `bg.subtle` / `border` after the §2.2 border consolidation, and lists the foreground-on-tint pairings as `text.on-selection`, `text.secondary`, `text.tertiary` and `state.positive` on `bg.selection`, plus `text.on-attention` on `bg.attention`. §12.3 lists `capturetool` among the callers that construct the loader with `log.Discard()` — the `theme` component records where a theme is *used*, never where one is *diagnosed*.

Note on AC 9: the task deliberately kept `--appearance` alive for exactly one phase, with Phase 3 deleting it and widening `--theme` to the slug-or-path form. Phase 3 has landed. `--appearance` is gone (`cmd/capturetool/main_test.go:25` `TestFlags_AreFixtureAndThemeOnly` now pins the flag set to `{fixture, theme}`), `--theme` accepts a path, and the phase-boundary source comment the task asked for is correctly absent rather than stale. AC 9's intent — every other fixture still resolves and renders — is met via `--theme` instead (`TestResolveProgramSessionsFixture`, `TestResolveModel*`). This is planned supersession, not drift.

IMPLEMENTATION:
- Status: Implemented (as amended by Phase 3)
- Location:
  - `internal/capture/swatch.go:17` — `NewContrastValidationModel(th theme.Theme)`; `swatchModel` holds a `theme.Theme` (`:22-27`); `modeFromAppearance` is gone and no `prefs` import remains (`:3-11`).
  - `internal/capture/swatch.go:102-140` — `renderSwatch` bands: `bg.selection` (`:113-118`), `bg.attention` (`:121-125`), `bg.subtle` (`:128-132`), and the single consolidated `border` rule (`:135-137`). Title sources the canvas hex from `th.Canvas.Value` (`:109`).
  - `internal/capture/swatch.go:55-60` — `View()` sets `BackgroundColor` to the theme's own canvas; `fillCanvas` (`:63-86`) pads with `th.Canvas`.
  - `cmd/capturetool/main.go:33` — `const defaultThemeSlug = theme.DefaultDarkSlug` (`internal/theme/builtins.go:13` = `"tokyo-night"`), read off the shipped default rather than restated.
  - `cmd/capturetool/main.go:86-99` — `resolveProgram` resolves the theme **before** either branch, so a screen capture and the swatch cannot be judged against different palettes; the swatch branch keys off `capture.ContrastValidationFixture`.
  - `cmd/capturetool/main.go:110-149` — `resolveTheme` / `resolveBuiltinTheme` / `resolvePathTheme`: `found == false` → error naming the slug and listing the available built-ins; `rejection != nil` → `%w`-wrapped rejection carrying the §6.2 reason and detail. No fallback on any path.
  - `cmd/capturetool/main.go:87` — the loader is `theme.NewSilentLoader()`, which is `NewLoader(NewEventLogger(log.Discard()))` (`internal/theme/load.go:43-45`).
  - `cmd/capturetool/main.go:104-106` — `themeLoader` interface exists solely to drive the otherwise-unreachable built-in rejection branch, exactly as the task prescribed.
- Notes:
  - AC 5 is met **beyond** the task's own `Do` bullet: that bullet listed three foregrounds on `bg.selection`, while the implementation renders all four §13.5 names (`text.on-selection` / `text.tertiary` / `text.secondary` / `state.positive`, `swatch.go:167-177`). The implementation follows the spec and the AC, which is correct.
  - `resolveProgram` returns an explicit `nil, err` rather than the concrete zero `tui.Model` (`main.go:94-97`), avoiding the typed-nil-in-interface trap — and the tests assert `m != nil` / `m == nil`, so the trap is pinned, not merely avoided.
  - AC 8, precisely: the *behavioural* invariant holds — `swatch.go` reads no config, `internal/theme` is guarded against any path resolution or `PORTAL_THEMES_DIR`/`os.UserHomeDir` access (`internal/theme/leaf_guard_test.go:23-67`), and `internal/capture/theme_panel_fixture_test.go:415-455` renders a fixture with `PORTAL_THEMES_DIR`/`XDG_CONFIG_HOME`/`PORTAL_PREFS_FILE` poisoned and asserts nothing was created. The *import-graph* form of the claim ("no `internal/xdg`") is already false transitively via `internal/capture` → `internal/tui` → `internal/state/paths.go` → `internal/xdg`; that edge predates this task, is untouched by it, and was never guard-asserted. Adding `internal/theme` smuggled in no new config discovery, which is what the AC is actually protecting. `cmd/capturetool/import_guard_test.go` (portal binary must not import `internal/capture`; the capture tool must) still passes by construction — neither direction changed.
  - `internal/capture/fixtures.go:10` still imports `internal/prefs`, but only for the `prefs.SessionListMode` fixture datum — no store construction, no read. AC 6 is scoped to `swatch.go` and holds there absolutely.
  - Docs are current, not stale: `testdata/vhs/README.md:125` states there is no `--appearance` flag, and `:209-213` documents the swatch as the deliberate non-`tui.Build` exception that takes a `--theme`. `main.go`'s package doc (`:1-14`) matches the shipped behaviour.

TESTS:
- Status: Adequate
- Coverage:
  - AC 1 — `cmd/capturetool/main_test.go:75` `TestResolveTheme_DefaultsToTheShippedDarkBuiltin` asserts both the value *and* the declaration form (AST-checks the const is written as `theme.DefaultDarkSlug`, so the capture default cannot drift from the shipped default), plus that resolving it returns the embedded palette. `TestFlags_AreFixtureAndThemeOnly:37` pins the flag's default to that const.
  - AC 2 — `internal/capture/swatch_test.go:67` `TestSwatch_UsesThemeOwnCanvas` runs dark (`#0B0C14`) and light (`#E1E2E7`) palettes and checks `View().BackgroundColor`, the title hex, the label painted on the theme's canvas and the border rule byte-for-byte. `cmd/capturetool/swatch_test.go:13` drives the real embedded `tokyo-night` end-to-end through `resolveProgram`.
  - AC 3 — `main_test.go:147` `TestResolveTheme_UnknownSlugIsAnError` (error names the slug, zero palette returned) and `:240` `TestResolveTheme_NoFallbackOnFailure`, which additionally asserts `resolveProgram` returns a **nil model alongside its error for both branches** — i.e. nothing renders.
  - AC 4 — `main_test.go:160` `TestResolveTheme_InvalidBuiltinIsAnErrorNotAFallback` drives the branch through the injected `rejectingLoader` (`:177-183`), exactly the mechanism the task prescribed, and asserts the message carries the slug, the §6.2 reason **and** the detail.
  - AC 5 — `internal/capture/swatch_test.go:104` `TestSwatchBandsCoverEveryPinnedTint` (four labelled bands, each exactly `bandWidth` wide, each present in the composed swatch, plus the accent bar/track composition) and `:219` `TestSwatchCoversForegroundOnTintPairings` (all five §13.5 pairings verified as *styled* substrings — fg token on its tint — not merely as caption text, plus the caption naming each and the `●`/`⚠` glyphs).
  - AC 6 — enforced by compilation; the deleted `internal/tui/theme` package no longer exists.
  - AC 7 — `main_test.go:459` `TestCaptureTool_ThemeResolutionIsSilent` runs four resolutions (default slug, reserved-name path, unknown slug, broken file) under a `logtest.Sink` and asserts zero records, then fires a **positive control** through `theme.NewEventLogger(log.For("theme")).Rejected(...)` to prove the sink is actually wired to the component. That control is what makes this assertion non-vacuous.
  - Theme-drives-render — `swatch_test.go:44` `TestSwatch_RendersFromInjectedTheme` is a swap-and-diff in miniature: every injected value present, every value from a disjoint palette absent, and two palettes must not render byte-identically. This is the assertion that would fail if the swatch regressed to a compiled-in palette.
  - Registry/guard integrity — `internal/capture/fixture_registry_test.go:27-53` and `theme_swap_guard_test.go:100-158` both account for the swatch explicitly, and the latter asserts the swatch is the *only* skip and must not become a resolvable `*Fixture` without going under the completeness guard. That closes the "a skip silently reads as coverage" hole §13.4 warns about.
- Notes:
  - Not over-tested. The three swatch tests have distinct jobs (theme-drives-render / own-canvas / band coverage) and the overlap is minimal. `TestSwatchViewFillsTheCanvasVerbatim` holds the fill to an independently-expressed reference rather than restating the implementation, which is the right shape for a change-detector-prone assertion.
  - One small gap: the *end-to-end* swatch test only exercises the dark built-in; AC 2's specific claim about a light theme being judged against `#e1e2e7` is covered by a synthetic light palette plus a separate dark-built-in resolve, not by the shipped light built-in in one path. Noted below, non-blocking.

CODE QUALITY:
- Project conventions: Followed. Small injectable interface for the one seam that needs driving (`themeLoader`, 1 method) per the DI pattern; `internal/capture` stays test/harness-only and out of the production binary; the loader is handed `log.Discard` via `NewSilentLoader`, matching the closed `theme`-component rule in CLAUDE.md (the component records where a theme is *used*, not diagnosed).
- SOLID principles: Good. `resolveTheme` splits cleanly into slug and path branches; `swatchModel` does one thing; the palette is injected rather than reached for.
- Complexity: Low. `resolveBuiltinTheme`'s switch is two rungs; `fillCanvas` is a single bounded loop.
- Modern idioms: Yes. `%w`-wrapped rejection, `slices.Contains`, `strings.Builder`, `filepath.Separator` rather than a hardcoded `/`.
- Readability: Good, and unusually well-commented at the *why* level. Several comments earn their place by encoding non-obvious reasoning rather than restating code: `main.go:118-120` (why the separator half of `isThemePath` is load-bearing — without it a real file with an odd extension reports the wrong problem), `:127` (why the slug form must not run the reserved-name check), `swatch.go:117` ("the order is the mapping" for the caption), `swatch.go:158-159` (why `fill` returns nothing rather than an empty styled span).
- Comment accuracy: Verified. No stale phase/`--appearance` references survive anywhere in `cmd/capturetool` or `internal/capture`; the one-phase-coexistence comment the task asked for was correctly removed by Phase 3 rather than left to rot. `swatch.go:54` ("the terminal background is set (OSC 11) …") matches `View()`. `swatch.go:21` and `main.go:73` both correctly describe the swatch as a non-`*Fixture` standalone model, which `FixtureNames`/`renderSizeFilter` bear out. `swatch.go:100-101`'s justification for literal token labels is true of the hand-built test palette, though see the note below.
- Issues: None blocking.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [bug] `cmd/capturetool/main.go:63-66` — the swatch branch never restores the terminal background. `tui.RestoreTerminalBackground` runs only when the final model is a `tui.Model`, but `swatchModel.View()` sets `BackgroundColor` (`internal/capture/swatch.go:58`), so quitting `--fixture contrast-validation` leaves the theme's canvas set on any terminal that ignores Bubble Tea's OSC 111 reset — the exact failure `internal/tui/restore.go` exists to prevent. This predates the task, but `--theme` widens the blast radius from two known MV canvases to any drop-in's canvas, and the swatch is precisely the surface a human runs in their real terminal at a visual gate. Concrete change: have `swatchModel` capture the original background (`tea.RequestBackgroundColor` in `Init`) and set it back on exit, mirroring `internal/tui/restore.go` including its canvas-echo guard.
- [quickfix] `internal/capture/swatch.go:33-35` — `canvasHex()` has no production caller; its sole use is the assertion at `internal/capture/swatch_test.go:80`, which duplicates the title assertion five lines below it (`:88-90`). Delete the method and that assertion.
- [quickfix] `cmd/capturetool/swatch_test.go:13-28` — `TestResolveProgramContrastValidation` exercises only the dark built-in end-to-end. Add a `tokyo-night-day` sub-case asserting the title reads `CONTRAST VALIDATION — canvas #E1E2E7`, so AC 2's specific claim ("a light theme is judged against `#e1e2e7`, not a dark reference") is pinned against the shipped light palette rather than only a synthetic one.
- [idea] `internal/capture/swatch.go:113/121/128/135` — the band labels are string literals, justified at `:100-101` on the grounds that a hand-built `Theme` carries values without names. A *parsed* theme does carry them (`internal/theme/validate.go:55` sets `Token{Name: ref.Name, …}`), so `tintLabel` could render `tok.Name` and be immune to a token rename, at the cost of populating `Name` in `swatchTestPalette` (`swatch_test.go:13`). Only a partial win — the pairing captions at `:118`/`:125` stay literal either way — so this is a judgement call about whether half-covering the drift is worth losing the "the label is exactly what a user types" property the comment claims.
