TASK: theming-system-17-14 — Rename `internal/sourceguard` to the test-only convention

ACCEPTANCE CRITERIA:
1. The package is named `sourceguardtest` and every importer compiles.
2. No non-test file in the tree imports it.
3. The three exported functions' signatures and behaviour are unchanged.
4. `go test ./...` and `go test -tags integration -p 1 ./...` are green, and every guard that used it still runs in the unit lane.

STATUS: complete

SPEC CONTEXT: No specification section governs this task. A grep of `specification.md` for `sourceguard` / `portalbintest` / `GoSourceFiles` returns nothing (the single `test-only` hit at :1671 is about the theme-swap entry point, unrelated). This is an architecture-remediation item raised by the plan's cycle-7 analysis phase, and its governing context is CLAUDE.md's test-helper package inventory (CLAUDE.md:85) plus the repo convention that a test-only helper package's name ends in `test` so the boundary reads at the import line. It is the third and last move in a chain the plan itself created: 15-6 added `ForEachFuncCall` to `portalbintest`, 16-5 (d4a6abf3) extracted the three source-scanning primitives into `internal/sourceguard`, and this task (ea6d95e5) renames that package to the `*test` convention. Judged against the current tree with that chain in force.

IMPLEMENTATION:
- Status: Implemented
- Location:
  - `internal/sourceguardtest/doc.go:1-13` — package charter (the one genuinely new file; the old 6-line `internal/sourceguard/doc.go` was deleted and rewritten).
  - `internal/sourceguardtest/gosourcefiles.go:13` — `GoSourceFiles(root string) ([]string, error)`, unexported `excludedGuardDir:36`.
  - `internal/sourceguardtest/packagegofiles.go:15` — `PackageGoFiles(dir string, includeTests bool) ([]string, error)`.
  - `internal/sourceguardtest/foreachfunccall.go:11` — `ForEachFuncCall(file *ast.File, visit func(funcName string, call *ast.CallExpr) bool)`.
  - Commit `ea6d95e5`, 30 files: git records the six code files as renames under `internal/{sourceguard => sourceguardtest}/`.
- Notes:
  - **Criterion 1 (name + importers).** Package clause is `sourceguardtest` in all four production files and `sourceguardtest_test` in all three test files (external test package, as before). All 21 importing files use the new import path and the `sourceguardtest.` qualifier; no file anywhere in the tree still references the `internal/sourceguard` path or a bare `sourceguard.` qualifier (checked across `*.go`, `*.md`, `*.yml`/`*.yaml`, excluding the `.workflows` review-history prose that legitimately narrates the old name). Every importing file uses the qualifier at least once, so no importer carries an unused import. Signatures are unchanged, so no call site's arguments could have gone stale. Compiles by reading; I did not execute the suite.
  - **Criterion 2 (no production importer).** Zero non-`_test.go` files import it. Verified both after (working tree) and before (`git grep` at `ea6d95e5~1` for `internal/sourceguard` — also zero), which is Do step 5's "before and after" check.
  - **Criterion 3 (signatures + behaviour unchanged).** Diffed each pre/post file pair: `foreachfunccall.go`, `gosourcefiles.go` and `packagegofiles.go` differ by exactly one line each — the package clause. Function bodies, parameter lists, return types, the `excludedGuardDir` exclusion set (dot-directories, `vendor`, `node_modules`), `PackageGoFiles`' empty-match error, and `ForEachFuncCall`'s whole-`FuncDecl` traversal and whole-walk stop semantics are byte-identical. This is a pure rename, exactly as the task scoped it (Do step 3, "no signature changes").
  - **Criterion 4 (lanes).** The package carries no build tag and imports stdlib only (`fmt`, `io/fs`, `os`, `path/filepath`, `strings`, `go/ast`). All 18 external consuming guard files are untagged — I checked each individually — so every guard it serves still runs in the unit lane and nothing moved lane. Its own three test files are plain untagged unit-lane `sourceguardtest_test`.
  - **Consumers re-pointed correctly** across the five consuming packages the task named — `cmd` (3 files), `internal/capture` (1), `internal/prefs` (1), `internal/theme` (5), `internal/tui` (9).
  - **Collateral kept in step.** `internal/tui/pagepreview_surface_audit_test.go:201` — the "no new package" surface-audit allow-list entry was updated from `sourceguard` to `sourceguardtest`, so the audit stays honest rather than silently failing or being widened. `CLAUDE.md:85` reads `restoretest / tmuxtest / portalbintest / sourceguardtest / transienttest` and describes the package accurately (three helpers, stdlib-only, untagged, row-level "production code must not import these").
  - **Convention match confirmed.** The ten test-only helper directories under `internal/` are now uniformly `*test`-suffixed (`logtest`, `portalbintest`, `portaltest`, `restoretest`, `sourceguardtest`, `spawntest`, `statetest`, `themetest`, `tmuxtest`, `transienttest`), so the doc comment's "as it does for every sibling helper package" holds.
  - **Do step 4 (record why not folded back into `portalbintest`)** is honoured in placement — the rationale is in `doc.go:10-12` where it cannot be lost — but its second clause is factually wrong against this tree. See the first non-blocking note.

TESTS:
- Status: Adequate
- Coverage: The package's own three test files moved with it intact — `gosourcefiles_test.go` (enumerate-under-root including `_test.go` sources; the exclusion rule with `vendorish`/`node_modulesish` near-miss directories so a naive substring implementation would fail; missing-root error), `packagegofiles_test.go` (production-only vs. with-tests enumeration, missing-dir error, the anti-vacuity empty-match error), `foreachfunccall_test.go` (calls at depth in arguments/closures/methods attributed to the top-level declaration; bodyless declaration yields nothing rather than panicking; `false` ends the whole walk, not just the current declaration). Diffing each against its pre-rename version shows only the package clause, import path and call qualifier changed — every assertion, fixture and want-value is untouched, and the local helpers (`writeFiles`, `relFiles`, `packageFixture`, `packageRelFiles`, `parseSource`, `callName`) moved with them rather than being left orphaned behind.
- Notes: No new test is warranted and none should be added — for a pure rename the compiler is the test, and the correct evidence is that the moved suite is unchanged (it is). The 18 consuming guards remain the behavioural coverage of the primitives. Not over-tested: the moved suite targets exactly the three failure modes that matter for helpers whose failure mode is silence (wrong enumeration, silent zero-match, missed AST node). Per my instructions I judged by reading and did not run either lane.

CODE QUALITY:
- Project conventions: Followed. The rename is the point of the task and it lands the package squarely in the repo's documented convention — CLAUDE.md's test-helper inventory, the `*test` suffix carrying the boundary at the import line, stdlib-only and untagged so unit-lane guards stay in the unit lane. The single-canonical-declaration rule for guard scaffolding is preserved (one home for the three primitives, no fork left behind in `portalbintest`, whose `build_test.go` still covers only `ProjectRoot` and `StagePortalBinary`).
- SOLID principles: Good. The package holds one responsibility (reading the repo's own `.go` files for guards) and is correctly kept separate from `portalbintest`'s compile-and-stage responsibility — that separation is the right call even though the doc comment argues for it on the wrong grounds.
- Complexity: Low. No control flow changed at all.
- Modern idioms: Yes — unchanged from the pre-rename code (`filepath.WalkDir` with `fs.SkipDir`, `os.ReadDir`, `parser.SkipObjectResolution` in the tests).
- Readability: Good, and improved at the call site: `sourceguardtest.PackageGoFiles(...)` states the test-only boundary where a reader meets it. The rewritten `doc.go` is longer than the one it replaced but each paragraph earns its place (what it is / the boundary / why it is not folded away).
- Issues: One comment-accuracy defect in the newly-authored `doc.go` (below), plus one inaccuracy carried over unchanged in a moved file. Neither affects behaviour.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/sourceguardtest/doc.go:10-12 — the "why not fold back into portalbintest" rationale's second clause is falsified by this tree. It claims portalbintest "builds binaries, which a package serving unit-lane guards must not drag in", but `internal/portalbintest` is itself untagged and stdlib-only (`build.go:1-3`: "any test package can import it"), and four unit-lane guards already import both packages side by side for `ProjectRoot` — `internal/prefs/appearance_api_guard_test.go`, `internal/theme/broken_builtin_test.go`, `internal/theme/loader_construction_guard_test.go`, `internal/theme/slug_collapse_guard_test.go`. There is no lane or dependency hazard to avoid; the real and sufficient reason is cohesion. Since the paragraph exists so the alternative is not re-litigated, it must not rest on a premise a reader can disprove in one grep. Replace the three lines with: "// These primitives stay here rather than folding back into portalbintest: guards\n// across several packages share them, and portalbintest is scoped to compiling and\n// staging the CLI binary — a separate concern from reading source, so the two stay\n// separate packages."
- [do-now] internal/sourceguardtest/gosourcefiles.go:11-12 — carried over unchanged by the move and still inaccurate: "Dot-directories, vendor and node_modules are skipped: Go's own tooling ignores them" is true of dot-directories and `vendor` but not of `node_modules`, for which the toolchain has no rule at all. Replace the justification clause with the true one: "// GoSourceFiles returns every .go file under root, test sources included, as\n// paths joined onto root. Dot-directories, vendor and node_modules are skipped —\n// one rule, shared by every repo-wide guard, so none can be narrower than its\n// siblings by accident." (Pre-existing, first raised in report-13-3 and still unfixed; re-reported because this task moved the file and the note did not get actioned.)
- [quickfix] no file — this task's stated outcome is that "the import line itself says test-only", but nothing mechanically enforces it: the boundary is now name + doc comment, and the three exported functions take plain strings rather than `*testing.T` first, so the structural enforcement the sibling packages get is absent here by design (the task explicitly forbade signature changes). Add a unit-lane source guard — driven by this package's own `GoSourceFiles` — asserting that no non-`_test.go` file imports any package under `internal/` whose name ends in `test`. That closes the gap for all ten helper packages at once, not just this one. (Repo-wide gap predating this task; also raised in report-13-14 — dedupe against that.)
