TASK: theming-system-8-17 — Panel Chrome Revision — Page-Matched Vertical Rhythm, Inner Gutter And A Wider Ladder

ACCEPTANCE CRITERIA:
1. `Themes` renders on the same terminal row as the page's `Sessions N`, asserted on the composed frame.
2. The panel's first list row renders on the page's first session row, and the two lists stay in step for every subsequent row.
3. The panel's header rule renders on the same row as the page's title rule.
4. The header region above the rule renders no content.
5. The left border is absent on every row above the header rule and present on every row from it down (asserted per row).
6. The `Themes` label, the list rows and the key list all sit two cells in from the left border; no surface sits at one.
7. `themePanelPreferredWidth` is 34 and `themePanelMinWidth` is 27, with task 8-11's ladder shape otherwise unchanged.
8. `themePanelMinHeight` is derived from the measured header rather than a literal, and grows by exactly the rows the header gained.
9. Task 8-13's entry gate refuses at the new floor and admits one row above it.
10. Every existing panel test that pins a row offset, a width or the floor moves with the change, and none is weakened to pass.
11. Both task 8-15 fixtures are re-captured, each verified as a fresh write.

STATUS: complete

SPEC CONTEXT:
§9.1 specifies the slide-over as a non-blanking surface inside the content region: canvas body, `border` left border, `Themes` header label in `accent.mode` above a one-row `border` rule, a vertical keymap footer, and a single-row message slot. §9.8 owns the width ladder and the minimum-height rule; §13.3 owns the fixture capture discipline (silent-write mitigation). This task re-cuts the panel's vertical rhythm so its rows and the page's are the same terminal rows, adds the inner gutter, starts the left border below the rule, and (at the time) widened the ladder. The reference frame `testdata/vhs/reference/theme-panel-chrome-revision-mv.png` is the agreed design; §9.1's token table remains the authority, and the frame's `#0C0C16`/`#2B3050` literals stay unadopted (confirmed: no such hex anywhere in `internal/tui`).

IMPLEMENTATION:
- Status: Implemented. Two criteria (7 and 8) were deliberately superseded by later plan tasks; the rest stand in the current tree exactly as specified.
- Location:
  - `internal/tui/theme_panel_geometry.go:30-71` — `themePanelHeaderShape` (`ruleRow`/`labelRow`/`rows` + `borderFrom()`), `themePanelPageAlignedHeaderShape()` measuring off `headerBand` / `renderHeaderBlock` / `sectionHeaderBlockRows()`, and `themePanelHeaderShapeFor` selecting page-aligned vs compact by available height.
  - `internal/tui/theme_panel_geometry.go:20-21,138-140` — `themePanelGutterWidth`, charged with the border exactly once in `themePanelInnerWidth`.
  - `internal/tui/theme_panel_render.go:35-41` — `themePanelHeaderBlock`: rule at `shape.ruleRow`, label at `shape.labelRow`, every other header row blank; rule rendered at the panel's own **outer** width so it spans the border column and continues the page's rule to the frame edge.
  - `internal/tui/theme_panel_render.go:59-81` — `themePanelBlock`: rows below `borderFrom` get `panelFrameSide` + `spaces(gutter)`; rows above it are padded to the full panel width with no border.
  - `internal/tui/model.go:2831-2836` — the panel is rendered at `contentH` and composited at content-region Y=0, which is what makes the row-for-row alignment structural rather than incidental.
  - Verified against the render: page header block = band(0) / rule(1) / blank(2), section header(3) / gap(4), first session row(5); page-aligned panel shape = ruleRow 1, labelRow 3, rows 5 → first theme row at 5. The rule glyph (`▁`) is unique to that lane — the footer separator uses `▔` — so the alignment cannot be satisfied by a coincidental second rule.
  - Visual check: `testdata/vhs/theme-panel-adaptive-pair.png` matches the reference frame's structure — unbroken header band and full-width rule, `Themes` on the `Sessions N` row, first theme row on the first session row, border starting below the rule, content inset from the border.
- Notes (supersessions, not drift):
  - Criterion 7 — `theming-system-13-11` ("Reconcile the panel width ladder with the specified column band", commit `e6d03898`) reverted the ladder to `themePanelPreferredWidth = 30` / `themePanelMinWidth = 24` and replaced the `contentW/2` clamp with the two-stage preferred-affordance ladder now documented in CLAUDE.md. The width criterion is therefore correctly *not* met by the current tree; the analysis cycles recorded the decision, the test moved with it (`TestPanelChrome_WiderLadder` → `TestPanelChrome_LadderEnds`, pinning 30/24), and all "~27–34 column" comments this task introduced are gone (grep finds no such figure anywhere).
  - Criterion 8 — `theming-system-15-2` ("Stop the panel's blank page-alignment rows from raising the height refuse threshold") split the header into two shapes: the floor and entry gate now charge `themePanelCompactHeaderRows = 2` (`themePanelMinHeight`, `:107-109`), while the page-aligned five-row shape renders only where the height affords it (`themePanelHeaderShapeFor`, `:61-67`). This intentionally reverses this task's "the floor grows with the header" clause so a short terminal is not refused a renderable panel. The compact 2 is a definition, not a restated measurement — `themePanelCompactHeaderShape()` builds exactly those two rows from the same constant — so the anti-literal discipline this task established is intact.
  - Criterion 11 — both fixtures were re-captured in this task's commit (`7d2c4df1`, both PNGs show binary deltas) and again downstream at `13-11`/`13-12`, so the set is currently consistent with the shipped geometry rather than stale at this task's numbers.

TESTS:
- Status: Adequate.
- Coverage: `internal/tui/theme_panel_chrome_test.go` carries all ten named tests (one renamed with the width supersession). They open the panel through a real `t` keypress (`openPanelForTestWithSessions`) and assert on `ansi.Strip(m.View().Content)` sliced to the content region — i.e. on the **composed frame**, which is the edge case this task named as the reason task 8-6's two-row header passed everything it had. `chromeFrame` also fails if any frame row is not exactly `termWidth` cells.
  - Criterion 1: `TestPanelChrome_LabelSharesTheSectionHeaderRow` (:112).
  - Criteria 2: `TestPanelChrome_ListsStartOnTheSameRow` (:130) and `TestPanelChrome_ListsStayInStep` (:147) — the latter walks all five sessions/rows, so a one-off alignment cannot pass it.
  - Criterion 3: `TestPanelChrome_RulesShareOneLane` (:167) — `chromeRuleRow` fails if *two* content rows carry the rule glyph (the two-lane failure) and the assertion demands the glyph across all content columns.
  - Criterion 4: `TestPanelChrome_HeaderRegionIsEmpty` (:178), with a fixture guard that fails if the rule sits on row 0 (nothing to assert).
  - Criterion 5: `TestPanelChrome_BorderStartsBelowTheRule` (:194) — per row, with the rule row itself required to carry the rule glyph in the border column, so a partial application fails.
  - Criterion 6: `TestPanelChrome_InnerGutter` (:215) — the blanket loop asserts column 1 is blank on every row below the rule (message and directory rows included), plus targeted sub-tests for the label, the cursor row (`▌` at column 2), an unselected row (column 2 + `leftBarColumnWidth`) and every key-list row.
  - Criterion 7 (as amended): `TestPanelChrome_LadderEnds` (:264) pins 30/24; the ladder's *shape* is pinned by `TestPanelGeometry_WidthLadder` (:14, including "exactly one step across the range") and `TestPanelGeometry_WidthFloor` (:90) — the sub-tests 13-11 deleted from the chrome suite have live equivalents there, so nothing was dropped on the floor.
  - Criteria 8/9: `TestPanelChrome_FloorFollowsTheHeader` (:297) derives the floor independently of the production arithmetic and additionally pins that `themePanelHeaderRows` returns the page-aligned cost at the affordance and the compact cost one row below it, then renders at the floor and proves the footer was not cut. `TestPanelChrome_EntryGateFollowsTheFloor` (:335) refuses one row below and admits at the floor. `TestPanelGeometry_HeaderShapeFollowsTheHeight` (:888) covers the same boundary from the render side across three heights above the affordance and one below.
  - Criterion 10: existing pins moved rather than weakened — `wantPanelHeaderRows` (geometry test :104) is written out deliberately ("a floor asserted against whatever the header measures pins nothing"), and the geometry suite's row-offset assertions were re-pointed at `panelPageHeaderRows()` / the compact band.
- Notes: The alignment suite runs at a single content geometry (96×26). That is defensible — the page-aligned shape only exists above the affordance and the compact band has its own coverage in `TestPanelGeometry_HeaderShapeFollowsTheHeight` — but a second width would be the only thing that could catch a width-dependent regression in the rule/label lanes. Not worth adding on its own evidence. One small redundancy is noted below.

CODE QUALITY:
- Project conventions: Followed. No raw hex at any call site (all surfaces resolve through tokens, so the colour-literal and swap-and-diff guards stay carve-out free); no `t.Parallel()`; no spec-section or task-id references left in production comments (grep for `§` / `task 8-` across the panel files returns nothing) — the comment-standard pass has already been through these files.
- SOLID principles: Good. The header shape is decided in exactly one predicate (`themePanelHeaderShapeFor`) and consumed by the floor arithmetic, the list-size arithmetic and the renderer, so reserved rows are by construction the rows that render. `themePanelChromeRows` is the single arithmetic the floor and the body budget share. The gutter is charged once in `themePanelInnerWidth`/`themePanelBlock`, so no renderer (delegate, footer, message) knows the panel has a gutter — `theme_row.go` composes purely against the width it is handed.
- Complexity: Low. `themePanelBlock`'s single loop with the `borderFrom` branch is the whole border-start mechanism; the header block is an indexed three-line assembly.
- Modern idioms: Yes — `max`/`min` builtins, `for i := range n`, small value types.
- Readability: Good. Every non-obvious decision carries a why-comment (rule-then-label ordering, the rule spanning the border column, "empty counts zero, not lipgloss.Height's 1").
- Issues: Two comment-accuracy nits, listed below; neither affects behaviour.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/tui/theme_panel_geometry.go:20 — the comment reads "Matches the page's Hinset" beside `themePanelGutterWidth = 1`, but `Hinset` is 2 (model.go:2736); it is border + gutter together that match it. Replace with: "// Border plus gutter equal the page's Hinset, so panel content sits the same inset from its border as page content does from the frame edge; charged once in themePanelBlock."
- [do-now] internal/tui/theme_panel_geometry.go:61 — `themePanelHeaderShapeFor` passes `themePanelKeymap()` (the standing scope) while `themePanelListSize` reserves against the live `themePanelFooterScope(p.message)`, so the header shape deliberately does not flip when the confirm's shorter footer frees a row. `themePanelMinHeight` documents the same choice for the floor (`:106-107`); this site does not. Append to its comment: "The standing keymap scope, never the live one: under the confirm's shorter footer the shape would otherwise flip and the header would jump mid-answer."
- [do-now] .workflows/theming-system/specification/theming-system/specification.md:954 — §9.1 still states "The header therefore costs **two rows**, which is what §9.8's minimum-height rule (header + footer + one row) resolves against", which the shipped page-aligned header (page header block + section header) contradicts for the render, and the spec nowhere records the inner gutter or the below-the-rule border start. This task's own edge-case note deferred the amendment to Phase 10, and no Phase 10 task took it (10-7/10-8/10-9 amend the MV spec's keymap, doctor and log vocabulary only). Replace the sentence with: "The panel's header region is measured off the page's own header and section-header blocks, so `Themes` lands on the `Sessions N` row and the panel's first row on the page's first session row; below the height that affords those alignment rows the header degrades to a compact two-row form (rule then label), and it is that compact cost — never the page-aligned one — that §9.8's minimum-height rule resolves against." Prior analysis cycles discarded the *width* half of this divergence as spec-process work after 13-11 restored the 24–30 band; the header half is still outstanding and is now the only place the two artefacts disagree about the panel's anatomy.
- [quickfix] internal/tui/theme_panel_chrome_test.go:301-307 — `TestPanelChrome_FloorFollowsTheHeader`'s first two assertions (`themePanelMinHeight(entries, false)` and the directory-inclusive variant) restate `TestPanelGeometry_HeightFloorArithmetic:117-123` against the same independently-derived arithmetic. Drop those two assertions from the chrome test and keep its distinctive half — the `themePanelHeaderRows` affordance boundary and the render-at-the-floor footer check — so the floor's composition is pinned in one place.
