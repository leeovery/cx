TASK: theming-system-3-1 (tick-a23d24) — Thread The Active Theme Through Every Renderer And Delete internal/tui/theme

ACCEPTANCE CRITERIA:
1. `internal/tui/theme` does not exist; no file in the repo imports it; `grep -r "theme.MV\|ColorFor\|theme.Mode"` returns nothing.
2. Exactly two classes of intended render delta (dark footer rule `#20232E` → `#292E42`; any light value §7.7 moved). Byte-exact goldens pass without expectation edits outside those two classes.
3. Dark palette SGR byte-identical despite uppercase canonicalisation.
4. An existing `"appearance": "light"`/`"dark"` pin still paints the equivalent built-in from frame one; `auto` still runs the detect-or-timeout gate with a dark no-answer fallback.
5. `NO_COLOR` renders colourless and glyph-backed exactly as today.
6. The model holds the active `Theme`; `internal/tui` declares no package-level `theme.Theme`/`theme.Token` var (guard test); `previewBorderColorToken` is gone.
7. `TestNoRawColourLiteralAtCentralisedSites` passes over the unchanged glob; its exemption wording is deleted rather than re-pointed.
8. `testdata/vhs/*.png` and `*.tape` deleted; `internal/capture` fixtures, `FixtureNames()`, `testdata/vhs/README.md`, `testdata/vhs/reference/` remain.
9. `go build ./... && go test ./...` green; integration lane green; `golangci-lint run` clean.
10. CLAUDE.md's `tui/theme` row corrected and relocated; the `tui` row untouched (3-3 owns it).

STATUS: complete

SPEC CONTEXT:
§3.1 collapses a theme to ONE palette of 19 values that is itself light or dark; §3.2 removes `Token.ColorFor` in favour of `Token.Color()`, removes `theme.Mode` "along with its threading through the render layer", and retires `theme.MV` as an exported package-level value. §3.4 is the load-bearing argument for this task's shape: `theme.MV` was read at ~182 call sites and split frees a parameter slot at exactly those sites, so "the model holds the active `Theme` and passes it where `mode` is passed today" — package-level mutable theme state was rejected because "its entire advantage was avoiding churn Portal is now paying anyway". §2.2 consolidates `border.separator` and `border.footer` into one `border` role with the accepted visual change that the dark footer rule becomes marginally more prominent. §11.2 requires `pagepreview.go`'s package-init `Token` copy be fixed outright while the guard stops it returning. §13.6 requires the colour-literal guard's `theme`-subpackage exemption be deleted, not re-pointed. §13.2 deletes today's images and tapes but NOT the Go fixture definitions or the harness.

IMPLEMENTATION:
- Status: Implemented (commit `7581cd4b`, 255 files, 23 production + 61 test files substituted). Mechanism partly and legitimately superseded by later plan tasks — see "Supersession" below.
- Location:
  - `internal/tui/theme/` — deleted (directory absent; `git ls-tree` at `7581cd4b~1` confirms it existed).
  - `internal/tui/theme_source_guard_test.go:18` (`TestOldThemeSubpackageIsGone`) — repo-wide `.go` walk via `sourceguardtest.GoSourceFiles`, asserting both directory absence and zero importers. The import path is deliberately split across a `+` concatenation (`:15`) so the guard is not itself an importer — a nice touch.
  - `internal/tui/header.go:40,50,70,86` — the shared leaf-paint helpers (`headerStyle`, `headerCanvasBg`, `ruleOfWidth`, `renderHeaderBlock`) now take `th theme.Theme` in the former `mode` position and resolve through `.Color()`.
  - `internal/tui/footer.go:135` — `footerTopRule` renders with `th.Border`, the same token as the title rule (`header.go:72`). `BorderFooter` has no successor anywhere.
  - `internal/tui/model.go:877,897,906,917,937,966,991,2716,2794` — delegates, `bubbles/list` cached styles (`canvasHelpStyles`, `canvasPaginationDots`, TitleBar, both filter inputs), `View`'s `BackgroundColor` and `fillCanvas` all source the theme rather than a mode.
  - `internal/tui/session_item.go:105`, `internal/tui/project_item.go:29` — `SessionDelegate.Theme` / `ProjectDelegate.Theme` replace `.Mode`, rebuilt through the existing `sessionDelegate()` / `applyProjectCanvasMode()` restyle path.
  - `internal/tui/pagepreview.go:198-200` — `previewModel.th` replaces `previewModel.mode`; the package-scope `previewBorderColorToken` is gone (repo grep: zero hits) and `th.AccentMode` is inlined at `:407`.
  - `internal/tui/builtin_themes.go` — the model's theme seed, loaded through `Loader.LoadBuiltin`. No package-level `theme.Theme`/`theme.Token` var anywhere in the package.
  - `internal/tui/colour_literal_guard_test.go` — glob unchanged (`PackageGoFiles(".", false)`, package dir, non-test); the `theme/`-subpackage exemption wording is deleted, not re-pointed.
  - `CLAUDE.md:63` — the `tui/theme` row is deleted from the TUI subtree and re-authored as a `theme` row in the internal-packages inventory (between `prefs` and `alias`), describing the 19 single-palette tokens, `Token.Color()`, `Theme` as a parse result, the embedded `.theme` files, and the auto-enumerating contrast test. The `tui` row is untouched in this commit.
- Verification against the criteria:
  1. PASS. Directory gone, zero importers, zero `theme.MV`/`ColorFor`/`theme.Mode` references. The only grep hits are the incidental substring inside the test helper name `armPanelUnderNoColorForTest`.
  2. PASS, independently verified. I extracted every changed ANSI golden expectation from the commit diff: **exactly one** expectation line changed across all 61 test files — `rename_modal_test.go`'s `wantDark`, where `38;2;32;35;46` (#20232E) becomes `38;2;41;46;66` (#292E42) on the footer rule glyph run and **every other byte of the ~1.2 KB expectation is identical** (same `wantLight`/`wantNoColour`, untouched). §7.7's second class is empty. `edit_modal_render_byte_exact_test.go` gained one line (a theme seed) with its expectations untouched.
  3. PASS, verified by value comparison rather than assertion. I diffed the retired `MV` struct at `7581cd4b~1:internal/tui/theme/theme.go` against `internal/theme/builtins/tokyo-night.theme`: all 19 surviving tokens carry byte-identical DARK values under the §2.4 rename (`TextStrong`→`text.secondary` #A9B1D6, `TextMutedBright`→`text.tertiary` #828BB8, `TextDetail`→`text.muted` #737AA2, `TextDim`→`text.subtle` #535C86, `AccentViolet`→`accent.primary` #BB9AF7, `AccentBlue`→`accent.key` #7AA2F7, `AccentCyan`→`accent.mode` #7DCFFF, `AccentOrange`→`accent.attention` #FF9E64, `StateGreen`→`state.positive` #9ECE6A, `StateRed`→`state.destructive` #F7768E, `BgWarning`→`bg.attention` #241B10, `BgTrack`→`bg.subtle` #26283A, `BorderSeparator`→`border` #292E42, `TextOnWarning`→`text.on-attention` #E8C9A0, and the five unchanged names). `BorderFooter` #20232E is the sole drop. The `.theme` file stores `canvas = #0b0c14` lowercase and `active_theme_test.go:11,16` pins the loaded value as `#0B0C14`, so the loader's uppercase canonicalisation is exercised, and the unchanged golden bytes prove it is SGR-neutral.
  4. PASS as authored (`TestPinnedAppearance_SelectsEquivalentBuiltin` in the 3-1 tree drove `Build(Deps{Appearance: …})` for both pins and asserted the equivalent built-in's canvas from frame one, with `modeResolved()` true). Superseded by 3-2/Phase 5 — see below.
  5. PASS. `internal/tui/colourless_nocolor_test.go:137` `TestColourless_NoTokenReachesTheWriter` asserts a colourless frame emits neither a foreground nor a background colour SGR, with `:147` `TestColourless_StateStaysGlyphDistinct` pinning the `●`/`▌` glyph backing and `:161` pinning structural parity. The carve-out is read before any token resolves (`headerStyle:41`, `noticeBandFgStyle:76`, `panelFrameStyle:58` all return a bare style first).
  6. PASS. `internal/tui/theme_source_guard_test.go:37` `TestNoPackageLevelThemeVar` AST-scans the production glob for a package-scope var holding theme data, and its current form covers three shapes — a `theme.`-qualified type or value, a var typed as a locally declared struct carrying `theme.X` fields, and a var initialised (at any nesting depth, so a style chain counts) by an in-package function returning theme data. That reach was extended in review attempt 2 after the first form was shown to miss `var x = defaultDarkTheme().AccentMode`; the current guard catches it. `previewBorderColorToken` is gone.
  7. PASS.
  8. PASS. The commit deleted 87 PNGs and 44 tapes (flat set plus `testdata/vhs/trail/`), retaining `reference/` (26 files), `README.md` and `LOCK-IN.md`, and every `internal/capture` fixture. Today's `testdata/vhs/*.png`/`*.tape` are the six theme-panel captures added by Phase 8 — expected under the harness's "written as work proceeds" retention rule, not residue from this task.
  9. Not independently executed (verifier does not run tests). Judged green by construction: 16 later phases compile against this substitution and the plan terminates on `impl(theming-system): complete implementation`.
  10. PASS.
- Notes on scope: the commit also deleted `testdata/vhs/trail/` (43 PNGs from the prior Modern Vivid feature), which the task's enumerative keep-list did not name either way. The commit message states the reasoning and records recoverability from git history. Given task 10-10's acceptance is scoped to "no `*.png` or `*.tape` **from this feature**", leaving `trail/` would have stranded it permanently. Correct call, correctly recorded.

SUPERSESSION (not drift — verified against the amended intent per the verifier context):
- The unexported `canvasAppearance` enum this task introduced (`appearance_gate.go`) was replaced by the exported `theme.Member` in task 14-10. The load-bearing property survives intact and is still pinned: `internal/theme/member.go:6-9` declares `MemberDark` first with an in-source comment naming the inversion hazard, and `internal/theme/nomination_test.go:101` asserts the zero `Member` is `MemberDark`. `TestCanvasAppearance_ZeroValueIsDark` died with the type it named.
- The transitional `builtinThemePair` holder and `Model.activeTheme` were replaced by task 3-2's injected `theme.Nomination` and, later, by `themeState.active` (`internal/tui/theme_state.go`). The task explicitly anticipated this ("Task 3-2 replaces the pair holder with the injected nomination"). `TestActiveTheme_SelectedByGateAppearance` and `TestPinnedAppearance_SelectsEquivalentBuiltin` were removed by 3-2, which owns the replacement input path; `assertActiveTheme` survives in `active_theme_test.go:24` reading `m.themeState.active`.
- The `prefs.Appearance` pin this task deliberately left untouched was retired by Phase 5/6 in favour of the three theme keys. That is the planned phase boundary, not a regression of criterion 4.

TESTS:
- Status: Adequate.
- Coverage:
  - The one structural risk of a 182-site mechanical substitution — a token silently mapped to the wrong successor — is covered where it matters: by the single changed golden (which would have shifted many more runs under a mis-map), by the byte-exact and row-anatomy suites re-pointed onto loaded built-ins, and by the value-level equivalence of the two palettes.
  - The named deltas each have a dedicated test: `TestFooterTopRule_UsesBorderToken` (`active_theme_test.go:35`) asserts the rule carries `th.Border`, pins its value at `#292E42`, and asserts the retired `#20232E` SGR appears in neither the rule nor a whole rendered footer — a positive and a negative leg, which is the right shape for a consolidation.
  - The two source guards are non-trivial: `TestOldThemeSubpackageIsGone` walks the whole repo (not just `internal/tui`), and `TestNoPackageLevelThemeVar` covers the three realistic reintroduction shapes rather than only the `theme.`-qualified one.
  - `TestNoRawColourLiteralAtCentralisedSites` retains its glob, so coverage grows with the package.
  - Test helpers are centralised (`testDarkTheme`/`testLightTheme` in `theme_testing_test.go:305,310` delegating to `internal/themetest`) rather than 600 ad-hoc loads, exactly as the task's edge case required, and are deliberately functions not package vars so the test side cannot grow the shape the production guard forbids.
- Not over-tested: the task's list of nine named tests is met by seven surviving tests plus two that died with the mechanisms they named; nothing redundant was added. The migrated suites reuse existing assertions rather than duplicating them per theme.
- Notes:
  - `internal/tui/project_row_anatomy_test.go:56` builds its bold-probe from `testDarkTheme(t)` inside a `for _, th := range {dark, light}` loop instead of the loop's `th`. The assertion at `:57` is about SGR bold (colour-independent), so it is harmless today — but it is a mismatch a reader has to reason about, and the pre-substitution form was mode-matched.
  - `internal/tui/pagepreview_helpers_test.go`'s helper constructs a `previewModel` leaving `th` zero, so those renders are silently colourless where they previously rendered dark. Every consumer strips ANSI so nothing is lost, but it is the same hazard the theme seeds elsewhere close.

CODE QUALITY:
- Project conventions: Followed. No new `t.Parallel()`; guards live in the unit lane and are stdlib-only via `sourceguardtest`; CLAUDE.md updated in the same commit; the substitution respects the "no raw hex at a call site" rule the guard enforces.
- SOLID principles: Good. The task's central design choice — passing the palette as a parameter rather than reading package-level mutable state — is the correct dependency-inversion call and is what makes §11.2's per-frame re-derivation property provable at all. The in-source comment on `Model.activeTheme` in the 3-1 tree recorded the rejection of `theme.Active` and its reason (order-dependent state on the render path in a suite that forbids `t.Parallel()`), which is exactly the kind of decision that needs a durable why.
- Complexity: Low. The substitution is one-for-one; no new branching was introduced. `appearance_gate.go` stayed a small state machine through the change.
- Modern idioms: Yes. `Token.Color()` at the leaf, `theme.Theme` passed by value (small struct of 19 two-field tokens — copying is cheaper than the pointer indirection and keeps the render path immutable).
- Readability: Good, with one systematic residue. The Go identifiers were renamed correctly everywhere, but the human-readable prose was left on the old vocabulary. Task 14-2 later swept the *comments* and added `TestNoRetiredTokenNameInComments` to hold them, which deliberately does not reach string literals — so 152 retired token names survive in test failure messages across 37 files, unguarded.
- Issues: None blocking. Three unused-parameter residues from "swap the slot, don't add one" survived Phase 4's sweep (see notes).

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [quickfix] `internal/tui` test files (37 files, 152 occurrences; e.g. `internal/tui/delete_modal_test.go:22` "state.red" while asserting `th.StateDestructive`, `:63` "text.detail" for `th.TextMuted`, `internal/tui/projects_footer_test.go:59,62,65` "accent.blue"/"text.detail"/"accent.violet", `internal/tui/empty_states_test.go:112` and `internal/tui/multi_select_footer_test.go:121` "border.footer rule role") — replace the retired token names in failure-message string literals with the current role names, and extend `retired_token_guard_test.go`'s scan from `file.Comments` to `*ast.BasicLit` string literals (reusing the existing `retiredTokenNames` map and exemption list) so the strings are held the way the comments now are. Today a failing assertion sends the reader to a token that no longer exists in the vocabulary.
- [quickfix] `internal/tui/panel.go:57` (`panelFrameStyle`), `internal/tui/notice_band.go:68` (`noticeBandTintStyle`), `internal/tui/notice_band.go:75` (`noticeBandFgStyle`) — each takes a `th theme.Theme` its body never reads, threaded in by callers (`panelFrameTop:64`, `noticeBandPadRight:172`). Drop the parameter from these three leaves and from the forwarding call sites. Phase 4's derived-style sweep was expected to take these and did not; `golangci-lint`'s configured set does not flag unused parameters, so nothing else will.
- [do-now] `internal/tui/model.go:991` — rename the local `orange` to `attention`: it names the retired `accent.orange` hue while reading `AccentAttention`, and hue names at call sites are precisely what §2.4's rename removed.
- [do-now] `internal/tui/project_row_anatomy_test.go:56` — change `Foreground(testDarkTheme(t).TextPrimary.Color())` to `Foreground(th.TextPrimary.Color())` so the bold probe uses the loop's theme rather than always the dark one.
- [quickfix] `internal/tui/pagepreview_helpers_test.go:22` (`newPreviewModelForHelpers`) — seed `th` from `testDarkTheme(t)`. It currently leaves the field zero, so the helper's renders resolve through `lipgloss.Color("")` and are silently colourless; consumers strip ANSI so nothing fails, which is what makes the omission worth closing rather than leaving as a trap for the next assertion added there.
