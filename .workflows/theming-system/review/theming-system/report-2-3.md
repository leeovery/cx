TASK: theming-system-2-3 — Auto-Enumerate The Contrast Floors Over The Embedded Set

ACCEPTANCE CRITERIA:
1. Every embedded theme passes every rule; the failure message names the slug, the token, the measured ratio and the floor.
2. No `canvasDark` / `canvasLight` constant exists in the new file — every reference background is `th.Canvas.Value`.
3. No test names a theme; adding a `.theme` file to `builtins/` enrols it with no test edit (proven by the loop over `BuiltinSlugs()`).
4. The enumeration helper fails when the embedded set is empty, so the suite cannot pass vacuously.
5. `TestContrastMath` still anchors black-on-white at 21.00 and self-contrast at 1.00.
6. Comparisons are plain `>=` / `<` against the exact floors — no epsilon, no rounding to two decimals before comparing.
7. `internal/tui/theme`'s tests still compile and pass unchanged.
8. The whole file runs in the unit lane with no tmux, no daemon and no built binary.

STATUS: complete

SPEC CONTEXT:
§13.5 ("Contrast checking") states the canonical, theme-independent rule set because "auto-enumerates" only means something against a complete list: three floors (4.50 normal text, 3.00 large/UI, 1.10 fill-perceptible), every ratio measured against *the theme's own `canvas`* rather than a constant. Foreground-vs-canvas: `text.primary/secondary/tertiary/muted` ≥ 4.50; `text.subtle` banded 3.00–4.49 (floor *and* ceiling); `text.faint` banded > 1.00 and < 3.00; `accent.primary` ≥ 3.00; `accent.key/mode/attention`, `state.positive`, `state.destructive` ≥ 4.50; `border` and `canvas` carry no numeric floor. Tint pairs: `bg.selection` and `bg.attention` three legs each (text-on-tint ≥ 4.50 · bar vs canvas ≥ 3.00 · fill vs canvas ≥ 1.10), `bg.subtle` fill only. Foreground-on-tint ≥ 4.50 for `text.on-selection`, `text.secondary`, `text.tertiary`, `state.positive` on `bg.selection` and `text.on-attention` on `bg.attention`. `state.positive` carries a named dual clearance (canvas *and* `bg.selection`) — the leg that caught the Nord green. §13.6 names the ten floor carriers that must be rewritten (they read `theme.MV.<Field>`, address `.Dark`/`.Light`, run `/dark` + `/light` subtest pairs and measure against `canvasDark`/`canvasLight` — all four removed by this feature) and records `TestContrastMath` as the one genuinely untouched member. §6.4: the bundled tier must be valid *and good*, with no relaxation for a named palette. §7.4's Nord table is a per-palette verification record, not the rule set.

IMPLEMENTATION:
- Status: Implemented (criterion 7 intentionally superseded — see Notes)
- Location: `/Users/leeovery/Code/portal/internal/theme/contrast_test.go` (266 lines, package `theme_test`)
  - Math port: `relativeLuminance` (`internal/theme/contrast_test.go:25`) uses `go-colorful`'s `LinearRgb()` + the 0.2126/0.7152/0.0722 weighted sum; `contrastRatio` (:36) is `(L_lighter+0.05)/(L_darker+0.05)`. Both match WCAG 2.x exactly; `go-colorful v1.4.0` is a direct module dependency (`go.mod:11`), so the linearisation the plan measured against is preserved and the hair's-breadth legs (`nord state.positive` on `bg.selection` = 4.500345 etc.) are not at risk from an implementation swap.
  - Enumeration helper: `embeddedThemes` (:223) loads every `theme.BuiltinSlugs()` entry through `Loader.LoadBuiltin` using `theme.NewSilentLoader()` — which is exactly `NewLoader(NewEventLogger(log.Discard()))` (`internal/theme/load.go:43-45`), i.e. the `log.Discard()`-backed event logger the task specified, reached through the constructor a later task introduced. It fatals on an empty slug set (:227-229), on `!found` (:236) and on any rejection (:239), so a broken or missing built-in fails loudly rather than silently shrinking the enrolled set.
  - Floors as constants (:15-21): `floorNormal = 4.5`, `floorLargeUI = 3.0`, `floorFillPerceptible = 1.10`, plus `ratioIdentity = 1.0` for `text.faint`'s lower band edge.
  - All ten §13.6-named carriers present and correctly renamed: `TestForegroundFloorAgainstOwnCanvas` (:67), `TestTextSubtleBand` (:96), `TestTextFaintDecorativeBand` (:108), `TestBgSelectionPairRule` (:120), `TestBgAttentionPairRule` (:132), `TestInlineFlashAttentionPairClearsFloor` (:144), `TestPreviewPeekChromeClearsFloorAgainstCanvas` (:155), `TestBgSubtlePairRule` (:178), `TestForegroundOnTintPairings` (:188), `TestStatePositiveClearsCanvasAndSelection` (:212). Plus `TestContrastMath` (:46) and the enrolment assertion `TestFloorsEnumerateTheEmbeddedSet` (:56) — 12 test functions, matching the task's list one-for-one.
  - Every carrier is a `for slug` loop wrapping the token rules, with `t.Run(slug + "/" + token)` inside, exactly as specified.
- Rule-set fidelity: I walked §13.5 leg by leg against the file. Every rule is encoded with the correct floor, including the two that are easy to get wrong — `accent.primary` takes `floorLargeUI` while every other accent takes `floorNormal` (:80-83), and `text.subtle`'s band asserts *both* edges (:102-103) so the light-only ceiling is now generalised to every theme. `text.faint`'s band is `> 1.00` (strict, via `assertAbove`) and `< 3.00` (:114-115), so reaching the UI floor is a failure as the spec requires. `border` and `canvas` appear in no floor assertion. The deliberately-omitted `text.primary` on `bg.selection` leg (§7.4's free-information figure) is correctly absent from `TestForegroundOnTintPairings`' pairing list (:197-201).
- Notes:
  - **Criterion 7 is superseded, not violated.** `internal/tui/theme` no longer exists — Phase 3's task "Thread The Active Theme Through Every Renderer And Delete internal/tui/theme" removed it, which the task itself anticipated ("it still guards the shipping render layer until Phase 3"). The swap is clean: the ten names ported over, and `grep` finds no `canvasDark` / `canvasLight` / `canvasHexFor` anywhere in the repo, so nothing was left behind.
  - Criterion 2 holds repo-wide, not just in the new file. Every reference background in the file is `th.Canvas.Value` or a sibling token from the same `th`.
  - Criterion 3 holds: `contrast_test.go` contains no theme slug. The only literal hexes are `#000000`/`#FFFFFF`/`#737AA2` inside `TestContrastMath`, which is the verbatim port the task mandated and is theme-independent (self-contrast). The `internal/tui` colour-literal guard is scoped to `internal/tui` and only inspects `lipgloss.Color(<literal>)` calls, so these are legitimately out of its scope.
  - Criterion 1's message shape is met on every path: `assertAtLeast`/`assertAbove`/`assertBelow` (:246-265) print `"<slug> <token> <leg>: <fg> on <bg> = %.4f, want >= %.2f"`, and the enclosing subtest is `slug/token`. The role-annotated legs (e.g. `"nord preview chrome frame + marker (accent.mode) vs canvas"`) still carry both slug and token.
  - Criterion 6 holds: `assertAtLeast` errors on `got < floor` (pass iff `>=`), `assertBelow` errors on `got >= ceiling` (pass iff `<`), `assertAbove` errors on `got <= floor` (pass iff `>`). The `%.4f` rounding is in the message only — never in a comparison. No epsilon anywhere except inside `TestContrastMath`'s own sanity anchors, which is the ported original.
  - `TestFloorsEnumerateTheEmbeddedSet`'s `slices.Equal(sortedKeys, theme.BuiltinSlugs())` is sound because `BuiltinSlugs()` sorts after extension-stripping (`internal/theme/builtins.go:41-55`).
  - Task 2-6's `light_pins_test.go` reuses `embeddedThemes`, `assertAtLeast` and `floorFillPerceptible` from this file rather than re-declaring them, so the two halves of §13.5 share one enumeration and one comparison primitive. No duplicate helper declarations exist in the package (checked across the repo).

TESTS:
- Status: Adequate (this task *is* the test)
- Coverage: All 12 named micro-acceptance tests exist. The three embedded themes (`nord`, `tokyo-night`, `tokyo-night-day`) enrol automatically through `BuiltinSlugs()`, so 2-5/2-7's palettes landed already floor-checked. The two vacuity guards the task called for are both present and independent: `embeddedThemes` fatals on an empty embedded set, and `TestFloorsEnumerateTheEmbeddedSet` fatals on an empty enrolled set. `TestContrastMath` is the anchor that stops every floor assertion passing on broken math — without it, a luminance bug that returned 0 for everything would make every `assertAtLeast` pass.
- Notes:
  - **Deliberate, spec-mandated redundancy** — several legs are asserted more than once: `text.on-attention` on `bg.attention` appears in `TestBgAttentionPairRule`, `TestInlineFlashAttentionPairClearsFloor` and `TestForegroundOnTintPairings`; `state.positive` vs canvas appears in `TestForegroundFloorAgainstOwnCanvas` and `TestStatePositiveClearsCanvasAndSelection`; `text.primary`/`text.muted`/`accent.mode` vs canvas are re-asserted by the preview-chrome carrier. This is over-tested in the narrow numeric sense, but §13.6 names ten carriers and §13.5 says the enrolment assertion "composes with all ten" — each carrier is the named record of a rule at a render site, and the task explicitly required the one-to-one name mapping so Phase 3's deletion would be a clean swap. Not a finding.
  - `TestFloorsEnumerateTheEmbeddedSet` is tautological *today* (the map is built from `BuiltinSlugs()`, so its key set cannot differ), but it is not worthless: it is the pin that fails the moment someone replaces the enumeration with a hardcoded slug list. Correct as specified.
  - Where the stricter of two overlapping floors applies, the stricter one is present: `accent.attention` takes 4.50 vs canvas in the foreground carrier and 3.00 as `bg.attention`'s bar leg, so the pair rule cannot mask a foreground regression.
  - Lane compliance: no build tag, no `t.Parallel()` (per CLAUDE.md's prohibition), imports are `maps`/`math`/`slices`/`testing` + `internal/theme` + `go-colorful`. No tmux, no daemon, no built binary — pure unit lane.
  - I assessed adequacy by reading only; per my instructions I did not execute the suite, so the numeric pass/fail of the shipped palettes against these floors is not independently re-measured here.

CODE QUALITY:
- Project conventions: Followed. Black-box `theme_test` package, no `t.Parallel()`, named subtests, table-shaped rule slices with a `t.Run` per row, error messages in the codebase's `got/want` idiom. Comments carry no task ids, phase numbers or spec-section references (checklist rule respected) — the "why" is expressed in plain terms ("deliberately not 3:1", "reaching the UI floor is a failure" is encoded structurally rather than as prose).
- SOLID principles: Good for a test file — one comparison primitive per relation (`assertAtLeast` / `assertAbove` / `assertBelow`), one enumeration source, floors named once.
- Complexity: Low. Every carrier is a flat loop over themes × rules; no branching beyond the comparisons.
- Modern idioms: Yes — `slices.Sorted(maps.Keys(...))` for deterministic subtest ordering, Go 1.22+ per-iteration loop variables (module is `go 1.26.0`), so the closures capturing `th`/`rule`/`slug` are safe.
- Readability: Good. Rule tables read as the spec's tables; the leg strings are self-describing in failure output.
- Issues: Only the non-blocking notes below.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/theme/contrast_test.go:20 — the const block's doc comment describes three floors but the block declares four names; add a line above `ratioIdentity`: `// ratioIdentity is self-contrast — text.faint must sit strictly above it to be visible at all.`
- [quickfix] internal/theme/contrast_test.go:67-221 — all ten carriers repeat the identical three-line preamble (`themes := embeddedThemes(t)` / `for _, slug := range slices.Sorted(maps.Keys(themes))` / `th := themes[slug]`). Extract `forEachEmbeddedTheme(t *testing.T, fn func(t *testing.T, slug string, th theme.Theme))` and call it from each carrier; the ten spec-named functions and their `slug/token` subtest names stay exactly as they are, and ~30 lines of identical scaffolding go away.
- [idea] internal/theme/contrast_test.go — the suite auto-enumerates *themes* but not *tokens*: a 20th token added to `Theme.fields()` would carry no floor and nothing would fail. Consider a coverage assertion that every name in `theme.TokenNames()` is either exercised by at least one rule or listed in an explicit no-floor exemption set (`border`, `canvas`, and the two `text.on-*` pairings which are only measured on their tints). Needs a design decision on how rules declare which token they cover, which is why this is not a mechanical edit.
