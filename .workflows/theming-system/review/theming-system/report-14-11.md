TASK: theming-system-14-11 — Split `theme_panel.go` along the concern lines its siblings and its tests already follow (tick-8f6034)

ACCEPTANCE CRITERIA:
1. `theme_panel.go` holds state, lifecycle and input routing only; geometry and render/composite live in their own files.
2. No function is renamed, no signature changes and no comment content changes.
3. Every package-file-enumerating guard still covers the moved functions.
4. Rendered output is byte-identical: the capture fixture frames and the swap-and-diff guard pass unchanged.
5. `go build ./... && go test ./...` pass; `golangci-lint run` is clean.

STATUS: complete

SPEC CONTEXT: The specification (`.workflows/theming-system/specification/theming-system/specification.md` §9) governs the slide-over's *behaviour and appearance* (full-height, right-edge, left-border-only, non-blanking overlay, canvas body, chrome re-themes with the previewed theme) and says nothing about source-file layout. This task is architecture-remediation from the analysis cycle (phase 14), not a spec-driven behaviour change; the spec constraint it must not violate is that rendered output is unchanged, which a pure relocation satisfies by construction.

IMPLEMENTATION:
- Status: Implemented (pure relocation, verified line-for-line)
- Location:
  - Commit `0fe2137c` "impl(theming-system): Ttheming-system-14-11 — split theme_panel.go along its concern lines": `internal/tui/theme_panel.go` −490, `internal/tui/theme_panel_geometry.go` +250 (new), `internal/tui/theme_panel_render.go` +263 (new). No test file, fixture or capture file touched.
  - Current state: `internal/tui/theme_panel.go` (409 lines — pinned copy constants, `themePanel` state struct, `newThemePanelList` with its `pinArrowOnlyNav` pin, entry/open/arm/close lifecycle, key routing `updateThemePanel`/`themePanelNavKey`, cursor skipping `skipUnselectableThemeRow`, delegate constructor), `internal/tui/theme_panel_geometry.go` (156 lines — width ladder, header-shape measurement, floor/chrome/list-size arithmetic, geometry constants), `internal/tui/theme_panel_render.go` (148 lines — `renderThemePanel`, header/dir/block/pad renderers, the canvas painter, `overlayThemePanel`, block helpers).
- Verification performed:
  - **Pure-relocation proof**: extracted the 490 deleted lines from `theme_panel.go` and the 513 added lines across the two new files at commit `0fe2137c` and diffed them sorted. Every deleted line reappears verbatim in a new file; the only lines *added* beyond the move are two `package tui` declarations, two `import` blocks and the two new file-level doc comments. No line was dropped, reworded or re-indented — so criterion 2 (no rename, no signature change, no comment-content change) holds exactly, and criterion 4 follows structurally (no render code changed, and the commit touches no `internal/capture` file or fixture).
  - **Placement**: comparing the pre-split declaration list (`0fe2137c^:theme_panel.go`, 1459 lines) against the post-split three-file split shows every function named in the task's Do-1 and Do-2 landed in the intended file — geometry: `themePanelWidthFor`, `themePanelMinHeight`, `themePanelChromeRows`, `themePanelFloor`, `themePanelListSize`, `themePanelInnerWidth`, the header-row helpers and the geometry constants; render: `renderThemePanel`, `themePanelHeaderBlock`, `themePanelDirRow`, `themePanelBlock`, `themePanelPadRow`, `themePanelPainter`/`newThemePanelPainter`/`paint`, `overlayThemePanel`, plus `appendBlock`/`clampBlockHeight`/`blockHeight`. The `overlayThemePanelOnContent` companion stayed in `model.go` where it already lived, which is what the conditional in Do-2 ("if it lives beside it") permits. `theme_panel.go` retains nothing geometric or render-shaped — it calls the arithmetic, never restates it (`grep` confirms single declarations of every moved symbol; no duplicate crept back).
  - **Guard coverage (criterion 3)**: every package-file-enumerating guard in `internal/tui` enumerates dynamically, so the new files are in scope automatically — `colour_literal_guard_test.go:17` (`sourceguardtest.PackageGoFiles(".", false)`, which errors on an empty match so it cannot pass by having stopped looking), `theme_source_guard_test.go:38` (via the same `centralisedColourSites`), `nomination_test.go:260`, `retired_token_guard_test.go:72`, `sessions_grouped_reskin_test.go:227` (`filepath.Glob("*.go")`). The two guards that *do* pin a filename still hold post-split because the mover left their targets behind: `theme_panel_test.go:706` requires the sole `themeRowDelegate` composite literal to be in `theme_panel.go` (it is — `theme_panel.go:403`), and `theme_panel_commit_test.go:215` requires at least one `Model.ApplyTheme` call in `theme_panel.go` (two remain — `theme_panel.go:182` in `applyInForceTheme` and `:389` in `previewSelectedThemeRow`). The theme-flash precedence guard (`theme_flash_precedence_test.go`) is behavioural, not file-scanning, so the move cannot affect it.
  - **Compile-level sanity** (read, not run): both new files' import sets are exactly what their bodies use (`geometry`: lipgloss for the zero-width height measurement + theme for the zero `theme.Theme{}`; `render`: strings, lipgloss, x/ansi, theme), and `theme_panel.go`'s surviving imports (`slices`, `key`, `list`, `tea`, `theme`) are all still referenced.
- Notes:
  - Later plan phases legitimately superseded parts of this task's *artifact* — not its outcome. (a) The two file-level doc comments naming each file's single concern (Do-4) were added by this task and later removed by the repo-wide comment-standard pass `e3fa1503` / `915e7fcb`, which stripped descriptive file headers across all of `internal/tui`. Re-adding them would now fight the newer standard, and no acceptance criterion requires them. (b) Phase 15/17 tasks then edited the new files in place as intended homes (`315e0e51` moved the blank alignment rows out of the height floor, `f14ded91` re-pointed the rule row at the shared header renderer, `75c69427` added the exported `ThemePanelMinWidthTerminal`/`ThemePanelFloorTerminalHeight`) — evidence the split took, since each landed in the file named for its concern rather than back in `theme_panel.go`.
  - `CLAUDE.md:176` was updated to name all three files by sibling task 14-14 (`6d2322c8`), so the architecture doc and the tree agree.

TESTS:
- Status: Adequate (correctly, no new tests)
- Coverage: A pure intra-package relocation is verified by the *existing* suite continuing to compile and pass against the moved symbols — `theme_panel_geometry_test.go` (42k), `theme_panel_chrome_test.go`, `theme_panel_keymap_test.go`, the behaviour/commit/confirm/cursor suites and the `internal/capture` panel fixtures all exercise the moved functions unchanged, and the commit touches no test file, which is itself the strongest available evidence that no signature or visibility moved (a changed signature would have forced a test edit). Adding a test that asserts "function X lives in file Y" would be over-testing — and the package already has two such pins (`theme_panel_test.go:706`, `theme_panel_commit_test.go:215`) which I verified still hold.
- Notes: The task's third test line (temporarily plant a raw hex literal in each new file, confirm the colour-literal guard fails, revert) leaves no artifact by design, so it cannot be verified after the fact. It is unnecessary as evidence: the guard's enumeration is `sourceguardtest.PackageGoFiles(".", false)` over the package directory, so coverage of the new files is structural rather than incidental, which I verified by reading the guard. Per the project rules I did not execute the suite; the judgement above is from reading.

CODE QUALITY:
- Project conventions: Followed. The `theme_panel*.go` prefix is kept so the panel's files still group (Do-6); each new file carries one concern, matching the sibling split (`_commit`, `_confirm`, `_message`, `_footer`, `_row`, `_seams`, `_state`) and mirroring the test surface's existing `_geometry` / `_chrome` / `_keymap` names, which is exactly the alignment the task set out to restore. No exported surface was added or changed by the move.
- SOLID principles: Good. The split is the single-responsibility fix itself: `theme_panel.go` drops from 1459 to 409 lines and now owns state + lifecycle + input routing; the layout arithmetic (which draws nothing) and the render/composite stack are each addressable on their own. The residual cross-file coupling — `themePanelChromeRows` (geometry) calling `themePanelDirRowHeight` (render), which measures by rendering a zero-width colourless row — is inherent to measuring chrome off its own renderers rather than off literals, and the codebase deliberately prefers that over restated constants.
- Complexity: Low. No control flow was touched.
- Modern idioms: Yes (unchanged by the move; `max`, `for range n`, `slices.IndexFunc` etc. all survive verbatim).
- Readability: Good. A reader after the width ladder or the render floor now opens `theme_panel_geometry.go`; after the composite, `theme_panel_render.go`. `appendBlock`/`clampBlockHeight`/`blockHeight` have generic names but are used only by the panel family (`theme_panel_render.go` and `theme_panel_message.go:139`), so their placement in the panel's render file is correct rather than a mis-home.
- Issues: None.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- None.
