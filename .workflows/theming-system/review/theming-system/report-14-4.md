TASK: theming-system-14-4 — Single-Source The Per-Package Go Source-Guard File Enumeration

ACCEPTANCE CRITERIA:
- One package-local enumeration exists in `internal/portalbintest`; no test declares its own `os.ReadDir(".")` or `filepath.Glob("*.go")` package scan.
- Every guard fatals rather than passing vacuously on an empty enumeration.
- Test-source inclusion is an explicit argument at each call site, matching what that guard covered before.
- Every guard still fails when its forbidden construct is reintroduced.
- `go test ./...` passes and no test moves lane.

STATUS: complete

SPEC CONTEXT: None — the specification (`.workflows/theming-system/specification/theming-system/specification.md`) has no mention of source guards or the enumeration helper. This is an analysis-remediation task sourced from a duplication finding, so the governing contract is the task text plus the project conventions in CLAUDE.md ("test-only helpers — production code must not import"; the unit-lane rule that no unit-lane test may drag in binary-building or daemon machinery).

IMPLEMENTATION:
- Status: Implemented (helper home later relocated by task 17-14 — not drift)
- Location:
  - `internal/sourceguardtest/packagegofiles.go:15` — `PackageGoFiles(dir string, includeTests bool) ([]string, error)`: non-recursive `os.ReadDir`, dirs and non-`.go` skipped, `_test.go` gated on the flag, error on an unreadable dir and error on a zero-file match. Doc comment states the vacuity rule as the package's decision.
  - `internal/sourceguardtest/doc.go:1` — states the package's scope and why the primitives do not fold back into `portalbintest` (binary-building must not be dragged into unit-lane guards).
  - Converted front ends: `cmd/open_theme_nomination_test.go:243` (`parsePackageFilesByName`; `parseCmdFiles` deleted — no reference survives), `internal/tui/nomination_test.go:252/258` (`parsePackageFilesByName` / `parsePackageFiles(t, fset)`), `internal/tui/colour_literal_guard_test.go:15` (`centralisedColourSites`), `internal/tui/theme_source_guard_test.go:64` (`parseProductionFiles`, names-subset retained as permitted), `internal/tui/theme_panel_test.go:712` (`themeRowDelegateLiteralSites`, inline glob loop deleted), `internal/tui/theme_flash_precedence_test.go:192` (now `parsePackageFiles`; `themeFlashProductionFiles` deleted), `internal/tui/retired_token_guard_test.go:72` (`includeTests=true`), `internal/theme/leaf_guard_test.go:152` (`themeSourceFiles`), `internal/theme/theme_test.go:264` (`exportedSymbols` now projects over `parseThemeSources`; its second ReadDir+parse deleted), `internal/capture/theme_panel_message_fixtures_test.go:324`.
- Notes:
  - The task named `internal/portalbintest` as the home; the commit (8a836cc0) put it there, and later task 17-14 (ea6d95e5) moved the source-scanning primitives to `internal/sourceguardtest`. That relocation is the amended intent (CLAUDE.md documents `sourceguardtest` as the owner of `GoSourceFiles`/`PackageGoFiles`/`ForEachFuncCall`, and `portalbintest`'s surface is now build/stage only, confirmed at `internal/portalbintest/build.go`). Judged against the amended intent, criterion 1's first clause is met.
  - Coverage parity (AC 3) holds at every converted site, checked against the pre-change bodies in the diff: capture/theme/tui colour+flash+panel guards were non-test-only → `false`; `retired_token_guard_test.go` previously globbed `*.go` with no `_test.go` filter → `true`. Explicit at each call site.
  - AC 4 (claims unchanged): the diff moves only enumeration; every forbidden-identifier / forbidden-literal / forbidden-import assertion is byte-identical. Two guards had their parse mode changed from `0` to `parser.SkipObjectResolution` (`themeRowDelegateLiteralSites`, the theme-flash guard). That is safe here — a repo-wide grep finds no guard reading `Ident.Obj`, `file.Scope` or `Unresolved`, which are the only fields the mode suppresses.
  - Stale comments were cleaned with the change (the "It is a GLOB…" prose at `colour_literal_guard_test.go:15` and `leaf_guard_test.go` was rewritten to describe the directory enumeration, and the now-redundant "fails on an empty match" sentences moved to the helper). No comment in the changed code contradicts it.
  - AC 5 (lane): `sourceguardtest` is stdlib-only, untagged, with no `os/exec`; every guard it serves stays in the unit lane. `portalbintest` was also untagged at the time of the commit, so no test moved lane at either step.
  - AC 1's second clause is met for the guard family the task's Do list enumerated, but is not true repo-wide — see the notes below for one in-plan regression and a set of pre-existing non-theming scans the Do list did not cover.

TESTS:
- Status: Adequate
- Coverage: `internal/sourceguardtest/packagegofiles_test.go` pins the enumeration exactly as the task's first test item asks — files found in a fixture (`TestPackageGoFiles_ProductionSources`), subdirectories not descended into (`nested/deep.go` absent from both want lists), non-`.go` skipped (`notes.md`, `README.md`), test sources included/excluded per the flag (`TestPackageGoFiles_WithTestSources`), error on a missing directory (`TestPackageGoFiles_MissingDirErrors`), and error rather than an empty slice on a zero-`.go` match (`TestPackageGoFiles_EmptyMatchErrors`). The fixture helper is shared with `gosourcefiles_test.go`, no redundant restatement of the same case, no mocking. The task's other two test items are one-shot manual verifications (reintroduce each forbidden construct; point a guard at an empty dir); the durable equivalent of the second is `TestPackageGoFiles_EmptyMatchErrors` plus the `t.Fatalf` at each front end, which is the right permanent shape.
- Notes: The helper documents "in filename order" but `packageRelFiles` sorts before comparing, so nothing pins the ordering claim. Minor — `os.ReadDir` already sorts — but the doc claim is currently unverified.

CODE QUALITY:
- Project conventions: Followed. Test-only package with the `test` suffix carrying the boundary at the import line (CLAUDE.md convention); no production file imports it (verified). Stdlib-only leaf, so the unit-lane rule holds. Errors are wrapped with `%w` and name the offending directory, per the error-handling convention.
- SOLID principles: Good. The helper does one thing (enumerate) and leaves parsing to callers, so a guard keeps its own parse mode and FileSet policy; `parseProductionFiles`' named-subset projection composes over it rather than being folded in.
- Complexity: Low. One loop, two guard clauses, two error returns.
- Modern idioms: Yes — `os.ReadDir` (not `ioutil`), `fmt.Errorf`/`%w`, `filepath.Join`. Callers use `slices.Sorted(maps.Keys(...))` for deterministic iteration where order matters.
- Readability: Good. The doc comment states the decision ("a guard that enumerates nothing must fail rather than pass having stopped looking") rather than restating the code.
- Issues: None blocking. The positional `includeTests bool` is opaque at the call sites, and the parse half of the two `parsePackageFilesByName` bodies the task cited as byte-identical is still duplicated — both below.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [quickfix] internal/tui/builtin_theme_table_test.go:63 — `TestNoLocalTwoBuiltinTable` declares its own `os.ReadDir(".")` package scan and errors only on a regex hit, so an empty enumeration passes vacuously — the exact class this task closed, reintroduced by the later task 15-5. Replace the ReadDir with `sourceguardtest.PackageGoFiles(".", true)`, filter the result to `_test.go` basenames, and `t.Fatalf` when that filtered set is empty.
- [quickfix] internal/sourceguardtest/gosourcefiles.go:33 — `GoSourceFiles` returns `(nil, nil)` for a zero-file walk while its sibling `PackageGoFiles` errors, so the vacuity rule is not uniform across the package. Return `fmt.Errorf("no .go files under %s", root)` when `len(paths) == 0`; the guards that would gain the protection are `internal/tui/restore_source_guard_test.go:236`, `internal/theme/slug_collapse_guard_test.go:24`, `internal/theme/loader_construction_guard_test.go:25`, `internal/theme/broken_builtin_test.go:262` and `internal/prefs/appearance_api_guard_test.go:37`.
- [quickfix] cmd/open_theme_nomination_test.go:243 and internal/tui/nomination_test.go:258 — the enumeration was single-sourced but the parse half of the two bodies the task cited as byte-identical still is not (same `parser.SkipObjectResolution`, same map-by-`filepath.Base`, same two `t.Fatalf` shapes). Add `sourceguardtest.ParsePackageFiles(dir string, includeTests bool, fset *token.FileSet) (map[string]*ast.File, error)` beside `PackageGoFiles` and reduce both front ends (and `internal/theme/leaf_guard_test.go:136`'s per-file-FileSet variant, which only differs in FileSet policy) to a call plus their own fatal wording.
- [quickfix] internal/sourceguardtest/packagegofiles_test.go:77 — drop `slices.Sort(rels)` from `packageRelFiles`; the two `want` lists (`{"alpha.go","beta.go"}` and `{"alpha.go","alpha_test.go","beta.go"}`) are already in filename order, so removing the sort makes the tests pin `PackageGoFiles`' documented "in filename order" contract, which nothing currently verifies.
- [quickfix] internal/resolver/log_free_test.go:17, internal/log/init_test.go:218, internal/tui/sessions_grouped_reskin_test.go:227, internal/tui/pagepreview_hermetic_test.go:83/106/184, internal/tui/pagepreview_surface_audit_test.go:18, cmd/state_daemon_lock_pid_ordering_test.go:96, cmd/hooks_cleanstale_single_caller_guard_test.go:19, internal/fileutil/atomic_classify_test.go:137 — non-theming guards still declare their own `os.ReadDir(".")` / `filepath.Glob("*.go")` package scan, and the glob-based ones pass vacuously on an empty match. Route each through `sourceguardtest.PackageGoFiles(dir, <includeTests matching that guard's current filter>)`, keeping every assertion as-is, so "what a package-local guard covers" is one decision repo-wide rather than only across the theming family.
- [idea] internal/sourceguardtest/packagegofiles.go:15 — the positional `includeTests bool` reads opaquely at the ten call sites (`PackageGoFiles(".", false)` gives the reader nothing without opening the signature), and exactly one site passes `true`. Decide between exported named constants (e.g. `sourceguardtest.WithTests` / `ProductionOnly`) and a second `PackageGoFilesWithTests` entry point; the task mandated an explicit parameter, so this is a shape question rather than a defect.
