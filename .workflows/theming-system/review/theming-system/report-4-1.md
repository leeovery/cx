TASK: theming-system-4-1 — Sweep The Init-Time Derived Styles And Record The Residue

ACCEPTANCE CRITERIA:
1. Every package-level `var` in `internal/tui`'s production files is classified, and none is colour-bearing.
2. `nameBase`, `projectNameBase`, `attachedSlotWidth`, `loadingBlockBannerWidth` classified as legitimate colour-free init values (sweep discriminates on colour, not scope).
3. Every member of §11.2's hand-maintained list has a passing assertion that it carries the new theme's colour after the restyle path runs: both lists' `HelpStyle`, both `ActivePaginationDot`/`InactivePaginationDot` *and* the rendered `Paginator.ActiveDot`/`InactiveDot`, both `TitleBar`s, both `FilterInput` style sets, both delegates.
4. Any offender found is fixed, never exempted or worked around.
5. The residue list exists beside the restyle path, carries a reason per entry, and includes `internal/capture`'s swatch.
6. The sweep's outcome (offenders found and fixed, or none) is recorded in the task's commit message.
7. No new standing guard test introduced by this task.
8. No panel / third `bubbles/list` instance introduced by this task.
9. `go build ./... && go test ./...` green; `golangci-lint run` clean.

STATUS: complete

SPEC CONTEXT:
§11.2 (specification.md:1364-1382) — "The real risk is completeness". Threading the theme fixes most sites because anything taking the theme as a parameter re-derives per frame; what remains is the **cached styles Portal does not own** (`bubbles/list` help styles, pagination dots, TitleBar, both filter inputs), assigned once, hand-maintained with no guard. §11.2 further states the two named offenders are "what was *found*, not the boundary of the class" — init-time copies of *derived styles* were never swept for — and that a sweep never run "leaves the guard doing work a five-minute grep would have done, and leaves the residue undocumented". §11.2:1375-1378 assigns the panel's third list to the *same* restyle path (Phase 8, extended later). §13.4 (:1651, :1680) deliberately rejects a structural guard for the cached-style question and assigns ongoing protection to the behavioural swap-and-diff guard (task 4-3).

IMPLEMENTATION:
- Status: Implemented; later phases superseded the mechanism's shape (not drift).
- Task commit: f6f25cd0 (an unmerged rework of the same task exists on branch `replay/phase-4-medium` at 271931d7 — not in main's history and not judged here).
- Location (current HEAD):
  - `internal/tui/model.go:905-913` — `applyCanvasMode`, the single restyle chokepoint; fans out to `styleFilterInput`, the preview palette re-point (`m.preview.th`, :909), `applyPageListCanvasMode` for both page lists, and `applyThemePanelCanvasMode` (`internal/tui/theme_panel.go:304-309`, added by Phase 8 onto this same path per §11.2:1378).
  - `internal/tui/model.go:917-940` — `applyListCanvasMode` / `applyPageListCanvasMode`, the shared per-list sequence extracted later by task 11-1 out of the two branches this task authored.
  - `internal/tui/model.go:944-946` — `stripListTitleColours` (offender 1 fix: the Sessions coloured branch was the one of four sites still holding `bubbles/list`'s hardcoded violet Title box).
  - `internal/tui/model.go:711-721` — `canvasNoItemsStyle` / `colourlessNoItemsStyle` (offender 3 fix, found by this task's review cycle: `Styles.NoItems` held the library's `#626262` and *does* reach a frame via `viewProjectList`'s `m.projectListEmpty() && !m.commandPending` gate, `model.go:3126`). Built fresh rather than layered — correct, since layering a background over the library default would keep its foreground.
  - `internal/tui/model.go:723-746` — `canvasPaginationDots` / `colourlessPaginationDots`, including the `Paginator.ActiveDot`/`InactiveDot` re-feed §11.2's trap turns on.
- AC1/AC2 re-verified independently against HEAD (not taken from the commit message): the package-scope sweep still holds. `internal/tui` production files declare `flushWarningsToStderr` (bootstrap_warnings.go:15), `emptyFooterKeys` (empty_states.go:64), `labelOrder`/`stepLabelTable` (loading_progress.go:29,39), `loadingWordmark`/`loadingBlockBannerWidth` (loading_view.go:13,46), two interface assertions (preview_adapter.go:28), one interface assertion (preview_attach.go:11), `projectNameBase` (project_item.go:16, `Bold(true)` only), `nameBase` (session_item.go:15-18, `Bold(true)` only), `attachedSlotWidth` (session_item.go:42, a `lipgloss.Width` measurement). None calls `.Foreground(`/`.Background(`/`.Border*Foreground(`/`lipgloss.Color(` or holds a `theme.` value. `internal/capture` has `errFixtureFatal` and one interface assertion — both colour-free. The four named legitimate values are correctly unflagged.
- AC4: all three offenders fixed outright at the restyle path, none exempted or guarded around. AC6: f6f25cd0's message states the sweep outcome (12 package vars classified, zero colour-bearing; three construction-time offenders named and fixed). AC7: no structural guard added — the only new file is an assertion suite. AC8: `list.New(` appears in production only for the two page lists at the time of this commit; the panel's third instance arrived in Phase 8 and was wired into this same path rather than duplicating it.
- Notes:
  - **AC5 is met at the task's commit but NOT at HEAD.** f6f25cd0 recorded the residue as a per-entry comment block above `applyCanvasMode` (swatch; `startupCanvasHex`; the StatusBar set + DividerDot; Spinner + `spinner.Model.Style`; `Help.Styles.*`; `Styles.Filter`; `ArabicPagination`, each with its reason). The out-of-plan comment-strip chore removed it (`e3fa1503`, then narrowed further by `915e7fcb`), leaving `model.go:902-904`: "Deliberately excluded: startupCanvasHex (see ApplyTheme)." The record — including `internal/capture`'s swatch, which the task names as its first entry — no longer exists anywhere in the tree (`grep -rn residue` over `*.go`/`*.md` outside `.workflows` returns nothing). No plan task re-homed it; this was a repo-wide comment-standard chore, not a superseding task. Non-blocking: the functional product (the fixes and the assertions) is intact, and the loss is documentary. See notes 1 and 4.
  - The surviving one-line doc now overstates the path's completeness — six colour-bearing once-assigned `bubbles/list` values are also excluded, one of which (`ArabicPagination`) is not unreachable. That is a comment-accuracy defect the code falsifies, and the reason note 4's latent bug is now invisible.
  - Documented deviation carried over from the task's own review: the `Styles.Title` member is asserted to carry *no* colour rather than the new theme's — correct, since the section-header surgery replaces the row and nothing paints the box, and it is stated at both the assertion (`restyle_repoint_test.go:147-149`) and the helper (`model.go:942-946`).

TESTS:
- Status: Adequate.
- Coverage (`internal/tui/restyle_repoint_test.go`, driven through the production entry point `Model.ApplyTheme`):
  - `TestRestylePath_RepointsListOwnedStyles` (:103) over both lists — `HelpStyle`, both dot styles (fg + bg), both rendered `Paginator` dot strings, `NoItems` (fg + bg), `TitleBar`, `PaginationStyle`, and `Title`-carries-no-SGR.
  - `TestRestylePath_RepointsBothFilterInputs` (:154) — `Focused.Prompt`, `Focused.Text`, `Cursor.Color`, both lists.
  - `TestRestylePath_RepointsBothDelegates` (:177) — rendered selected-row `text.on-selection`, both pages.
  - `TestRestylePath_RepointsPreviewChrome` (:192) — the offender-specific assertion for the preview's copied palette.
  - `TestRestylePath_NoStaleColourSurvivesOnAnyList` (:258, added by task 11-1) — whole-surface stale scan across sessions/projects/panel.
- Quality of the assertions: strong, not decorative. `assertRepointed` (:93) demands the post-swap SGR *and* forbids the pre-swap one, so a site that is never re-pointed fails on the second half rather than passing vacuously. `populateRestyleProbe` (:35) renders both pages *before* the swap (:65-71), so caches assigned at construction are genuinely populated first, and it `t.Fatal`s if either list has fewer than two pages (:58-63) so the dot assertions cannot pass on an unrendered paginator. `themetest.SyntheticPalette` gives disjoint, fixed-width-SGR palettes, so no token's sequence can be a substring of another's — the "stale is absent" half cannot pass by accident. The panel probe (:213-235) fails setup if the open did not paint the pre-swap palette.
- Not over-tested: one assertion per member, no redundant variants, no mocking beyond the existing `Deps` fakes. The whole-surface scan overlaps the per-member assertions by design (it is the panel's only available observation and a different failure mode).
- Gap: the NO_COLOR arm of the offender this task fixed (`colourlessNoItemsStyle`) is asserted nowhere — no unit assertion and no fixture reaches the state (`projectsCommandPendingFixture`, `internal/capture/fixtures.go:684-690`, seeds a non-empty project list, and `TestColourless_NoTokenReachesTheWriter` scans a populated Sessions frame). Low risk (the style is a bare `lipgloss.NewStyle()`), but it is the one branch of a fix this task owns that nothing observes. See note 3.

CODE QUALITY:
- Project conventions: Followed. No raw hex at call sites (the colour-literal guard covers it); every new style derives from a `theme.Token`; no `t.Parallel()`; the restyle path stays O(1) with no `rebuildSessionList`, no file read and no write to `startupCanvasHex`, per §11.1/§11.4.
- SOLID principles: Good. `stripListTitleColours`, `canvasNoItemsStyle`/`colourlessNoItemsStyle` are single-purpose and mirror the existing `canvas*`/`colourless*` helper pairing; the later `applyListCanvasMode` extraction (task 11-1) removed the branch duplication this task's shape carried.
- Complexity: Low. One chokepoint, one shared per-list sequence, two arms.
- Modern idioms: Yes.
- Readability: Good.
- Issues: One comment-accuracy defect at `model.go:902-904` (see note 1) — the doc's single stated exclusion is falsified by six further once-assigned colour-bearing values the path does not re-point.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] internal/tui/model.go:902-904 — the `applyCanvasMode` doc names `startupCanvasHex` as the deliberate exclusion, but the path also leaves `bubbles/list`'s `StatusBar`/`StatusEmpty`/`StatusBarActiveFilter`/`StatusBarFilterCount`/`DividerDot`, `Spinner` (and the `spinner.Model.Style` `list.New` copies), `Help.Styles.*`, `Styles.Filter` and `ArabicPagination` un-re-pointed. Replace the last sentence with: "Deliberately excluded: startupCanvasHex (see ApplyTheme); the capture swatch, which takes one palette per process; the status-bar, spinner and built-in-help styles, whose renderers are switched off; Styles.Filter, which bubbles v2 never applies; and ArabicPagination, reachable only when the dot row outgrows the list width."
- [bug] internal/tui/model.go:917-933 — `Styles.ArabicPagination` keeps `bubbles/list`'s hardcoded `#9B9B9B`/`#5C5C5C` (`bubbles/v2@v2.1.0/list/style.go:83`), and `paginationView` escalates to it whenever the rendered dot row is wider than the list (`list/list.go:1188-1192`) — a colour belonging to no theme reaching a real frame, on a narrow terminal with many pages. It is the same class as the `Styles.NoItems` offender this task's review caught, it is invisible to task 4-3's swap-and-diff guard (no fixture paginates that far and the grey is neither theme), and since the residue block was stripped it is no longer recorded either. Fix: set it alongside the dots in `applyListCanvasMode` — `l.Styles.ArabicPagination = lipgloss.NewStyle().Foreground(th.TextFaint.Color()).Background(th.Canvas.Color())` on the canvas arm and a bare `lipgloss.NewStyle()` on the colourless arm — with a matching assertion in the `probedLists` loop.
- [quickfix] internal/tui/restyle_repoint_test.go:20-28 — `probeThemeBefore`/`probeThemeAfter` call `themetest.SyntheticPalette` twice with separate constants, bypassing `themetest.SyntheticPair`'s equal-reds `t.Fatalf` (the guard that stops a future edit making every "stale is gone" assertion pass vacuously). Build both from one `themetest.SyntheticPair(t, probeRedBefore, probeRedAfter)` call, as `internal/capture/theme_swap_guard_test.go:30` already does.
- [quickfix] internal/tui/restyle_repoint_test.go:133-137 — add the NO_COLOR counterpart for the `Styles.NoItems` fix: assert `colourlessNoItemsStyle` leaves the zero-items body free of any SGR on both lists (a colourless probe model plus `pl.list.Styles.NoItems.Render("x")` containing no `\x1b`). It is the only branch of an offender this task fixed that neither a unit assertion nor a capture fixture reaches.
- [idea] internal/tui/model.go:902-904 — beyond restoring the one-sentence exclusion list above, decide where the *full* residue record (per-entry reasons, `internal/capture/swatch.go` included) should live now that the comment-standard chore has ruled out the original 30-line block: a trimmed bullet list beside `applyCanvasMode`, or a short section in `docs/theming.md` that the doc line points at. The task's stated outcome requires it "in a durable, discoverable place", and at HEAD it exists nowhere.
