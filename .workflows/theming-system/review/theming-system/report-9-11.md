TASK: theming-system-9-11 — The Panel Behaviour Test Through The ThemeEnumerator Seam

ACCEPTANCE CRITERIA:
- The suite drives the panel through the faked seam and a fake persister only — no directory, no `prefs.json`, no real `prefs.Store`.
- Each of §13.6's seven named behaviours has its own test group, named to match, with a checklist comment mapping group → item.
- The union group asserts one-slug-one-row for a persisted built-in, a persisted invalid file, and the `reserved name` two-row case.
- The ordering group asserts the built-in-first tie resolution and the case-insensitive/byte-wise comparison through the rendered row order.
- The composition group asserts one delegate line per row and the reason-dropped-before-badge priority at the panel's minimum width.
- The badge group asserts all three derivation rows, including the virgin-install shipped-default case.
- The commit group asserts a row appearing, a row disappearing, a re-sort, a badge move and the identity-anchored cursor.
- The confirm group asserts `y`/`Y`, `n`/`N`/`Esc` and the swallow-everything-else rule.
- The failure group asserts the message-versus-state split, the discharge on close, and the forced-close precedence.
- Assertions are panel-observable rather than re-assertions of `internal/theme` internals.
- Unit lane, no `t.Parallel()`, passes with `-race`.

STATUS: complete

SPEC CONTEXT:
§13.6 (line 1741) names this a NEW test covering behaviour nothing else covers, driven through the §13.3 seam (an architectural commitment: an invalid-theme row must be renderable with no themes directory, and §7.1's import guard forbids `internal/capture` reaching config). The named set maps to concrete spec rules verified against the implementation:
- §9.4 union / one-slug-one-row (line 1063): "a persisted slug that names a built-in IS that built-in's row, and a persisted slug that names an existing-but-invalid file IS that file's row, carrying both the reason and the badge".
- §9.5 sort key (line 1091): the `reserved name`/built-in tie is guaranteed by construction; the built-in sorts first.
- Row composition (lines 1101–1103): badge outranks the terse reason for the same right edge; the reason is the first element dropped.
- Badge derivation table (lines 1129–1139): set-and-loadable → persisted slug; set-but-unloadable → still the persisted slug, the fallback's own row carries no badge; never-set → the shipped default (the virgin install).
- §9.2 commit recompute (lines 1018–1023): a successful commit recomputes the full row set (rows added/removed), from this instance's own keys.
- §9.13: a failed commit does not move the `●`; the message dies on the next keypress, the state until a later commit succeeds.

IMPLEMENTATION:
- Status: Implemented (with two deliberate, better-reasoned deviations from the task's "Do" wording; see Notes)
- Location: `internal/tui/theme_panel_behaviour_test.go:1-702` (703 lines). Supporting production code exercised: `internal/tui/theme_panel.go`, `theme_panel_commit.go`, `theme_panel_confirm.go`, `theme_row.go`, `theme_panel_render.go`, `internal/theme/union.go`, `internal/theme/badge.go`, `internal/theme/dir_theme_source.go`.
- Eight test functions exist, one per named behaviour plus the premise guard: `TestThemePanelBehaviour_Union` (:211), `_Ordering` (:269), `_RowComposition` (:310), `_Badges` (:385), `_CommitRecompute` (:420), `_Confirm` (:473), `_FailureStateMachine` (:547), `_NoConfigAccess` (:659) — matching the task's named test list exactly.
- Notes / deviations:
  1. **Seam not faked "wholesale"** — `behaviourEnumerator` (:18) embeds the production `theme.DirThemeSource` and overrides only `Open` (:30), so `Reassemble`, `Resolve` and `LoadSlot` are production's own. I verified against `internal/theme/dir_theme_source.go:18-39` that none of the three touches the filesystem (both carry in-source "must not consult Dir" comments), so the no-I/O requirement holds. This is *stronger* than a declared-row fake: a hand-written `Union` would make every membership and ordering assertion a statement about the fixture rather than about the panel. Intent-preserving deviation, not drift.
  2. **The persister fake is reused, not redeclared** — the task said both fakes "live in the test file"; `fakeThemePersister` is the package's existing recorder in `theme_persister_seam_test.go:18` with a settable `err`. It is still test-only, and no production seam gained a test-only shape (the AC's actual concern). Redeclaring it would be duplication.
  3. **The checklist comment and the added-value doc comment are gone.** They were present at this task's own commit (`aac6eed6`) — a 60-line preamble with the seven-item checklist mapping each group to its item and to the sibling suite proving the per-effect detail. They were removed wholesale by the later `e3fa1503 chore(comments): strip internal/tui to the code-quality standard` (23099 comment lines → 2850 across 243 files, explicitly "removes … structure enumerations, workflow citations and test preambles") and `f1e2e95f`/`915e7fcb`. Per the plan's later remediation phases this is a deliberate supersession of the *mechanism*, not drift from the outcome: the group-per-item structure the checklist described is intact and the group names carry it. Judged against intent as amended — not blocking. See the first non-blocking note for the one fact that was lost with it.

TESTS:
- Status: Adequate
- Coverage (each AC verified against production behaviour, not just against the test's own prose):
  - **Union** (:211-267). Persisted built-in (`nord`): asserts the row is `SourceBuiltin` and selectable, badged once, and that no second `not found` row was minted — this is exactly `persistedRows`/`listedUnder` (`union.go:176-191`). Persisted invalid file (`sunset`, bad colour): asserts unselectable, reason is the file's own, carries the badge, and the cursor lands on the fallback `tokyo-night`. `reserved name`: asserts two rows for one slug, the collided row's `Slug` equals the built-in's, unselectable, and only the built-in badged — which pins `Row.BadgeKey()`'s reserved-name `""` return (`badge.go:41-47`), the exact double-badge trap the production comment names.
  - **Ordering** (:269-305). The case fixture (`Alpha.theme` / `alpha.THEME` / `Zed.theme`) genuinely exercises both legs of `rowBefore` (`union.go:223-232`): folded keys tie, so the byte-wise leg decides `A`(65) < `a`(97), and `Zed.theme` is dragged behind every valid theme by the fold. The guaranteed tie asserts adjacency, the rendered cursor bar *and* `list.Index()` on the built-in, pinning `themePanelRowIndex`'s `Selectable` filter (`theme_panel.go:230-236`).
  - **RowComposition** (:310-370). Opens at content width 40 and asserts via `Fatalf` that the ladder actually produced `themePanelMinWidth` before asserting anything else. Every body line is checked for exactly one line and exactly `themePanelMinWidth` cells, and `requireRenderedOrder` re-parses those same lines into rows — together that is a real 1:1 row↔line proof, which is the pagination invariant `themeRowDelegate.Height() == 1` rests on. The reason-dropped-before-badge assertion is done properly *with a control* (the same entries, unbadged, do show the terse reason) — without the control the first half would prove nothing. This matches `themeRowReason`'s `badge != ""` early return (`theme_row.go:127-138`).
  - **Badges** (:385-418). All three derivation rows, and in the unloadable case it also asserts the fallback row (`tokyo-night-day`) carries no badge and the persisted row carries `⚠` — the two halves of §9.5 line 1134. The virgin-install case uses `theme.RawKeys{}`.
  - **CommitRecompute** (:420-467). The strongest group in the suite and the one that actually earns the task's premise. Under a constant with stale hand-edited slots, `d` + `y` makes the pair live: the `ghost` row **appears** (persisted slug resolving to nothing), rows **re-sort**, badges **move** constant→dark+light, and the cursor stays on `sunset` at `before+1`. `Enter` then reverses all four and returns the cursor to `before`. The index arithmetic is correct against `union.go`'s sort and `recomputeThemePanel`'s identity anchor (`theme_panel_commit.go:93-105`). It also pins "a commit is a write, not a navigation" (`m.themeState.active` unchanged).
  - **Confirm** (:473-526). All five inputs resolve the question without closing the panel. The swallow half is a *closure* over `themePanelProbes`, not a sample: it partitions the panel's live descriptor and asserts the count against `len(themePanelKeymap()) - 1`, so a newly bound panel key cannot be silently omitted. Per swallowed key it asserts the pending assignment, the persister, the cursor index and a nil cmd — the "a move mid-question re-themes the screen behind the answer" hazard.
  - **FailureStateMachine** (:547-634). The message/state split is asserted with the discriminating middle step (the keypress clears the message, still moves the cursor — "it clears AND acts" — and leaves `commitFailed` set), matching `clearThemePanelCommitFailed`'s kind check (`theme_panel_message.go:57-62`). Discharge-on-close is proven by a *second* close raising nothing. The forced-close precedence has a control run asserting the narrow-close flash, so the precedence claim is a real comparison; it matches `resizeThemePanel`'s `willReport` read-before-close ordering (`theme_panel.go:261-281`).
  - **NoConfigAccess** (:659-702). Renders a rejected row with `DirPath == ""`, then AST-parses its own file to ban `os`/`io/fs`/`path`/`path/filepath` and `TempDir`/`Setenv`/`MkdirTemp`/`WriteFile`/`ReadFile`/`ReadDir`/`NewStore`. I traced every package helper the suite calls transitively (`openPanelForTest` → `pressThemeKey`; `arrowPalette` → `themetest.SyntheticPalette`, pure in-memory; `themePanelProbes` → `commitPairPanelDeps` → `fakeThemeSource`) — none reaches the filesystem, so the premise holds beyond the single-file scan.
- Lane and hygiene: no build tag (unit lane), no `t.Parallel()`, no `tea.Program`, no tmux, no built binary; driven through `Model.Update` with real `tea.KeyPressMsg` values. No goroutines or shared state, so `-race` adds no surface.
- Not over-tested: the suite deliberately does not re-assert `internal/theme`'s unit-level facts, and the per-effect proofs stay in their own suites (`theme_row_test.go:258` `TestThemeRow_LabelTruncationFloor`, `theme_panel_confirm_test.go`'s `CancelIsInert`/`ConfirmsOnEitherCase`, `theme_panel_commit_recompute_test.go`, `theme_panel_close_report_test.go`). Overlap is composition-level, which is the stated added value.

CODE QUALITY:
- Project conventions: Followed. Test-only fakes in `_test.go`, no `t.Parallel()`, `t.Helper()` on every helper, failure messages state the invariant and the observed value rather than restating the assertion. Fatal-vs-Error is used with judgement (fixture preconditions `Fatalf`, behavioural misses `Errorf`).
- SOLID principles: Good. The seam override is the minimum surface (`Open` only), leaving the derivations to production.
- Complexity: Low. The render→parse→assert helper stack (`renderedPanelRows` / `parseRenderedPanelRow` / `require*`) is a clean four-function layer that keeps every test body declarative.
- Modern idioms: Yes — `slices.Equal`/`IndexFunc`/`Contains`, `for i := range n`, `strings.Fields`, `go/ast` for the premise guard.
- Readability: Good. `renderedPanelRows` asserts `TotalPages == 1` up front so a paginating fixture fails loudly instead of silently asserting over a partial body — a well-placed guard.
- Comment accuracy: the surviving comments (:16-17, :117-118, :653-654) all hold against the code. No stale claims, no process-artifact references, no spec-section citations.
- Issues: none blocking. Four small notes below.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [do-now] `internal/tui/theme_panel_behaviour_test.go:1` — the comment strip removed the one fact about this suite that is not recoverable from the code: why its assertions are composition-level and where the per-effect proofs live. Add a two-line suite comment above `behaviourEnumerator` (no spec citations, so it stays inside the current comment standard): `// The panel's own behaviour: that what the seam assembled is what the list holds,\n// in that order, with the badge on the row the derivation put it on. The per-effect\n// proofs live in theme_row_test.go, theme_panel_confirm_test.go,\n// theme_panel_commit_recompute_test.go and theme_panel_close_report_test.go.`
- [quickfix] `internal/tui/theme_panel_behaviour_test.go:326` — `from := panelHeaderRowsOf(badged)` omits the `themePanelDirRowHeight(...)` term that `renderedPanelRows` (:105) includes; the two agree only because every fixture here leaves `DirUnusable` false. Extract the shared start index (e.g. `func themePanelBodyStart(m Model) int { return panelHeaderRowsOf(m) + themePanelDirRowHeight(m.themePanel.union.DirUnusable) }`) and call it from both, so a future `DirUnusable` fixture cannot silently index one row short.
- [quickfix] `internal/tui/theme_panel_behaviour_test.go:358-369` — at the panel's minimum width the label budget is ~20 cells, so `len([]rune(label)) < themeRowLabelFloor` (4) can never fire; the assertion is vacuous and the real floor is proven by `theme_row_test.go:258`. Replace it with the budget the panel actually grants: assert `lipgloss.Width(label) == themePanelInnerWidth(themePanelMinWidth) - leftBarColumnWidth`, which fails if truncation over- or under-charges.
- [quickfix] `internal/tui/theme_panel_behaviour_test.go:474-489` — the "three answers resolve it" loop treats `y`/`Y` identically to `n`/`N`/`Esc`, so a cancel that wrote would pass this group (the divergence is caught only in `theme_panel_confirm_test.go`'s `CancelIsInert`). Return the persister from `behaviourConfirmModel` (:528) and assert `len(persister.slugs)` is 1 for `confirmYes`/`confirmYesShift` and 0 for the other three.
- [do-now] `internal/tui/theme_panel_behaviour_test.go:18,23,30,64,76` — `behaviourEnumerator` / `newBehaviourEnumerator` predate task 13-10's rename of the seam to `ThemeSource`; the package's other fake is `fakeThemeSource`. Rename to `behaviourThemeSource` / `newBehaviourThemeSource` (compiler-verified, file-local). `keymap_dispatch_guard_test.go:262`'s `newEntryEnumerator` carries the same staleness if a sweep is wanted.
