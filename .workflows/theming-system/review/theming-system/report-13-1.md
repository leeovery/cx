TASK: theming-system-13-1 — Single-Source The Theme-Panel Fixture Test Scaffolding In internal/capture

ACCEPTANCE CRITERIA:
1. Exactly one frame→lines parser exists in `package capture_test`; no other file splits a panel frame or re-derives the row shape.
2. The drop-the-cursor-bar rule appears exactly once in the package.
3. Strip behaviour is uniform across all matchers; `raw` remains reachable for style assertions.
4. One registration/guard test covers every `theme-panel-*` fixture; the five partitioned tests are gone.
5. The `"theme-panel-"` prefix literal appears once.
6. Every claim made by the deleted tests is still made by the consolidated test.
7. `go test ./internal/capture` passes.

STATUS: complete

SPEC CONTEXT:
This is a duplication-remediation task against the capture harness, not a spec-behaviour task, so the spec constrains it only indirectly. The load-bearing spec line is §13.4's completeness argument (specification.md:1637): "A missing fixture is a blind spot the guard structurally cannot report: §13.4 enumerates whatever fixtures exist, so absence reads as coverage." That is exactly what the five partitioned registration/guard tests existed to close, and it is why the consolidated test must range over the registry rather than over any task's hand-written list — which is what was built. specification.md:1600 additionally pins the fixture definitions and harness as permanent (only the PNGs/tapes are scaffolding), so consolidating their test scaffolding is durable work rather than churn on something due for deletion. CLAUDE.md's capture-harness section repeats the same coverage argument ("deleting one silently shrinks the guard rather than failing it").

IMPLEMENTATION:
- Status: Implemented
- Location:
  - `internal/capture/panel_frame_test.go` (new, `package capture_test`) — the single shared projection: `panelBorder` (:10), `panelCursorBar` (:12), `panelFixtureNamePrefix` (:14), `panelLine{visible,raw}` (:16), `fields()` (:21), `panelLines` (:29), `panelText` (:46), `panelFieldText` (:54), `panelLineIndex` (:62), `uniquePanelLine` (:81), `panelLineWith` (:100), `panelRowLine` (:107), `panelRows` (:156).
  - `internal/capture/theme_panel_fixture_render_test.go:214` — the consolidated `TestPanelFixture_Registered`.
  - `internal/capture/theme_panel_remaining_fixtures_test.go:304` — `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet` left intact per Do step 7.
  - Commit `80775a8c` (+200 / −316 across the three partitioned files).
- Notes:
  - AC1 met for the row-reading path: the three independent line loops (`panelRows`' own split, `panelLinesOf`, `uniquePanelLine`'s split) are gone and every row/line consumer now projects over `panelLines`. Two residual page-side splitters remain in `theme_panel_remaining_fixtures_test.go` — `panelOuterWidth` (:52) and `footerLine` (:291). Both want the text *left* of the border (page width / the covered footer), which `panelLines` discards, so they are a different projection rather than a second copy of this one; `panelOuterWidth` does still restate the find-the-border loop and its fatal message. Neither re-derives the row shape. Non-blocking note below.
  - AC2 met exactly: `▌` appears once in the whole package (`panelCursorBar`, panel_frame_test.go:12) and the drop rule lives only in `fields()`. The second copy at the old `panelRowLine` is deleted. `panelBadges` in the sibling `package capture` locates `●` by index and does not re-derive the row shape, so it is not a third statement.
  - AC3 met: every matcher now reads `visible` (`panelLineIndex`:68, `panelLineWith`:103, `panelRowLine` via `fields()`, `panelRows`:161, `panelCarriesDots`:242) and `raw` is returned by `uniquePanelLine` for the SGR assertions that need it (e.g. remaining:83/86 reading `fgSeq`). The pre-change semantics survive: the old `panelRows` stripped the whole frame before cutting, the new one cuts the raw line then strips the panel side — equivalent, since an SGR sequence cannot contain `│`. The added `TrimRight(…, " ")` on `visible` is inert for every live call site (no matcher searches a trailing-space string).
  - AC5 met within the task's stated scope (Do step 5 named `allPanelFixtureNames()` and `registeredPanelFixtureNames()`, both `package capture_test`); both now build from `panelFixtureNamePrefix`, as do the three per-file name slices. A second literal survives at `theme_panel_fixture_test.go:34` (`panelFixturePrefix`) — but that file is `package capture`, a different Go package, so the constant is structurally unreachable from `capture_test`. It predates this task (introduced by 12-5) and could only be unified by exporting a test-only prefix from production code. Judged not a miss; raised as an idea below.
  - AC6 met, and strengthened rather than preserved: the deleted tests ranged over three hand-written name lists, the consolidated one ranges over `registeredPanelFixtureNames()` (registry-derived), so a panel fixture added later is covered with no new test. The one direction that derivation cannot see — a name specified but absent from the registry — is deliberately carried by `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet`'s exact-set equality against `allPanelFixtureNames()`, which is precisely why Do step 7 ordered it left alone. The pair together makes every claim the five made.
  - Later-task interaction (expected per the verifier brief, not drift): `d939ae76` ("strip the capture harness to the code-quality standard") removed the explanatory comments this commit shipped, including the `theme_row.go` citation Do step 2 asked for on `fields()`. `e6d03898` (task 13-11) then added `panelSlugRow`/`labelTruncates`/`panelEllipsis` to `panel_frame_test.go` built directly on `panelLines`/`fields()` — good evidence the extracted primitive is doing its job, since a new consumer projected over it instead of authoring a fourth parser.

TESTS:
- Status: Adequate
- Coverage: This is a test-scaffolding refactor, so the "tests" are the assertions being moved. All 26 panel tests across the four files still exist and their claims are unchanged; the five partitioned registration/guard tests collapse into one (`TestPanelFixture_Registered`) whose three legs (resolves / enumerated / under the guard) are the union of what the five asserted. Assessed by reading the diff assertion-by-assertion against `git show 80775a8c` — no assertion was dropped, weakened or re-pointed at a different subject. `panelCarriesDots` correctly gained a `*testing.T` (it now goes through the fataling `panelLines`), and all its call sites were updated.
- Notes:
  - Not executed — per the agent's no-test-execution rule, adequacy was judged by reading. Every helper referenced by the four files resolves to exactly one definition in `panel_frame_test.go`, no symbol is defined twice, and no helper is now unused (`panelText` 7 sites, `panelFieldText` 1, `panelLineIndex` 9, `panelLineWith` 3, `panelRowLine` 3, `panelRows` 9, `panelSlugRow` 3), so AC7 has no visible obstacle.
  - The task's Tests section asked for three throwaway mutation probes (drop a fixture from the registry; drop one from guard coverage; change the cursor column). These leave no committed artefact, so they cannot be verified after the fact. Reasoning them through against the current tree: dropping a fixture still fails, though now via `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet`'s set mismatch rather than `TestPanelFixture_Registered` (see the first non-blocking note); dropping guard coverage still fails `TestPanelFixture_Registered`'s `guarded` leg, which survives because `guardedFixtures` applies a live `excludeColourless` filter; changing the cursor column touches only `fields()`. The probes' intent holds.

CODE QUALITY:
- Project conventions: Followed. `package capture_test` placement is right — this is external-test scaffolding for the harness and needs no access to `capture` internals. No `t.Parallel()` (project ban). Helpers take `*testing.T` first and call `t.Helper()` uniformly. Fatal messages keep the codebase's house style of explaining why the failure matters rather than just what differed (e.g. panel_frame_test.go:71 and :90 both justify the uniqueness requirement).
- SOLID principles: Good. `panelLine` is one concept (a panel-side frame line in two forms), `panelLines` is the one producer, and the five public helpers are genuinely thin projections over it — `panelLineWith` and `panelRowLine` collapse to two-line delegations to `uniquePanelLine` with different predicates, which is the correct shape for "same search, different match rule".
- DRY: The task's whole point, and it lands. One frame→lines parser, one cursor-column rule, one prefix constant per package.
- Complexity: Low. No function exceeds ~20 lines; `panelLines` and `fields()` are both single loops / single branch.
- Modern idioms: Yes — `strings.SplitSeq` range-over-func (matching the `modernize` linter the project runs), `strings.Cut`, `strings.CutSuffix`, named multi-returns used where the second value is genuinely a distinct fact (`fields() (fields, cursor)`).
- Readability: Good, with one gap. Changing `uniquePanelLine`'s predicate from `func(visible string) bool` to `func(line panelLine) bool` is the right call — it is what lets `panelRowLine` reach `fields()` instead of re-tokenising — and it is package-private with both call sites in the same file, so the Do-step-3 "no call site changes" constraint is honoured where it mattered. The gap is that `fields()` now carries no comment at all (see the do-now note): the AC names it the single statement of the row-shape rule, and post-comment-strip nothing records *why* a leading `▌` is dropped.
- Comment accuracy: The surviving comments hold true against the code. Worth recording that the comment-strip commit incidentally removed two stale claims this commit had inherited — the "FixtureByName's switch and FixtureNames()'s slice are two hand-maintained lists" framing was already becoming untrue and is fully false after 17-12 collapsed both onto `fixtureBuilders()`. No process-artefact references (task ids, phases, spec section numbers) remain in the changed code; the commit itself removed two `§9.5` / `§13.4` citations from fatal strings.
- Issues: None blocking.

BLOCKING ISSUES:
- None.

NON-BLOCKING NOTES:
- [quickfix] internal/capture/theme_panel_fixture_render_test.go:222-227 — two of `TestPanelFixture_Registered`'s three legs are now unreachable. `registeredPanelFixtureNames()` (render:26) builds its result *by filtering* `capture.FixtureNames()`, so `slices.Contains(capture.FixtureNames(), name)` at :225 can never be false, and since task 17-12 collapsed `FixtureByName` and `FixtureNames` onto the single `fixtureBuilders()` list (fixtures.go:133), `FixtureByName(name)` at :222 can never error either (the one non-builder name, `contrast-validation`, is filtered out by the `theme-panel-` prefix). This is 17-12's residue landing on 13-1's code, not drift introduced here — both legs were live claims when written. Concrete change: drop the two dead legs and their failure messages, keeping only the `guarded` check at :228 (which stays live via `excludeColourless`), and add a one-line comment pointing at `TestPanelFixture_RegistryHoldsTheSpecifiedPanelSet` as the test that now carries the registration claim. Alternatively re-point the loop at `allPanelFixtureNames()` to make both legs live again — but that reintroduces the hand-written list the consolidation deliberately removed, so the drop is preferred.
- [quickfix] internal/capture/theme_panel_remaining_fixtures_test.go:52-64 — `panelOuterWidth` is the one remaining function that splits a panel frame itself, restating the border-find loop and duplicating `panelLines`' fatal message verbatim (:62 vs panel_frame_test.go:41). It cannot use `panelLines` today only because it needs the *page* side of the cut, which `panelLine` discards. Concrete change: add a `page string` field to `panelLine`, populate it from the first return of the existing `strings.Cut` in `panelLines`, and re-express `panelOuterWidth` as `termW - tui.Hinset - lipgloss.Width(panelLines(t, ansi.Strip(frame))[0].page)`. `footerLine` (:291) can follow the same route if wanted, though it is a weaker case since it searches by page-side content.
- [do-now] internal/capture/panel_frame_test.go:21 — `fields()` is designated by AC2 as the package's single statement of the row-shape rule but carries no comment, so the non-obvious part (why a leading `▌` is dropped, and what guarantees it is a separate field) is now unrecorded. The original commit had it and the later harness-wide comment strip removed it. Restore two lines above the func: `// fields is the line's text past the cursor column, tokenised, and whether that column is painted.` and `// theme_row.go composes a row as [2-cell cursor column][label][pad][badge], so a leading ▌ is dropped and the first field that survives is the label.` Note the tension with the deliberate strip — this is the one comment in the moved block that states a fact about *another file* rather than restating the code, which is why it is worth an exception.
- [idea] internal/capture/panel_frame_test.go:14 and internal/capture/theme_panel_fixture_test.go:34 — the `"theme-panel-"` literal exists twice, as `panelFixtureNamePrefix` (`package capture_test`) and `panelFixturePrefix` (`package capture`). They are in different Go packages sharing a directory, so the unexported constant cannot cross, and unifying them means deciding between exporting a test-only prefix from production `capture` (pollutes the production surface for a test concern) and accepting the split (a `theme-panel-` rename would need both edited). Both constants are single-sourced *within* their own package, so the AC's intent holds; the decision is whether the cross-package duplication is worth a production export. Recommend accepting the split and, if anything, cross-referencing the two in a comment.
