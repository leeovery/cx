## Attempt 1

ISSUES:
- The sweep misses a fixture that the executor's own rule condemns, and the new guard structurally cannot see it. cmd/cleanstale_transient_listpanes_doctorfix_integration_test.go:53 and :92 bring up a tmux server against a state dir isolated by isolateCleanStaleTestEnv (cmd/cleanstale_transient_listpanes_shared_test.go:21-34, which calls portaltest.IsolateStateForTest at :27 and repoints PORTAL_STATE_DIR at it). Neither file registers the guard. Because the isolate call and the tmuxtest.New call live in DIFFERENT FILES, the file-level check at internal/portaltest/teardown_guard_coverage_test.go:64 skips both and the pair falls through the crack entirely — so criterion 2's enforcement mechanism reports green over a violation of the rule it exists to enforce. The comment at internal/portaltest/teardown_guard_coverage_test.go:31-33 asserts "every fixture in the tree arranges both calls in one place", which is the false premise that justified the scoping; this fixture is the counterexample. (Hazard at this specific site is low — the server hosts a bare new-session -d with no daemon — but it is the same shape as the two TestTransientListPanesHelpers_Smoke subtests that DID get the guard, so the coverage is inconsistent as well as unenforced.)
  FIX: Register the guard inside isolateCleanStaleTestEnv, immediately after t.Setenv("PORTAL_STATE_DIR", stateDir) at cmd/cleanstale_transient_listpanes_shared_test.go:28 and BEFORE the initTestLogToStateDirAs call at :32 — that placement covers all three subtests, and LIFO then runs kill-server → log close → guard → RemoveAll. Then rewrite the comment at internal/portaltest/teardown_guard_coverage_test.go:31-33 to state the scoping honestly instead of denying the case: the rule is judged per file, a fixture that splits the isolate and the server across files is outside its reach, and such a fixture carries the registration in its shared arrange.
  ALTERNATIVE: Make the guard resolve package-local arrange helpers — collect the func decls in each package whose body calls IsolateStateForTest, then treat a call to one of those names as an isolate call. That closes the blind spot generally (and is a step toward per-function scoping, which would also catch a second test function added to an already-guarded file). Cost is ~20 lines of cross-file AST bookkeeping. The reviewer recommends the primary fix: the guard's value is mostly in catching NEW files, the cross-file arrange is rare (exactly one in the tree today), and an honest comment plus a covered fixture beats a more clever rule that is harder to read.
  CONFIDENCE: high

NOTES:
- The added source guard is ACCEPTED. Its rule (isolated state dir + live tmux server => register the guard) is right; keying on the server rather than on "spawns a daemon" matches CLAUDE.md's wording and the actual hazard, and over-inclusion costs a measured ~52ms with no correctness risk. It matches the four existing repo-wide source guards in shape, is stdlib-only and unit-lane, parses ignoring build tags, carries the scanned==0 self-check, and emits an actionable failure. The reviewer mutation-tested it: removing one registration produces a failure naming the exact file.
- The "5/5 does not discriminate" reading is ACCEPTED as honest. The reviewer also could not reproduce the flake in 8 pre-fix runs under CPU load. But it instrumented the guard and measured it closing a real, repeatable ~100ms window: on the named fixture the recorded daemon PID is still alive ~52ms after kill-server, and the state dir needs a further ~55ms to quiesce (total=105ms pidwait=52ms, stable across 3 runs). That is stronger evidence than the 5/5 and points the same way.
- The composition_e2e hand-rolled partial guard replacement was verified clean: pidAlive and time are still used elsewhere, no dead code, and the flagged trade measured benign (pidwait 52us-52ms, never the 3s budget, so the quiescence loop always runs).
