AGENT: standards
FINDINGS:
- FINDING: The sweep's stand-down vocabulary and the `doctor --fix` copy diverge from the fixed set the spec decided
  SEVERITY: medium
  FILES: cmd/run_hook_stale_cleanup.go:16-20, cmd/doctor.go:215-220, cmd/doctor.go:308-313
  DESCRIPTION: The specification fixes three stand-down reasons (`restoring`, `lock-timeout`, `empty-pane-read`) and three exact user-facing lines: "Skipped stale hook prune: restore in progress", "… hooks.json is locked", "… could not read live panes", the last of which it explicitly assigns to the empty-live-set guard. The implementation carries five reasons — adding `store-read-failed` and `pane-read-failed` — and five rendered phrases. In doing so it moves the spec's exact string "could not read live panes" onto the new pane-read-failure branch and invents "live pane list came back empty" for the guard the spec named. Net effect: a user-facing string the spec pinned now names a different condition than the one it was written for, and the closed `reason` value set the spec described as "three distinct reasons … distinguishing the three is the point" has grown by two without the spec being amended. The two added conditions are real (a failed `hooks.json` snapshot read and a failed `list-panes` read both predate this work and previously exited through other paths), so the divergence is in the enumeration and the copy rather than in behaviour, but the copy is not what was signed off.
  RECOMMENDATION: Either restore the spec's wording — `empty-pane-read` renders "could not read live panes" — and fold the two added conditions into the three the spec fixed, or carry the five-reason ladder forward as a deliberate amendment and record it, so the shipped strings and the specified strings agree.

- FINDING: The shape predicate's home, and the refactor that moved it, depart from the spec's argued placement
  SEVERITY: low
  FILES: internal/nanoid/nanoid.go:1-56, internal/session/panetoken.go:1-11, internal/hooks/leaf_guard_test.go:24-31, internal/spawn/ackid.go:7, internal/spawn/burst.go:58, cmd/open.go:414
  DESCRIPTION: The spec places the token predicate in `internal/session` beside the generator, argues that is the only home permitting derivation from the unexported width, and records that `internal/hooks` may import `internal/session` cycle-free (still true today — nothing in the `session`/`tmux`/`state` tree imports `hooks`). The implementation instead extracted a new stdlib-only `internal/nanoid` leaf holding the alphabet, the width, the generator and `IsTokenShaped`, and added a hooks leaf guard that now forbids the very import the spec sanctioned. The invariant the spec actually cared about is preserved — recognition reads the generator's own `width` and `Alphabet`, so generation and recognition cannot drift — and `session.NewPaneToken` remains where the spec put minting. The cost is blast radius the spec did not scope: `internal/spawn` (ack-id generation, burst wiring) and `cmd/open.go` were re-pointed at the new package purely to carry the move, and `session.NewPaneToken` is left as a one-line pass-through.
  RECOMMENDATION: No code change if the leaf-package placement was a deliberate, accepted improvement — but the spec's stated home and its cycle analysis are now wrong, so record the supersession rather than leaving the spec asserting a placement the tree contradicts.

- FINDING: A second lock-acquisition bound the spec did not decide
  SEVERITY: low
  FILES: internal/hooks/lock.go:17-33, internal/hooks/store.go:44-50
  DESCRIPTION: The spec fixes one bound — "The bound is 2 seconds" — and describes the sweep's advisory pre-read as degrading by the same rule as every other read. The implementation adds `snapshotLockTimeout = 20 * time.Millisecond`, a second package-level bound applied only to the clean's pre-read, to cap a clean's worst case at one full `lockTimeout` rather than two. The reasoning is the spec's own (an unbounded or doubled wait parking the daemon's 1s tick), and the pre-read is genuinely advisory, so correctness is unaffected. But it is a decision the spec did not take, and it changes observable behaviour: under ordinary contention — a concurrent `hook set` holding the exclusive lock for a few milliseconds — the pre-read will routinely fall through to the unlocked read and emit `op=load-unlocked` at DEBUG, where the specified single bound would almost never have done so.
  RECOMMENDATION: Confirm the second bound is intended and record it alongside the 2s figure, or drop it and accept the doubled worst case the spec's single bound implies.

- FINDING: A store-read stand-down is both logged and returned, unlike its sibling branches
  SEVERITY: low
  FILES: cmd/run_hook_stale_cleanup.go:203-219, cmd/state_daemon.go:211-213, cmd/doctor.go:201-204
  DESCRIPTION: `declinedSweep` deliberately returns nil for the lock-timeout branch with the reason that "the nil error keeps the caller from adding a second report for the same event", then does the opposite for `ErrSnapshotRead`: it emits the `clean-stale-skipped reason=store-read-failed` WARN *and* returns the error, so the daemon adds `hooks stale-cleanup failed` and `doctor --fix` adds `doctor --fix: stale-hook prune failed` for the same event. Two WARN lines for one condition, inside a work unit whose stated purpose is making a single grep answer why the prune declined. The project's error-handling convention is likewise log-or-return, and the spec waives it explicitly only for `hook set` / `hook rm`, where the log line and the stderr line serve different audiences.
  RECOMMENDATION: Apply the lock-timeout branch's own reasoning — emit the stand-down and return nil — or, if the caller must genuinely see a failure, drop the stand-down emission on this branch so the event is reported once.
SUMMARY: The four specified changes land faithfully — token-only keys, the two-read existence probe, shape-aware retention with named deletions, and the sidecar-locked read-modify-write are all implemented as decided, with the `@portal-id` machinery, `migrate-rename` and both literal guards removed as §7 requires and the docs rewritten in step. The drift is at the edges: the sweep's stand-down reason set and its `doctor --fix` copy grew past the three the spec fixed and reassigned one of the spec's exact strings, and three smaller decisions (the predicate's package home, a second lock bound, a double-reported store-read failure) were taken during implementation rather than in the spec.
