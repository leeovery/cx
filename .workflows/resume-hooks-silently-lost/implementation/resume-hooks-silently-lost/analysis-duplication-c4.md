AGENT: duplication
FINDINGS:
- FINDING: The repo-wide "project root → enumerate → keep _test.go → parse → relativise" scan driver is authored three times
  SEVERITY: medium
  FILES: internal/portalbintest/lane_guard_test.go:55-88, internal/portaltest/teardown_guard_coverage_test.go:161-193, internal/restoretest/literal_guard_scan_test.go:29-58, internal/logtest/install_guard_test.go:57-72
  DESCRIPTION: Three guards written by separate tasks in this work unit (8-49, 6-17, 8-14) each open with the same ~28-line preamble: `portalbintest.ProjectRoot()` with a `resolve project root` fatal, `sourceguardtest.GoSourceFiles(root)` with an `enumerate .go files` fatal, a `strings.HasSuffix(path, "_test.go")` filter loop, `sourceguardtest.ParseSources(t, testPaths)`, and a per-file relativisation of the path so findings read as repo paths. `lane_guard_test.go` and `teardown_guard_coverage_test.go` are near line-for-line identical across that whole span. `install_guard_test.go` repeats the first half with the opposite filter. The relativisation has already drifted: two use `filepath.Rel` with a fatal, `literal_guard_scan_test.go` uses `strings.TrimPrefix(finding, root+separator)` — so the same rule now has two behaviours for a path outside the root. `internal/sourceguardtest` is the declared home for exactly these primitives (it owns `GoSourceFiles`, `PackageGoFiles`, `ParseSources`, `AssertDepsWithin`) and was extended by this work unit without absorbing the driver that every consumer of those primitives writes on top of them.
  RECOMMENDATION: Promote the driver into `internal/sourceguardtest` as one helper — e.g. `RepoSources(t, lane)` returning the resolved root alongside `[]ParsedSource` already narrowed to `_test.go` (or to non-test sources) with root-relative `Path` set — and have the four guards call it instead of restating root resolution, enumeration, filtering, parsing and relativisation. That also gives the "scanned nothing" tripwire one home rather than one per guard.

- FINDING: Build-constraint extraction from an *ast.File is implemented three times in three packages
  SEVERITY: medium
  FILES: internal/portalbintest/lane_guard_test.go:112-128, internal/restoretest/session_restorer_literal_guard_test.go:174-188, internal/sourceguardtest/leaf_guard_test.go:45-58
  DESCRIPTION: `compiledInUnitLane`, `isIntegrationTagged` and `buildConstraintLine` each walk `file.Comments`, break at `group.Pos() > file.Package`, and run `constraint.Parse` over the leading comment group to recover the file's `//go:build` line. All three were added by this work unit (tasks 8-49, 8-14, 8-41) in isolation from one another, and all three then evaluate or report the `integration` tag. They already disagree on the details that matter: `compiledInUnitLane` gates on `constraint.IsGoBuild` and treats an unparseable constraint as "in the unit lane", `isIntegrationTagged` skips a parse failure and keeps looking, `buildConstraintLine` returns the first parseable comment whatever it says. Three readings of one file can therefore classify the same file differently, and the tag literal `"integration"` is declared separately in two of them.
  RECOMMENDATION: Add one primitive to `internal/sourceguardtest` — e.g. `BuildConstraint(file) (constraint.Expr, bool)` plus a `SatisfiedWith(expr, tags...)` evaluator — and route all three call sites through it, with the `integration` tag name declared once beside it. Each guard then keeps only its own policy (which lane it polices), not its own parser.

- FINDING: The two restore-Exe literal guards are a copy-paste pair
  SEVERITY: medium
  FILES: internal/restoretest/orchestrator_literal_guard_test.go:112-162, internal/restoretest/session_restorer_literal_guard_test.go:136-203
  DESCRIPTION: `orchestratorLiteralsIn` (orchestrator_literal_guard_test.go:151) and `sessionRestorerLiteralsIn` (session_restorer_literal_guard_test.go:190) are identical bodies differing only in the type-name constant they pass to `isRestorePkgType`. `scanTestOrchestratorLiterals` (:138) and `scanIntegrationSessionRestorerLiterals` (:160) are identical apart from the include filter and the wording of the scanned-nothing fatal. `TestOrchestratorLiteralGuard_FatalsWhenItEnumeratesNoTestFiles` (:112) and `TestSessionRestorerLiteralGuard_FatalsWhenItEnumeratesNoIntegrationTestFiles` (:136) are the same ~25-line test twice: a `harnesstest.Recorder` over an empty temp dir asserting one fatal, then over a tree with no matching sources asserting one fatal, then a `strings.Contains(…, "stopped looking")` check. The session-restorer half was written a task later (8-14) than the orchestrator half (7-22) against the shared `scanGuardTestFiles` seam, and reproduced everything above it rather than parameterising it.
  RECOMMENDATION: Collapse the pair into one parameterised guard in restoretest: a `literalGuard{typeName, include, constructors, fatalWording}` descriptor driving a single `scanRestoreTypeLiterals` (which owns the scanned-zero fatal) and a single `compositeLiteralsOf(typeName)` collector, with the two guards and the two fatals-when-empty tests table-driven over the two descriptors.

- FINDING: The Cobra root-command drive is written four ways in the cmd test suite
  SEVERITY: medium
  FILES: cmd/testhelpers_test.go:176-211, cmd/root_test.go:92-101, cmd/hooks_test.go:46-49, cmd/hooks_test.go:485-489, cmd/hooks_test.go:911-914
  DESCRIPTION: `runRootCmd` (root_test.go:92) is the package's shared driver, added by task 8-33 as "one root-command driver". `runHookSet`, `runHookRm` and `runHookList` (testhelpers_test.go:176, :189, :202) were added by an earlier task (2-9) and each repeat `runRootCmd`'s body verbatim — new buffer, `resetRootCmd()`, `SetOut`, `SetErr`, `SetArgs`, `Execute` — differing only in the argv they build and in routing both streams to one buffer. Separately, `cmd/hooks_test.go` inlines that same 4-line sequence 20 times (lines 46-49, 66-69, 109-112, 290-293, 311-314, 335-339, 359-363, 379-382, 386-389, 406-409, 463-466, 485-489, 505-509, 527-530, 556, 584, 611, 680, 703, 911) while calling the purpose-built drivers 13 times in the same file — two routes to the same behaviour inside one file. Every sibling hook suite added by this work unit (hooks_rm_exit_test.go, hooks_write_lock_test.go, hooks_read_lock_test.go, hooks_seams_test.go) routes exclusively through the drivers, which is what makes hooks_test.go the drifted copy rather than the convention.
  RECOMMENDATION: Reimplement `runHookSet`/`runHookRm`/`runHookList` as thin argv-composing wrappers over `runRootCmd` (adding a combined-stream variant there if the single-buffer return is load-bearing), and convert the 20 inline sequences in `cmd/hooks_test.go` to those drivers so the file has one route to running a hook command.

- FINDING: The restore-Exe rationale paragraph is restated six times
  SEVERITY: low
  FILES: internal/restoretest/orchestrator.go:11-19, internal/restoretest/orchestrator_staged.go:14-22, internal/restoretest/session_restorer_staged.go:11-20, internal/restoretest/orchestrator_literal_guard_test.go:22-31, internal/restoretest/session_restorer_literal_guard_test.go:21-32, internal/restoretest/restoretest.go:55-58
  DESCRIPTION: The same explanation — `Exe` falls back to `os.Executable()`, which under `go test` is the test binary, so an armed pane re-runs the suite inside itself, exits 0 and takes the session with it — is written out in full in six places across the package, plus twice more in the two guards' `t.Fatalf` messages. It is a single fact about one field, and each restatement is an independent claim that has to be kept true; the wordings have already diverged ("re-runs the suite inside itself" / "re-runs its own suite inside the pane" / "respawns into the suite itself"). The project's own comment standard treats every claim as a maintenance liability, so six copies of one claim is six times the liability for no added information.
  RECOMMENDATION: State the trap once — on `StagedHydrateExe`, the helper every pinned route ends at — and have the four constructor/guard doc comments say only what their own site does ("pins Exe through StagedHydrateExe; see its doc for why an unpinned Exe is silent"), keeping the guards' user-facing `t.Fatalf` text as the only other place the consequence is spelled out.

- FINDING: hookstest.AssertDegradedRead restates the shared record assertion its file-neighbour delegates to
  SEVERITY: low
  FILES: internal/hookstest/hooks_lock.go:132-150, internal/hookstest/hooks_lock.go:107-131, internal/logtest/assert.go:24-41
  DESCRIPTION: `AssertLockWarn` routes its level/message/component/op/via checks through `logtest.AssertRecord` and adds only the attrs specific to the lock WARN. `AssertDegradedRead`, ten lines below it in the same file, hand-rolls the level, op and via comparisons instead — the same three checks `AssertRecord` owns, with its own `t.Errorf` wordings — and consequently never asserts the message or the component at all. `cmd/logging_capture_test.go:51`'s `assertHooksRecord` shows the intended shape (a component-bound wrapper delegating to `AssertRecord`), so this is one site out of step rather than an accepted second route.
  RECOMMENDATION: Rewrite `AssertDegradedRead` to call `logtest.AssertRecord` with `RecordWant{Level: slog.LevelDebug, Msg: "load-unlocked", Component: "hooks", Op: "load-unlocked", Via: wantVia}` and keep only the non-empty `error` attr check locally, so the degraded-read breadcrumb is pinned by the same assertion as every other audit-trail line.
SUMMARY: The production code under this topic (the hook-key/staleness/lock machinery in cmd and internal/hooks) is well consolidated — the staleness rule, stand-down vocabulary, id vocabulary and lock bounds each have exactly one home. The duplication that remains is concentrated in the test-guard and test-helper layer, where separately-executed tasks each re-authored the repo-wide source-scan driver, the build-constraint reader, the restore-literal guard, and the Cobra root-command drive rather than reaching for the shared primitive the work unit itself introduced.
