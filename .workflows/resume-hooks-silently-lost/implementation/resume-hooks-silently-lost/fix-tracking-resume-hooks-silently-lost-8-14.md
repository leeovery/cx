## Attempt 1

ISSUES:
- `internal/restoretest/session_restorer_literal_guard_test.go:80-104` (fixture line 91; predicate at `:183`, `hasField` at `:192`) — the guard's rule is key-presence, so `restore.SessionRestorer{StateDir: "/state", Exe: nil}` passes, and the subtest asserting this is titled *"it ignores an integration fixture that pins Exe"*. `Exe: nil` is not a pin: `hydrateExe` (`internal/restore/session.go:327-330`) treats a nil resolver identically to an absent field and falls back to `os.Executable` — the exact silent test-binary arming this guard exists to foreclose. The test therefore asserts a false claim and documents the hole as sanctioned. The sibling guard 60 lines away argues the opposite design explicitly (`orchestrator_literal_guard_test.go:31-34`: "The rule is therefore not 'set Exe' but 'never compose the struct': a rule about a field that may legitimately be absent cannot be checked, while a rule about the literal can") — and within the integration-tagged scope the stronger rule is available at zero cost today, since no integration file composes the struct at all and `restoretest.NewSessionRestorer` covers the one fixture that needs one.
  FIX: Adopt the sibling guard's rule inside the integration-tagged scope — flag *any* `restore.SessionRestorer` composite literal in an integration-tagged `_test.go`, drop `hasField`/`exeFieldName`, and rewrite the two "ignores" fixtures so the passing integration fixture routes through `restoretest.NewSessionRestorer` only (the unit-lane fixture is unchanged and still proves the lane scoping). This also closes the undetectable-by-any-key-rule case of `Exe: someVarThatIsNil`, and makes the two adjacent guards state one philosophy.
  ALTERNATIVE: Keep the key-presence rule but treat an `Exe` key whose value is the bare `nil` identifier as unpinned (three lines in `hasField`), and change the passing fixture to `Exe: stagedExe` so the subtest name is true. Cheaper and stays literal to the task's wording, but leaves the weaker rule and the philosophy split standing. The reviewer recommends the first.
  CONFIDENCE: medium

COMMENT_CORRECTIONS:
- `internal/bootstrapadapter/adapters.go:67` — the rewritten doc comment carries forward a false claim that the executor's own new test violates: `Logger` is explicitly nil-tolerant (`internal/restore/logger_nil.go:9-17`, "these forwarders let Logger stay unset"), and `adapters_test.go:108` and `:125` both pass a nil logger.
  OLD: `// with the arming of every restored pane pinned to exe. logger must be non-nil.`
  NEW: `// with the arming of every restored pane pinned to exe.`

NOTES:
- The dispatch's three verification asks were answered. Production's `cmd/bootstrap_production.go:43` composition is **not** a third door: `os.Executable()` there resolves to the running portal binary, which is precisely the intended pin, and the wrapper is built as a `RestoreAdapter{Inner: …}` literal so it never passes through the constructor. The error-returning signature is **not** the smaller of the two options the task offered — the executor took both (required parameter *and* nil rejection) — but it is defensible: `restore.ExecutableResolver` is a func type, so a required parameter alone is nil-able and would enforce nothing. The guard cannot pass vacuously: `scanIntegrationSessionRestorerLiterals` errors at `scanned == 0`, covered for an empty tree and for a unit-only tree.
- Independent verification of the completeness claim: the reviewer re-implemented the `//go:build` predicate over the tree in a scratch program — 55 integration-tagged `_test.go` files, none composing `restore.SessionRestorer`. Every `internal/restore` integration file is `package restore_test`, so the guard's qualified-selector match reaches all of them.
- `bootstrapadapter.NewRestoreAdapter` has no production caller — production composes the wrapper directly. The constructor, its new signature and the exported sentinel exist in a production package for `restoretest` alone. Pre-existing placement the task directed work *at*, so not a finding here; worth remembering if that package is ever tidied.
- Both guards match only the qualified `restore.X` selector form, so an integration test written inside `package restore` would evade them. Harmless today — all eight `internal/restore` integration files are `package restore_test`. Pre-existing property of the orchestrator guard, now inherited.
- One cohesion wrinkle: the shared `literal_guard_scan_test.go` reads `restorePkg`, a const declared in `orchestrator_literal_guard_test.go`, so the shared file depends on one of its two consumers.
- `literal_guard_scan_test.go:106-107` carries an approximate count ("~70") that will quietly drift. Not raised as a correction — it reads as legitimate rationale.
