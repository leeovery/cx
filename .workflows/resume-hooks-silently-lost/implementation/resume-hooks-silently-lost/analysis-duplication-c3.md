AGENT: duplication
FINDINGS:
- FINDING: The Go-source guard scan skeleton is re-authored at eleven sites; two packages independently extracted their own local version
  SEVERITY: high
  FILES: cmd/hooks_pane_token_width_guard_test.go:25, cmd/run_hook_stale_cleanup_decline_error_guard_test.go:17, cmd/doctor_stand_down_phrase_guard_test.go:96, cmd/deps_seam_guard_test.go:31, cmd/deps_seam_guard_test.go:100, internal/hooks/cleanstale_staleness_guard_test.go:63, internal/hooks/leaf_guard_test.go:34, internal/theme/leaf_guard_test.go:137, internal/tmux/target_composition_guard_test.go:179, internal/restoretest/orchestrator_literal_guard_test.go:148, internal/portaltest/teardown_guard_coverage_test.go:70
  DESCRIPTION: `internal/sourceguardtest` owns the enumeration primitives (`GoSourceFiles`, `PackageGoFiles`) and the AST primitives (`ForEachFuncCall`, `CalleeName`) but stops short of the step between them, so every consumer writes the same ten-line loop itself: enumerate → `parser.ParseFile` per path → `t.Fatalf("parse %s: %v")` → count what was scanned → fatal when the count is zero. Two packages recognised the repetition and each extracted a private version of it — `internal/hooks`'s `scanPackageCalls` (with its own dedicated coverage test at internal/hooks/cleanstale_staleness_guard_scan_test.go:12) and `internal/theme`'s `parseThemeSources` — which is the same abstraction discovered twice and shared zero times. The copies have already drifted in ways that matter to what the guards promise: the parse mode varies (`SkipObjectResolution` at most sites, `ImportsOnly|SkipObjectResolution` at internal/hooks/leaf_guard_test.go:40, bare `0` at cmd/run_hook_stale_cleanup_decline_error_guard_test.go:25, cmd/doctor_stand_down_phrase_guard_test.go:105 and internal/theme/leaf_guard_test.go:137), and the scanned-nothing tripwire — the property that stops a guard passing by having stopped looking — is present at some sites (cmd/deps_seam_guard_test.go:65, internal/hooks/cleanstale_staleness_guard_test.go:85) and absent at others (internal/hooks/leaf_guard_test.go:34, internal/tmux/target_composition_guard_test.go:179, internal/theme/leaf_guard_test.go:148, which rely on `PackageGoFiles` erroring instead). A guard that silently scans nothing reports a safety it is not providing, and that invariant is currently a per-copy decision.
  RECOMMENDATION: Move the parse step into `internal/sourceguardtest` beside the primitives it already owns — a `ParsedSource{Path, Fset, File}` value plus `ParsePackageSources(t, dir, includeTests)` and `ParseSources(t, paths)` that fatal both on an unparseable file and on an empty result, taking the existing `TestingT` subset so their own failure paths stay testable. Re-point all eleven sites at it and delete `scanPackageCalls` (internal/hooks/cleanstale_staleness_guard_test.go:63) and `parseThemeSources` (internal/theme/leaf_guard_test.go:129); keep `scanPackageCalls`'s existing coverage test by re-aiming it at the shared helper. Each guard then holds only its predicate and its rationale.

- FINDING: The failing-write audit-trail assertion is hand-rolled at six sites despite logtest.AssertRecord, which their own file-siblings already use
  SEVERITY: medium
  FILES: internal/hooks/store_test.go:1288, internal/hooks/store_test.go:1441, internal/project/store_logging_test.go:102, internal/project/store_logging_test.go:219, internal/project/store_logging_test.go:320, internal/alias/store_logging_test.go:224
  DESCRIPTION: `logtest.AssertRecord`/`RecordWant` (internal/logtest/assert.go:20) exists to pin the five properties every audit-trail line shares, and the happy-path cases in all three store-logging suites were migrated onto it (internal/project/store_logging_test.go:39, internal/alias/store_logging_test.go:37, internal/hooks/store_test.go:1190 and neighbours). The WARN-on-failed-write cases were not: each still spells out `if rec.Level != slog.LevelWarn` / `if rec.Msg != …` / `AttrString(t, "op")` / `AttrString(t, "component")` inline, and then repeats the same two-part tail — an `error_class` string check followed by `rec.ErrorAttr(t, "error")` + `errors.Is(…, fileutil.ErrWriteTempCreate)` — at internal/hooks/store_test.go:1300, internal/hooks/store_test.go:1453, internal/project/store_logging_test.go:121, internal/project/store_logging_test.go:235 and internal/project/store_logging_test.go:333. The result is one contract asserted two ways inside the same file, with the divergence invisible: internal/project/store_logging_test.go's hand-rolled blocks assert no `via` at all, so the failed-write breadcrumb's `via` attr is unpinned on three of the four project mutations while every successful one pins it.
  RECOMMENDATION: Route the six blocks through `logtest.AssertRecord` exactly as their siblings do, and lift the recurring tail into one shared assertion beside it — e.g. `logtest.AssertWriteFailure(t, rec, wantClass, sentinel)` checking the `error_class` value and that the carried `error` wraps the named `fileutil` sentinel — so the failed-write breadcrumb contract lands in one place for hooks, projects and aliases alike.

- FINDING: The hooks-store lock WARN is asserted by two independently written helpers, one per package, plus four inline restatements of its tail
  SEVERITY: medium
  FILES: internal/hooks/lock_write_test.go:18, cmd/hooks_write_lock_test.go:47, internal/hooks/lock_write_test.go:185, internal/hooks/lock_write_test.go:203, cmd/hooks_write_lock_test.go:61
  DESCRIPTION: Both suites assert the same emission — the single WARN `hooks.Store.Set`/`Remove` leave when the sidecar acquire times out (internal/hooks/store.go:112, internal/hooks/store.go:168) — through two helpers written independently and named almost the same thing: `assertLockWarn(t, sink, wantOp, wantKey, wantVia)` (internal/hooks/lock_write_test.go:18, 26 lines, hand-rolling level/msg/op/component/via/hook_key/error) and `assertOneLockWarn(t, sink, wantOp, wantKey)` (cmd/hooks_write_lock_test.go:47, which pins the same properties through `assertHooksRecord` → `logtest.AssertRecord` and additionally requires exactly one WARN). Neither covers what the other adds, and the negative half of the contract — that a lock WARN carries no `error_class` and no `value`, because no write phase ran — is then restated verbatim three more times as inline `if _, ok := rec.Attrs["error_class"]; ok` / `if rec.HasAttr("error_class")` pairs. `internal/hookstest` is already the cross-package home for exactly this shape: it owns `AssertDegradedRead` (internal/hookstest/hooks_lock.go:112), the sibling assertion for the DEBUG `load-unlocked` breadcrumb, alongside `HoldHooksSidecar` which both suites use to produce the timeout.
  RECOMMENDATION: Add `hookstest.AssertLockWarn(t, sink, op, key, via)` next to `AssertDegradedRead`, covering the whole line in one call — level, message, `op`, `component=hooks`, `via`, `hook_key`, a non-empty `error`, and the absence of `error_class` and `value` — and have `internal/hooks/lock_write_test.go` and `cmd/hooks_write_lock_test.go` both call it, deleting the two local helpers and the three inline tails. Keep cmd's "exactly one WARN" check at its call site if that count is specific to the command path.

- FINDING: shellQuoteSingle was re-authored byte-for-byte beside the existing spawn.shellQuote, with a third inline restatement in session.BuildShellCommand
  SEVERITY: medium
  FILES: internal/restore/session.go:362, internal/spawn/recipe.go:67, internal/session/create.go:29
  DESCRIPTION: `internal/restore/session.go`'s `shellQuoteSingle` and `internal/spawn/recipe.go`'s `shellQuote` are the same function to the byte — `return "'" + strings.ReplaceAll(s, "'", `'\''`) + "'"` — introduced independently in different packages, and `internal/session/create.go:29` performs the same close-escape-reopen a third time inline (`strings.ReplaceAll(joined, "'", "'\\''")`) without wrapping it in a helper at all. This is a correctness rule, not a formatting convenience: all three sites are composing a string that a shell will later word-split (a tmux `respawn-pane` command, a `{command}` handed to a terminal recipe, a tmux shell-command), and CLAUDE.md already records that naive concatenation on the spawn path corrupts the command. Three separately-maintained copies of a quoting rule is three places for it to be fixed once and stay wrong twice.
  RECOMMENDATION: Give the rule one home the three callers can all reach. `internal/spawn`, `internal/restore` and `internal/session` have no shared leaf today (`tmuxout` is tmux-output parsing, `nanoid` is the id vocabulary), so the consolidation is a small stdlib-only leaf in the same mould — one exported quote function plus the existing `renderCommandString` join if that is worth moving with it — with `internal/restore/session.go:362` and `internal/spawn/recipe.go:67` deleted in favour of it and `session.BuildShellCommand` calling it instead of inlining the replace.

- FINDING: doctor.go bypasses the stand-down phrase vocabulary with two inline copies of one of its strings
  SEVERITY: medium
  FILES: cmd/doctor.go:295, cmd/doctor.go:299, cmd/run_hook_stale_cleanup.go:41, cmd/run_hook_stale_cleanup.go:51
  DESCRIPTION: `cmd/run_hook_stale_cleanup.go` declares two surface vocabularies for stand-down copy, `skippedPrunePhrases` and `notEvaluableDetails`, and `restoreStandDownPhrase` (cmd/run_hook_stale_cleanup.go:34) was extracted as a const precisely because one phrase was shared between them. Its siblings were not given the same treatment: `"could not read hooks.json"` is authored at cmd/run_hook_stale_cleanup.go:41 and again at :51, and `"could not enumerate live panes"` at :42 and again at :52. Worse, `checkStaleHooks` re-authors the first of those a third and fourth time as bare literals in its two early returns (cmd/doctor.go:295 for a nil store, cmd/doctor.go:299 for a failed `Load`) rather than rendering through `phraseFor(notEvaluableDetails, skipReasonStoreReadFailed)` the way its later returns do (cmd/doctor.go:303, cmd/doctor.go:309). The two vocabularies are covered by a phrase guard (cmd/doctor_stand_down_phrase_guard_test.go:44) and the exact rendered lines are pinned (cmd/doctor_stand_down_copy_test.go:43), but neither can see the two literals in doctor.go: re-wording the map entry leaves those two branches printing the old copy for the same condition, silently.
  RECOMMENDATION: Have both early returns in `checkStaleHooks` render through `phraseFor(notEvaluableDetails, skipReasonStoreReadFailed)` so the not-evaluable detail has one source for every branch that reports it, and lift the two strings shared by `skippedPrunePhrases` and `notEvaluableDetails` into consts beside `restoreStandDownPhrase` so each phrase is written once and the two maps compose from them.

- FINDING: Four leaf-package guards each restate the "transitive deps confined to an allowlist" check
  SEVERITY: medium
  FILES: internal/hooks/leaf_guard_test.go:57, internal/nanoid/leaf_guard_test.go:14, internal/prefs/leaf_guard_test.go:18, internal/theme/leaf_guard_test.go:23
  DESCRIPTION: All four guards assert the same property over `sourceguardtest.PackageDeps` — that a package's transitive dependency set stays inside a declared allowlist — and each writes the loop itself with a different shape. The allowlist is a `map[string]bool` in hooks, a `[]string` of forbidden entries plus an inline switch in prefs, a single forbidden path in theme, and an implicit "no dot in the first path segment" stdlib predicate in nanoid. The failure verb differs (`Errorf` in hooks and nanoid, `Fatalf` in prefs and theme), so an offending dep aborts two of the guards and lets the other two carry on reporting. Most consequentially the vacuity check has drifted: prefs asserts it still sees `internal/fileutil` so the guard cannot pass over an empty or unresolved dep set (internal/prefs/leaf_guard_test.go:44), and the other three have no such assertion at all — the property the check exists to protect is present in one copy of four.
  RECOMMENDATION: Add one assertion to `internal/sourceguardtest` beside `PackageDeps` — e.g. `AssertDepsWithin(t, pkg, allowed []string)` that skips the package itself, reports every dep outside the allowlist, and fails when the resolved set is empty or contains no allowlisted internal dep — and reduce each of the four guards to its allowlist plus its rationale comment. Keep nanoid's stdlib-only predicate as its own allowlist form if a path list cannot express it.

- FINDING: A fourth hooks.json staging route in cmd, with different sidecar behaviour from the other three
  SEVERITY: low
  FILES: cmd/state_hydrate_test.go:1234, cmd/testhelpers_test.go:156, cmd/testhelpers_test.go:208, internal/hookstest/staging.go:47
  DESCRIPTION: `internal/hookstest.StageStore` is the described route for handing a staged hooks.json to a seam, and cmd/testhelpers_test.go:150 documents `hooksFileInTempDir` as "the second of the two staging routes". There are in fact four: `StageStore`, `hooksFileInTempDir` (+ `writeHooksJSON` at cmd/testhelpers_test.go:208), and `seedHookStore` (cmd/state_hydrate_test.go:1234) — a private twelve-line marshal-and-write that stages `dir/hooks.json` and returns a store, driven from nineteen call sites across cmd/state_hydrate_test.go, cmd/state_hydrate_exec_log_test.go and cmd/state_hydrate_empty_hookkey_test.go. It differs from `StageStore` in one behaviour that is not obviously deliberate: it stages no sidecar, so every hydrate fixture's `LookupOnResume` takes the degraded unlocked-read path and leaves a `load-unlocked` breadcrumb, while a fixture staged through `StageStore` takes the shared lock unless it asks for `SidecarAbsent`. The doc comment in cmd/testhelpers_test.go asserting there are two routes is already false, which is how the drift stays invisible.
  RECOMMENDATION: Fold `seedHookStore` into `hookstest.StageStore` — its `Entries` field already seeds `{key: on-resume command}` pairs; extend it (or add a sibling field) for the multi-event `map[string]map[string]string` shape the hydrate suites pass — and re-point the nineteen call sites, so every cmd fixture stages the sidecar by the same default and the "two routes" claim in cmd/testhelpers_test.go becomes true again.

- FINDING: The restore integration setup preamble is written two ways, one of which discards the isolation helper's state directory
  SEVERITY: low
  FILES: internal/restore/armed_restore_integration_test.go:20, internal/restore/prefix_sibling_integration_test.go:32, internal/restore/rename_reboot_shared_test.go:52
  DESCRIPTION: The same six-line preamble opens each of these suites — `restoretest.BuildPortalBinaryDir` → `portaltest.IsolateStateForTest` → `t.Setenv("PORTAL_STATE_DIR", …)` → `state.EnsureDir` → `portaltest.RegisterStateDirTeardownGuard` — and the copies disagree about which directory the middle three operate on. internal/restore/armed_restore_integration_test.go:23 keeps the helper's own isolated dir (`_, stateDir := portaltest.IsolateStateForTest(t)`), while internal/restore/prefix_sibling_integration_test.go:33 and internal/restore/rename_reboot_shared_test.go:54 discard both return values and point `PORTAL_STATE_DIR` at a fresh `t.TempDir()` instead, leaving the isolation helper's dir allocated, registered with the sandbox and unused. The coverage rule (internal/portaltest/teardown_guard_coverage_test.go:63) accepts both forms, so nothing flags the divergence.
  RECOMMENDATION: Settle on one form — the helper's returned `stateDir`, which is what the sandbox registry and the fingerprint backstop are keyed on — and extract the preamble into a single `restoretest` fixture helper returning the binDir, stateDir and socket, so the three suites cannot drift apart on which directory they isolated.

- FINDING: The ENOENT-tolerant file read is implemented twice across a package boundary
  SEVERITY: low
  FILES: cmd/testhelpers_test.go:114, internal/hookstest/hooks.go:102
  DESCRIPTION: `readFileBytes` (cmd/testhelpers_test.go:114) and `hookstest.HooksFileBytes` (internal/hookstest/hooks.go:102) are the same ten-line rule — read the path, return nil on `os.ErrNotExist`, fatal on any other error — and the cmd copy carries its own test pinning the rule (cmd/testhelpers_test.go:126). `HooksFileBytes` is already path-generic and already takes the `logtest.TestingT` subset, so nothing about it is hooks-specific except its name; the cmd file imports `hookstest` and delegates its neighbouring assertion (`assertHooksFileUnchanged`, cmd/testhelpers_test.go:230) to it while re-implementing the read.
  RECOMMENDATION: Have `readFileBytes` delegate to the shared helper, renaming `hookstest.HooksFileBytes` to something path-generic (or relocating it) if the hooks-flavoured name is what discouraged reuse, and keep the single pinning test over whichever one survives.
SUMMARY: The production code is tightly single-sourced, with one exception each for shell quoting (a byte-identical helper re-authored beside the existing one) and stand-down copy (a phrase vocabulary bypassed by two inline literals in doctor.go). The test scaffolding carries the bulk of the duplication: the Go-source guard scan skeleton is written eleven times and was independently extracted twice, the leaf-package dependency assertion four times with its vacuity check surviving in only one copy, and the store audit-trail and lock WARN assertions are restated inline in files whose own siblings already route through the shared helper.
