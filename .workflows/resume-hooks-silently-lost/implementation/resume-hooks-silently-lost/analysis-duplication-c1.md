AGENT: duplication
FINDINGS:
- FINDING: Two independent staleness guard ladders — `checkStaleHooks` and `runHookStaleCleanup`
  SEVERITY: medium
  FILES: cmd/doctor.go:303-334, cmd/run_hook_stale_cleanup.go:69-128
  DESCRIPTION: The diagnosis and the sweep both run the same three-decision ladder over the same enumeration — restore-window stand-down, enumeration-failure, zero-rows-with-entries-persisted — and each carries its own copy of every branch. The shared *pieces* were extracted (`restoreWindowActive`, `liveTokensFrom`, `hooks.StaleKeys`/`staleKeys`), but the *order and the set of guards* is duplicated prose in two functions that the design requires to agree: the sweep must not delete on a pane-less read and the check must not report those entries stale, and both must stand down for the identical restore window. Divergence is silent and asymmetric — a fourth guard added to one (or a reordering) leaves the other reporting a state it does not act on, which is precisely the "diagnostic asks the reaper's question and gets a different answer" failure this work unit set out to close. The ordering already differs deliberately (the sweep gates on the restore marker before loading the store; the check loads first), which is the argument for a shared derivation with an explicit knob, not two hand-kept-in-sync ladders.
  RECOMMENDATION: Extract the ladder in `cmd/run_hook_stale_cleanup.go` as one function that answers "what can be judged right now" — e.g. `evaluateHookStaleness(lister AllPaneLister, persisted hooksFile) (liveTokens []string, standDownReason string, err error)` returning the empty reason when the set is judgeable. `runHookStaleCleanup` maps a non-empty reason onto its `standDownAttrs`/`reportSkip` path; `checkStaleHooks` maps the same reason through `skippedPrunePhrases` onto a `checkNotEvaluable` result. Neither then owns a private copy of the guard set.

- FINDING: `fakeHookLister` is a second copy of `stubAllPaneLister` in the same package
  SEVERITY: medium
  FILES: cmd/doctor_test.go:798-809, cmd/hookkey_vocabulary_test.go:111-130
  DESCRIPTION: Both are `AllPaneLister` fakes over the identical four fields (`rows`, `err`, `restoring`, `restoringErr`), both implement `ListAllPaneHookKeys` and `TryGetServerOption`, and both delegate the marker answer to the shared `restoringOption` helper. The only difference is a value vs pointer receiver and `stubAllPaneLister`'s optional `during` hook. `cmd/hookkey_vocabulary_test.go` declares itself the home for "the seam fakes that answer with them", and `fakeHookLister` sits outside it with 41 references in `doctor_test.go` and 2 more in `hook_sweep_lock_timeout_test.go` — so the two fakes are already interleaved inside single test files (`cmd/hook_sweep_lock_timeout_test.go:271` uses `stubAllPaneLister`, `:290` uses `fakeHookLister`), which is copy-paste drift arriving in real time.
  RECOMMENDATION: Delete `fakeHookLister` and re-point its call sites at `stubAllPaneLister` in `cmd/hookkey_vocabulary_test.go`. The doctor-side factories `staleHookLister()` / `restoringHookLister()` (cmd/doctor_test.go:876-887) and `seedStalePruneFixture`'s parameter type move with it.

- FINDING: Two hooks.json staging helpers in `cmd`, diverging on the sidecar
  SEVERITY: medium
  FILES: cmd/doctor_test.go:811-826, cmd/testhelpers_test.go:24-73, cmd/hooks_read_lock_test.go:29-33
  DESCRIPTION: `seedHooksJSON(t, keys...)` and `newStagedHooksStore`/`newTempHooksStore` both stage a hooks.json in a temp directory and return `(*hooks.Store, path)`. They have already drifted: `newStagedHooksStore` creates the sidecar lock file (deliberately, so a read takes its shared lock instead of degrading), and `seedHooksJSON` does not. The drift is visible at a call site — `cmd/hooks_read_lock_test.go` has to add `transienttest.CreateHooksSidecar` by hand with a three-line comment explaining that "seedHooksJSON stages no sidecar, so without this the baseline would itself degrade on ENOENT". Every other `seedHooksJSON` caller silently gets a degraded read it did not ask for, and `cmd/hook_sweep_lock_timeout_test.go` uses both helpers within one test function.
  RECOMMENDATION: Fold `seedHooksJSON` into `newStagedHooksStore` — give `hooksStoreStaging` a `keys []string` (or keep the JSON `seed` and have callers build the body from the seed vocabulary), so the sidecar decision is made once. Drop the hand-rolled `CreateHooksSidecar` compensation at `cmd/hooks_read_lock_test.go:33`.

- FINDING: The reboot fixture is arranged and driven three times
  SEVERITY: medium
  FILES: internal/restore/rename_reboot_hook_integration_test.go:76-158, internal/restore/rename_reboot_durability_integration_test.go:26-63, internal/restore/rename_reboot_durability_integration_test.go:123-152, cmd/noncontiguous_window_reboot_integration_test.go:362-380
  DESCRIPTION: Two blocks are duplicated. (1) The ~30-line arrange — build the binary, prepend PATH, `IsolateStateForTest`, state dir + `EnsureDir`, `PORTAL_HOOKS_FILE`, hook-fire file, `store.Set(stableKey, …)`, `tmuxtest.New`, `new-session`, `WaitForSession`, `StampPaneToken` — appears verbatim in `runRenameRebootFire` (:76-106) and `TestRenameRebootHook_DurableAcrossRepeatedReboots` (:26-56), differing only in the socket prefix. (2) The ~27-line reboot-and-hydrate — `KillServer`, the `list-sessions` must-fail guard, `EnsureServer`, `OpenTestLogger`, `restore.Orchestrator{…}`, `RestoreWithMarker`, the `list-panes` "0:0" check, `DriveSignalHydrate`, `WaitForSkeletonMarkersCleared` — is open-coded at `rename_reboot_hook_integration_test.go:132-158` even though `rebootAndHydrate` in the *same package* (`rename_reboot_durability_integration_test.go:123`) already is that function; `cmd/noncontiguous_window_reboot_integration_test.go`'s `reboot` method is a third copy of its first half. These two files share `rename_reboot_shared_test.go` as a declared home, so the split is accidental rather than intentional.
  RECOMMENDATION: Move the arrange into `internal/restore/rename_reboot_shared_test.go` as one `newRenameRebootFixture(t, socketPrefix) (*tmuxtest.Socket, *tmux.Client, hooksPath, hookFireFile string)`, and have `runRenameRebootFire` call the existing `rebootAndHydrate` (move it into the shared file with the fixture). For the cross-package third copy, promote the kill-server → EnsureServer → Orchestrator → `RestoreWithMarker` sequence into `internal/restoretest` beside `RestoreWithMarker` (e.g. `RebootAndRestore(t, ts, client, stateDir) error`), which `cmd/noncontiguous_window_reboot_integration_test.go` can then reach.

- FINDING: `hooksDeps` install-and-restore repeated 69 times with no helper
  SEVERITY: medium
  FILES: cmd/hooks_test.go (38 sites), cmd/hooks_rm_exit_test.go (11), cmd/hooks_write_lock_test.go (8), cmd/hooks_pane_token_test.go (8), cmd/hooks_read_lock_test.go (2), cmd/root_test.go (2)
  DESCRIPTION: Every seam-injecting test writes the same two statements — `hooksDeps = &HooksDeps{…}` followed by `t.Cleanup(func() { hooksDeps = nil })`. That is ~140 lines of pure boilerplate, and the pairing is load-bearing rather than cosmetic: the package-level `hooksDeps` var leaks into every later test in the package if the cleanup is ever omitted, and nothing in the codebase makes the pair inseparable. `cmd/testhelpers_test.go` already exists as the staging home and already owns the `runHookSet`/`runHookRm`/`runHookList` drivers these same tests use.
  RECOMMENDATION: Add `func withHooksDeps(t *testing.T, deps HooksDeps)` to `cmd/testhelpers_test.go` that assigns `hooksDeps = &deps` and registers the cleanup, and convert the call sites. The restore then cannot be forgotten.

- FINDING: The "--pane-key path takes no tmux call" poison fixture is re-authored at seven sites, already drifting
  SEVERITY: medium
  FILES: cmd/hooks_rm_exit_test.go:109-127, cmd/hooks_rm_exit_test.go:162-178, cmd/hooks_rm_exit_test.go:215, cmd/hooks_rm_exit_test.go:262, cmd/hooks_pane_token_test.go:170-187, cmd/hooks_write_lock_test.go:236-248
  DESCRIPTION: The same fixture — a `mockKeyResolver` and a `recordingPaneStamper` armed with "must not be called on the --pane-key path" errors, followed by the paired `resolver.calls != 0` / `len(stamper.calls) != 0` assertions — is written out at seven declaration sites across three files. The drift is already present: `cmd/hooks_write_lock_test.go:236-237` uses `errors.New` where the other five use `fmt.Errorf` for the identical message, and three sites poison only the resolver while three poison both, so which seam is actually guarded varies by site rather than by intent.
  RECOMMENDATION: Add one helper beside the other hook-key fakes in `cmd/hookkey_vocabulary_test.go` — e.g. `paneKeyPathSeams() (*mockKeyResolver, *recordingPaneStamper)` returning both poisoned — plus an `assertNoPaneTmuxCalls(t, resolver, stamper)` for the assertion half, and convert all seven sites so every `--pane-key` case guards the same two seams.

- FINDING: `assertHooksFileUnchanged` exists but six sites open-code it
  SEVERITY: low
  FILES: cmd/run_hook_stale_cleanup_test.go:331, :391, :413, :583, :612, :728, cmd/testhelpers_test.go:177-187
  DESCRIPTION: `assertHooksFileUnchanged(t, path, before, context...)` is the shared "prove the route left the file byte for byte" assertion (13 call sites), and it takes an optional context string for exactly the per-case wording these six sites want. All six instead write `if after := readFileBytes(t, path); !bytes.Equal(before, after) { t.Errorf("hooks.json … \nbefore: %s\nafter:  %s", before, after) }` inline; one of them (:308-332) additionally re-implements `readFileBytes` with a raw `os.ReadFile` + `t.Fatalf` pair on both ends. Half the suite routes through the helper and half bypasses it, so an improvement to the failure message reaches only half.
  RECOMMENDATION: Replace the six inline comparisons with `assertHooksFileUnchanged(t, path, before, "<the existing per-case wording>")`, and replace the raw `os.ReadFile` pair at :308-315/:326-332 with `readFileBytes` + the same helper.

- FINDING: Two stand-down record assertions differing only in level
  SEVERITY: low
  FILES: cmd/run_hook_stale_cleanup_test.go:538-554, cmd/hook_sweep_lock_timeout_test.go:30-42
  DESCRIPTION: `standDownRecord` and `lockStandDownRecord` both assert that the sink holds exactly one stand-down record, run it through `assertHooksRecord(t, rec, standDownWant(level))`, check the `reason` attr and return the record. They differ only in the expected level (DEBUG vs WARN), the expected reason, and whether the "exactly one" count is taken over all records or over WARN records. `standDownWant` was already extracted as the shared shape, so the remaining split is the wrapper around it.
  RECOMMENDATION: Collapse into one `assertStandDown(t, sink, level slog.Level, reason string) logtest.Record` in the file that owns `standDownWant`, selecting the record set from the level argument.

- FINDING: The "returns at the bound rather than hanging" case is duplicated verbatim
  SEVERITY: low
  FILES: cmd/hooks_write_lock_test.go:180-204, cmd/hooks_write_lock_test.go:252-277
  DESCRIPTION: Two ~25-line subtests in the same file are line-for-line identical apart from `runHookSet` vs `runHookRm` and the command name in the two failure messages — same lowered bound, same held sidecar, same `mockKeyResolver{key: "tok123"}`, same `elapsed < lockBound` and `elapsed > 20*lockBound` assertions including the same "generous multiple" reasoning. Only two instances, but each is a whole subtest rather than a few lines, and the 20× multiplier now lives in two places.
  RECOMMENDATION: Extract one `assertReturnsAtLockBound(t *testing.T, verb string, run func() (string, error))` in the same file and drive both from it.

- FINDING: Three ways to stage a hooks.json whose writes are denied
  SEVERITY: low
  FILES: internal/hooks/store_test.go:30-51, internal/hooks/store_test.go:73-92, cmd/testhelpers_test.go:39-66
  DESCRIPTION: `readOnlyDirPath`, `seedThenDenyWrites` and `newStagedHooksStore{writesDenied: true}` all perform the same sequence: make the directory writable, create the sidecar (`transienttest.CreateHooksSidecar`), chmod the directory to 0500, register a 0700 cleanup, return the hooks.json path. `seedThenDenyWrites`'s doc comment claims it "cannot use readOnlyDirPath" because the seed write must precede the lock, but `readOnlyDirPath` already creates the directory at 0700 and denies only at the end — the two differ solely in whether a body is written before the chmod.
  RECOMMENDATION: Collapse the two `internal/hooks` helpers into one that takes the optional seed body (`seedThenDenyWrites(t, nil)` giving today's `readOnlyDirPath`), and correct the comment that asserts the split is necessary. If the `cmd` variant is worth unifying too, the natural home is `internal/transienttest` beside `CreateHooksSidecar`.

- FINDING: The sidecar-is-free probe is written three ways
  SEVERITY: low
  FILES: internal/hooks/lock_test.go:36-49, cmd/hook_sweep_snapshot_order_test.go:50-61, internal/transienttest/hooks_lock.go:64-71
  DESCRIPTION: "Open `<path>.lock`, take `LOCK_EX|LOCK_NB` from a fresh fd, then unlock" appears as `assertSidecarFree` in `internal/hooks`, inline inside the enumeration `during` hook in `cmd`, and as the `openSidecar` half of the `transienttest` holders. `internal/transienttest/hooks_lock.go` is the declared cross-package home for sidecar fixtures and already exports `CreateHooksSidecar`, `HoldHooksSidecar` and `HoldHooksSidecarShared`, so the free-probe is the one member of that family that was not promoted.
  RECOMMENDATION: Export `transienttest.AssertSidecarFree(t, hooksPath)` beside the holders, implemented over the existing `openSidecar`, and use it from both `internal/hooks/lock_test.go` and `cmd/hook_sweep_snapshot_order_test.go`.

- FINDING: `replayCfg` and `timeoutCfg` are near-duplicate hydrateConfig builders
  SEVERITY: low
  FILES: cmd/state_hydrate_replayed_log_test.go:16-29, cmd/state_hydrate_test.go:938-951
  DESCRIPTION: Both build a `hydrateConfig` from the same seven positional parameters and set the same fields; they differ only in the `OpenFIFO` seam (`openFIFOWithTimeout` vs `instantTimeoutOpenFIFO`), whether `HandleFileMissing` is set, and whether the tmux `Commander` is supplied or fixed. The new `cmd/state_hydrate_empty_hookkey_test.go:73-74` then bypasses both and writes the struct literal inline again.
  RECOMMENDATION: Keep one builder taking the `OpenFIFO` seam (and the commander) as its varying arguments, and have the timeout, replay and empty-hook-key suites all construct through it. Note the pre-existing inline literals elsewhere in `cmd/state_hydrate_test.go` are outside this work unit's changes and need not be swept.
SUMMARY: The production surface is well consolidated — the pane-token literal, the staleness rule, the lock primitives and the stand-down vocabulary each have a single home — with one genuine parallel: `checkStaleHooks` and `runHookStaleCleanup` keep independent copies of the same three-guard ladder that the design requires to agree. The remaining findings are in the test surface, where the phase consolidations extracted most of the vocabulary but left duplicate seam fakes (`fakeHookLister` vs `stubAllPaneLister`), duplicate hooks.json staging that has already drifted on the sidecar, an un-shared reboot fixture across two sibling files, and several already-extracted helpers that half the call sites bypass.
