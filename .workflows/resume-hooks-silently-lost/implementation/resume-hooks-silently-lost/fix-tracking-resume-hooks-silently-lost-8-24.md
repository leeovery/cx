## Attempt 1

ISSUES:
- Two of the three recording stubs the dispatch explicitly named as fold-in candidates were retained on a rationale that does not hold for them, and that rationale is now written into CLAUDE.md as doctrine. `internal/tmux/target_composition_guard_test.go:562-576` (`recordingT`, used at :275 and :298) and `internal/restoretest/literal_guard_scan_test.go:91-105` (`recordingT`, used at `orchestrator_literal_guard_test.go:114,121` and `session_restorer_literal_guard_test.go:138,145`) discard the scan's return value at every fatal-path call site — they assert only `fataled` and `msg`, so there is nothing "returned after the fatal" for a stopping stand-in to be unable to observe. Traced: `scanBareTargets` (`target_composition_guard_test.go:334-361`) raises exactly one fatal on both paths, and both `scanGuardTestFiles` fatal paths carry "stopped looking" in the *first* fatal as well as the last, so `harnesstest.Recorder` + `Run` reproduces every current assertion. The same is true of `stubT` (`internal/sourceguardtest/assertdepswithin_test.go:123-147`): `AssertDepsWithin` returns nothing at all, so the stated reason cannot apply to it. Only `recordingT` in `internal/sourceguardtest/packagedeps_test.go:49-63` (asserts `deps != nil` after the fatal, :38) and `recordingTB` in `internal/commandertest/scripted_test.go:16-29` (asserts the empty `(out, err)` pair after the strict `RunRaw` fatal, :172) genuinely need a non-stopping stub — and `commandertest` has the further reason that it reports through `tb` from a mutex-held, possibly non-test goroutine, where a panic would take the binary down. `recorder.go:19-21` already states the correct criterion ("a helper whose fatal path is meant to return rather than abort"), which the three retained stubs do not meet.
  FIX: Fold in the two stubs the dispatch named. Replace `recordingT` in `internal/tmux/target_composition_guard_test.go` and in `internal/restoretest/literal_guard_scan_test.go` with `harnesstest.Recorder`, wrapping each call site in `rec.Run(func() { … })` and asserting `len(rec.Fatals) == 1` plus `strings.Contains(rec.Fatals[0], …)` against the same substrings. Do the same for `stubT` in `internal/sourceguardtest/assertdepswithin_test.go` (its `errored` substring helper becomes a two-line local over `rec.Errors`). Then narrow the CLAUDE.md `harnesstest` row so the retention clause names only the two cases where it is true — `sourceguardtest`'s `PackageDeps` and `commandertest`'s strict `RunRaw` — dropping `AssertDepsWithin` and "the two literal-scan guards" from the parenthesis, and dropping the `errors []string` fields added to the two `recordingT`s along with them.
  ALTERNATIVE: keep all three and correct the CLAUDE.md sentence to state the real reason — that a non-stopping stub also exercises the helper's explicit `return` after each `Fatalf`. Cheaper, but it preserves three near-duplicate stand-ins the task exists to remove and documents a property nothing asserts. Reviewer recommends the fold-in.
  CONFIDENCE: medium

COMMENT_CORRECTIONS:
- `internal/sourceguardtest/doc.go:3` — the diff made the "stdlib alone" claim false (the package now imports `internal/harnesstest`); the identical claim on `commandertest` was amended, this one was missed.
  OLD:
  // files rather than by executing them. It depends on stdlib alone and carries no
  // build tag, so every guard it serves runs in the unit lane.
  NEW:
  // files rather than by executing them. Beyond the shared *testing.T stand-in its
  // helpers report through, it depends on stdlib alone, and it carries no build
  // tag, so every guard it serves runs in the unit lane.
- `CLAUDE.md` (the `restoretest / tmuxtest / … / commandertest` table row) — same claim, same falsification, unique occurrence in the file.
  OLD: stdlib-only and untagged so every guard driven by it runs in the unit lane
  NEW: stdlib-only beyond the shared `harnesstest.TestingT` and untagged, so every guard driven by it runs in the unit lane

NOTES:
- All four verification asks were answered. (a) The `hookstest` criterion is literally unmet but the executor's reading is right: the residual import is `hooks_lock.go` using `logtest.Sink` for a logging assertion, which CLAUDE.md's row already documents; `hooks.go` is clean, and that was the defect the criterion was written from. (b) The broken-recover diagnosis is accurate — the old `captureAssert` never asserted its own sentinel, so an unrelated panic surfaced as "recorded nothing"; `logtest`'s and `cmd`'s copies carried the same blanket recover and all three are gone. The replacement genuinely re-panics, proved by mutation: swallowing every panic fails the re-raise case, and removing the panic from `Fatalf` fails the stop case. (c) See ISSUES — the rationale holds for two of five, not five. (d) The leaf guard bites: adding a non-stdlib import to a scratch copy fails it, naming the offender.
- `internal/restoretest/waitfor_file_exists.go:10`'s `fataller` survives and is correctly not a duplicate — `Helper`/`Name`/`Fatalf`, no `Errorf`. Banked.
- The `errors []string` fields added to `internal/sourceguardtest/packagedeps_test.go:52` and `internal/tmux/target_composition_guard_test.go:565` are written but never read — the widened union forces an `Errorf` on stubs whose subject never calls it. Both disappear if the ISSUE is fixed by fold-in.
- `Recorder` is not goroutine-safe (unsynchronised slice appends), matching every fake it replaced. Worth knowing before handing one to `commandertest.Scripted`, whose doc says reporting from a non-test goroutine is legal — a further reason its own suite's `recordingTB` should stay put.

## Attempt 2

ISSUES:
- `internal/restoretest/waitfor_file_exists.go:9-14` declares `fataller` (`Helper`/`Name() string`/`Fatalf`) — a third home for the very seam this task exists to unify, in one of the four packages it re-points. Its doc comment ("lets a unit test drive the timeout branch without aborting itself") states the identical rationale as `harnesstest.TestingT`. It serves three helpers (`waitfor_file_exists.go:23`, `restore_marker.go:39`, `restoretest.go:67`). It is not substitutable with `harnesstest.TestingT`, which lacks `Name()` — and `Recorder.Name()` (`recorder.go:36`) exists *only* to satisfy it, an undeclared coupling between the two packages. This also makes the CLAUDE.md claim the diff introduced at `CLAUDE.md:86` false: the row asserts `logtest`, `sourceguardtest`, **`restoretest`**, `hookstest`, `tmuxtest` and `commandertest` "all take it rather than declaring their own", while `restoretest` does both.
  FIX: Home the `Name()`-bearing variant in `harnesstest` beside `TestingT` — e.g. `type NamingT interface { TestingT; Name() string }` — and re-point `fataller`'s three consumers at it, deleting the local declaration. `Recorder` already satisfies it, so the three call sites in `waitfor_file_exists_test.go`, `restore_marker_test.go` and `staged_hydrate_exe_test.go` need no change. The CLAUDE.md claim then becomes true as written.
  ALTERNATIVE (i): add `Name() string` to `harnesstest.TestingT` itself — one declaration exactly, at the cost of forcing `Name()` onto the two retained local stubs that have no use for it, an interface-segregation cost for a simpler surface. ALTERNATIVE (ii): doc-only — keep `fataller` and amend the CLAUDE.md sentence to state the carve-out honestly. Cheapest, but leaves the duplication the task was raised to remove; reviewer does not recommend it. Reviewer recommends the primary fix: it removes the declaration without widening the common interface.
  CONFIDENCE: medium

NOTES:
- All four verification asks were answered. (a) Fold-in fidelity verified call site by call site: no site lost an assertion. The two `sourceguardtest` sites that *did* previously observe continuation past a first fatal only ever asserted the boolean; their replacements assert exactly one fatal, which is tighter and matches what a real `*testing.T` does. The `restoretest` and `internal/tmux` sites moved from last-message to first-message assertions, and both emit exactly one fatal on those paths. (b) Both retained stubs genuinely need to be non-stopping — each asserts a returned value after the fatal, matching the criterion documented on the new type; exactly two survive tree-wide. (c) Criteria 3 and 4 are met outright. (d) The leaf guard still bites: any dep that is neither stdlib nor the package itself falls to the reporting arm.
- Criterion 2 (`hookstest` no longer imports `logtest`) remains literally unmet and the executor's reading is again upheld: the residual import is `hooks_lock.go` using `logtest.Sink` for log-breadcrumb assertions — subject-matched, pre-existing, untouched by the diff, and documented in CLAUDE.md's own `hookstest` row. Removing it would be a regression. The offence the criterion was written against is resolved in `hooks.go`.
- The scope expansion beyond the task text (`tmuxtest.TestingT`, `commandertest.TestingT`, `cmd`'s `seamGuardT`, the three folded-in stubs) is justified — criterion 1 is stated absolutely, and leaving those would have failed it. Both packages' CLAUDE.md rows were amended to match, correctly.
- `harnesstest.Recorder` is not safe for concurrent use (bare slice appends), whereas the `*testing.T` it stands in for is. Realised only if a `Recorder` is handed to `commandertest.New`, whose package doc states reporting is legal from a non-test goroutine and whose strict-`RunRaw` `Fatalf` runs outside the fake's mutex. No current call site is concurrent and the pre-existing local stub had the same property, so this is latent rather than live. A one-line warning on the type, or a mutex, closes it.
- `fatalSentinel` is an empty struct, so a `Recorder.Fatalf` reached *outside* `Run` crashes the suite with `panic: {}` and no message. Giving the sentinel the recorded message (or an `Error()`/`String()`) would make that misuse self-diagnosing.
- `logtest/assert_test.go:39` calls `AssertRecord(spy, …)` without `rec.Run` — correct today (that helper only `Errorf`s), but inconsistent with every other site and would surface an opaque `panic: {}` if it ever gained a `Fatalf`.
- `AssertDepsWithin`'s defensive `return` after its `moduleOf` fatal is no longer exercised now that the stand-in stops. Under a real `*testing.T` it is unreachable regardless, so no live behaviour lost coverage — recorded so it is not mistaken for a gap later.
