## Attempt 1

ISSUES:
- `internal/restore/rename_reboot_shared_test.go:25-31`, `cmd/bootstrap/reboot_roundtrip_test.go:105-108`, `internal/restore/exit_closes_pane_integration_test.go:127-130` — three new ways to stamp a pane token were introduced, bypassing the shared fixture helper `tmuxtest.Socket.StampPaneToken` (`internal/tmuxtest/stamp.go:11`) that the immediately preceding task created for exactly this. That task's commit is titled "one real-tmux fixture preamble and **one way to stamp a pane**" (70c7e50a) and converted eight sites across `cmd` and `internal/tmux` onto it; this task adds a package-local `stampPaneToken` helper plus two inlined `client.SetPaneOption` calls beside it. All three sites have a `*tmuxtest.Socket` in scope. Beyond the duplication, the helper's own doc states the reason it exists — "so a fixture never stages itself through the client call under test" — and at 3-2 `client.SetPaneOption` becomes the production call `internal/restore` is being tested on. 3-2 and 3-5 both add further pane-stamping fixtures, so whichever pattern this task establishes propagates.
  FIX: delete `stampPaneToken` from `internal/restore/rename_reboot_shared_test.go` and call the shared helper at each site — `ts.StampPaneToken(t, tmux.PaneTarget(renameOldName, 0, 0), renamePaneToken)` at `rename_reboot_hook_integration_test.go:107` and `rename_reboot_durability_integration_test.go:56`, the two-pane equivalent at `multipane_legacy_integration_test.go:81-82`, `ts.StampPaneToken(t, hookPaneTarget, savedHookKey)` at `reboot_roundtrip_test.go:105-108`, and `ts.StampPaneToken(t, paneTarget, paneToken)` at `exit_closes_pane_integration_test.go:127-130`. `Socket.Run` already `t.Fatalf`s on a non-zero tmux exit (`internal/tmuxtest/socket.go:58-65`), so failure behaviour is unchanged. Keep `readPaneToken` where it is — it already reads through raw tmux and is the right side of this line.
  ALTERNATIVE: keep a package-local `stampPaneToken` that delegates to `ts.StampPaneToken` rather than to `client.SetPaneOption`. This preserves the `(sessionName, window, pane)` call shape the restore fixtures use, at the cost of a second name for one operation. The reviewer recommends calling the shared helper directly — the `tmux.PaneTarget(...)` argument is the same expression the wrapper builds internally, and one name is the point of the consolidation.
  CONFIDENCE: high

NOTES:
- All acceptance criteria met, verified independently. The per-pane lift is proven twice (fake-row unit case and real-tmux `TestMultiPaneLegacy_PerPaneHookRouting`). The skipped-pane merge holds. All three reboot firing fixtures keep their firing assertion on the right pane and none is vacuous — remove the stamp and the baked key is `""` and the assertion fails.
- `TestMultiPaneLegacy_GracefulLegacyDegradation` retired whole is correct: every assertion in it belongs to the name-fallback branch the spec deletes.
- `TestRenameRebootHook_DurableAcrossRepeatedReboots` is a real reduction but was not repairable at this task. Banked for after 3-2 lands.
- The executor's own banked entry proposing a pane-stamp helper in `internal/restoretest` is aimed at a helper that already exists in `internal/tmuxtest`; fixing the issue above makes it moot.
- `internal/state/schema_test.go:14` — `fullyPopulatedIndex()` no longer populates every field (all three panes leave `PortalPaneID` empty). Not a gap in practice, since a dedicated round-trip test covers the new field, but the fixture's name over-promises slightly.
- `savedPaneArmInfo`'s rewritten doc comment refers to "whatever the restore re-stamp does" while no re-stamp exists at this commit — a forward reference the plan's own Context uses and 3-2 makes exactly right, so no correction filed.
