## Attempt 1

ISSUES:
- internal/restoretest/restoretest.go:56-59 and cmd/reattach_integration_test.go:50-54 — StagedHydrateExe("") returns filepath.Join("", "portal") = "portal", silently producing the exact bare PATH lookup this task exists to remove, with no error and no test failure. stagedRestoreAdapter reads the package-level reattachBinDir, which is populated only as a side effect of ensurePortalOnPATH's sync.Once — an undocumented ordering dependency between two differently-named functions. Every call site happens to satisfy it today (setupReattachEnv at :71 and setupConcurrentColdBootEnv at cmd/concurrent_coldboot_integration_test.go:35 both run first), so there is no live defect. But a future cmd integration test that reaches stagedRestoreAdapter without that prologue arms its panes from PATH against the developer's installed release, and when a hook key is baked that run passes against the wrong binary — silent misattribution, the failure class the whole task exists to close, reintroduced inside its own scaffolding.
  FIX: give stagedRestoreAdapter the t it needs and make the dependency explicit rather than positional — func stagedRestoreAdapter(t *testing.T, client *tmux.Client, stateDir string, logger *slog.Logger) calling ensurePortalOnPATH(t) as its first statement (idempotent: the build is sync.Once-guarded and t.Setenv is per-test), then reading reattachBinDir. Update the two call sites at cmd/reattach_integration_test.go:64 and cmd/concurrent_coldboot_integration_test.go:123,312.
  ALTERNATIVE: harden the helper instead — func StagedHydrateExe(t *testing.T, binDir string) restore.ExecutableResolver with t.Helper() and if binDir == "" { t.Fatalf(...) }, updating all eight call sites. This covers every caller rather than just the reattach path and matches the *testing.T-first convention BuildPortalBinaryDir and PrependPATH already use in the same file, but it is a wider edit and still leaves the ordering coupling implicit. The reviewer recommends the primary fix; doing both is cheap and would be better still.
  CONFIDENCE: medium

NOTES:
- Splitting TestPhase5_OrchestratorEndToEndSmoke out into an untagged cmd/bootstrap/phase5_smoke_test.go rather than sweeping it into the integration lane is the right call — it drives NoOpRestorer and NoOpSaver, so it execs nothing, and tagging it would have silently shrunk the unit lane.
- internal/restoretest now imports internal/restore, which permanently bars internal/restore's in-package test files from importing restoretest. Nothing needs it today, but it is a constraint a future contributor will meet as an import-cycle error rather than a documented rule.
- Orchestrator -> SessionRestorer propagation of Exe (internal/restore/restore.go:52) has no unit-level pin; dropping that line would only surface as an integration-lane failure. Acceptable, but the one link in the new seam with no fast feedback.
- When the resolver returns ("", nil) the WARN at internal/restore/session.go:333 emits error=<nil>. Cosmetic.
- AC1's literal wording cannot be satisfied — the Do list itself mandates a bare-name fallback at internal/restore/session.go:315, and the daemon/hook/init sites must stay bare (pgrep identity contract, hook bodies persisting across upgrades, init.go emitting a user shell function). Recorded as an accepted deviation rather than an unmet criterion.
