## Attempt 1

ISSUES:
- `internal/hooks/store_test.go:775,781` — `TestCleanStaleRemovesExactlyStaleKeys` still spells four hand-rolled token-shaped literals (`"keyA00"`, `"keyB00"`, `"keyC00"`, `"keyD00"`), two of them re-used as the live set at `:781`. This is the last surviving token-shaped seed literal in the repo (verified by a repo-wide scan of every `"[A-Za-z0-9]{6}"` in files touching `hooks.`), and it defeats acceptance criterion 3 as written. It also undercuts the finding's stated payoff — "the next re-point is one edit rather than eighteen" — for the reaper's own parity test, which phase 2 will have to find by hand. The positional carve-out does not cover it: the "live" set here is a hand-passed `[]string`, not a tmux enumeration, and the keys are token-shaped rather than positional, so converting them cannot invert the test's meaning. It sits inside a file this task already rewrote, one page below the vocabulary var block — exactly the half-converted end state the executor argued against when it widened scope elsewhere in the same file.
  FIX: add `reapableSeedC = transienttest.ReapableHookKey(2)` to the var block at `internal/hooks/store_test.go:477-481` (currently A, B, D with C missing — `cmd/doctor_test.go:813-821` already declares all four), then at `:775` use `[]string{reapableSeedA, reapableSeedB, reapableSeedC, reapableSeedD}` and at `:781` use `live := []string{reapableSeedA, reapableSeedC}`. Two live, two stale — identical to today, so `predicted` stays non-empty and the `t.Fatal` guard at `:790` keeps doing its job. No assertion, count or name changes.
  CONFIDENCE: high

NOTES:
- The sorted `want` slice at `internal/hooks/store_test.go:754` preserves what the test measured. `StaleKeys` returns map-iteration order, so the comparison was always set-equality-after-sorting; sorting `want` removes only an assertion about the fixture's authored order, and it is a no-op at today's values. A defensible technical deviation from "change no assertion".
- The widened scope in `internal/hooks/store_test.go` was the right call: every additional re-point was a seed site in the same sense (token-shaped, absent from the live set, before and after), and none changed a test's role.
- Live-set-coupled positional keys were verified independently, seed by seed, against every live-set declaration across all 21 files. Not one was converted — the error class that would silently invert a test did not occur.
- Three names in package `cmd` now hold the same value `ReapableHookKey(0)` (`reapableSeedA`, the local `staleKey` in two files, `staleHookKey`). Harmless — each is a distinct temp store — but the one-declaration goal is partially undone within the package.
- `cmd/hook_retention_shape_test.go:16` lost the non-zero coordinate its old seed carried (`old-session:3.1` → `unjudgeable-session-0:0.0`). The subtest measures shape, not coordinates, so nothing is lost.
