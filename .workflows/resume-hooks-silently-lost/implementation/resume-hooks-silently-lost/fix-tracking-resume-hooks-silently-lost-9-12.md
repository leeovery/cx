## Attempt 1

ISSUES:
- `internal/hooksweep/sweep_test.go:223` — `TestUnjudgeableHookKeyRetention` was moved wholesale into `hooksweep`, but two of its three subtests had the `cmd` caller as their subject and were dropped rather than left behind: `"it retains a non-token-shaped key across doctor --fix"` (drove `runDoctorWith(t, deps, "--fix")` and asserted the file still held the entry *and* that stdout carried no `Pruned stale hook:` line for it) and `"it exits 0 from portal doctor with only retained non-token-shaped entries present"` (drove `runDoctorWith(t, deps)` and asserted `✓ stale hooks: no stale hooks` with a nil Execute error). The second has no equivalent anywhere: `checkStaleHooks` stayed in `cmd`, and the surviving unjudgeable case at `cmd/doctor_test.go:1121` asserts the *mixed* install (`checkFail`, "1 stale hook entry"), while `cmd/hook_prune_verdict_parity_test.go:24` asserts only that an all-unjudgeable install is judgeable, not that it passes. Nothing now pins that an install holding only unjudgeable keys is a *healthy* one — which is the user-visible face of the "retention is the safety property" invariant. This also contradicts the task's own Do-list instruction to leave the `cmd` suites that assert on rendered output where they are.
  FIX: Restore `TestUnjudgeableHookKeyRetention` in `cmd` holding exactly those two subtests, verbatim from `git show HEAD:cmd/run_hook_stale_cleanup_test.go` lines 254-293, in a new file `cmd/hook_prune_unjudgeable_retention_test.go` (matching the `hook_prune_*` naming the other re-homed caller suites took). Every helper they need — `seedHealthyStateDir`, `staleDeps`, `runDoctorWith`, `hooksBody`, `stubStaleSweepReader`, `tokenRows`, `seedProjectsJSON` — is still declared in `cmd`, so the restoration is mechanical. Leave the third subtest where it now is in `hooksweep`.
  ALTERNATIVE: Fold only the second subtest into `TestDoctorStaleHooksCheck` in `cmd/doctor_test.go` beside the existing case at line 1121, and let the first go as redundant with the moved sweep case. Cheaper, but it drops the `--fix` end-to-end arm entirely — the reviewer recommends the full restoration, since that arm is the one a user actually runs against a file they cannot get back.
  CONFIDENCE: high

COMMENT_CORRECTIONS: none.

NOTES:
- The move was verified genuine by literal diff rather than by the executor's own characterisation test: the only string-literal deltas between the old file and the three new ones are the seven copy-table phrases (reappearing byte-for-byte in `cmd/doctor.go:213-253`), the new component argument, the import path, and one message collapsing from two inline occurrences to one const used twice. Every `op`, `via`, `reason`, message and level is unchanged, attr order included.
- All nine guards verified individually; none left pointing at a departed package. The single-caller guard's widening is strictly stronger than what it replaced.
- The exported surface is minimal and the two-gate justification holds; `StandDown.emit()` staying unexported is a strict improvement, since `cmd` can now read a decline's reason but structurally cannot emit a stand-down line.
- `hooksweep.PaneHookLister` and `cmd.PaneHookLister` are two declarations of the same one-method interface with byte-identical doc comments. Unavoidable and idiomatic, but the prose will drift silently.
- `View.Enumerated` is exported but read only inside `hooksweep`. It was exported before the move too, so preserving the field set was right for a move — noted so a later pass knows it is not a caller requirement.
- `cmd/hookkey_vocabulary_test.go:216-220` re-declares two log messages as test-local literals, invisible to the phrase guard. Acceptable as reasoned, but the comment frames it as a choice the package does not offer, since both consts are unexported there.
