## Attempt 1

ISSUES:
- `cmd/hook_sweep_restore_standdown_test.go:161-182` — the subtest "it leaves the daemon's tick behaviour unchanged" cannot fail for the reason it exists. Its two assertions (zero `list-panes`, `gone01` survives) are satisfied by the new in-sweep guard alone, so it passes even when `tick`'s early return is relaxed. Verified: with the guard intact and `maybeRunHookCleanup(deps)` moved inside `tick`'s `if restoring` branch (`cmd/state_daemon.go:180-182`), all seven subtests still pass. No other test covers this either — `TestDaemonTick_SkipsEntireTickWhenRestoring` (`cmd/state_daemon_run_test.go:291`) touches `save.requested` so it exercises the capture branch, not the idle branch, and asserts only on `list-sessions`. The spec states this early return "is now load-bearing for hook retention and must not be relaxed or reordered", and acceptance criterion 6 names "its idle branch never reaches the new read" — the pin for that is currently absent.
  FIX: Add a call-count assertion on the marker read itself, immediately before the existing `list-panes` check at line 172. With the marker set, `tick` makes exactly one `show-option` (its own `state.IsRestoringSet`); reaching `runHookStaleCleanup` makes a second:
    if got := fc.callsContaining("show-option"); len(got) != 1 {
        t.Errorf("tick made %d show-option reads, want 1 (its own marker read; the sweep must not be reached): %v", len(got), got)
    }
  Verified against both trees: it fails with "tick made 2 show-option reads, want 1" under the relaxed `tick` and passes against the current one.
  CONFIDENCE: high

NOTES:
- `cmd/run_hook_stale_cleanup.go:39-41` and `cmd/hook_sweep_restore_standdown_test.go:18-21` both say panes carry no token "until the re-stamp". "token" is already grounded in the tree (`session.IsTokenShaped`, landed by task 1-1), but the restore re-stamp is phase 3. The claim becomes true within the same release, so correcting it now would only mean correcting it back.
- `restoringOption` (`cmd/hook_sweep_restore_standdown_test.go:200`) is a shared driver for fakes living in three other test files but is defined in this feature-specific one. Legal in a single package; worth relocating beside the fakes if phase 5 touches them again.
- Criterion 7 (doctor stays bootstrap-exempt) has no dedicated new test, and none was prescribed. Covered by the existing `skipTmuxCheck["doctor"]` pins plus the fact that the added call is a read that never starts a server.
- The doctor subtest deliberately discards the Execute error because `checkStaleHooks` still counts token-shaped keys as stale in a restore window — that is task 1-5's stand-down, correctly not pre-empted here.

## Attempt 2

ISSUES:
- `cmd/cleanstale_transient_listpanes_doctorfix_integration_test.go:50-53` — the two transient subtests now fail, and the suite they drive no longer tests what it exists to test. Their stub is `&transienttest.Commander{Inner: &tmux.RealCommander{}}`, and `transienttest.Commander` intercepts only `list-panes -a` (`internal/transienttest/commander.go:33-37`), so the new marker read delegates to real tmux against `TestMain`'s poisoned socket, errors, and stands the whole cycle down before the `list-panes` failure modes are ever reached. Observed: `--- FAIL: TestDoctorFix_TmuxTransient_DoesNotWipeHooks/mode_a_list_panes_exit_nonzero` and `/mode_b_list_panes_empty_stdout`, both at `cmd/cleanstale_transient_listpanes_shared_test.go:87-92` ("no `stale-hook cleanup:` lines found"), with the log showing `DEBUG hooks: clean-stale-skipped … reason=restoring error=tmux show-option -sv @portal-restoring: exit 1: error connecting to /nonexistent/portal-test-must-set-tmux-socket`. Confirmed a regression, not pre-existing: the same test passes against a clean `HEAD` export. This is one of the two destructive consumers `CLAUDE.md` names for the transient scaffolding, and the doctor half of it now proves nothing about the mass-deletion guard — it stands down for an unrelated reason. The production behaviour is spec-sanctioned (a down server stands the sweep down under `reason=restoring`); the fixture is what must move.
  FIX: Give the two subtests a marker read that resolves as not-restoring, so they still exercise the list-panes failure modes. In `doctorInvoker` (`:47-56`), add `tmuxtest.SkipIfNoTmux(t)`, create a per-test socket with `tmuxtest.New(t, "ptl-doctorfix-transient-")` plus a seeded `new-session -d`, and set `Inner: &transienttest.SocketCommander{SocketPath: sock.SocketPath()}` — the same shape the passing `normal_path_legitimate_stale_removal_still_works` subtest in the same file already uses (`:97-100`). Verified in a scratch copy of the working tree: with that single change all three subtests pass, and `transienttest.Commander` is untouched.
  ALTERNATIVE: Teach `transienttest.Commander` (or a local wrapper in the test file) to answer `show-option -sv @portal-restoring` with an option-absent `*tmux.CommandError`, keeping the two subtests server-free and hermetic. Cheaper and faster, but it widens a helper whose documented contract is "intercepts only `list-panes -a`" and whose single canonical declaration exists so the failure-mode contract cannot drift between its two destructive consumers. The reviewer recommends the socket-backed `Inner`: it matches the sibling subtest, keeps the helper single-purpose, and raises production fidelity of the read that now gates the sweep.
  CONFIDENCE: high

NOTES:
- `logRestoreStandDown` (`cmd/run_hook_stale_cleanup.go:12`) is declared above the exported `AllPaneLister` type it has nothing to do with; moving it below `runHookStaleCleanup` would read in call order. Cosmetic.
- The three seam fakes ignore the option name (`TryGetServerOption(string)`), so any second option read routed through this seam later would be answered as if it were `@portal-restoring`. Harmless today; worth a name check if the seam ever carries a second option.
- The prior round's judgement on `cmd/run_hook_stale_cleanup.go:39-41` ("panes carry no token until the re-stamp") stands: the re-stamp is phase 3, so the claim becomes true within the same release.
- Criterion 7 (doctor stays bootstrap-exempt) has no dedicated test and none was prescribed; `TryGetServerOption` → `show-option -sv` starts no server, and `doctor` remains in `skipTmuxCheck`.
- Full unit lane, `go vet ./...` and `go vet -tags integration ./...` are green. `TestRenameRestoreCleanupSurvival_KeepsRestoredIdKeyedHook`, `TestDaemon_ThrottledHookCleanup_ReapsStaleRetainsLiveOnIdleServer` and `TestTransientListPanesHelpers_Smoke` all pass.
