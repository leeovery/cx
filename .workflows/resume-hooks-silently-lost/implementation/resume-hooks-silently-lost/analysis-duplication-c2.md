AGENT: duplication
FINDINGS:
- FINDING: A second log-capture handler lives beside logtest.Sink, and this work extended it
  SEVERITY: high
  FILES: cmd/bootstrap_production_test.go:14-131, cmd/logging_capture_test.go:35, internal/logtest/capture.go:105-232, cmd/run_hook_stale_cleanup_test.go:31, cmd/run_hook_stale_cleanup_test.go:711-718, cmd/run_hook_stale_cleanup_lock_timeout_test.go:61
  DESCRIPTION: `recordingLogger` is a hand-rolled `slog.Handler` that reproduces `logtest.Sink` structure for structure — the same `shared`/`bound`/`owner()` indirection, the same `WithAttrs`/`WithGroup` re-binding, the same per-record attr map. The cmd package already has the shared route for exactly this job: `newCaptureLoggerForComponent(t, component) (*slog.Logger, *logtest.Sink)`, used at ~75 call sites across cmd, and `logtest.NewCaptureLogger` beneath it. This work unit did not merely leave the twin in place — it grew it: the new `recordedLog.attrs` map, `recordedLog.intAttr` and `onlyMatching` are line-for-line re-implementations of `logtest.Record.Attrs`, `logtest.Record.IntAttr` and `logtest.Sink.OnlyRecordWith`, and the new sweep suites now capture logs two different ways in the same file (`recordingLogger` for the injected logger, `logtest.Install` for the package-level one). `internal/logtest` is declared the single source of truth for the capture handler and its rendered-record contract; a second handler with its own level-to-string table and its own attr accessors is exactly the drift that ownership exists to prevent — a change to the Sink's semantics now silently leaves the sweep suites asserting against a different contract.
  RECOMMENDATION: Delete `recordedLog`, `recordingLogger`, `intAttr`, `countMatching` and `onlyMatching` from cmd/bootstrap_production_test.go. Replace every `&recordingLogger{}` with `newCaptureLoggerForComponent(t, "bootstrap"|"daemon")`, `countMatching(entries, level, comp, msg)` with `len(sink.RecordsWith(comp, msg).AtExactLevel(level))`, `onlyMatching(...)` with `sink.OnlyRecordWith(t, comp, msg)`, and `rec.intAttr(t, "panes")` with `rec.IntAttr(t, "panes")`.

- FINDING: The "error attr is a real error" assertion is written out nine times
  SEVERITY: medium
  FILES: internal/hooks/lock_write_test.go:39-51, internal/hooks/store_test.go:1182-1192, internal/hooks/store_test.go:1358-1368, internal/hooks/store_test.go:1514-1524, internal/project/store_logging_test.go:124-134, internal/project/store_logging_test.go:245-255, internal/project/store_logging_test.go:350-360, internal/project/store_logging_test.go:544-554, internal/storelog/clean_stale_test.go:75-85
  DESCRIPTION: All five of these files were rewritten by the task whose stated purpose was that logtest owns the sink, the filters and the record assertion — yet each still hand-rolls the same eight-line block to get an error out of a record: index `rec.Attrs["error"]`, fatal on absent, type-assert `.Any().(error)`, fatal on the wrong kind. Six of the nine then follow with the identical `errors.Is(loggedErr, fileutil.ErrWriteTempCreate)` check. The same files also hand-roll `if _, ok := rec.Attrs["took"]; !ok` (internal/hooks/store_test.go:1024, :1179; internal/project/store_logging_test.go:450, :541; internal/storelog/clean_stale_test.go:32, :68) although `logtest.Record.RequireDuration` already exists and is used for precisely this in cmd/bootstrap and internal/state. `logtest.Record` carries `AttrString`, `IntAttr`, `RequireDuration` and `HasAttr` — the error accessor is the one member of that family that was never added, so every caller re-derives it and each derives its own failure wording.
  RECOMMENDATION: Add `func (r Record) ErrorAttr(t TestingT, key string) error` to internal/logtest/capture.go beside `IntAttr`, fatal on absent and on a non-error value, and route all nine sites through it. Replace the six hand-rolled `took` presence checks with the existing `rec.RequireDuration(t, "took")`.

- FINDING: hooks.json test staging is reimplemented seven ways across two packages
  SEVERITY: medium
  FILES: internal/hooks/store_shape_test.go:17, internal/hooks/lookup_test.go:14, internal/hooks/read_lock_test.go:20, internal/hooks/store_test.go:51, internal/hooks/cleanstale_snapshot_test.go:237, cmd/testhelpers_test.go:50, cmd/run_hook_stale_cleanup_outcome_test.go:15
  DESCRIPTION: `internal/hookstest` was created by this work unit as the shared home for hooks.json test scaffolding, and it holds the sidecar half (`CreateHooksSidecar`, `HoldHooksSidecar`, `AssertSidecarFree`) plus the env-based seeders. The store-at-a-path half was left to each suite, and seven independent versions now exist. Two pairs are outright duplicates: `storeWithContent` (lookup_test.go) and `seedHooksFile` (store_shape_test.go) have identical bodies — write `content` to `<t.TempDir()>/hooks.json` at 0o644 and return `hooks.NewStore(path)` — differing only in return arity, in the same test package; and `bogusHooksStore` (cmd) and `seedHooksDirectory` (internal/hooks) both stage a *directory* at the hooks.json path so every read fails, carrying near-identical comments explaining why. The remaining three (`seedReadFixture`, `seedThenDenyWrites`, `newStagedHooksStore`) are the same base staging plus one axis each — sidecar present, writes denied, seed body — which `newStagedHooksStore`'s `hooksStoreStaging` struct already models as a single parameterised shape. The cost is drift, not verbosity: the sidecar-creation rule (create it before any chmod denial, or the mutation fails at the wrong place) is currently encoded in two of the seven and absent from the rest.
  RECOMMENDATION: Move cmd's `hooksStoreStaging` + `newStagedHooksStore` into `internal/hookstest` as the one path-based staging entry point, with the `dir`/`seed`/`sidecarAbsent`/`writesDenied` axes it already has plus an `unreadable` axis for the directory-at-the-path case. Retire `storeWithContent`, `seedHooksFile`, `seedReadFixture`, `seedThenDenyWrites`, `seedHooksDirectory` and `bogusHooksStore` onto it; keep cmd's thin wrapper only if the call sites read better for it.

- FINDING: Three doctor drivers, two of them byte-identical apart from one argv element
  SEVERITY: medium
  FILES: cmd/doctor_test.go:111-127, cmd/doctor_test.go:1317-1332, cmd/doctor_test.go:1334-1349
  DESCRIPTION: `runDoctorFixCmd` and `runDoctorCmd` are the same sixteen lines — same `isolateTerminalsFile` call with the same comment, same `doctorDeps` install and cleanup, same two buffers, same `resetRootCmd`/`SetOut`/`SetErr`/`Execute` — and differ only in whether `"--fix"` is appended to `SetArgs`. `runDoctor` is a third copy of the same body that additionally builds the deps from a state dir. Any change to how a doctor run is isolated or driven (a new eagerly-read config file, a third stream) has to be made in three places and will be made in one.
  RECOMMENDATION: Collapse to one `runDoctorWith(t *testing.T, deps *DoctorDeps, args ...string) (*bytes.Buffer, *bytes.Buffer, error)` that installs the deps, isolates terminals.json and executes `append([]string{"doctor"}, args...)`. Keep `runDoctor(t, dir)` as a two-line wrapper that builds `withHealthyRuntime(&DoctorDeps{StateDir: dir})` and delegates.

- FINDING: hook rm's failing-route table restates four subtests that precede it
  SEVERITY: medium
  FILES: cmd/hooks_rm_exit_test.go:13-114, cmd/hooks_rm_exit_test.go:162-229, cmd/hooks_rm_exit_test.go:230-305
  DESCRIPTION: The `it leaves hooks.json byte-identical on every failing route` table drives five rows — gone pane, unset TMUX_PANE, live pane with no token, resolved token naming no entry, `--pane-key` naming no entry. Four of those five are already staged and asserted individually earlier in the same function (`:13`, `:52`, `:75`, `:94`), each ending in the same `assertHooksFileUnchanged` call the table exists to make. The table therefore adds exactly one new case while re-staging four. Separately, the two tables at `:162` and `:230` carry the same row struct (`name`/`paneID`/`resolver`/`stamper`/`extra`), the same "supply a plain stamper when the row did not set one" comment, and the same eight-line loop preamble (`hooksFileInTempDir`, `t.Setenv("TMUX_PANE", …)`, seed, default stamper, `withHooksDeps`, `runHookRm`, conditional `assertNoPaneTmuxCalls`).
  RECOMMENDATION: Fold the four already-covered rows out of the byte-identity table, leaving it to cover only `unset TMUX_PANE`, or — preferably — delete the four standalone subtests' duplicated staging and let the table own the byte-identity claim while the standalone subtests keep only their message-text assertions. Extract the shared row struct and loop preamble of the two tables into one `runRmCase(t, tt)` helper in the same file.

- FINDING: Two shape-rule subtests are duplicated verbatim across two files in internal/hooks
  SEVERITY: medium
  FILES: internal/hooks/store_shape_test.go:31-62, internal/hooks/store_shape_test.go:88-106, internal/hooks/cleanstale_snapshot_test.go:177-191, internal/hooks/cleanstale_snapshot_test.go:193-211
  DESCRIPTION: `cleanstale_snapshot_test.go`'s "it still deletes an empty key present in both the file and the snapshot" is the same test as `store_shape_test.go`'s "it deletes an empty key": same seed string (`{"":{"on-resume":"malformed"}, liveKey:{…}}`), same `enumerating(liveKey)` call, same `slices.Equal(removed, []string{""})` assertion, same reload-and-check — only the failure wording differs. "it still retains a non-token-shaped key" is likewise a trimmed copy of "it retains a non-token-shaped key absent from the live set". Neither snapshot-file copy varies the snapshot at all (both use the ordinary path where the file and the snapshot agree), so they exercise no behaviour the shape file does not. Two files now assert one rule, and a change to the rule will be reflected in whichever file the author happens to open.
  RECOMMENDATION: Delete the two "it still …" subtests from cleanstale_snapshot_test.go; the file's own subject — the snapshot narrowing — is already covered by "it retains a key written after the snapshot" and "it derives the delete set from the file under the lock, not from the snapshot". Leave store_shape_test.go as the single home for the shape rule.

- FINDING: The two hooks source guards duplicate their whole scan loop, and the callee-name helper
  SEVERITY: low
  FILES: internal/hooks/cleanstale_staleness_guard_test.go:16-42, internal/hooks/cleanstale_staleness_guard_test.go:61-100, internal/hooks/cleanstale_staleness_guard_test.go:44-52
  DESCRIPTION: `TestCleanStaleDoesNotCallStaleKeys` and `TestMutationsDoNotCallExportedLoadOrSave` share a twenty-line skeleton written twice: `sourceguardtest.PackageGoFiles(".", false)` with the same fatal wording, the per-path `parser.ParseFile(… SkipObjectResolution)` with the same fatal wording, the `scanned++` counter, the `ForEachFuncCall` visit, and the closing `if scanned == 0 { t.Fatal("guard scanned no files") }`. Only the predicate inside the visitor differs. `calleeName` — unwrap an `*ast.Ident` or `*ast.SelectorExpr` to a name — is also a copy of the `callName` helper that `sourceguardtest`'s own test carries, and it is the natural companion to the exported `ForEachFuncCall` it is always used with.
  RECOMMENDATION: Extract a local `scanPackageCalls(t, visit func(path, funcName string, call *ast.CallExpr))` in the guard file that owns the enumerate/parse/count/empty-check skeleton, and give both guards only their predicate. Promote `calleeName` to `sourceguardtest.CalleeName(call *ast.CallExpr) string` beside `ForEachFuncCall`.

- FINDING: Two parallel reason-to-phrase maps with identical lookup functions in doctor.go
  SEVERITY: low
  FILES: cmd/doctor.go:215-231, cmd/doctor.go:308-320
  DESCRIPTION: `skippedPrunePhrases`/`skippedPrunePhrase` and `notEvaluableDetails`/`notEvaluableDetail` are two maps keyed on the same `skipReason*` constants with two lookup functions whose bodies are identical (`if v, ok := m[reason]; ok { return v }; return reason`) and whose doc comments make the same "an unmapped reason falls through to its raw value" promise twice. The wordings genuinely differ per surface, so the maps must stay two — but the lookup and its fallback rule need not, and the two maps' key sets have already diverged (`skipReasonLockTimeout` is present in one and absent from the other) with nothing to notice it.
  RECOMMENDATION: Keep both maps and replace the two functions with one `phraseFor(m map[string]string, reason string) string`, called as `phraseFor(skippedPrunePhrases, reason)` and `phraseFor(notEvaluableDetails, reason)`.

- FINDING: `go list -deps` leaf guards each carry their own exec block
  SEVERITY: low
  FILES: internal/nanoid/leaf_guard_test.go:27-33, internal/hooks/leaf_guard_test.go:59-62
  DESCRIPTION: Both new leaf guards run the same five lines — `exec.Command("go", "list", "-deps", pkg).CombinedOutput()` with the same `go list -deps %s: %v\n%s` fatal — before applying their own filter. nanoid wrapped it in a `packageDeps(t, pkg)` helper; hooks inlined it. `internal/sourceguardtest` is the declared home for the guard primitives that the repo's guards share, and it has no dependency-enumeration member.
  RECOMMENDATION: Add `sourceguardtest.PackageDeps(t *testing.T, pkg string) []string` and route both new guards through it; the three pre-existing copies can follow opportunistically.

- FINDING: Two exported entry points mint the same id
  SEVERITY: low
  FILES: internal/session/panetoken.go:8-10, cmd/hooks.go:97, cmd/open.go:414, cmd/open.go:567, internal/spawn/burst.go:58
  DESCRIPTION: `session.NewPaneToken` is a one-line pass-through over `nanoid.NewGenerator()()`, added when the vocabulary lived in `internal/session`. The vocabulary since moved to the `internal/nanoid` leaf, and every other minting site now calls `nanoid.NewGenerator()` directly. The wrapper adds no width, no charset and no validation of its own, so the package now exposes a second name for the same value and keeps `cmd/hooks.go` depending on `internal/session` for nothing else.
  RECOMMENDATION: Delete `internal/session/panetoken.go` and default `hookSeams().TokenMinter` to `nanoid.NewGenerator()`, dropping the `internal/session` import from cmd/hooks.go if nothing else there needs it.

- FINDING: The seed-key vocabulary block is declared twice
  SEVERITY: low
  FILES: cmd/hookkey_vocabulary_test.go:20-35, internal/hooks/store_test.go:598-605
  DESCRIPTION: Both packages declare `reapableSeedA`…`reapableSeedD` over `hookstest.ReapableHookKey(0..3)`, under the same names and with the same explanatory comment about what a reapable key means. `internal/hookstest` exports the generator but not the named values, so each consumer restates the naming convention and each is free to re-point a name at a different index.
  RECOMMENDATION: Export the four named seeds (and the live/unjudgeable ones cmd adds) from `internal/hookstest` as vars, and have both packages reference them rather than re-declare.

- FINDING: A third and fourth minimal testing-reporter interface
  SEVERITY: low
  FILES: internal/restore/marker_assert_test.go:14-18, internal/restore/marker_assert_meta_test.go:16-45, internal/restoretest/waitfor_file_exists.go:10-16, internal/restoretest/waitfor_file_exists_test.go:12-25, internal/logtest/capture.go:22-26
  DESCRIPTION: `markerReporter` (Helper/Errorf/Fatalf) is a fresh declaration of the same narrowed-`*testing.T` seam that `logtest.TestingT` already declares with the identical method set, and that `restoretest.fataller` declares a subset of. `recordingReporter` plus its `fatalSentinel` panic-and-recover trick is likewise a second copy of `restoretest`'s `fakeFataller` pattern for observing a helper's failures without failing the test.
  RECOMMENDATION: Have `markerReporter` be `logtest.TestingT` (or promote a single `TestingT` to a shared test-only package if the logtest dependency is unwelcome in internal/restore), and reuse `restoretest`'s recording-fataller rather than redeclaring it in marker_assert_meta_test.go.
SUMMARY: The production code for this work unit is well single-sourced — the staleness rule, the restore-window read, the stand-down shape and the pane-token literal each have exactly one home — but the test surface grew several parallel implementations of things the repo already owns. The two that matter most are a second slog capture handler in cmd that this work unit extended rather than retired, and a nine-site error-attr assertion that survived the very task meant to give logtest ownership of record assertions; the rest are hooks.json staging, doctor drivers and guard skeletons written a few times each.
