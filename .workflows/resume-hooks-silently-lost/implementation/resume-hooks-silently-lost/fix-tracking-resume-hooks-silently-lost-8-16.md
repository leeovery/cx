## Attempt 1

ISSUES:
- `internal/tmux/tmux_test.go:1519-1533` — the refactor **removed** the unit lane's only detection of a production change to the split, at the only production `RunRaw` call site. `CapturePane` (`internal/tmux/tmux.go:708`) is the sole `RunRaw` consumer in the whole repo. Mutation-tested via `go test -overlay` (no repo writes): flipping `c.cmd.RunRaw("capture-pane"…)` to `c.cmd.Run(…)` leaves the **entire unit lane green** (zero failures). Against `HEAD` (pre-refactor) the same mutation fails: `CapturePane() = "", want "abc\n  \x1b[31m"`. The cause is that the fixture `raw := "abc\n  \x1b[31m"` ends in `m`, so `strings.TrimSpace` is a no-op on it — the old fake caught the flip only incidentally (`RunFunc` was nil, so `Run` returned `""`), and the new fake, which honours the contract properly, returns a value the assertion accepts. The subtest name "preserves trailing whitespace … via RunRaw" is now false of its own fixture: there is no trailing whitespace in it. This is the task's stated outcome — "so a production change to the split fails somewhere" — regressed by the change meant to deliver it.
  FIX: give the fixture trailing whitespace so trimming is observable — `raw := "abc\n  \x1b[31m\n\n"` at line 1520. Verified overlay-side: it passes against the real `CapturePane` and fails against the `Run` mutation with `CapturePane() = "abc\n  \x1b[31m", want "abc\n  \x1b[31m\n\n" (raw output must not be trimmed)`. One-line change, no other test touched.
  ALTERNATIVE: add a raw-call accessor to `Scripted` (it currently records `Run` and `RunRaw` into one `calls` log, by design, so `Calls()` cannot discriminate) and assert `capture-pane` appears among the `RunRaw` calls. That pins the method directly rather than inferring it from output shape, but widens the shared fake's surface for one call site and re-introduces the two-log bookkeeping the promoted fake deliberately avoids. Reviewer recommends the fixture change.
  CONFIDENCE: high

- `internal/tmux/hooks_test.go:9-30` — the `TestShowGlobalHooksForEvent` resolution is correct in diagnosis but weakens the fixture where it could have strengthened it. The old assertion (output returned including its trailing `\n`) was indeed unreachable in production — `ShowGlobalHooksForEvent` reads through `Run` (`internal/tmux/tmux.go:687`), which trims — so the fixture, not the assertion, was wrong, and no production defect is hidden (consumers parse line-wise; a trailing newline is immaterial). But deleting the trailing newline removed the only whitespace that made the trim observable: confirmed by overlay that flipping this site's `Run` to `RunRaw` now leaves `./internal/tmux` and `./cmd/...` green. The subtest name "returns raw output" and the failure message `want %q (verbatim)` now mean "unparsed", not "untrimmed", which will mislead the next reader of a test whose whole subject is that distinction.
  FIX: keep the trailing newline on the commander's output and assert the trimmed value — `raw := "pane-focus-out[0] …\npane-focus-out[1] …\n"`, `want := strings.TrimSpace(raw)`, compare `got` to `want`; retitle the subtest to "calls show-hooks -g <event> and returns the unparsed output" and drop `(verbatim)` from the failure message. That keeps the assertion true, keeps the two-entry unparsed claim, and makes the site fail if it ever moves to `RunRaw`.
  CONFIDENCE: medium — the substance (assert the trim rather than delete the whitespace) is clear; the exact retitling is a judgement call.

NOTES:
- The three verification asks were answered. (a) The promoted fake does genuinely pin the contract in its own suite. (b) The two surface additions are forced, not creep: `FromFunc`/`Answering` has 193 + 7 call sites (the tmux/restore/state fakes are argv-modelling functions, not static tables — no static script could express them), and `ResetCalls` is the encapsulated form of the old `mock.Calls = nil` field write at `internal/tmux/hooks_unregister_test.go:306`, forced by the field→method change. (c) The `TestShowGlobalHooksForEvent` change hides no production defect. (d) Semantics were preserved: subtest and `t.Run`/`func Test` counts are identical before and after in every touched file; assertion counts identical everywhere except `internal/state/capture_test.go` (183→180, exactly the three `t.Fatalf` arms that moved into the shared fake's loud default and strict mode).
- Structural typing against the interface rather than importing `internal/tmux` is the right call — importing would cycle with the in-package `tmux` test that now uses the fake.
- A fifth in-package fake collapses the split and was not re-pointed: `internal/tmux/option_discriminator_internal_test.go:9-20` (`internalMockCommander`). Not blocking — all three construction sites set only `Err` and never `Output`, so the trim contract is unobservable there and both methods return `("", err)` exactly as the contract requires. Already banked; re-pointing it in a fix round would leave `internal/tmux` with zero hand-rolled `Commander`s.
- `Doing` (`scripted.go:165`) and `CallsMatching` (`scripted.go:259`) have no consumer outside `commandertest`'s own suite. Pre-existing — neither had `cmd` consumers at HEAD — carried over faithfully rather than introduced, but the promotion turned unexported dead helpers into exported shared API.
- `Strict()` on a fake built by `Quiet`/`Delegating`/`FromFunc` would nil-panic in `RunRaw` (`scripted.go:180`), since those constructors leave `tb` nil. The doc comment names the constraint and the one consumer obeys it — a documented foot-gun rather than a defect.
- Import grouping drifted in 27 files: `internal/tmux/*` and `internal/restore/*` put `commandertest` in a third import group of its own, while `cmd`, `internal/state` and `internal/restoretest` put it in the shared project group. Both gofmt-clean; the split-group form is inconsistent with the file's own remaining project imports.
- CLAUDE.md's amended row lists `commandertest` last in the header cell but describes it before `hookstest` in the body. Cosmetic.
- `Verbatim` (`scripted.go:203`) carries no doc comment of its own; the combined comment sits on `Trim` and names both.
- The reviewer did not run the integration lane: every changed file is untagged, integration tests drive real tmux rather than these fakes, and the integration-tagged type-check (which builds the test packages, catching any integration file referencing a deleted helper) is clean.
