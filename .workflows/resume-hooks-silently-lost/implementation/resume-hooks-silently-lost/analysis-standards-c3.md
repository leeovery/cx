AGENT: standards
FINDINGS:
- FINDING: Unspecified session-name refusal and exact-target hardening shipped inside this work unit
  SEVERITY: medium
  FILES: internal/tmux/errors.go:63-80, internal/tmux/tmux.go:281-289, internal/tui/model.go:2665-2671, internal/tui/sessions_flash.go:54, README.md:195, CLAUDE.md:64
  DESCRIPTION: The implementation introduces `ErrUnaddressableSessionName` / `ValidateSessionName`, makes `Client.RenameSession` refuse any name containing ":" before composing its argv, adds a matching refusal to the TUI rename modal with new user-facing flash copy (`":" isn't allowed in a session name — tmux reads it as a separator`), and converts a broad set of previously-bare tmux `-t` targets to the new `ExactSessionTarget` / `ExactCoordTarget` exact-match forms (`ListPanesInSession`, `ShowEnvironment`, `SetSessionOption`, `ActivePaneCurrentPath`, `QuickStart`'s chained argv). None of this is in the specification. The spec decides four changes — durable pane identity, registration verification, shape-aware cleanup, `hooks.json` locking — and §1.3 explicitly scopes the positional/structural addressing siblings as "checked against the change, not changed by it". This is a user-visible behaviour change (a rename the picker accepted yesterday is now refused, with copy that went through no visual gate) riding a release whose stated contract is the hook-key repair. It is also undocumented: README:195 still tells the user "Hooks stay attached to a session even if you rename it, whether from the picker's `r` modal" with no mention that some names are now refused, and neither README nor CLAUDE.md's `tmux` row records the refusal or the new exact-target helpers, so the repository's own architecture description does not describe the shipped behaviour. (The related deletions of `ResolveStructuralKey` / `ListAllPanes` / `ListPanes` are benign — their only consumers were the hook paths this work unit replaces — but they too are named in §1.3 as unchanged.)
  RECOMMENDATION: Either document the refusal and the exact-target hardening in README (the rename section) and CLAUDE.md's `tmux` row so the shipped behaviour is described where the project describes behaviour, or lift the refusal out of this work unit. If it stays, the flash copy needs the same visual-gate check every other user-facing string in the picker gets.

- FINDING: A failed `@portal-restoring` read is reported to the user as "restore in progress"
  SEVERITY: medium
  FILES: cmd/run_hook_stale_cleanup.go:136-141, cmd/run_hook_stale_cleanup.go:40, cmd/run_hook_stale_cleanup.go:50, cmd/doctor.go:302-304
  DESCRIPTION: `hookStalenessStandDown` folds a failed marker read into the `restoring` reason via `state.RestoreWindowActive`, which is exactly the fail-safe posture the spec mandates. But the reason then drives both user-facing surfaces: `portal doctor --fix` prints `Skipped stale hook prune: restore in progress` and the read-only diagnosis reports `restore in progress (not evaluable)`. `doctor` is bootstrap-exempt and starts no server, so with the tmux server down — the routine state after a reboot, and precisely when a user reaches for `doctor` — `TryGetServerOption` fails, no pattern in `optionAbsentStderrPatterns` matches "no server running", and the user is told a restore is running when none is. Before this change that path rendered the honest "zero live panes with hooks present (not evaluable)". The spec's own 2026-09-01 corrigendum withdrew a stand-down phrase for exactly this class of mismatch — words that describe a condition other than the one that produced them — and the corrigendum's fix was to give each condition its own phrase rather than to let one borrow another's.
  RECOMMENDATION: Keep the fail-safe fold (a failed read must still stand the cycle down), but separate the rendered phrase from the marker-set phrase — carry the read failure as its own reason so `skippedPrunePhrases` / `notEvaluableDetails` can say the marker could not be read rather than asserting a restore. The log line and the stand-down semantics are unaffected.

- FINDING: `--pane-key` help text and README narrow the flag below the contract the spec gives it
  SEVERITY: low
  FILES: cmd/hooks.go:310, README.md:191, README.md:200
  DESCRIPTION: The flag help reads `Pane token of the pane whose hook should be removed`, and README:191 says it removes "a hook for any pane token". §4.3 makes `--pane-key` a literal pass-through with no validation of any kind, and §4.3/§8.3 assign it a specific second purpose: it is the route by which an old-format (non-token) entry — the class the shape-aware reaper retains forever — is removed by hand, alongside a direct file edit. An old-format key is not a pane token, so a user following the help text has no indication that the one sanctioned removal route for a retained entry is this flag. CLAUDE.md gets this right ("a literal pass-through to remove an entry for any hook key"); the CLI help and README do not.
  RECOMMENDATION: Reword the flag help and the README sentence to say the flag takes any hook key verbatim (defaulting to the current pane's token), so the retention design's escape hatch is discoverable from the CLI itself.

- FINDING: The minting layer diverges from the superseding corrigendum
  SEVERITY: low
  FILES: cmd/hooks.go:100, internal/nanoid/nanoid.go:33-38, internal/nanoid/nanoid.go:57
  DESCRIPTION: §2.1 has minting reached through an exported `NewPaneToken` in `internal/session`, and the 2026-08-30 corrigendum — which supersedes the body — restates it: "`internal/session/panetoken.go` forwards to it". No such file or function exists; `cmd/hooks.go` reaches `nanoid.NewPaneTokenGenerator()` directly. Separately, §2.1 has the token drawn from "the shared generator over its 62-alphanumeric alphabet (no `-`), width 6" and the corrigendum lists `internal/nanoid` as holding "the unexported `width`" singular; the implementation adds a second generator and a second width (`paneTokenWidth`). Both deviations preserve the properties the spec argued for — `hook set` names no width of its own, and `IsTokenShaped` reads the same width and alphabet the mint reads, so generation and recognition cannot drift — and the second width is a genuine improvement (it decouples `hooks.json`'s on-disk key-recognition contract from the session-name suffix width, which CLAUDE.md documents). The drift is that neither change is recorded in the Corrigenda, so the specification as it stands names a file and a shape that do not exist.
  RECOMMENDATION: Record both in the Corrigenda — the forwarder was dropped, and the single width became two with the persisted contract pinned to `paneTokenWidth` — so the spec keeps describing the shipped code. No code change is warranted.

SUMMARY: The four specified changes (durable pane identity, tmux-native registration verification, shape-aware and self-recording cleanup, and the bounded sidecar lock with its two-bound split) are implemented faithfully, including the ordering rules, the five stand-down reasons, the closed log-vocabulary amendment, the empty-key guards at both boundaries, and the §7.2 removals; `go vet` and `golangci-lint run` are clean on both lanes. The substantive drift is scope rather than mechanism: an unspecified session-name refusal with new user-facing copy shipped alongside the hook work and is documented nowhere, and one fail-safe path reports a failed marker read to the user as an in-progress restore. Where the spec conflicts with the golang-error-handling skill's single-handling rule (the store logs and returns the same lock failure), the spec explicitly decided both emissions are required and the implementation correctly followed the spec.
