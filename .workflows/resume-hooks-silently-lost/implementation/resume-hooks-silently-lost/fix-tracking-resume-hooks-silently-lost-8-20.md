## Attempt 1

ISSUES:
- `internal/sourceguardtest/packagedeps_test.go:20-27` — this diff falsified the premise of a sibling test in the package it edited. `TestPackageDeps_EnumeratesTransitiveDependencies` asserts `{"go/ast", "go/token"}` are in the dep list, with the rationale "go/ast is imported directly; go/token arrives only through it, so the pair distinguishes a transitive list from an immediate one". `parsesources.go` now imports `go/token` (and `go/parser`) **directly** — `go list -f '{{.Imports}}'` confirms both are direct — so the pair no longer distinguishes anything, and the test would pass identically against an immediate-imports-only implementation. That is precisely the "passes by having stopped looking" failure mode this task exists to close, one layer up.
  FIX: swap the transitive half of the pair for a dependency that is still transitive-only. `go list -deps` reports `go/scanner` (reached through `go/parser`) and `go/build/constraint`; use `go/scanner` — assert `{"go/parser", "go/scanner"}` (or keep `go/ast` and add `go/scanner`) and rewrite the comment to name the new pair, e.g. `// go/parser is imported directly; go/scanner arrives only through it, so the pair distinguishes a transitive list from an immediate one — the depth the guards behind this primitive police.`
  CONFIDENCE: high

- `internal/tmux/target_composition_guard_test.go:301` — the assertion the task introduced has a dead disjunct: `if !strings.Contains(stub.msg, "target-taking") && !strings.Contains(stub.msg, "already-composed target")`. The only message the fatal can carry is `"no method taking an already-composed target was found, so nothing would be checked at a call site"`, which never contains `"target-taking"`; the first clause can only ever be true, so the OR means the assertion rests entirely on the second. Deterministic output asserted through a two-way substring OR is the shape that quietly stops discriminating.
  FIX: drop the first clause and assert the one substring — `if !strings.Contains(stub.msg, "already-composed target") { t.Errorf("fatal message %q does not say the method set was empty", stub.msg) }`.
  ALTERNATIVE: assert the whole message by equality, since it is fully deterministic and takes no format arguments; stricter and matches code-quality's "no substring assertions when exact output is deterministic", at the cost of re-touching the test on any rewording. Reviewer recommends the single-`Contains` form for consistency with the sibling fatal assertions in this diff.
  CONFIDENCE: high

NOTES:
- All four verification asks were answered. (a) The two error→fatal conversions lose nothing: every prior caller immediately `t.Fatalf`'d the returned error, so there was no recovery path, and two converted guards got *stronger* — they now assert the "stopped looking" wording, which the error-returning versions never did. (b) The `ParseComments` requirement is real, verified independently: `isIntegrationTagged` iterates `file.Comments` and feeds each to `constraint.Parse`, so it is dead without it. (c) No tripwire was lost — every site-specific one survives, and `internal/portaltest` plus three `internal/theme` guards *gained* one they lacked. (d) The reviewer mutation-checked two guards the executor did not use, staged and removed in-command: a `declinedError{}` probe in `cmd` and a bare `restore.Orchestrator{}` in `internal/restore`, both correctly flagged with file:line, the second confirming the new relativisation works on real paths.
- The report's justification for `ParseComments` is right about the requirement but wrong about the failure mode: dropping it would make `isIntegrationTagged` match nothing, `scanned` fall to 0, and the guard fatal on its own tripwire — loud, not silent. The mode choice stands regardless.
- The new relativisation in `scanGuardTestFiles:56` is untested; no fixture asserts finding *text*. Verified by mutation against the real tree and it produces the right relative path. Cosmetic (findings only feed the fatal message) and it degrades to the old absolute-path output if the prefix ever fails to match.
- `internal/theme/embedded_test.go:190` compares `source.Path` against the bare basename `"broken_builtin.go"`, holding only because `PackageGoFiles(".")` yields `filepath.Join(".", name)` = `name`. The three sibling helpers in the same package wrap in `filepath.Base`. Correct today, inconsistent, would break silently if the dir argument changed.
- Four test names now say "Errors" for behaviour that fatals, while their own subtests correctly say "it fatals when…". Cosmetic drift from the conversion.
- `internal/portaltest/teardown_guard_coverage_rule_test.go:148` still calls `parser.ParseFile` inline, correctly — it parses an in-memory fixture string, which the path-based helper cannot express — and it now shares `sourceguardtest.ParseMode`, which is the right amount of coupling.

## Attempt 2

ISSUES:
- Three of the fourteen sites were not re-pointed and still write their own enumerate-parse loop under their own parse mode: `internal/theme/slug_collapse_guard_test.go:23-45` (enumerate at 23, parse at 38, mode `0`), `internal/theme/loader_construction_guard_test.go:25-42` (parse at 41, mode `SkipObjectResolution`), `internal/theme/broken_builtin_test.go:273-292` (`TestBuiltinSource_HasNoProductionCallSite`, parse at 291, mode `SkipObjectResolution`). This leaves acceptance criteria 1 and 4 unmet, and it falsifies the sentence this diff added to `CLAUDE.md`'s `sourceguardtest` row — "so no guard re-authors the enumerate-parse-count loop or decides the scanned-nothing tripwire for itself" — which is not true of these three today. The census reconstruction is unambiguous: the task states "bare `0` at three", and at HEAD exactly three inline/variant sites parsed at `0`, one of them `slug_collapse_guard_test.go`; and "eleven inline" is reached only by including theme's three (cmd 5 + hooks 1 + theme 3 + restoretest 1 + portaltest 1).
  FIX: re-point all three. Each is a `GoSourceFiles(root)` walk with per-path filtering, so the shape is: run the existing filter into a `scanPaths []string` first, then `for _, source := range sourceguardtest.ParseSources(t, scanPaths)`, deriving `rel` from `source.Path` inside the loop and using `source.Fset` for positions. Two need care: `broken_builtin_test.go` counts `builtinSourceOwners[rel]` hits *before* parsing, so do the `found++` tally in the same filter pass that builds `scanPaths` and exclude those paths from it; `loader_construction_guard_test.go`'s `scanned == 0` fatal at line 106 becomes redundant once the helper owns the empty-scan fatal and can be deleted, while `slug_collapse_guard_test.go`'s `want` comparison is the guard's real subject and stays.
  ALTERNATIVE: narrow the `CLAUDE.md` sentence and record the three as remaining work. Not recommended — the task's own parse-mode census only balances if `slug_collapse_guard_test.go` is inside the set, and the mode-uniformity criterion is the one these three most directly defeat.
  CONFIDENCE: high

NOT TO BE ACTIONED IN THIS TASK — orchestrator ruling on the reviewer's second issue:
- The reviewer proposed exporting a `RecordingT` from `internal/sourceguardtest` to end the three-way duplication of the fatal-recording stub (`packagedeps_test.go:49`, `restoretest/literal_guard_scan_test.go:90`, `tmux/target_composition_guard_test.go:562`, two of them added by this diff). The finding is correct and it now clears the Rule of Three. But task 8-24 in this same phase already owns this decision, and owns it differently: it homes `TestingT` **and one exported recording fataller** in a *new neutral leaf* (`internal/harnesstest`), then re-points `logtest`, `sourceguardtest`, `restoretest` and `hookstest` at the single declaration — on the stated grounds that `TestingT` is generic assertion scaffolding and `sourceguardtest` is a special-purpose home for Go-source scanning. Exporting the recorder from `sourceguardtest` now would be undone by 8-24 and would repeat the category error 8-24 exists to correct. Left for 8-24; already banked twice against it.

NOTES:
- Verification asks (a), (b) and (c) all came back clean. (a) The replacement dependency pair was proved independently: `go list -f '{{.Imports}}'` shows `go/parser` present and `go/scanner` absent; `go list -deps` shows `go/scanner`. The pair discriminates again. (b) Two mutation checks in a scratch copy: a staged hex literal in `internal/theme/badge.go` was caught with correct file:line through the new `source.Path`/`source.Fset`, and re-pointing a re-pointed guard at an empty temp dir fired the shared helper's fatal naming the directory. (c) The fix round's collateral is name-and-comparison only — the sole semantic change in `embedded_test.go` is the `filepath.Base` wrap, and `TestBareTargetGuard_ErrorsWhenTheImportScanResolvesNothing` correctly kept its name because that path still returns an error.
- `internal/theme/leaf_guard_test.go:31` binds `name := source.Path` — `name` now holds a path, not a base name. Correct today only because `PackageGoFiles(".", …)` yields `filepath.Join(".", n)` = `n`; the identifier will quietly lie if the dir argument changes. `path` would be the honest name. Same latent coupling the fix round addressed one file over.
- `TestBareTargetGuard_FatalsWhenItEnumeratesNoFiles` asserts only `stub.fataled`, not which fatal fired, though `scanBareTargets` has two fatal paths reachable from a temp dir. Its sibling one function down does assert the message. Given the task explicitly valued "asserting which fatal it exercises", this one is a step short.
- `ParseSources` mints a fresh `token.FileSet` per file rather than sharing one. Correct for every current consumer, and the struct pairing makes misuse unlikely, but positions are not comparable across files — worth knowing before a future consumer sorts findings across a package by `token.Pos`.
- With a stub `TestingT`, `scanGuardTestFiles` can fatal twice for one empty scan, the second overwriting `msg`. Harmless — both contain "stopped looking", and a real `*testing.T` Goexits at the first — but it is why the restoretest assertions pass regardless of which fatal they observe.
