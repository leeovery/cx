AGENT: standards
FINDINGS:
- FINDING: Stand-down reason vocabulary grew from the spec's five to seven, with new user-facing copy
  SEVERITY: medium
  FILES: cmd/run_hook_stale_cleanup.go:23-30, cmd/run_hook_stale_cleanup.go:53-61, cmd/run_hook_stale_cleanup.go:69-100, cmd/run_hook_stale_cleanup.go:177-186, cmd/doctor.go:200-211
  DESCRIPTION: The specification enumerates the reasons a hook-staleness cycle declines as a closed set, and was amended by corrigendum once already (2026-08-30) precisely to move it from three to five: `restoring`, `lock-timeout`, `empty-pane-read`, `store-read-failed`, `pane-read-failed`. The implementation declares seven, adding `marker-read-failed` and `sweep-failed`, each with its own `Skipped stale hook prune: …` phrase (`could not read the restore marker`, `the sweep could not complete`) and its own `notEvaluableDetails` entry. No corrigendum records either addition. Two consequences follow. First, the `reason` attr's value set — which an operator greps by, and which the spec's whole argument for naming the decline rests on ("one grep to answer whether the prune stood down and why") — now carries values the spec never sanctioned; the project convention is that log vocabulary is spec-governed and never invented at a call site. Second, and more visibly, the spec is explicit that a failed marker read is *treated as set* and produces the same result as a set marker; the implementation instead reports a distinct reason. Because `hook`/`doctor` are bootstrap-exempt and start no server, a read-only `portal doctor` on a machine with no tmux server running is the ordinary case for this branch — so the common user-facing line for that state is now `stale hooks … could not read the restore marker` where the specification prescribes the restore-in-progress wording. `sweep-failed` is a further departure in kind: it is not a decline at all (the sweep ran and failed), yet it is rendered through the skipped-prune vocabulary as though the prune stood down.
  RECOMMENDATION: Either fold `marker-read-failed` back into `restoring` and drop the skipped-prune rendering for a sweep that errored (returning the implementation to the spec's five-reason set and its "failed read is treated as set" rule), or raise a corrigendum recording the split to seven, the rationale for not claiming a restore the user is not running, and the two new rendered phrases on both surfaces — so the closed vocabulary the spec asserts and the one the code emits are the same list.

- FINDING: Design-argument and cardinality-claim comments violate the project's comment standard
  SEVERITY: medium
  FILES: internal/hooks/lock.go:29-61, cmd/run_hook_stale_cleanup.go:32-52, cmd/hooks.go:81-83
  DESCRIPTION: code-quality.md names two things a comment must never carry: "The design argument … State the conclusion the code needs, not the debate; the reasoning lives in the project's design artifacts", and "Cardinality claims — 'the single caller', 'the only site that…', 'nothing consumes this yet'". `snapshotLockBound` is a three-line function carrying a ~33-line comment that reproduces the whole of the specification's corrigendum argument — the two-bounds justification, the headroom-versus-thousandth comparison, the behaviour across three separate bound ranges, and a restatement of the floor rationale that appears twice in the same block ("The floor keeps that figure at one poll interval, below which the figure named stops being the figure waited" repeats the earlier "the acquire re-tests its deadline only after a poll sleep, so every bound shorter than one interval costs that same one interval"). The `skipReasons` block asserts cardinality outright — "sweep-failed is the --fix repair's line alone" and "lock-timeout cannot reach the read-only diagnosis at all" — both falsifiable by ordinary additive change far from the comment, which is the exact failure mode the standard names. `cmd/hooks.go`'s "A new seam costs one fill line here rather than a builder of its own" is the same class, smaller. These are maintenance liabilities no compiler or test checks, sitting on the code this work unit exists to make trustworthy.
  RECOMMENDATION: Reduce `snapshotLockBound`'s comment to the conclusions the code needs — that the pre-read is advisory and may degrade, that the bound is derived from `lockTimeout` so lowering one lowers both, and that the floor of one poll interval is load-bearing because the acquire re-tests its deadline only after a sleep — and let the specification hold the argument. Strip the two cardinality claims from `skipReasons`, keeping the reachability facts only where they change what a reader must do (e.g. that `lock-timeout`'s diagnosis phrase exists for vocabulary completeness). Drop the `hookSeams` aside.
SUMMARY: The mechanism the specification decided — token-only hook keys, the single `@portal-pane-id` home, the two-read existence probe, lazy stamping, shape-aware retention, the restore re-stamp with its WARN and pair-count limit, sidecar locking with its two bounds and degrade-by-side rule, and the full `@portal-id` removal including `state migrate-rename` — is implemented faithfully and the prescribed test surfaces all exist. The drift is at the edges: the stand-down reason vocabulary grew past the closed set the spec enumerates (changing what a server-down `portal doctor` tells the user), and two comment blocks carry the design debate and cardinality claims the project's comment standard forbids.
