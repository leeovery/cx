AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project conventions. styleFilterInput() now routes both the Sessions and Projects bubbles/list FilterInputs through a shared styleListFilterInput(l *list.Model) helper (the exact "natural shape" the spec suggested), so the two are byte-identical by construction; the git diff confirms the Sessions styling is a pure parameterization (m.sessionList -> l) with no behavioural change, and both the coloured (Dark/Light) and colourless (NO_COLOR) branches match the spec verbatim (Prompt = filterPromptPrefix, Focused.Prompt/Focused.Text orange-or-bare, Cursor.Color orange/NoColor{}, Cursor.Blink = false). styleFilterInput() is invoked unconditionally at the top of the single mode-application chokepoint applyCanvasMode() (model.go:1309) and applyProjectCanvasMode() never re-touches FilterInput styles, so the project input is styled everywhere the session input is with no bypass or overwrite. Net-new tests (projects_filter_input_reskin_test.go) cover both the coloured Dark/Light and colourless branches, mirroring the sibling filtering_reskin_test.go as the spec directed. All exclusions are respected (renderFilterQueryHeader, the contextual footers, the Sessions input styling and its tests, the filter engine, and the reused filterPromptPrefix / theme.MV.AccentOrange are untouched). go build ./... is clean and go test -count=1 ./internal/tui/ passes.

NOTES (informational, not drift):
- The new tests iterate over []theme.Mode{Dark, Light} with a for-range loop and [%v]-prefixed failure labels rather than named t.Run subtests. This is a deliberate, spec-directed mirror of the sibling filtering_reskin_test.go and is the entrenched idiom across the entire internal/tui test package; introducing t.Run here would make the new file inconsistent with the file it was told to mirror. The spec-vs-convention choice landed correctly.
- Token assertions use strings.Contains against the exact mode-resolved SGR sequence (tokenFgSeq). Rendered runs carry reset codes + content, so exact-equality on the token alone is not possible; the substring-of-exact-SGR check is the established package idiom and catches a token swap (not merely glyph presence). Proportionate.
